# 双栏首页断言（附则；公共规则L101/L194引用）

适用范围：双栏内容件（2026-09-03选必1十件＋此后全部双栏件）；单栏配页件不适用。逐件在其**自身**PDF上跑、合并样册**前**跑。例外件型登记区（合法「首页无正文」件型，登记须**用户明示批准并注明用户令日期**——自助登记无效）：当前空。

## 前置步0（修复轮首件定稿即时执行；登记完成前全部件按「不判定＝禁止交付」处理）
断言①机读正则与「文件→锚点模式」映射表（十件10行）登记于本附则；与三族（题号块「N-N-N．」式／条目号「N-N．」式／正文首个节标题统计行式）皆不匹配之件（如首块纯图），登记其实际首块机读特征。**反向校验**：映射表各行锚点禁取L101头部要素串（全件统计行／章首导航表／〔基〕〔进〕图例行）——黑名单按**全串**匹配、非子串。当前状态：未登记（待修复轮）。

## 断言集（PDF侧PyMuPDF＋COM侧）
① 首页正文成立（正向锚）：第1页须存在**锚点块**＝命中该件登记锚点模式、落**正文区**内（上下界由该件pgSz/pgMar现算、剔除页眉页脚带，禁用固定常数）、**不得与文件名主干同串**（文件名主干＝去扩展名、去范围括注、去题量括注后的文件名主体）、且**不得为该件头部要素（全件统计行／章首导航表／〔基〕〔进〕图例行）之任一**；内容块＝文本∪drawing∪图像块；第1页可抽取块数=0或断言脚本异常退出＝**不判定＝禁止交付**（fail-closed）。
② 头部要素通栏：逐件按该件实际头部要素（导航表或统计行/图例行等段落型要素）取件内sectPr现算x0与宽度基准（x0＝左pgMar；宽＝pgSz宽−左右pgMar；容差±2pt）；无该要素的件**逐要素登记「不适用原因」、禁止整条跳过**；命中栏宽挤压形态＝缺陷。
③ 页数对账：③a 对**装订单/册目录页所载页数**独立对账（独立源，禁用本轮自测数）；③b COM实测页数记基线（修复轮首测建立）。
④ 工具门与对照件：双栏改制工具与双栏件落盘工具**内建**本断言（断言集执行器：三族匹配＋①反向校验＋豁免族逐张登记落盘——工具债〔案5d/6〕行）——不过即拒绝生成交付件（隔离区暂存）；首件修复通过件＝阳性对照件，登记条件＝断言通过＋**人工看渲染首页双确认**，路径登记于此（当前空）；**阴性对照**：断言脚本定稿后先对「阴性对照证据/衔接2缺陷态.pdf」（存档于工作区/体系-双栏首页与PDFCreator主路径-0903/阴性对照证据/，含p1块dump）跑一遍须判FAIL并记录结果于此，未做此步不得宣布任何一件修复通过。
⑤ 分卷标题断言：每卷第1页不得出现与该件文件名主干同串的标题块；首卷标题块须全章口径（题量＝全章、无范围标注）。选必1实证（2026-09-03）：卷2+文内零标题、卷1全章口径——已合规；用户所见标题串仅存在于文件名/docProps title/装订单三处（非页面内容）——本案登记为「解释案＋回归防护」。

## 修复纪律（选必1版式复合修复轮·空白缺陷子步）
① 假设集按成本序排查、命中即回填本附则：H1段落级显式分页（w:br type="page"／pageBreakBefore／栏break／表格keepNext＋cantSplit／widowControl）→H2全宽表落栏（**仅讲练件附加缺陷候选**——衔接2无表仍现缺陷，表非唯一成因；最小复现件＝衔接2）→H3两节docGrid不等→H4 sectPr落位（表后缺承载段）→H5页眉页脚/titlePg不对称→全部落空＝「渲染例外」，走⑦出口判定树。
② 落点修正与触发属性消除系**并列手段**；验收门＝本断言集（结果层），不得以落点修正当修复完成。
③ **本纪律仅约束空白缺陷子步**：文本流（段落与run内容＋顺序）零变更；唯一授权结构变更＝头部要素整体位移，逐件附「位移前后段号」对账表（其余子步的结构变更口径见各附则）。
④ 先内容后页码：子步序见看板⓪（空白→底纹减法→讲部补挂→表格→图定尺寸→导航表四列化→COM重测页数→册目录页→装订单→页数账→逐页巡检门）；**任一回修触发即作废其后全部盖章，自「COM重测页数」起重跑下游子步**；计数表（七类/四值/恒等式）在讲部子步落定后**一次重算、一次盖章**。
⑤ 禁止增删内容或改页边距绕过（各子步另有授权口径者从其附则）。
⑥ 首页断言PDF获取：以 Word PrintOut From=1 To=1 送 **PDFCreator 虚拟打印机**、按主路径链路定义渲染（公共规则§14主路径款）、输出本地临时目录副本；**断言用PDF禁止经 ExportAsFixedFormat／Word 原生导出生成**（该路径＝§14备用路径①，默认禁用）。
⑦ 出口判定树（假设集穷尽首页仍空）：停等用户三选一——(a)该件回退单栏＋登记工具债；(b)接受「头部要素独占首页」形态（须同步改本附则断言①口径）；(c)Word内手工调分节符后回写工具规则。若根因要求**改分节机制本身**（非属性/落位），须另起一轮修订公共规则L101，本附则不改L101。

## 历史实证（2026-09-03主会话实测；口径：A4页高842pt、PyMuPDF blocks、y自页顶）
衔接2第1页仅3块（页眉y21-35／页脚y786-800／文件标题y44-62）、约86%空白、正文140块全部第2页起；讲练1上头部节sectPr（continuous/cols=1/headerReference·footerReference/pgNumType start=1）vs文末sectPr（cols=2 sep=1 space=425/docGrid linePitch=312）页面尺寸边距逐属性一致；导航表tblW=10200缇vs栏宽约4870缇；讲练1上全章零讲部；fill分布＝C9C9C9×883/E0E0E0×131/ADC2DA×10/C6D4E3×52；125图宽0.4~8.6cm高0.4~9.0cm；册目录页章行/件级行页码列全为写死「1」。原始证据＝工作区/体系-双栏首页与PDFCreator主路径-0903/阴性对照证据/（PDF＋块dump永久存档）；过程件probe_blank/修复轮收尾可删；十件同构＝2件实测＋8件推断，由修复轮逐件断言建立。勘误记：qwen r3-N12「§7节标题计数恰217」系误并（217为公共规则文件行数恒等、非节计数），撤案。
