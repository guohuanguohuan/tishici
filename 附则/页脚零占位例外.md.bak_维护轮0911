# 附则《页脚零占位例外》

公共规则§7 页面条款「页脚距页底边固定 1.5 厘米（850 缇）」的零占位例外机制。锚点 FZ-1..4。

- **FZ-1（判据）**：页脚距边 `w:footer` 满足「w:footer ＋ 页脚单行行高（9pt/18半点≈266缇）≤ 下边距 850 缇」时，页脚整行沉入下边距空白区，正文容量零损失（＝页脚零占位）；默认 850 缇＝页脚距边与下边距同值，页脚整行压进正文区、每页正文容量损约一行高。
- **FZ-2（现行登记）**：人教B版选必1 第1章讲练下卷（C 件）与第2章讲练 2.8 卷（H 件）＝ **567 缇**（页脚距页底 1 厘米）。同册其余件仍 850 缇；本3/本6 交界页页脚垂直位差约 5mm 为已登记代价。其余册/件一律默认 850 缇；改零占位须逐册拍板（页数变化联动页码账/分批清单/巡检批次）。
- **FZ-3（机制）**：`工具/册级连续页码.py` 的 parts.json 顶层可选 `footer_twips{件basename:缇}` 按件覆盖（缺省 850）；盖章不动点迭代自动消化「改页脚→页数变→重盖」。重建类工具（页眉面包屑挂载.py／repair_lib.py／footer_fix.py／部分封面产件.py）按 850 缇复位 sectPr——重建后收尾重跑盖章即按配置复写覆盖值，覆盖登记以各册现行 parts.json 为准。
- **FZ-4（边界）**：本例外只动页脚距边，不动四边边距（850 缇不动）、不动页眉（283 缇本已零占位）；页脚文字不下沉裁边底线为 ≥500 缇（1cm≈567 缇为现行值）。
