# M3 第2章 拓展册（S4）汇编报告 — 0914 收尾臂

交付面：`成卷/拓展册/上册|下册/`（main.tex 本体＋main-true.tex/main-false.tex 双档壳＋qp-m3.sty＋值台账-上/下册.json）；门谱与读数档在 `工作区/_tmpM3S4拓展册0914/门谱/`（门-守恒对号.py、门-值快照钉值.py＋读数-01~06）。红线执行：零 git；写入域仅 `成卷/拓展册/`＋`_tmpM3S4拓展册0914/`；题面库/定稿/工具全只读；qp-m3.sty md5＝7c3930362be8a0a2bdf21bbf8ac16573（编译后复核未动）；编译 cwd＝册目录。

## 一、收编读数（裁断口径复核）
- 总口径：拓区 221 题次 → 实收编 **202**（批A 10＋批C 77〔78−拓27撤1〕＋批D 115）；**章末 19 席缺料不收编**（题面库无键、定稿无件，批E 补产在途 `批E执行记录.md:60`），tex 不留空占位。
- 撤席跳号不重编号：10缺1、11缺2、12缺27、14缺13/31、16缺18 —— 门05 断言「台账缺席集≡裁断」逐课时核实（含课时14 母席 1..33 模型）。
- 挤出 8 席续编号段在册：10-拓13/14/15、11-拓15/16/17、13-拓5/6（门05 键序逐位核实全在册）。
- 蒙日族拓展 8 席超「组合≤2」：照收编口径全 8 收录（裁断原文照录）；实测旁证＝课时12 拓块蒙日系 7 席在册（12-17/19/20/21/22/25/26）。**分流建议（报备）**：批E 回冲时如需压族限额，可优先将蒙日系组合迁章末专版。
- 族限额复核（非空【题型】/族名口径）：156 族中非空 155 族逐族 ≤2 零超限；超限集合仅为空族名 47 席（课时12×41＋课时13×6，源定稿无【题型】行，属源态非编造）。
- 形态：解答103／单选54／填空33／多选12；详解>8 行 30 席（全线括线模覆盖）；`\ansblockgrayfalse` 全线置定。

## 二、门谱逐门读数（详档见 门谱/读数-01~06）
1. **门01 槽宽门 strict**（扫 main.tex 本体）：上册 槽72（过72/旗0）形旗0 → PASS；下册 槽128（过128/旗0）形旗0 → PASS。前轮 FAIL（上72/旗7、下136/旗5）根因＝盘上 main.tex 为估宽器修复前的生成物；重跑 `生成册tex.py`（幂等）后门过。降档梯读数：4连21／2连29／单列16（合计66＝单选54＋多选12）。重生件抽验：下册 `\sqrt{2}`×143、上册 12-17 `x∈\{-2,-1,0,1,2\}`、`k_{1}`×19、花括号三态 `\{`×9。
2. **门02 编译三零双档**（TinyTeX xelatex，4 壳各 ×2 遍，8 遍全 exit=0；Err/Overfull/MissChar 计）：上true 0/0/0 **28页**（Underfull 5）、上false 0/0/0 **11页**（Uf 2）、下true 0/0/0 **34页**（Uf 10）、下false 0/0/0 **12页**（Uf 6）；页数 false≤true：11≤28、12≤34 ✅。页数较上窗（28/10/33/12）微涨系降档梯生效，预期内。
3. **门03 [D] 静默溢出审计**：4 件全 **PASS（红旗0／警告0）**；承件族＝multicol 可回流栏（非静默族）；锚账 87/115 集合对平；印面 [答案]×87（true）/×0（false）、×115/×0；纸面 210×297mm 无越界，页脚签名带豁免条最大底 286.0mm。
4. **门04 [E] 答案印面计数**（双册）：上册 键账(41数号·跨课时重号降级对集合)↔锚↔块↔两档 log 恒发全 PASS，true 印面 [答案]总现 **87＝键数**、false ×0；下册 30数号同构 PASS，**115＝键数**、false ×0；[详解]×87/115、[点睛]×0（本册 `\ansnote` 仅详解形，工具标注行点睛读数为同串双标显示癖，已核 sty 无点睛印路）。键集核查：无 32-1 形子席降级歧义（子席见门05）。
5. **门05 守恒对号**（自写 `门谱/门-守恒对号.py`，结构照抄 S3 母版）：**全绿 61 断言/0 红**。锚≡块≡台账键序逐位（87/115）；`\ansitem` 席号序≡台账末段（含子席 `32-1` 印面连字符 U+2011 归一后逐位全对）；ANSKEY true≡false≡台账 逐位恒等；PDF 号序逐位（对号门）；[答案]/[详解]/泄答词 false 档全零；`\tailfill` 恰 1/册；页数 false≤true；两册键数合计 202、键集互斥；撤席≡裁断。
6. **门06 值快照＋钉值逐字**（自写 `门谱/门-值快照钉值.py`）：**全绿**。A 快照逐字 202/202（`\ansitem` 第二参≡convert(源值)，未知字符零）；B 批A/批D **125 键** manifest 逐键哈希（题面块＋答案块 sha256，配方照抄题面库对号门）＋答案侧值行零漂；C 批C **77 键** 定稿 §9.5 复提【答案】行（截「｜【题型】」尾）零漂；D 点检 17-01 源值 CJK 括注 `C（弦长 8/5）` 在印 `\(C\)（弦长 \(8/5\)）` 未被数学化截断。快照落 `门谱/值快照-读数.json`。

## 三、残余项
1. 章末 19 席（2章-拓-章末-01~19）：待批E 补产＋冻片后回冲，本册不预留占位。
2. 17-01 选项瑕疵：『〔图嵌，数值未回〕』照录印面（clean_text 保留〔〕口径），待数值回填改版。
3. 图债席：含 15-01/15-09（题面「尺寸如图」类照录），待图件到位回冲。
4. 批C 拓席键为自构键（`2章-拓-课时NN-两位席号`），未入题面库片与 canonical 键名总表——挂键务臂回填（题面库课时10~13 manifest 拓键=0 已由门05 断言在案）。
5. 蒙日族 8 席超「组合≤2」分流建议（见 §一，批E 回冲时裁）。
6. 页数预算：上 28+11、下 34+12（true+false）；Underfull 仅计数（5/2/10/6）非门。

—— 收工回执（≤12 行）见会话最终回复；本报告即 `_tmpM3S4拓展册0914/汇编报告.md`。
