# F-工具补丁轮·落盘报告（2026-08-31）

文件范围（实际改动，仅此3个共享工具＋本子文件夹）：`工具/题号块三段式.py`、`工具/字号双档改版.py`、`工具/全库对齐垃圾图回扫.py`。禁改成品＝未触碰任何成品docx；禁git＝本轮零git操作（提交由主会话执行）。提示词根＝`C:\Users\28120\Desktop\提示词`（旧syncall路径已迁移）。

## 0. 版本预检（N20，实测落盘 版本预检-N20.txt）
Python 3.12.10｜lxml 6.1.1｜PIL 12.2.0。三工具改后均 `python -W error::SyntaxWarning -m py_compile` 编译净过（零警告）。

## 1. 任务A：题号块三段式.py 增 --qstart（7处hunk，逻辑改动3处）
需求来源：W-F/G/H/C报告——A1断言硬编码`seq[0]==1`，续卷件（C起62/F起93/G起183/H起251）无法直跑，四代理各自用了工作区包装副本。
改动（改前→改后）：
1. 模块docstring：题号识别节增--qstart说明；A1行「1..N」→「qstart..qstart+N−1（qstart默认1）」；用法行增`[--qstart N]`。
2. `def migrate(path, regmd, linkage=False)` → `…, qstart=1)`。
3. `expected = 1` → `expected = qstart`（门控区间＝qstart..qstart+题量−1；默认1＝原行为逐字节不变）。
4. A1：`seq[0] == 1` → `seq[0] == qstart`，报错信息增「期望qstart=%s」。
5. 登记md口径行/统计行/stdout：硬编码「序列1..%d」→动态`seq[0]..seq[-1]`＋标qstart值（W-F旧包装副本曾显示「序列1..182」的显示错误一并消除）。
6. `__main__`：增--qstart解析（参照字号双档改版.py同款参数名）。
其余逻辑（前缀消费/三重判据/N6底纹只盖「N．」）零改动；N6口径兼容性审计＝实证通过（见回归：对T4已改版件二跑零改动零回退）。
回归（回归A-输出.txt，实跑）：
- F卷副本（题号93..182，base模式）`--qstart 93`：题号块90（93..182连续）｜幂等跳过90｜改写0/缩底纹0/重建run0/补底纹0/补加粗0｜A2断言90段全过｜交叉核对90＝90 PASS｜document.xml sha256 跑前=跑1后=跑2后（字节恒等＝幂等零改动）。
- X2副本（衔接件，题号1..13，linkage模式）默认qstart：幂等跳过13、全计数0、13＝13 PASS、document.xml字节恒等。
- 阴性测试：F卷默认qstart跑 → A1拒绝（`起=93 期望qstart=1`）、退出码1、文件未写（document.xml未变）＝首卷门控默认行为不变。

## 2. 任务B：字号双档改版.py exempt_ids GC假失败修复（移植W-E已验证修正＋2处审计修）
需求来源：W-E报告——`exempt_ids`存lxml代理`id()`，代理GC后地址回收致复核断言「存在N个非豁免run缺显式sz」随机假失败（首跑3/复跑10处）。
改动：
1.（W-E修正移植）`exempt_keep = []` 持活列表声明；两处 `exempt_ids.add(id(r))` → 追加 `exempt_keep.append(r)`。
2.（审计扩展，同缺陷第3处add位点）标题行内统计段run切分分支的 `exempt_ids.add(id(rs[i]))` 同样追加持活。依据：机制口径「exempt对象列表显式保存至断言结束」应覆盖全部豁免对象；W-E修正版漏此位点是因E卷复跑场景该分支不触发（E卷json实测`标题行内统计段run切分=0`，run已在前次执行拆分），首跑未拆分态仍会暴露同款假失败。
3.（审计修·预存潜伏崩溃bug）删除 `rc['标题行内统计段run切分'] += 1`（rc=log['run计数']，该键只在分类计数有初值——site3首触发即KeyError，且与下一行重复计数；保下一行唯一计数）。
4.（审计修·预存潜伏断言bug）文字零增删断言由列表级比对改拼接流比对：原 `text_stream(doc) == stream_before` 在工具自身的「run级切分」把1个w:t拆成2个后必假失败（列表不等而字符零增删）——docstring明文设计该切分属N11合并后合法操作，原断言自相矛盾、site3路径实际不可完成；改 `''.join(...) == ''.join(...)`，零字符语义不变。
W-E本地修正版中的无功能`pass`残留（其556行）未移植。
回归（回归B-输出.txt/回归B-输出-终版.txt，实跑）：
- E卷副本（题号1..92，曾实测假失败件）`--qcount 92` 连跑3次：退出码全0（断言零假失败）、zip成员级DIFF=0×3（89成员逐字节恒等＝幂等零改动，工具docstring幂等铁证口径）、分类计数三跑一致（标题段跳过79/正文184/解析854/题号块92/待人工0——与W-E报告数字差异系其后N11合并致段数减少，正常）。
- site3合成件（统计段跨界run首跑触发）：切分=1；切分后标题段run＝「2.1 …（第1—20题）　」(sz=None，留T4)｜「本节2」(sz=18)｜「0题：…」(sz=18)；文字零增删；退出码0。

## 3. 任务C：全库对齐垃圾图回扫.py ROOT失配修复（3处hunk，扫描/判定逻辑零改动）
需求来源：W-X1报告——硬编码`ROOT=C:\sync\syncall\...`，本机提示词根在桌面，files()空转、输出空合计假成功。
改动：
1. `ROOT` 默认值＝`os.path.dirname(os.path.dirname(os.path.abspath(__file__)))`（脚本位于 提示词根/工具/ 下，向上推导）。
2. main() 增 `--root <路径>` 显式覆盖（argv先滤--root再取mode，两种参数序均可）。
3. 启动打印自查行 `ROOT=…（模式=…，扫描N件）`——失配从「静默空合计」变为首行可见；docstring用法行同步（原`[--only 目录]`系从未实现的死文档，顺手改标真实参数--root）。docstring改raw string（消除`\s`转义SyntaxWarning）。
回归（回归C-输出.txt，实跑）：
- ①--root覆盖：临时树（高中数学/高中数学同步/）内X1副本.docx（真实件）＋jc命中合成件 → scan退出码0、ROOT行正确、扫描2件、命中清单行「回归C-带命中.docx | jc非左:1」、合计落盘。
- ②默认推导：真库scan（只读）→ `ROOT=C:\Users\28120\Desktop\提示词`、扫描74件（数学28＋物理46＋大学0）、命中清单（册目录页ind 31等）＋合计、退出码0。

## 4. 工作区清点（本子文件夹）
脚本×4（_回归A/_回归B持活/_回归B_site3/_回归C）＋回归输出txt×4＋登记md×2＋json×4＋site3合成/输出docx＋版本预检txt；大体积重复docx副本与临时root树已删（中间件清查，证据以输出文件为准）。工具__pycache__未动。

## 5. 需主会话裁决/知悉事项
1.（知悉）任务B超出W-E两行补丁的3处扩展：site3持活＋KeyError修＋断言拼接流修——均系「审计既有语句兼容性」在回归中实锤的预存缺陷（site3路径原状不可用），改动各1行级、已注明日期与理由，回归全过；如不认可可单独revert第3/4项（第3项revert后site3回归将KeyError）。
2.（知悉）E卷3连跑分类计数与W-E报告差异（正文184 vs 208段）系其后N11合并所致，非本补丁影响（成员级DIFF=0证明）。
3.（无待决）三补丁均回归通过，无阻塞项；git提交由主会话执行。
