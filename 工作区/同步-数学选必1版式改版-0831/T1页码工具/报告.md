# T1页码工具轮·落盘报告（欠账A·工具债①；2026-08-31）

## 一、文件范围（严守分派边界）
- 改动：`C:\Users\28120\Desktop\提示词\工具\册级连续页码.py`（整文件重写）、`C:\Users\28120\Desktop\提示词\工具\节页码定位.py`（整文件重写）。
- 工作区：`工作区\同步-数学选必1版式改版-0831\T1页码工具\`（全部验证脚本/输出/记录）。
- 未动：产出文件夹任何成品——6原件（讲上/讲下/四卷）sha256[:16]与T1开跑前预检值逐字节一致（`原件核对.py` 输出在案）；未执行任何git操作。

## 二、版本预检（N20，实测值）
- Python 3.12.10（`C:\Users\28120\AppData\Local\Programs\Python\Python312\python.exe`）
- pywin32 312；Word COM 16.0（Build 16.0.14334）
- 开卷冒烟（6副本，旧页脚口径）：B=79 / C=76 / E=50 / F=53 / G=38 / H=64 页

## 三、任务A：册级连续页码.py 改部分内累计 —— 已完成
- 新接口：`--parts parts.json`（`book`＋`parts:[{tag,files}]`按装订序P1..Pn＋`skip_files`配页件清单——封面/册目录页/使用说明页/错题记录/部分封面显式列入即跳过不计数不盖章）＋`--record 记录.md`（无时间戳，以文件mtime作「先内容后页码」先后核对锚；`节页码定位.py` 按件名basename匹配start/件标识/N）。用法注释写在工具头。
- 语义：每部分独立从1连续；同部分后件 sectPr pgNumType start＝前件累计页数＋1（COM实测页数为源）；N＝部分总页数写死文本；页脚整段重建为「件标识（共N页）＋全角空格＋第X页」（例「第1章·讲练（共155页）　第5页」）；X＝PAGE复杂域（新建fldChar begin/separate/end＋instrText，禁fldSimple，缓存结果run刷为首页实测值＝start）；NUMPAGES域检测到即随整段重建拆除；页码9pt（18半点）中文宋体西文TNR；左对齐；全文唯一页脚部件断言＋页脚多段落归一；w:pgMar footer归一850缇；titlePg与evenAndOddHeaders剔除（不勾首页/奇偶不同）；settings.xml保障updateFields；落盘后zip回读断言。
- **不动点收敛迭代（本轮新发现，已内建闭环）**：footer距边850＝下边距850（零余量），页脚行高参与正文区计算——旧件页脚21半点改9pt后正文区微增，临界件repaginate（实测讲上B 79→78页）。「先测量后盖章」一步制会给自己盖出作废数字；工具改为实测→盖章→复测→页数有变以新页数重盖，循环至复测与所盖数字一致（上限5轮，实测2轮收敛、重跑1轮即稳）。

## 四、任务B：节页码定位.py 适配部分内页码 —— 已完成
- 输出＝件内页＋该件start−1（列名改「部分内页码」，JSON `part_page`）；start从单件参数/批量配置/`--record`盖章记录（按件名匹配）读；支持 parts.json 直传（与盖章工具同源配置，M2直接复用）。
- 顺延工具债修复：批量模式任一件0命中＝单件stderr告警＋跳过，其余正常出结果、退出码0（衔接件/知识清单无「（第X—Y题）」节标题不属错误）；`--strict`恢复旧口径（纯讲练配置防呆，任一0命中非零退出）；单件模式0命中仍样式诊断＋非零退出。
- SEC_RE兼容N11新形态：节标题行与统计行合并为一行（「2.4 …（第101—119题）　本节19题：简单1｜中档12｜难6」）行尾统计段照常命中；非统计段尾巴/缺节号形态仍拒绝（防误报）。

## 五、验收实测数字（全量输出：`验收输出.txt`；分件证据见各阶段脚本/JSON）
1. **P3两件副本**：不动点收敛后 B=78页start=1、C=76页start=79、N=154（第1轮按旧页脚实测79/76盖完→复测B=78→第2轮重盖→复测一致收敛；全跑合计359页）。
2. **P6四件副本**：E=50/F=53/G=38/H=64页，starts级联=1/51/104/142，N=205（首件1、后件=前件累计+1恒等）。
3. **XML域形态断言**：6件×13项＝78/78全绿（唯一页脚部件、PAGE复杂域begin/end/separate各1、instrText含PAGE、无fldSimple、无NUMPAGES、sz全18半点、宋体+TNR、jc=left、pgNumType start精确、pgMar footer=850、无titlePg、updateFields、页脚可见文本=N写死串）。
4. **幂等二跑**：6件全部zip成员逐字节DIFF=0（成员数B133/C176/E89/F70/G60/H77，含document.xml/footer1.xml/settings.xml）。
5. **COM实测页数为源＋缓存值抽查3页**（COM调整页码==start+件内页−1）：B[1,40,78]、C[79,116,154]、E[1,25,50]、F[51,77,103]、G[104,122,141]、H[142,173,205] 全过。
6. **节页码定位B副本重测**：7节行（1.1.1…1.2.4，件内页1…45），部分内页码=件内页+start−1恒等成立；批量含X1衔接＋I1清单→0命中各1条警告跳过、退出码0、B节行照常输出；--strict退出1；缺start且无record退出2；parts.json直传6件级行starts=1/79/1/51/104/142与盖章记录一致。
7. **手工抽查2节（独立复核，不走工具正则、整段精确匹配取页）**：1.1.1空间向量及其运算（第1—10题）亲测COM件内页=1↔工具=1；1.2.4二面角（第49—61题）亲测=45↔工具=45。
8. **单测**：N11签名6用例全过（含行尾统计段2正例＋2负例拒绝）；页脚域拆除3旧起态全过（PAGE+NUMPAGES并存/fldSimple/多段落页脚→整段重建后均无NUMPAGES/fldSimple残留）。

## 六、异常红旗
- **页脚9pt化的repaginate效应（新发现）**：规格书§0「基线页数」列（B=79、P3=155）系旧页脚21半点口径实测；页脚改9pt后P3实际78+76=154页。W波N1/N2本将大幅改页数、M1以W后实测为准，规格书无需改动，但主会话与Z波过程对账应收编此口径差（Y=433旧值在9pt页脚下亦会漂移）。
- **Word COM拆解噪音（环境观察）**：Quit/CoUninitialize偶发RPC SEH（0x800706be/0x800706ba），一次致验收进程segfault（结果未损、根因＝释放已断代理）。处置：验收编排改为COM阶段各自独立短生命周期子进程＋编排器拉起的WINWORD残留全清（末态tasklist零winword）；两个工具自身finally Quit照旧。M1若遇同类拆解噪音：结果件已先落盘/先回读断言，进程退出码不作唯一依据。
- **页脚缓存值口径说明**：全文唯一页脚定义只有一份域缓存，缓存=该件首页实测值（start），Word/PDF开卷逐页重算（沿用2026-08-29 flush_page_cache先例）；任务书「缓存值抽查3页」以COM调整页码3页断言等价实现。

## 七、需主会话裁决事项
- 无必须裁决项。两件知会事项（§六红旗1/2）均已在工具与流程内闭环；如需规格书§0基线页数列加注「旧页脚口径」由Z波随过程对账处理即可，无需本轮停等。

## 八、遗留/边界（明示）
- N11合并行在真件上的实配：当前真件节标题仍是旧行形态（统计行独立），N11新形态以单元测试＋负例覆盖验证；W波合并落地后M2实测复核（签名已兼容）。
- M1使用提示：parts.json按规格书§0列全六部分（P1~P6，件标识第1/2章·衔接/清单/讲练），五类配页件放skip_files；--record产物同时供M2册目录页/装订单与节页码定位同源引用。
- T1子文件夹内验证副本（P3/P6/衔接/清单/封面副本）均为工作区副本，随Z波清理；产出原件零触碰。
