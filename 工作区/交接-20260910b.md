# 会话交接（2026-09-10 · 体系侧：qwen 防限流治理落配置＋登记＋推送；**片G 续做入口转本件，差四项**）

接续件：不覆盖 交接-20260909.md／b／c／d 与 交接-20260910.md，六件并存；**本件为最新开工入口**。用户主动断开另开新会话，盘上无在途代理，variantF 工作树干净。

## 一、盘上实况（接手先核这几行）

- 交付件 `工作区/字替对照-0909/variantF/main.pdf`＝**7 页、md5 `f80297ffeeb872438943da5bdc5cebc1`**（pymupdf 复核一致；`_tmp取证0909c/片G/交接.md:3` 的 `5c4c9e85…` 系 12:41 旧值，已过期）。`body.tex` 内 `\input{figs/` **6 处**（L76 g6／L152 g1／L170 g2／L250 g3／L305 g4／L337 g5）、`\includegraphics` **0 处**。
- 编译真值（本会话独立复核）：**error 0／Overfull 0／Missing character 0 成立**；**Underfull \hbox 实为 6 处**（`main.log:1376/1439/1444/1489/1494/1499`＝body.tex 126/156/168/263/291×2；片G 前 12:13 验态基线 5 处，**片G 净增 1 处＝body.tex:168 badness 5802**）。口径注记：交付报告的「三 0」＝error/Overfull/Missing char，**underfull 自片F 起「登记不设门」**（见 `交付报告-修复轮0909c.md:112`，片F 时代基线 18 处）。
- **工具坑①（本会话新发现，须修）**：`_tmp取证0909c/片G/logscan.py:12-13` 正则多写一层反斜杠（`Underfull \\\\hbox`／`Overfull \\\\hbox`）→ 两项**恒报 0＝假零**，历次「underfull 全 0」结论皆出自该扫描器。正确式参照 `_tmp取证0909c/片F/扫参.py:53`；临时核对可直接 `grep -c Underfull main.log`。
- **工具坑②（沿用）**：`Read` 工具带 `line_offset` 在本机恒报 schema 错——大文件一律用 Bash python 行切片读。
- git：HEAD **`93ad3ff`**（体系侧防限流治理：附则 ZH-1 配置注记＋ZH-6 指针、看板十八行、《多脑调用登记》留痕，三件各带 `.bak_上下文治理登记0910`）**已推送并核远端一致**；其上 `86d2d76`（0910 交接校正）→ `969a50f`（片G 半成品入库）→ `ceb324e`（E/F/H1/H2/H3＋figs）。
- **配置变更（本会话落，新会话或 `/reload` 后生效）**：`~/.kimi-code/config.toml` 增 ①`[models."qwen/qwen3.8-flash".overrides] max_input_size=300000`（`max_context_size` 仍 1000000——压缩/溢出检查触发点前移，非降窗口）②`[background] max_running_tasks=2`（并发硬顶）。**思考档 max、force=true、钉档 qwen 一律未动**（用户明令）。回退备份 `config.toml.bak_qwen上下文0910`；机理与纪律入 `附则/主脑协审制.md` ZH-1 配置注记。

## 二、片G 剩余（0910 件所列「七项」，实测已完成三项半）

**已完成（盘上有证据，交接未记）**

- ① g6 三联簇复核：并簇阈值已放至 20mm，g6 成一簇 **82.38×20.87mm、栏心偏 +0.31mm**（`片G/run3-终版实测.txt`）。
- ④ postproc 文书：8c 台账已改「片G 0910 六图下置集成台账」，docstring 与逐图注释 40 处「片G」到位。
- ⑤ 维持项复测：13:15 全量跑已覆盖——**46 ✓／5 ✗**（含拉伸异常行=0、判断题序号隙 ink 0.18mm、⑭ 栏线芯 207、⑤a/⑤b 栏利用率、页脚出血、句末）。
- ③ 半件：`断言顶格.py` 已换矢量口径**并通过**（位图 0／矢量图 6／签名 128／[答案] 14／表线 **64**／违规 0）。注：0910 件所写「表线 74」系矢量图内竖棱污染的旧读数，已加豁免（`断言顶格.py:205`）回到 64 档。

**剩四项（按序做）**

1. **③ 后半·五门改写**：`_测v4断言.py` 现 **5 ✗**＝`⑱ L684`／`⑱-2 L752`（＋reg 754）／`⑱-3 L831`（＋reg 835）／`N6 L1217`／`⑮ L1929`（行号为本会话实测现值，0910 件所列系插 helper 前旧号）。五门全是被片G 主动废除的「并排绕图」口径。共用矢量口径 helper 已就位：`L201-287`＝`_diag_rects()`／`vec_clusters(pno)`（非轴对齐矢量墨聚簇、簇内跨度≥5mm 斜长笔≥3、同 y 带 x 隙≤20mm 并簇）／`in_fig(pno,…)`（供表线排除图内墨）；③ 灰档白名单已 +64（cubegray）。改法＝按已定 D 方案整族改写为「六图下置」口径，任何门值改动逐条 `reg()` 登记旧→新，目标 5✗→0、全绿。
2. **② 六图逐图实测表**：三套脚本三套数互斥，**须先定权威口径**——`片G-新断言探针.py`＝声明盒内墨（g1 30.70×57.10，漏图外标签）／`终版实测.py`＝带内全墨（g1 41.27×63.71，比素材预测矮 5.12mm）／`片G实测.py`＝全墨＋线框双列（全墨列被正文污染：p3/p5/p6c1/p6c2 正文相交 1/1/1/3，栏心偏 −9.50 系假值；**线框列最干净**：g1 31.62×58.00 vs 预测 31.62×58.04，Δ≈0.04mm）。**推荐以线框法为唯一口径**（矢量种子连通域∪图内标签、正文相交必须＝0），前距/下距随之重算，再定 ⑱-2／N6 门值。
3. **⑥ 取证**：六张目视裁片已在（13:00，170dpi clip，`片G/目视-g1…g6*.png`）；**`variantF/png/page1-7.png` 是 11:32 渲的，早于 13:06 终版＝过期，须重渲**；六处「终版 vs 源位图 `media/media/imageN.png`（三联 `sub3_B_4.png`）同尺对照」＋所在整块上下文片未做。
4. **⑦ 最终报告**：`交付报告-修复轮0909c.md` 现「片G」出现 **0 次**＝未动笔。须含：素材盘点/逐图集成口径、改动文件＋行号、三0 对比（**并更正 underfull 假零**）、**页数 7→7 的显著说明**（并排基线 `raisebox` 声明盒本已占纵向，下置净增被抵消）、六图实测表、维持项复测表、断言适配登记、裁片路径、遗留风险（旧位图仅取消引用未删；`alt={@@@…}` 溯源标记随 token 下置消失，断言无依赖）。

报告后链条不变：**独立复审计（未参与代理重测）→ 主脑逐页目验 → 邮件发终版 PDF（用户已两次点名要邮件）→ git 收口**。

## 三、等用户点头的三件（新会话先要答案再动手）

1. 六图实测**权威口径**取哪套（推荐线框法，见上）。
2. `⑱` 族五门**整族改写**为下置矢量口径、不保留旧并排断言（旧门值走 `reg()` 登记）。
3. **附则授权**：`附则/全品基准总表.md` 三处已与我方形制不符——L41「图文缝（并排横缝）6.7±1.5」、L69「变式图→并排小图」、L70「详解图→绕图回流」；另 L46 图档行仍引旧源尺寸档（＝0910 件待办 3① 同源）。片G 后六图全下置，改不改、改哪几行须用户明示。

## 四、其余待办（结转不变）

- 待拍板：① 正文句末「．」U+FF0E 105 处是否全件「.」化；② 片H2 页数开关（撤 `qp-layout.tex:45 \let\frac\dfrac` 一行可回 6 页，代价＝分式回 61px）。
- 残留：③ H1（E1 pitch −1%、右墨缘松 0.5mm、p3「1|中」8.32pt 单处）；④ 片D（变 7.08px＝VF wght700 封顶、√ 宽 3.56 vs 全品 2.89–2.96）；⑤ 条目缝端点 0.07–0.17mm、表2 单行顶净空 +1mm、表3 移 p3。
- 旧账：⑥ 零件库 5 占位件数值化／题池图源缺 3 处（探二/三/八变式1）／瘦身轮欠账 14 笔／用户侧待回（v4.2 过目、v4.3/v4.4/F 终审、本轮下半场终审）；⑦ **v4.5 轮**（零件库回写含本轮新拍板＋成套零件库扩充＋字体入库策略）待终审后开，规则修订均须用户明示；⑧ 看板 L7/L57/L73 残留 deepseek「现行口径」字样待同步（《多脑调用登记》本会话已以「留痕标明现行以 ZH-1 为准」处理，未删旧文）。
- cron：不随新会话；原 14:07 deepseek 到期复查已无意义，**不必重建**。

## 五、红线与工具指针

- 片G 红线不变：只动 `variantF/`；临时产物只落 `_tmp取证0909c/片G/`；**片G 期间禁 git**（收口提交须用户明示）；禁碰 附则/总控/进度看板（本会话动附则系用户逐次明示的体系侧授权，不构成片G 放行）。
- 限流纪律：429 后 sleep 90–120s 重试 ≥3 次；命令分批、长输出重定向落盘后小段读；**限流（`token-limit`/RPM/TPM）≠额度尽，不构成切备用通道**（ZH-6 判据补注）；**胖代理禁 resume**——续做一律新派实例＋盘上交接（本会话起 config 已把压缩触发点前移到 30 万、并发硬顶 2，reload 后生效）。
- 回退点：H3 态五件备份齐在 `variantF/`——`body.tex.bak_片G集成0910`、`qp-blocks.tex.bak_片G集成0910`、`postproc_daoxue.py.bak_片G集成0910`、`断言顶格.py.bak_片G集成0910`、`_测v4断言.py.bak_片G集成0910`。
- 产物指针：六图素材与逐要素核对表/同尺对照/叠加图＝`工作区/_tmp片G素材/`（残差 ≤0.1mm 级）；各片报告＝`工作区/_tmp取证0909c/{片A…片H3,复审计}/`；意见登记册＝`工作区/字替对照-0909/意见登记册-0909c.md`（29 条）；答案册草案＝`工作区/答案册式样-草案0909/`。
- 出口：邮件 `python 工具/通知发送.py "主题" "正文" 附件…`（自发自收，大附件实测可用）；推送 `git -c http.proxy=http://127.0.0.1:7897 push origin main`（NW-2 单次注入，禁写 config）。
- 子代理：现行＝qwen/qwen3.8-flash·思考 max（force 钉死，用户明令不得擅改）；备用 zhipu/glm-5.3-flash 仅可用性例外，未经用户明示不得启用。
