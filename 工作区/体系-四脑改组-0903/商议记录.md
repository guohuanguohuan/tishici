# 商议记录（四脑共识门改组决议）——商议体收编qwen＋kimi换型
日期：2026-09-03　单位：体系级（公共规则 §1:12、§1:15 修订）　轮次：2轮往返，三方绝对同意（本轮口径；四方自下一次过门起）
来源：用户明示点名（拍板）——「将qwencli加入到大脑名单中」＋「将kimi那个大脑换成kimik2.7code思考强度改成最大」（同一修订轮合并执行）
锚点（开工前基线）：HEAD=9062ee58b421c1b7a56bcfdea8fafd3b24beab22；公共规则.md 无未提交改动（基线即回滚点）。

## 议题
① 收编本机 qwen CLI 入商议体；② kimi 模型 k3-256k → kimi-for-coding（K2.7 Coding）。

## 实测锚点（主会话实测＋dsh独立复核双源一致）
- **qwen**：@qwen-code/qwen-code **0.22.3**（npm latest＝GitHub Latest 稳定版 2026-08-28 交叉核对）；`qwen -p` 非交互冒烟×2 通过（响应正常、零工具调用、零写操作）；模型 **qwen3.8-flash**（~/.qwen/usage/token-usage-2026-09.jsonl 实测 model 字段）；思考＝settings 默认 `reasoningEffort="max"`（合法档位阶梯 low<medium<high<xhigh<max；flash 非 qwen3.8-max 家族，传输层坍缩为 `enable_thinking:true` 全开——即该型号可达最强思考态；thoughtsTokens=124/128 实证思考激活；dsh 复核请求日志 enable_thinking:true 属实）；认证＝千问AI平台（qianwenai.com，官方MaaS门户，key 实测通过阿里云 DashScope 系端点）Token Plan 国内专用端点 token-plan.cn-beijing.maas.aliyuncs.com＋sk-sp- 订阅密钥（用户已购会员，套餐额度计费）。
- **kimi**：~/.kimi-code/config.toml 实测 `default_model="kimi-code/kimi-for-coding"`（display_name=K2.7 Coding；max_context_size=262144 与旧 k3-256k 平级无回退；capabilities 含 always_thinking、**无 support_efforts 字段**——思考常开即该型号最大思考态、无档位可调）；本议题第1/2轮商议调用即以此默认模型执行（kimi 0.40.1）。
- **dsh 独立复核**（第1轮自行实测）：三 CLI 版本、qwen 配置面/运行时面、kimi 配置面、修订边界 grep 全部与提案一致；并实证旧条款「k3-256k、思考max」字面与配置不符（k3-256k default_effort="high" 非自动 max）——**此为换型的实证动因之一，注记防沿革回查无据**。

## 各方观点
**dsh（第1轮）**：同意，无阻塞异议。事实面全部闭环（其自行实测核对）；四方门语义自洽；kimi 换型把「思考max」从名义档位变为型号固有属性，语义更严更可验；风险面既有条款已覆盖（qwen 端点波动→§54 重试→§1 停等）。补充 S1（思考态语义）、S2（qwen 调用约定）、S3（生效时点）；问 Q1（qwen 配额）、Q2（qwen -p 接续能力）。
**dsh（第2轮）**：确认全部补充项；附两核实点——①S3 边界写清（「下一轮过门」以最先发生的下一次过门为准，含本议题续轮）；②确认§1:12/§1:15 活动句「三脑」为全局替换（含门名、三方绝对同意、三脑推荐、按三脑共识门呈报、三脑不一致），只改「三脑推荐」一处将触碰修订纪律①③。
**kimi（第1轮）**：同意。建议：§1:12 qwen 条目保持「qwen3.8-flash（模型名以运行时实测值为准）」与 dsh/kimi 口径一致；§1:15「三脑推荐」径改「四脑推荐」不加字。
**kimi（第2轮）**：确认 S1/S2/S3、建议采纳、Q1/Q2 处置，均无异议。
**主会话（对 dsh 两核实点的回应，入记录闭环）**：①全局覆盖确认——本次替换覆盖 §1:12/§1:15 全部活动句（门名「三脑共识门」→「四脑共识门」、「往返至三方绝对同意」→「四方绝对同意」、§1:15 内「三脑推荐」「按三脑共识门呈报」「三脑不一致」三处同步），全库其余命中（历史商议记录、过程对账、报告、commit 信息）均为沿革语境按 dsh 切换先例不动；②本议题**本轮闭合**，四方绝对同意自下一次过门起执行（含本议题若有续轮）。

## 结论
三方绝对同意（主会话＋dsh＋kimi，2轮往返，qwen 系本轮决议对象按 S3 不与会）：按用户点名执行 §1:12、§1:15 两处限定修订（商议体＝dsh＋kimi＋qwen 三成员；门名三脑→四脑；kimi 条目换型 kimi-for-coding），其余条款零改动，历史文件沿革保留。

## 操作约定注记（不入公共规则条文）
- **S1 思考态语义**：四成员档位语义不一（DS 多档取 max／kimi 恒定全思考／qwen flash 单档全开）；共识判定以观点质量为准，不比思考时长/档位。
- **S2 qwen 调用约定**：qwen-code 带 tool_use（dsh 实证其系统提示词为工程 agent 口吻、工具集含文件读写/computer-use 等），与 dsh/kimi 纯问答态不同——主会话每次经 qwen 过门的议题文本须明示「仅观点交流、禁止任何文件/系统操作」；沿用无状态一问一答＋完整往来历史内联（dsh 切换轮既立约定，qwen 同适用）。
- **S3 生效时点**：本轮决议按现行三方过门落盘生效；含 qwen 的四方绝对同意自下一次过门起执行（以最先发生的下一次过门为准，含本议题续轮）。
- **登记待实测**：Q1 qwen Token Plan 配额/限流上限（实测时注明日期时段）；Q2 qwen -p 是否支持接续（kimi 侧实证其自身支持 `-r session_<id>` 接续；qwen 侧待测，若支持则按「接续仍逐轮含摘要与双方观点」的操作约定省 token，不改条文）。
- **教训落库三选一**：选②经验要点已注记于本记录（旧条款 k3-256k「思考max」与 default_effort=high 配置不符的实证动因；「思考max」跨型号语义差异按 S1 口径理解）——不另升格条款、无需独立经验文件条目。

## 体检（修订后实测）
- 体检1a：活规则文件「三脑」残留＝**0**（公共规则/00总纲/进度看板/各总控/经验文件）。
- 体检1b：「k3-256k」「三方绝对同意」活规则残留＝**0**。
- 体检2：新词族「四脑」仅公共规则.md（2处）＋本轮商议记录（4处），无越界扩散。
- 正文↔证据句对照：§1:12 新增 qwen/kimi 条目各锚点（版本 0.22.3／模型名／思考态／端点／未装先装包名）与实测锚点节逐一对应（主会话实测＋dsh 独立复核双源）。
