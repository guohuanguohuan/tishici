# 商议记录（三脑共识门）——dsh按需成员回归轮
日期：2026-09-03　单位：体系修订轮（公共规则L12＋看板两处）　轮次：1轮往返，三方绝对同意
来源：用户明示拍板——「将deepseekv4flashvision加入到大脑中吧！但是这个太贵了，尽量在核心重要场合使用」。

## 议题
DeepSeek 以**按需成员**身份回归商议体：常任三脑（主会话＋kimi＋qwen）与门名不动；dsh（deepseek-v4-flash-vision-exp、思考max）仅核心场合召集、与会同权、平时不花费。

## 三方门（1轮）
kimi同意（附建议：凭证登记记实测模型名——已纳入记账字段）；qwen附M1-M6硬条件＋S2/S3建议（全采纳零驳回，其结论明示「M1-M6全部纳入即绝对同意」）：
- M1到席不能分支：②③类登记缺席原因于K1后降级三方共识（明示不阻塞）、①类（用户点名）不得降级停等上报；
- M2③改可数判据＝「跨≥3个规则文件的锚点重排轮」，10号拆行降为历史例证非定义；
- M3点名外延钉死（dsh/DeepSeek字样或「召集按需成员」「四方全召」整句明示，语境暗示不构成）；召集权＝主会话，常任成员不得自行拉会或自扩②③；
- M4只引用不内联（「条目以§13现行文本为准，此处不复制」）；
- M5在机改条件句（「以运行时实测在机为准，不在机则按未装先装处置」）＋模型名系示例引用非运行时断言（防体检误报）；
- M6费用熔断（一次召集＝一轮门、主会话记账、凭证含实测模型名与日期；单会话>3次不再自动召集②③、先报用户）。
- S2体检口径：「四方」命中仅允许按需成员语境（L12「四方全召」引用1处）；L12字符数显式入体检。S3落盘前全域dsh枚举分类。

## 执行
L12按需成员段插入（M1-M6全量落入）；L12行尾指针顺带补正上轮漏网（「简报·封顶」已删但指针未同步→改「检查分级·总览·高危轮·证据可达」）；看板K1块加dsh召集登记（费用观测＋熔断）；S3枚举发现看板L63卸任移交行「不再召集」与新条款冲突——同步改「恢复为按需成员（召集判据与降级分支见公共规则L12，召集时照常版本预检与凭证登记）」。

## 体检（全过）
- 217行恒等；L12字符数＝970（<2000预算）。
- 按需成员词族：公共规则×1（L12）＋看板×1（K1块）＋本轮记录＝无越界。
- 「四方」：公共规则仅1处＝L12「四方全召」引用语境（M3定义内整句明示例，合法）；「四脑」公共规则0、看板仅九节header时为四脑口径沿革注记。
- S3全域枚举分类：活规则dsh仅L12新段1处（L49已清实证）；看板4处＝K1登记（现行）＋L62处置记录＋L63移交行（已同步按需口径）＋L65历史署名（沿革）；其余6文件0。

## 锚点
基线HEAD=6e78cdb；git范围＝公共规则.md＋进度看板.md＋本文件夹。首次按需召集待核心场合触发（当前看板待指示项中无≥3文件锚点重排轮；体检10号拆行轮届时属③类必召）。
