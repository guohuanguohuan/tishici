已通读 v2、五查结果、补签报告与 err/运行记录，并对盘上证据做了独立核对。核销结果与终审票如下。

## 一、独立核证（本席实测，非转述）

| 项 | 核证结果 |
|---|---|
| 条件①映射 | `_输入_配置A.json` 实物＝9件 `C:\提示词\高中数学\高中数学同步\` ＋ 清单1指向 `子步7\mirror\`，与「十件减清单1＝九件」陈述一致；`差集清单.json:29444 original_vs_mirror_sha256` 十对 `mirror_exists:true`/`sha_equal:true` 在盘，mirror 与原件同名 ✓ |
| 条件② | 五查报告明记十件 `_rels` `TargetMode="External"`＝0条，引用维度闭合 ✓ |
| 条件④ | `复测_配置A.err` 全文仅78字节一行（`wrapper: timeout after 180s…`），无 COM 异常原文；`__运行记录_A.json` 确无逐件计时字段——0字节stdout无定位价值的判断成立 ✓ |
| B8～B16/A1～A7 落实 | v2 逐条对号，全部命中文本（§〇⑦⑧、§一、§三、§四0/1/2/3/4/5、§六）。**未落实一项：B14 与 §四.5 保护名单不覆盖两跑产物**（见 G4） |
| 数字自洽（本席回算） | 件级 start 与页数互推闭合：讲练1上61页→下起62 ✓；第2章各本独立起算 55→56、56→112、44→156 ✓；括注 24+116=140=61+79 ✓；2章 1+38+61+19+63+36+32+89=339=92+90+68+89 ✓。三源恒等期望值无内在矛盾 |
| **硬门现态** | 实读 `子步8\复测_补签_codex\复测报告_补签.md`：首行仍 `FAIL`、**全文无「附录」节**（本目录镜像件同）。v2 自身前置硬门（未PASS禁派发）现时不成立 |

## 二、票1r2（闭环口径＋两跑方案）：**同意**（附四项为执行前置，须随任务书/附录落地）

口径实质成立：`原件↔副本↔镜像` 三段字节全同＋外链0条＋B对参照逐字段全等，构成断言5的等价闭合；副本跑与诊断跑不互斥而互补（观测闭合＋定性），顺位正确。1.7 三分（实质需A仅断言5）与本席核证一致，1.9 不写「个别受阻」先验亦与九件同形全灭一致。

- **C1 副本跑禁写「原配置」**。原配置实义＝九原件＋清单1走镜像，字面直传即静默重跑A、且可能在侥幸成功时被误报为「副本跑成功」——这是本方案唯一的假PASS入口。改为派生配置 `_输入_配置A_全同副本.json`（十件 path 全指 tmp 副本，`name/tag/start` 逐字段不动），记为**配置A′**，报告显式声明 A′ 与 A 的唯一差异＝清单1取值来源，该差异已被 10/10 SHA 全同吞并。
- **C2 两跑各自墙钟帽**（A′≤20min、诊断跑≤25min，含≤2次尝试、单件120s）。§〇⑧的45min属子步8臂，不覆盖这两跑。
- **C3 不得覆写已登记证据**：`复测_配置A.json`/`__运行记录_A.json` 是 FAIL 正文的引用件；两跑产物一律新名落 tmp。
- **C4 附录两处字面**：诊断复刻件 SHA≠`844d244a…`，其数字**不得**充当断言5闭合证据，仅作挂点定位；附录须带证伪条款——「若A′十件副本全挂，则『字节全同⟹COM行为全同』前提被实测证伪，本附录等价闭合失效，断言5回退未闭合、重开裁决」。

## 三、票2r2（v2 可否派发）：**不同意（现文不可派发）**——非实质，四点字面级

- **G1 时序自指（阻断级）**：v2 前置硬门依「附录PASS」判定，而附录按1.8将于「原件↔镜像全同」写就、其强度又取决于A′实测；现文附录未在盘。故「两票同意→随即并派两跑与子步8」会让子步8在自身硬门未满足时派发。**改串行**：派A′（仅依票1）→ 附录追加＋双口签认 → 硬门满足 → 派子步8。
- **G2 死权限冲突**：§〇⑥「一次性脚本放 `子步8\` 内」与 §一「诊断探针须放 `子步8\tmp\`」并存，而 §四.5 清点面只覆盖 tmp 与根下 `probe_*/__*/_*` 三前缀——非三前缀命名即成清点盲区。⑥改为「一律落 `子步8\tmp\`」。
- **G3 夸大措辞**：§〇⑦末句「路径无关性成立」——这正是A′尚未实测的命题，与你定的「不夸大」自相矛盾。改「外链0条（实测）；路径无关性待配置A′实测确认」。
- **G4 B9 名单缺口**：保护名单未含两跑产物（A′的JSON、逐件诊断日志）。它们将成为附录/报告引用件，落入删除清单即致附录指针失效。

另二项建议（不阻断）：P1 恒等式口径钉死为 python-docx `document.paragraphs`（与主会话验证的20同源），禁混 COM `Paragraphs`，否则表格单元致 Σ≠20 假告警；P2 §四.0/1/4 对两配页件重复开卷至多3次，合并为一次同取页数＋版式，收窄撞阻断面。

## 四、共识状态

票1同意、票2不同意 → **共识未闭合**。G1–G4 均为字面级修补且不重开范围：v2→**v2.1** 落这四处（P1/P2 一并采纳更稳）后，本席直接记为同意，无需再开一轮快审。第一跑（A′）不受票2牵制，可即派。残留风险登记照旧保留「原件COM直开在本机存在阻断风险」，五查归因（运行时瞬态/COM首启竞争）与之一致。
