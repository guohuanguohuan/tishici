你是「人教B版选必1 ⓪版式复合修复轮」商议体单脑（qwen席）——首门回票（r2）。纪律同前：只表态不执行。本目录参考件：复测报告_补签.md、**派发规格书_子步8收尾.md（v2修订版，覆盖你上次审的v1）**、归因五查_结果.md、复测_配置A.err、__运行记录_A.json。请通读v2与五查结果后终审。

【主会话对首门表决的回应：全量采纳】

票1四条件核销情况：
- 条件①（SHA配对语义）：配对双方＝原件docx（`C:\提示词\高中数学\高中数学同步\`）↔镜像目录（`子步7\mirror\`）**同名字节级副本**，十对十 sha256 逐对全等（差集清单.json original_vs_mirror_sha256 节在盘）；映射＝十件减清单1＝九件，清单1＝第1章知识清单件；镜像确为「换目录拷贝」，断言5实际覆盖＝路径无关性——附录照此写明，不夸大。
- 条件②（外部引用）：十件 `_rels` 扫描实测 `TargetMode="External"`＝**0条**（归因五查查②b，引用维度闭合）。
- 条件③（更优路径）：采纳且按你的排序——**第一跑＝SHA全同临时副本重跑**（自进程字节拷贝入tmp、哈希复核全等、锚定脚本原参数原配置、单件护栏120s；副本成＝观测闭合「同字节异路径实测全等」）；**随后第二跑＝诊断变体对原件**（锚定脚本复刻件＋逐件flush日志、放tmp、锚定件不动），用途＝「原件COM直开阻断」残留风险定性（挂则日志指明挂点件、成则原件直开观测亦闭合）。两跑均派qwen臂执行类形态，任务书内嵌十件绝对路径＋各自期望SHA、要求臂回显实开件SHA；只建副本、只写tmp、禁碰原件元数据。
- 条件④（防假PASS）：诊断变体逐件日志＝A分支实际进入Open的正面证据；静态走查已核：`_输入_配置A.json` 十件路径接线正确；脚本主循环无中途打印、结果集中末尾输出→0字节stdout无定位价值；stderr干净＝180s内无逐件异常抛出。
- 1.7采纳：附录按「实质需A＝断言5／仅措辞需A＝断言1/2／B＋独立层已闭合＝断言3/6/7」分列。1.8采纳：正文FAIL一字不改、改签仅附录追加；判定字面＝`PASS（等价链以原件↔镜像字节全同＋B对参照全等闭合；配置A为环境阻断非内容缺陷，见附件）`；双口签认；「原件COM直开在本机存在阻断风险」入残留风险登记。1.9采纳：九件同形全灭＝共性因素，不写「个别原件受阻」先验措辞。

【归因五查实测（只读子代理，归因五查_结果.md落盘）】19件（原件9＋镜像10）全无 Zone.Identifier；Attributes 全仅 ARCHIVE——脱水/占位/MOTW 全部证伪；原件目录 ~$ 锁 0 件；九件独占探测现时刻全 FREE（不排除A跑时瞬态占用）；C:\提示词 不在 OneDrive 命名空间。归因指向运行时瞬态（用户Word实例当时占用致不可见模态挂死／COM首启竞争）。

【票2：A1–A7、B8–B16 全量落实】v2逐条对应：
- A1→§〇⑦（90s护栏/≤2次尝试/挂即标环境阻断跳项禁原地重试/原件COM受阻改走SHA全同临时副本）；A2→§〇⑧（总墙钟45分钟、超限「未完成＋需裁决」上报继续下一项）
- A3→T1行型谓词四条＋判定优先级＋空行未归类单列＋Σ＝段落总数20恒等式（不成立上报dump与计数、禁凑数）
- A4→T2点名3式样：讲练2d「2.8.2-1」题型级白底加粗／讲练2d「2.8-1」随块C9C9C9／衔接1首个题号段，题号文本前缀定位
- A5→行4修法二元判定（读实物行15现文：含甲案→指向行15支；否则→题型级限定支）＋范围断言探针（讲练件存C9C9C9题号块则现文案失实、据实撰写禁套模板）
- A6→页数抽测点名：讲练2d期望70／衔接2期望5／讲练2b期望56；三件全挂则「未完成＋理由」结案
- A7→§三唯一许可贴行：只追加禁改原文、被贴件＋字面＋时间登入报告
- B8→前置硬门改依复测报告附录判定＋§六入复测证据件路径；B9→tmp清点glob面＋保护名单（复测_补签_codex全件与原件全同副本目录）；B10→自检八项第三态NA＋条款字面；B11→节级行不等即判目录行错、更正方向写死；B12→「确无该二级节标题」绑定差集清单表外0条；B13→括注不等双面记录（实物值＋导航表重算值）；B14→实得N件与19不符须列差异；B15→列头行数实测＝0；B16→删装订单md死权限（§一可改面收缩为两配页件）

【请终审表决】
票1r2＝闭环口径与两跑方案（副本跑优先、诊断跑定性）；
票2r2＝v2规格书可否派发。
逐项 同意/不同意＋理由；两票全同意即共识闭合——主会话随即派qwen臂跑第一跑、并派子代理执行子步8收尾。
