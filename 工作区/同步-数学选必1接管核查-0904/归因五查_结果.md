# 归因五查·结果（配置A COM开卷超时）　2026-09-04

执行：只读调查子代理（explore，多智能体）；全程未写改删任何文件、未触碰任何 Word 实例。
背景：`工具\节页码定位.py`（sha256 844d244a…de85）配置B（十件镜像）55.6s 全成；配置A（九件原件直开＋清单1走镜像）两撞 180s 护栏、stdout/stderr 无输出。两配置 docx 字节逐一全同（sha256 10/10）。

## 逐件结论表

| 短名 | MOTW(Zone.Identifier) | Attributes | 独占探测(共享=0) |
|---|---|---|---|
| 衔接1 / 清单1 / 讲练1上 / 讲练1下 / 衔接2 / 清单2 / 讲练2a / 讲练2b / 讲练2c（原件9件） | 全无 | 全仅 ARCHIVE | 现时刻全 FREE |
| 镜像10件（子步7\mirror\ 下同文件名） | 全无 | 全仅 ARCHIVE | — |

探测方法：MOTW＝ADS 直读 `路径:Zone.Identifier`；属性＝GetFileAttributesW 全位解码；锁＝CreateFileW(GENERIC_READ, 共享=0) 探后立即关闭。

## 附加事实

- `高中数学同步\` 下 ~$ 锁文件 **0 件**。
- C:\提示词、两目标目录均无 REPARSE_POINT，不在 OneDrive/同步盘命名空间（「同步-」仅目录名）。
- 19 件全无 OFFLINE / REPARSE_POINT / RECALL_ON_DATA_ACCESS / RECALL_ON_OPEN——云占位假设直接证伪。
- 十件镜像 `_rels/*.rels` 中 `TargetMode="External"` 共 **0 条**——换目录排版不可能引入不同外链，等价链在引用维度闭合（qwen首门条件②核销）。

## 归因判断

文件侧三大嫌疑（脱水占位／MOTW保护视图／此刻占用锁）全部排除；A/B 在文件内容与文件状态两维度无可区分差异。「A 全灭、B 秒开」形态最契合**运行会话侧/时机侧**：

1. 运行时瞬态占用→不可见模态挂死（最契合）：A 两次运行时刻某原件可能被用户 Word 实例打开着，不可见自动化实例弹「文件正被使用——以只读打开？」无限等待。此刻 FREE 不排除当时有锁。
2. COM 会话竞争：自动化实例与 5 个用户实例并存的首启初始化阻塞，B 恰未撞上。
3. 路径侧实时监控差异（杀软/索引）：无直接证据，备查。

## 静态补充（主会话）

- `复测_配置A.err` 仅包装器一行（78字节），无 COM 异常原文；`__运行记录_A.json` 无逐件计时——查⓪无法直接定位慢件。
- 脚本主循环无中途打印、结果集中于末尾输出（行299）→ stdout 0 字节无定位价值；stderr 干净＝180s 内无逐件异常抛出。
- `_输入_配置A.json` 十件路径接线正确（9 原件绝对路径＋清单1镜像）；`Documents.Open(path, ReadOnly=True, AddToRecentFiles=False)` 参数极简，DisplayAlerts=0 已设（行256）。
