# 派发规格书·子步10页数账·v1（⓪复合修复轮｜执行＝平台继承普通子代理｜qwen派发口快审后派发）

派发：主会话　2026-09-04　前置硬门：子步9已闭步（装订单406页口径重写落盘、双口过门）。
任务依据：任务书-子步7-10 行23-24「子步10 页数账（断言③a独立对账＋册数复评）」＋快审修订1/2/5＋公共规则§12三上限/§11分本制/§7；残留风险收窄（商议记录节二十）：**讲练1上原件 COM 直开确定性阻断**，本子步全程绕开该件直开。
本子步性质：**纯测量对账、零改动成品**（可改面为空），故免高危复测、报告直送放行口表决。

## 〇、纪律短版（内联，§1行20）

①中途禁止停等空转、禁以「等待确认」暂停；②不向用户提问——决策事项写入报告交主会话；③规则有明确答案照规则继续；④git 一律禁止（归主会话）；⑤报告落盘 `C:\提示词\工作区\同步-数学选必1复合修复-0903\报告-子步10.md`，摘要回传≤25行。⑥**COM 护栏**：Word COM 自建不可见实例（DispatchEx）、ReadOnly、用完 Quit；禁动现存 WINWORD×5（28012/26168/5988/13320/10308，用户实例）、禁全局杀进程（仅按差分名单逐 PID 清理自建实例）；单件开卷护栏 90s。⑦**降级路径（讲练1上强制、其余件触发式）**：讲练1上**禁止直开尝试**（0次——第二跑已两次确定性挂死于 Documents.Open，直接走副本）；其余9件各给 1 次直开机会，挂即对该件降级、禁原地重试、登记后跳下一件。降级＝自进程字节拷贝至 `子步10\tmp\原件全同副本\` → sha256 复核与原件全等并回显 → 开副本实测。副本只建只写 tmp、禁碰原件元数据。⑧预算与停止：总墙钟 45 分钟；任一项超限标「未完成＋需裁决」写入报告并继续下一项，到线未竟全功亦照此结案上报。

## 一、身份与权限

任务执行子代理（⓪复合修复轮·子步10），平台继承普通子代理。权限＝执行类：可写＝工作区本轮子文件夹（同步-数学选必1复合修复-0903\子步10\）；**可改面＝空**（禁改任何成品/台账/看板/工具/总控/附则；一次性脚本落 `子步10\tmp\`）。

## 二、先读（全文通读不豁免）

任务书-子步7-10（行23-24＋快审修订1/2/5）＋公共规则§7/§11/§12＋`高中同步总控.md` 行58＋新版装订单（`高中数学\高中数学同步\人教B版选必1·装订单.md`）＋`子步7\盖章记录_子步7.md`（**只作被对账方，禁止作数据源**）＋`复测_补签_qwen\复测报告_诊断跑.md`（阻断证据）＋`工具\本厚复核.py`（用法自读）。

## 三、任务（页数账＝独立对账→本厚复核→§12三上限→落盘）

**T1 独立对账（三方零差异）**：
1. 独立源重测：COM 逐件实测十件页数（讲练1上走§〇⑦强制副本路径；其余9件先直开、挂即降级）。实测记录须含：件名／路径形态（原件直开 or SHA副本，副本附 sha256 全等回显）／实测页数／开卷耗时。同次会话另读十件 sectPr `pgNumType@start`（可用 python-docx 读 XML，免 COM）得 start 实测列。
2. 三方对照（判据＝零差异）：
   - 实测页数列 vs **装订单**页数列（10行逐件）；
   - 实测页数列 vs **册目录页**件级行「·本N页」——注意聚合制（裁2定稿）：册目录页讲练件级行＝部分总（121/225），对照口径＝讲练上+下之和=121、讲练2四卷之和=225，其余6件逐件等；册目录页 dump 用 python-docx 只读；
   - start 实测列 vs 盖章记录 start vs 装订单「部分内页码区间」左端值（1/1/1,62/1/1/1,56,112,156 ↔ P1 1–14 等）三方一致。
3. 任一差异 → 停、如实列差异表入报告（**禁自行改任何文件找平**），交主会话裁决（修订3精神）。

**T2 本厚复核**：工具/本厚复核.py 按装订单「本」列逐本实测合计（14/13/121/5/28/225），逐本≤400 判✓/✗；装订单本列↔实测本划分一致性核对；结论与实测数字入报告（无数字视为未检——§11配页前检查⑤口径，但注意修订2：本子步只产数字，**不执行配页前检查任何项**）。

**T3 §12三上限全件实测**（修订5列集）：逐件登记 ①题数（文件名口径：29/47条/61/79/13/67条/92/90/68/89——清单件以「条」计）②页数（T1实测列）③体积（字节实测，两级：>50MB 预警登记、>80MB 上限）；判据：题≤100、页≤80、体积≤80MB；**任一件>50MB 须跑未引用媒体审计动作**（预期全部远低于，仍须实测值列表作证，禁预期式填写）。超限处置＝停报主会话另案拍板（禁自行拆件/重划卷界——用户2026-09-03拍板＋快审修订6）。

**T4 页数账落盘**：`子步10\页数账_子步10.md`——声明「A'旧记全部作废、以本轮重测为准」；全量表：十件×（题数｜页数实测｜start实测｜体积｜路径形态）＋三方对照结论＋本厚六行＋§12判定行。报告-子步10.md 附：完成/未完成分界、护栏与降级事件登记（含讲练1上副本路径执行证据）、WINWORD 差分核对（跑前/跑后快照、受保护五实例原样在位、自建实例已清理）、tmp 清单（副本与脚本列清单留主会话统一清点，勿删）。

## 四、边界明示（防越位）

- 本对账＝过程账，≠§13独立复审计（主会话另派异族审计代理按抽样配方执行——修订1）。
- 配页前检查①—⑥（含⑥成书视角通读终审）属子步11任务书，本子步禁越位执行（修订2）。
- 「其余7件原件直开」系未采样本：本子步9件直开结果即采样面扩展——直开成败逐件登记入报告（成功件即摘掉未采样本标记）。
- §12成因消失复评（公共规则行173）：若实测显示某拆分件三值均已降至限内，只登记复评结论建议入报告，**不执行并回**。

## 五、关键路径

成品＝`C:\提示词\高中数学\高中数学同步\`（十件清单照装订单取件序）；装订单＝同目录 人教B版选必1·装订单.md；册目录页＝同目录 人教B版选必1·册目录页.docx；盖章记录＝`子步7\盖章记录_子步7.md`；本厚工具＝`工具\本厚复核.py`；落盘＝`工作区\同步-数学选必1复合修复-0903\子步10\` 与 `报告-子步10.md`。
