# 商议记录（三脑共识门）——商议子代理切换 DeepSeek Harness
日期：2026-09-03　单位：体系级（公共规则 §1 三脑共识门条款修订）　轮次：2轮往返，三方绝对同意
来源：用户明示点名（拍板）——「联网搜索了解并安装官方deepseekharness，将三脑商议的三脑中的子智能体换成这个deepseekharness，模型选择deepseekv4flashvision思考强度选择最大」

## 版本预检实测值（§49⑧，dsh属本轮新引入外部软件）
- 安装：官方 npm 包 `@deepseek-ai/dsh@0.1.1-rc.2`（npm 全局，node v24.18.0 / npm 11.16.0）；官方仓库 github.com/deepseek-ai/deepseek-harness，官方页 deepseek.com/harness
- 无头调用形态：`dsh --profile headless "议题文本"`（一问一答即退出，与 kimi -p 同形态）；冒烟实测通过（返回正常）
- 配置实测（~/.dsh/settings.yaml → agent-default-model）：provider=deepseek-official、model=deepseek-v4-flash-vision-exp、reasoningEffort=max；凭证在位（~/.dsh/.credentials.yaml）
- 冒烟会话日志运行时真值：model=deepseek-v4-flash-vision-exp、reasoningEffort=max（非仅配置面）
- 换机或 dsh 更新后须按 §49⑧ 重查

## 议题与结论
| 议题 | 商议子代理观点 | kimi观点 | 结论 |
|---|---|---|---|
| 1. 商议子代理载体切换为 DeepSeek Harness（公共规则 §1:12 商议体定义） | 同意；模型名以带 -exp 的运行时实测真值为准（题干无 -exp 写法以实测消除歧义）；新轮次商议记录列名沿用「商议子代理观点」（角色名非工具名，防术语分叉） | 同意；-exp 差异加「以运行时实测值为准」注防两词族 grep 互判漂移；「未装先装」补官方包名（npm i -g @deepseek-ai/dsh）防 npm 撞名装错；双侧带括号规格消除旧文括号只挂 kimi 的含糊 | **按合并方案改 §1:12 一处**（最终文字见下） |
| 2. 切换边界（不动项） | 任务子代理（§1:19-21 检查/审计/修复/装配执行代理）不切换，仍走原 Agent 工具——用户点名范围仅三脑商议的子智能体；历史商议记录「商议子代理观点」列名不动（沿革注记） | 同意；逐字核对拟改仅触商议体定义一处；全库 grep 实证「商议子代理/kimi -p/k3-256k」活句仅 §1:12 一处 | **边界确认：仅改商议体定义，其余条款与历史记录不动** |
| 3. 配套与合规 | 商议记录落盘新轮文件夹；§53 两道体检＋§4 整体提交；dsh 调用失败衔接 §54 前台重试≥3次、无法召集走 §1 停等条款 | 同；另提 §49⑧ 版本预检实测值须落盘（本文件上部已落）；体检①本轮无废止词族，grep 重点复核全库无「Agent工具承担商议」类旧描述残留 | **配套采纳：预检值落盘本文件＋两道体检＋整体提交＋既有重试/停等条款直接适用** |

## 最终修订文字（§1:12 商议体定义）
- 现文：商议体＝商议子代理＋本机kimi（kimi -p、k3-256k、思考max、未装先装）
- 改为：商议体＝商议子代理（DeepSeek Harness：dsh --profile headless、deepseek-v4-flash-vision-exp（模型名以运行时实测值为准）、思考max、未装先装（npm i -g @deepseek-ai/dsh））＋本机kimi（kimi -p、k3-256k、思考max、未装先装）

## 调用层操作约定（不入条款文字）
- dsh --profile headless 为无状态一问一答：三脑门多轮往返＝主会话每轮把完整往来历史内联进议题文本再问（与 kimi -p 同约定）。

## 往返核实
- 第1轮：主会话发议题＋预检实测＋拟改文字→商议子代理「同意」＋2条（-exp实测真值／列名沿用）＋1条调用层语义确认；kimi「同意」＋2条（npm包名／§49⑧预检落盘）。
- 第2轮：合并方案发两方最终确认→商议子代理「同意，可拍板执行，无剩余阻塞意见」；kimi「最终确认：同意，无异议，可执行」→三方绝对同意，共识达成。
