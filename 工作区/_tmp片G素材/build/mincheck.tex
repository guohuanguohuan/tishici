% 最小依赖验证：除 tikz 外不加载任何宏包（无 fontspec/xeCJK），验证片段"仅依赖 tikz"成立
\documentclass{article}
\usepackage[papersize={60mm,60mm},margin=2mm]{geometry}
\usepackage{tikz}
\pagestyle{empty}
\begin{document}
\input{image2_重绘.tex}
\end{document}
