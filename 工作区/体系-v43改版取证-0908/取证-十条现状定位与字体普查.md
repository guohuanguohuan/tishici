调查完成（只读，未改任何文件）。以下按十条逐条给出「现状（file:line）＋根因＋杠杆」，行号均为当前文件实测。文件均位于 `工作区/全品结构提取/数学选必一/样张v4/导学件/`（下文省略前缀）。

## 十条定位简报

**1. 「◆探究点一」与「例1」连排同行**
- 现状：宏 `\tjdnr`（qp-blocks.tex:57-61）——`◆探究点#1`、`{\heiti #2}`（例1）、8pt 灰题侧、`#4` 题干全部写在**同一个** `\noindent…\par` 段落内；组装点 postproc_daoxue.py:529（有图变体，题干传空）与 ：534（无图变体）。
- 根因：这是 v4 拍板16/28 的刻意设计「◆标题与例1 题侧标签、题干同段接排」（qp-blocks.tex:53 注释、交付报告.md:23 §二-9），不是排版事故——标题后只隔一个 `\quad` 就接例1。
- 杠杆：只动 `\tjdnr` 一处——在 `{\heiti #2}`（:59）之后插 `\par\nopagebreak\noindent`（或把题干拆成第二个段落），宏签名 4 参不变，postproc 两个调用点零改动。注意这是翻拍板16/28，需授权；连带影响＝分页整体漂移，④⑤⑦需重测（⑥/⑲计数不受影响；「例1〔」行不匹配断言顶格签名集，无连带）。

**2. 花形行现值清单（qp-blocks.tex:38-49）**
- 菱形 `\huadia`（:39-40）：tikz diamond、白底、边线 0.5pt、**inner sep 1.5pt**、字 **10pt/12pt \heiti**。
- 行宏 `\huaxing`（:46-49）：前距 `\addvspace{4pt}`；四字间距 **\hspace{-2mm}**（:47）；右小字（:48）＝**6.5pt/8pt、\color{midgray}(777777)、常规字重（无 \bfseries）、主字体 SimSun**、`raisebox{0pt}`；`\nopagebreak`＋**`\vspace{1pt}`**＋底线 `{\color{midgray}\rule{\linewidth}{0.7pt}}`（:49）；后距 `\addvspace{3pt}`。
- 即 v4.2-A7 收过后的现值＝inner sep 1.5pt／右小字 raisebox 0pt／底线距 1pt，与登记一致。
- 杠杆：底线线宽改 ：49 的 `0.7pt`；菱形字与底线垂距改 ：49 的 `\vspace{1pt}`；右小字字号/字重改 ：48 的 `\fontsize{6.5pt}{8pt}`（加 `\bfseries` 即加粗）。

**3. 小字体系与 9pt 连带面**
- 现状：编注 `\zhuzhu`＝8pt/11pt 黑（qp-blocks.tex:37，使用点 body.tex:61、63、65、77 等）；判断解析行＝`\zhenti` 第 4 参 8pt/11pt 黑（qp-blocks.tex:87）；简析 `\jiexi`＝8pt/11pt 黑（:89）；另题侧〔难度（知识点N）〕也是 8pt 但为 midgray 灰（qp-blocks.tex:60、:67、postproc_daoxue.py:576-578）。body 内无「【注意】」字面，此类编注全走 `\zhuzhu`。
- 9pt 连带影响面：改 3 处宏（:37、:87、:89；若题侧同升再加 ：60/:67/postproc:578）＋**断言⑰**（_测v4断言.py:360 越档窗 `7.5<=size<=8.5`、:363 `\zhuzhu` 定义级 8pt 正则）必须同步改窗与正则；⑥的【解析】×20 计数不受影响；②行距主峰只统计 10–11pt 行，8→9pt 无连动；断言顶格 (c) 的【解析】签名行仍顶格。风险点：`\zhenhead` 说明文字已是 9.5pt（:83），编注升 9pt 后与 9.5pt 层级近乎齐平；行数增多→分页漂移→④⑤⑦全量重测。

**4. 「投影向量」条目（1）未另起行**
- 现状：知识点三·条目3，body.tex:65＝`\tiaomu{3}{投影向量：（1）在空间…投影：\zhuzhu{…}}`——**（1）与条目号同段连排**；（2）（3）已各自成段（body.tex:69、:73，`\bindp`）。
- 漏掉原因：C11 修复（postproc_daoxue.py:289-292）是**针对条目2 的手工硬拆**（逐条写死 em 行），不是通用 pass；条目3 走通用路径 ：373 `em('\tiaomu{3}{投影向量：'+blk8[0]+…')`，而源条目8 首块（sec.tex:221）本身以「（1）」开头，被原样并进条目号段。log 5c 的「全件扫描」（postproc:308-309）只核了（2）（3）是否已分段，未核「首块以（N）开头且接在条目号后」这种形态——pattern 差异＝**条目2 的（1）紧跟号后无前置文字（授权形态），条目3 的（1）前面有「投影向量：」正文**。
- 断言级防漏思路：python 级——postproc 第 8 步对 body_text 加一条 assert：正则 `\tiaomu{\d}{<非（N）开头的文字>…（\d）` 命中数==0（条目2 因参数以（1）开头自然豁免）；grep 级——body.tex 上跑同一正则；或 pdf 层在 _测v4断言.py 加检查：行首签名 `^\d．` 的行内出现（N）子项标且（N）前有 ≥2 个汉字。

**5. 投影向量三联图（sub3_B_4.png）**
- 现状：FIG 字典档位＝`('center','45mm')`（postproc_daoxue.py:115，注释「v4.2-A6 维持」）；居中独立行输出在 reflow :152-153。p2 左栏目测（png/page2.png 实测裁片）：三联图整体仅 45mm，单幅约 14mm，①②③角标字极小，观感偏小。
- 可提档：栏宽 86.2mm，居中档可提到 **55mm／60mm**（60mm 已近 0.7 栏宽，视觉与三组 minipage 图≈34mm 档明显区分）。连带：图高增加会压 p2 版面（⑤b 登记的 p2 左 32.3mm 栏末空本就偏紧），表3／课中探究花形线位置会后移，改后需重跑双断言。

**6. 加粗体系现状全集**
- `\heibf`（FakeBold=2.2）：声明 qp-fonts.tex:13、:16；**仅 4 级标题用**——章 22pt（qp-titles.tex:16）、节 18pt（:20）、小节 15pt（:22）、课时 14pt（:25）。**xeCJK 全局 AutoFakeBold 未开启**：qp-fonts.tex 无该选项，main.log 仅 1 处 FakeBold（:934，heibf 族局部选项）。
- 其余加粗元素全部是 SimHei 原字重或 BoldFont 映射：例N＝`\tjdnr` 内 `{\heiti #2}`（qp-blocks.tex:59）；变式N＝`\li` 内 `{\heiti #1}`（:66）；条目号＝`{\heiti #1}．`（:35，G20）；检测题号＝postproc 输出 `{\heiti N．}` ×5（postproc:592、604、614、624、634）；【】标签＝`\biaoqian{\heiti …}`（:16，用于【学习目标】qp-titles.tex:28、【答案】:76、【素养小结】:79、【诊断分析】:83）；◆知识点/◆探究点＝`\bfseries`（:52、:58，经 SimSun BoldFont=SimHei 映射到 SimHei）；表头＝`\textbf`（postproc:247）；页脚小字与页码＝`\bfseries`（qp-headfoot.tex:26、28）。
- 推广路径：把上列 `\heiti`/`\bfseries` 位换 `\heibf`（约 10 处宏体），或仅调 qp-fonts.tex:13 一处的 FakeBold 值。风险：10.5pt 正文级 SimHei＋FakeBold 笔画易粘连糊字（四级标题 14–22pt 尚有余量，题号/标签 10.5pt 需逐级目检）；墨密实测口径（0.292→0.391，qp-fonts.tex:7 登记）作废需重测；断言连动＝⑲ 的 `{\heiti #1}．` 正则与 `{\heiti N．}` ×5（_测v4断言.py:400-403）及 postproc:718/:735 计数；断言① overfull 需复测（FakeBold 略增字宽）。

**7. 行距与字体＋系统字体普查**
- 行距：`\normalsize=10.5pt/18pt`（qp-layout.tex:26）＋`\linespread{1}`（:27，撤 ctex linestretch 1.3）；全件无其他 `\baselineskip` 改动。
- 字体声明（qp-fonts.tex:10-14 抄录）：`\setmainfont{Times New Roman}`；`\setCJKmainfont[BoldFont=SimHei]{SimSun}`（正文＝SimSun，粗体映射 SimHei）；`\setCJKfamilyfont{hei}{SimHei}`（标题黑体）；`\setCJKfamilyfont{heibf}[FakeBold=2.2]{SimHei}`；`\setCJKfamilyfont{kai}{KaiTi}`。
- 系统普查（fc-list :lang=zh family 实测，fc-list 在 TinyTeX 自带）：**中易全系** SimSun/NSimSun/SimHei/KaiTi/FangSong ✓；**华文系** STSong/STZhongsong/STKaiti/STFangsong/STXihei＋装饰类（华文琥珀/隶书/行楷/新魏/彩云）✓；微软雅黑（含 Light）✓；等线 DengXian（含 Light）✓；**方正仅舒体/姚体**（无书宋、无方正黑体）；**思源黑体系＝Noto Sans SC**（可变字体，Thin~Black 全字重，C:\Windows\Fonts\NotoSansSC-VF.ttf）；**无思源宋体/Noto Serif SC**；`%LOCALAPPDATA%\Microsoft\Windows\Fonts` 为空。TinyTeX texmf 内**带 Fandol 全套**（kpsewhich 实证 `texmf-dist/fonts/opentype/public/fandol/`：FandolSong-Regular/**Bold**、FandolHei-Regular/**Bold**、FandolKai、FandolFang）。
- 结论（真实可选）：①**FandolSong**——texmf 自带、有真 Bold 面（补 SimSun「伪粗」短板），换字体零网络依赖；②**华文中宋/华文宋体**（STZhongsong 印刷感优于中易宋体）；③标题换 **Noto Sans SC**（真字重黑体，替代 SimHei/FakeBold）；④等线/微软雅黑作现代黑体备选。方正书宋/方正黑体、思源宋体本机无，需另行取字库文件。换字体只动 qp-fonts.tex:10-14 五行，但须复测 Missing character（断言①）与 ③灰档（无连动）。

**8. 题号辨识度（【答案】/【解析】→下一题间隔）**
- 现状：`\ansline`＝普通 10.5pt/18pt 段（qp-blocks.tex:76，前 glueguard{2} 无实距）；`\jiexi`＝8pt/11pt 黑，**上下零额外间距**（:89）；判断解析行同（:87）。下一题起距：`\jiancestem` 仅 `\addvspace{2pt}`（:91）、`\li` 2pt（:65）、`\tjdnr` 3pt（:57）——即解析行→下一题号实际缝≈一行 18pt＋2~3pt，且题号（10.5pt \heiti）＋8pt 灰题侧与 8pt 解析行字号接近，视觉分隔弱。
- 目测最挤一例：**p6 检测3【解析】→检测4 题号**（png/page6.png 裁片证实，缝≈20pt 无空行感）；次挤 p5 变式1【解析】→◆探究点九（仅 3pt 前距，png/page5.png）。
- 杠杆：`\jiancestem`/`\li` 的 `\addvspace{2pt}` 提到 6~8pt，或在 `\jiexi`（:89）尾部加 `\addvspace`；题号升 `\heibf` 见第 6 条。连带：p5 版面已满（⑤a 95/97%），加距会引起分页漂移，p6 有余量（68/75%）较安全。

**9. ★撤除落点（恢复路径）**
- 现状：`\tjdnr` 已是 4 参（qp-blocks.tex:57-61，:54 注释登记撤 #4 ★位）；STAR 字典已撤，仅在 postproc:429-432 留撤除 log 登记（原 :412 区域现为注释）；断言⑥★口径＝0：postproc:712 `n_star=count('典型性理由：')`＋:735 `assert n_star==0`，_测v4断言.py:184 计数＋:186-187 门 `n_star==0`。
- 若恢复★需动四处：① qp-blocks.tex:57-61 `\tjdnr` 加回第 5 参（★块＋理由排版位）；② postproc 重建 STAR 字典并恢复 ：529/:534 调用传参＋COMPOSED 重新收入理由文字（:482 起分号对账联动）；③ postproc:735 与 _测v4断言.py:187 两处 ★ 断言翻 0→3；④ 理由文字字级建议按新口径重定（原 6.5pt 灰已与 v4.2 小字 8pt 黑体系冲突）。

**10. 行距对比材料出处**
- 现成品「行距主峰 18.0pt×6 页」的**纸面出处＝交付报告.md:174**（§9.4 断言摘要；§五-2 :52 为 v4.1 轮同值记录）。main.log 内**无**此值——主峰由 `_测v4断言.py:74-112` 每次从 main.pdf 基线差直方图现算，仅打印不落盘。设定出处＝qp-layout.tex:26-27。对照材料：全品实测 17.7pt 档（测评卷/交付报告.md:62 拍板28 复审）、演示件 17.9pt（滚动小卷/交付报告.md:65、章末总结:59、衔接预备:59）——现 18.0pt 与全品仅差 0.3pt，若再压行距杠杆在 ：26 的 18pt 与断言② 17.5–18.5 窗（_测v4断言.py:93）。

## 杠杆汇总表

| 条 | 文件 | 宏/参数 | 行号 | 影响面 |
|---|---|---|---|---|
| 1 | qp-blocks.tex | `\tjdnr`（#2 后插 \par） | 57-61 | 翻拍板16/28；分页漂移→④⑤⑦重测；断言⑥⑲不动 |
| 2 | qp-blocks.tex | `\huaxing` 右小字 6.5pt（:48）、底线 0.7pt/\vspace 1pt（:49）；`\huadia` inner sep 1.5pt（:39） | 39-40, 46-49 | 断言⑦(b)(c) 缝检；③灰档不动 |
| 3 | qp-blocks.tex | `\zhuzhu`:37、`\zhenti` 解析：87、`\jiexi`:89（题侧 ：60/:67/postproc:578 可选） | 37, 87, 89 | 断言⑰（_测v4断言.py:360,363 双改）；分页漂移→④⑤⑦ |
| 4 | postproc_daoxue.py | 条目3 组装 ：373 拆（1）段＋第8步加 \tiaomu 内嵌（N） assert | 371-374, 289-292, 308-309 | 新 assert；body.tex 重生成 |
| 5 | postproc_daoxue.py | FIG['sub3_B_4.png'] 45mm→55/60mm | 115 | p2 版面重排；⑤b/⑦复测 |
| 6 | qp-fonts.tex:13,16＋qp-titles.tex:16-25＋qp-blocks.tex:35,52,58,59,66,76,79,83＋postproc:592-634 | `\heibf` 推广或 FakeBold 值 | 见左 | 断言⑲（_测v4断言.py:400-403）、postproc:718/735、断言① overfull、墨密口径重测 |
| 7 | qp-fonts.tex | 字体族 5 行（Times/SimSun/SimHei/heibf/KaiTi） | 10-14 | 断言① Missing char；候选＝FandolSong/华文中宋/Noto Sans SC |
| 8 | qp-blocks.tex | `\jiancestem`/\`\li` addvspace 2pt（或 `\jiexi` 尾加距） | 91, 65, 89 | p5 分页漂移→⑤；p6 余量大 |
| 9 | qp-blocks.tex:57-61＋postproc:429-432/529/534/712/735＋_测v4断言.py:184,187 | `\tjdnr` 回 5 参＋STAR 字典重建＋★断言 0→3 | 见左 | ⑥、COMPOSED 分号对账 |
| 10 | qp-layout.tex:26-27（设定）；交付报告.md:174（记录）；_测v4断言.py:74-112（计算） | \normalsize 18pt | 26 | 断言② 窗 ：93；全件分页 |

两条提醒：第 1、9 条均涉翻既有拍板（16/28 与 25），任务书里需带授权语句；第 3、6、7 条任一改动都会引起全文分页漂移，双断言（_测v4断言.py＋断言顶格.py）须整链重跑并重出 6 张 png。