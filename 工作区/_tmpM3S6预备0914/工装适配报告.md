# M3 S6 装配工装适配报告（装配前置研制·不装正件）

日期：2026-09-14｜执行＝M3 S6 装配工装适配臂｜依据＝`_tmpM3S6预备0914/装配方案.md` 全文（§一/§二/§三/§五）
红线自查：零 git（全程未运行 git 命令）；成卷件全只读（沙箱试装走副本树，源 copy 只读）；写入域＝`成卷/装配/工装/`（自建）＋`_tmpM3S6预备0914/`＋沙箱 `_tmpM3S6沙箱0914/`（自建）；写前核 mode＝auto。

---

## 〇、口径变更（否决窗·军师 §九报备义务）

1. **基线漂移回填 2 片**（A1 红旗首件实证）：导学 课时02 true 5→**6** 页、课时14 true/false 18/8→**19/9** 页（换装轮 0914 改件后在位实测；练习 21 片零漂移）。工装基线已按在位件刷新并在 `装配三本.py` 头注留痕；**S2 收验报告 §1.4 基线回填义务移交收验侧**。本三实测页数（拓展上 28/11、拓展下 34/12、测评 4/3、滚A 3/2、滚B 3/2，合计 true 71/false 30）超 §一.1 预估（≈35/≈19），系 S4 出口实数，装订单页码区间以实测回填（§一.1「最终以装配实测为准」条款覆盖）。
2. **卷件页脚自印序随注入连续**（新发现，见 §三.1）：卷件「卷01/02 卷」系 `\thepage` 驱动，装配注入后自动入连续链——D1 断言覆盖全册含卷件域，较方案 §三.2 预期更强。装配层零回改件内结构 ✓。

## 一、交付工装三件（`工作区/M3-第2章量产0913/成卷/装配/工装/`）

| # | 件 | M2 先例→M3 适配要点 |
|---|---|---|
| ① | `终验页脚.py` | `_tmp装配装订0912/终验页脚.py` → ①BOOKS 三本参数化（起页/页数/件制改读 `装配读数.json` 同源真源，不硬编码）；②三位数页码域（§三.3）：want≥100 须页脚带完整三位精确匹配（无容差），<100 沿 ±1；③册目录页不计页豁免：`--tocpdf` 逐页断言页底 60pt 带无数字；④D2 标识链按页码奇偶 recto 形（奇）断言章名＋件名（verso 形页脚＝页码＋丛书名，由 D1 覆盖，沙箱实证后修订）；⑤书签 outline 独立复核 |
| ② | `装配三本.py` | `装配四本.py` → ①取件序 46 件参数化＝导学 21＋练习 21＋本三 5（canonical 序：衔接→01~06→**06B**→07~19；本三内序＝拓展上→拓展下→测评→滚A→滚B，U1）；②副本树剔除 png 目录/aux/log/pdf/py/md/json，**figs 随目录**（§一.2）；③`\setcounter{page}{起始页}` 注入（起页由基线页数自动累计跨本连续，不手填；壳文件自动解析 `\input{main.tex}` 落注入位）；④编译入口路由：ke 件制＝`main-true/false.tex` 双壳、juan 件制＝`main.tex`/`main-pure.tex`（§四双档各跑一遍）；⑤pypdf 合并＋`add_outline_item` 47 书签断言（21/21/5）；⑥A1 逐件页数 vs 基线漂移即 FAIL 红旗退出；⑦E2 每副本 qp-m3.sty md5 钉 `7c393036…` 断言；⑧产出 `装配读数.json`（工装①唯一输入）；`--sandbox` 态写入沙箱、正式态写 `成卷/装配/` |
| ③ | `册目录页骨架-main.tex` | M2 `册目录页/main.tex` 六宏零改动复用 → 三级新结构（§二）：本带 `\tocchapter` 01~03（无页码列）→节行 `\tocsec`（衔接/2.1~2.8/章末＝10 节行/本，页码 P 前缀）→件型行 `\toclesson`/`\toclink`；行数＝32＋32＋6＝**70 行＋3 带**；`\pagestyle{empty}` 不计页；页码列＝true 基线占位（正式＝装配实测回填，节行由 `节页码定位.py` 生成，§二.1「先内容后页码」）；false 版另出（结构同构仅页码列换源） |

## 二、沙箱冒烟读数（`工作区/_tmpM3S6沙箱0914/`，46 件真件副本试装双档全绿）

- **页数链（A1/A2/A4）**：true＝本一 156（起 1）＋本二 90（起 157）＋本三 72（起 247）＝**全册 318 页**；false＝75（1）＋43（76）＋30（119）＝**148 页**；逐件实测＝基线 46/46（红旗下基线刷新后）；false≤true 46/46。
- **编译卫生（E1/E2）**：46 件×双档×2 遍＝**184 编**，err/Overfull/Missing character/rc **全 0**；46×2 副本 qp-m3.sty md5 钉值 assert 全过。
- **书签（D3）**：21/21/5＝**47 条×双档**（outline 独立复核 ✓）。
- **页脚链（D1/D2）**：true 1~318 连续（含卷件域 10 页）、false 1~148 连续（卷件 7 页）全无断点；三位数域抽验 true 219 页/false 49 页精确匹配全过；recto 形页标识链（「第二章 平面解析几何」＋导学件/练习件/拓展册（上/下）/测评卷/滚动卷）全过。
- **册目录页豁免**：骨架试编产物喂 `--tocpdf`——3 页页底均无页码 ✓ 不计页。
- **骨架 3 页制（§七.6）**：初编 4 页（本一溢末 1 行）→ 降级顺位①页首留白压缩（13.58/17.86→2.0/6.0mm＋带上收 -5.0mm，宏层零改动）→ **3 页达标**（p1 本一末行 150／p2 本二末行 242／p3 本三末行 316），已登记入骨架头注。

## 三、沙箱暴露的新况（正式装配轮注意）

1. **卷件页脚自印序随注入连续**：`卷NNN/NNN 卷` 形态（如「卷309」「310 卷」）＝`\thepage` 驱动，装配后自动成连续页码列——三本页脚链全册统一，成书翻卷件亦可辨页位（优于 §三.2 预期）；终验工装已按此统一断言。
2. **奇偶页脚随页码计数器而非物理位**：false 档本二起 76（偶），实体本二首页纸正面印 verso 形页脚（页码＋丛书名，无章名件名）——G7 方案B 跨本连续制固有形态（M2 答案本起 90 同先例），装订形态归打印执行人裁（U3）时知悉。
3. **基线漂移哨价值实证**：首跑 A1 红旗精确捕捉换装轮改件（课时02/14），机制按设计工作；正式装配轮凡收验基线与在位漂移，以装配 FAIL 报表为信号回填两侧。

## 四、断言族覆盖对照（§五清单：本轮机证范围）

- **已机证（沙箱全绿）**：A1 逐件页数、A2 本合计（156＋90＋72＝318／75＋43＋30＝148）、A3 逐本 ≤400（156/90/72 与 75/43/30 全过）、A4 false≤true 46/46、D1 页脚连续双档＋三位数域、D2 标识链＋三位数抽验、D3 书签 47×2、E1 184 编三零、E2 sty md5 钉。
- **待正式装配轮**：B 族键数（572/336/测 19/滚 16×2——对号门/对平门/印面计数随装配复跑）、C1/C2 序位三单一致机扫、E3 印前销案门（S2-A 6 项＋S3-A 2 项＋图债回冲未销不装配）、E4 配页前检查①~⑥；册目录页页码列正式回填（件级＝装配实测、节级＝节页码定位.py）＋false 版另出。

## 五、残余与移交

1. 本三页数实数（71/30）与 §一.1 预估差（≈35/≈19）→ 装订单「本三」列按实测回填（表已在 `装配读数.json`）；S4 若再变动四件卷/册，重跑 `装配三本.py --sandbox` 即可复验（工装取件序/入口路由已含本三五件）。
2. 册目录页骨架为**研制件**：页码列占位待回填，六件套编译依赖拷贝步骤见头注③；3 页制在 M2 六件套下已验证，正式件随装订单页数定稿复验。
3. U2（拓展册上下界）已按现行两册实测承接；U3 装订形态、U4 本二压缩预案仍待裁（工装双预案支持：3 页制已达标，压缩预案不需触发）。
4. 沙箱留证：`_tmpM3S6沙箱0914/装配/{true,false}/`（副本树＋三本 PDF＋装配读数.json）、`册目录页试编/main.pdf`；装配全报表见后台日志（92 件次逐件行）。
