# 体系修订提案（2026-09-03主会话拟）——跨脑复审计制（执行验收层异族交叉）

来源：用户问「子智能体完成任务时有没有其他大脑检查的规则」→主会话评估：§13独立复审计条款存在，但复审计「新审计子代理」默认仍是k3（kimi族）＝干活的和查错的同族、相关性盲区；qwen扩编轮已论证「不同模型族相关性误差更低」。用户拍板：「补」。

## 修订案（3笔；公共规则217恒等零新行；附K1块轮值指针）
案1　L180独立复审计句：「（多智能体模式下由未参与原任务的新审计子代理执行——§1）」→「（多智能体模式下由未参与原任务的**异族**审计代理执行——**跨脑复审计制（2026-09-03立）：复审计者与任务执行者不得同模型族**；族别判读＝实测model字段——kimi-k*/kimi-for-coding＝kimi族（k3属之）、qwen*＝qwen族、gpt-*＝gpt族、opencode系模型ID（nemotron/ling/mimo/muse/big-pickle等）＝opencode族；执行态默认k3（kimi族）→复审计按qwen→codex→opencode固定轮值、指针登记看板K1块；用户明示其他执行形态时同样避同族——§1）」。
案2　L180高危复测句：「由未参与代理（复测类权限）重测」→「由未参与**且异族**代理（复测类权限、只读全开）重测」。
案3　L19：「独立复审计（§13）由**未参与原任务的新审计子代理**实测重测」→「独立复审计（§13）由**未参与原任务的异族审计代理**（跨脑复审计制——§13）实测重测」。
案4　看板K1块尾追加：「**跨脑复审计轮值指针（2026-09-03立）**：当前＝qwen（用毕顺延codex→opencode循环；复审计调用的model族别随K1凭证摘记核验，异族不合规＝该次复审计作废重做）。」

## 口径说明
- 单会话模式（用户明示「单会话」）不适用——多智能体不生效时照旧「本会话脚本重跑＋换角度亲算复核」。
- 制定轮自身复测（L180尾「由未参与商议的执行体当场复测」）**维持k3执行体不动**：五脑全表决即无「未参与商议的异族脑」，且制定轮已有门内实证核验（M6式五值取证＋词族扫描）兜底——不与本案冲突。
- 抽样配方（1高危＋1最短耗时＋1真随机）与同册横向diff不变，仅换执行者为异族。
- 成本注记：复审计自k3改轮值脑，单轮审计调用成本上升（qwen＝token计划、codex＝订阅、opencode＝免费）；如需成本熔断另案。
- 词族体检：新词「跨脑复审计」预期分布＝公共规则L180×1＋L19×1＋看板K1×1；「异族」同三处。

## 附：改前原文（qwen M4流程承诺——上轮起派发随附pre原文）
L19现文：「…子代理一律禁止 git。独立复审计（§13）由**未参与原任务的新审计子代理**实测重测，主会话比对两份报告差异并裁决——子代理自检结论不得作唯一放行依据。」
L180现文两锚：「①…必须随机抽自检记录≥3项实测重测（多智能体模式下由未参与原任务的新审计子代理执行——§1）（候选：页脚域形态与缓存…」／「…任何计数改写——命中者任务完成当场由未参与代理（复测类权限）重测，不等阶段7；复测须复用同一脚本…」
看板K1尾现文：「…③GitHub不可达时经Clash控制API自切节点（secret从配置文件读、实密钥不入看板）。」

## 请五脑表决
同意/不同意＋必改（编号自A1起避免与既往M/N/Q串号）。主会话（召集人）票：同意。
