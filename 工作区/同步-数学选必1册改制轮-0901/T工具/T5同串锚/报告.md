# T5（工具债⑥）全量报告——同串挂载＋节名锚挂载两工具改版与副本实测

> 代理：A'改制轮 T5（工具债⑥）｜日期：2026-09-01｜工作区：工作区\同步-数学选必1册改制轮-0901\T工具\T5同串锚\
> 任务基准：规格书§2工具债⑥＋口径F/口径G；规则源：公共规则§7页面条款（页脚款/页眉款/节名锚机制＋域行为实测注记五条）。
> 精读范围：公共规则全文＋高中同步总控全文＋ai自行经验积累全文＋规格书全文（多智能体通读义务）。

## 1. 两工具改造点

### 1.1 工具/页眉面包屑挂载.py（整体改版；旧「右停靠制表位＋STYLEREF"标题 3"」级B形态退役）
- **节名锚段（口径G）**：每个挂「标题3」样式段落（＝节标题，styleId按name=heading 3解析，实测'Heading3'）之前插入锚段；样式新建 styleId=`JieMingMao`/name=`节名锚`（STYLEREF按样式名解析）：sz=2半点(1pt)、color=FFFFFF、无w:vanish（非隐藏——注记①）、spacing line=20 lineRule=exact（固定1磅）＋before/after=0、jc=left、无shd/pBdr、无keepNext/keepLines/pageBreakBefore（§7⑥禁排属性）；锚段另带同款直接格式双保险；**basedOn＝该件styles.xml真实Normal styleId（实测='a'，非字面量悬空——2026-08-31定论）**；锚段内容＝「节号 节名」＝节标题原文剥离统计段（regex `（第\d+[—–-]\d+题|　?本节\d+题`）；断言锚段数=节标题数且逐节标题直接前驱。
- **页眉页脚同串重建（口径F）**：两部件整体替换为同一单段（jc=left、全run 18半点、宋体/TNR）：串＝`[羿郭工作室·]册名 第X章 章名·件型（共N页）　节名域　第X页`——X＝PAGE复杂域（begin/instrText/separate/缓存run/end，禁fldSimple），缓存初值＝sectPr现start；节名段＝STYLEREF复杂域（instrText=` STYLEREF "节名锚" `），缓存＝首锚段文本；N＝'N'占位符（盖章阶段填实值）；件型token参数化（`--token 第X章·衔接/清单/讲练`，工具校验既有页眉前段含章段且以件型尾段收尾）；前段取自既有页眉文本（整体替换旧左串页眉与「件标识（共N页）　第X页」旧页脚）。
- **版面**：pgMar header=283/footer=850强制；恰1个headerReference＋1个footerReference（A1断言）；剔titlePg与evenAndOddHeaders；settings确保updateFields。
- **串宽实测断言（A5）**：字符宽估算（CJK/全角=180缇、ASCII=90缇、N/X按3位最坏值、节名按最长锚）超版心10206缇即省「羿郭工作室·」前缀；另Word COM实测worst-case串行数（ComputeStatistics(wdStatisticLines)，A4＋1.5cm边距草稿doc，宋体/TNR 9pt）＝1；带前缀实测>1行自动降级省前缀重测。**实测发现：B估算10170≤10206判可容，但COM实测带前缀=2行——估算偏乐观（混合中西文实际渲染更宽），COM断言是必要兜底；B自动降级省前缀**。
- **幂等**：锚段在位且计数相符→跳过插入；同串在位→前段截「（共」前重推导；确定性重建；实测X2双重挂载输出zip成员逐字节相等（DIFF=0）。
- **同部分一致性参数**：新增`--no-brand`（部分内任一卷实测放不下品牌前缀时，各卷强制同形态省前缀——B/C本次实测即此情形）。
- 安全断言A1—A5全过才写输出；A3＝除document/styles/settings/页眉/页脚五类目标件外其余zip条目字节逐一相等。

### 1.2 工具/册级连续页码.py（适配新同串；既有职责审计兼容）
- **退役语句（审计结论）**：`rebuild_footer`/`_run`/`page_field`/`RPR`/`esc`（旧页脚整段重建「件标识（共N页）　第X页」）——与新同串不兼容（工具无册名/章名重建前段、会毁STYLEREF域），且整段重建职责已上收挂载工具；删除。
- **保留语句（0901幂等盖章法兼容）**：`measure`（COM只读实测页数）、`load_parts`（部分划分读取）、`stamp_document`（pgNumType start/footers850/剔titlePg）、`ensure_update_fields`、不动点迭代（实测→盖章→复测，上限5轮）、--record记录格式——全部原样保留。
- **新盖章语义**：①`flush_page_cache(xml,start)`——PAGE复杂域缓存run刷写（经验定论：PAGE域缓存不随sectPr start自动更新），**页眉页脚两处同步写该件start实测值**；②`set_part_total(xml,n)`——「（共N页）」处占位/旧值→部分总页数实测值（两部件同步，恰1处断言）；③节名STYLEREF域缓存不动（Word/打印/PDF链路必然重算，§7备案口径）；④新增`preflight`——整批先查「同串骨架在位」（恰1页眉＋恰1页脚部件、两处STYLEREF+PAGE域、（共N页）段、jc=left单段、18半点）后盖章，未挂载件报错退出（先挂载后盖章，不代行挂载）。
- **幂等（0901铁证复验）**：外科手术式确定性替换，重复盖章页数零漂移时输出zip成员逐字节不变——本次实测三件重盖DIFF=0。

## 2. 副本实测（复制产出文件夹三件到本子文件夹，原文件零触碰）

测试件：X2=第2章衔接件(13题,原4页)、B=第1章讲练上(61题,原78页)、C=第1章讲练下(79题,原78页)。

### 2.1 挂载
| 件 | 节标题数=锚段数 | 品牌前缀决策 | COM行数断言 |
|---|---|---|---|
| X2 | 1（'2.8 直线与圆锥曲线的位置关系'） | 保留（估算9990≤10206） | 带前缀1行=1 ✓ |
| B | 9（1.1/1.1.1/1.1.2/1.1.3/1.2/1.2.1/1.2.2/1.2.3/1.2.4） | **省略**（估算10170≤10206但COM实测带前缀=2行→自动降级） | 省前缀1行 ✓ |
| C | 1（'1.2.5 空间中的距离'）） | 省略（--no-brand，与B同部分同形态） | 省前缀1行 ✓ |
- 锚段内容实测：统计段剥离正确（如'1.1.1 空间向量及其运算（第1—10题）　本节10题：…'→锚='1.1.1 空间向量及其运算'）。
- 独立断言脚本（不复用工具逻辑）：三件×45项全OK——锚段数/直接前驱/1pt白/无隐藏底纹边框/无禁排属性/内容剥统计段/basedOn='a'/域形态（两部件各恰两组begin-separate-end、instrText={' STYLEREF "节名锚" ',' PAGE '}、无fldSimple无NUMPAGES）/全run 18半点/页眉=页脚可见文本一致/pgMar 283+850/无titlePg/settings。

### 2.2 盖章（parts.json：P1=第2章·衔接[X2]；P2=第1章·讲练[B,C]）
- X2独立部分：4页、start=1、N=4；B：78页、start=1、N=156；**C跨卷级联：start=79（=B78+1）、N=156**；第1轮收敛（锚段1pt×9未引发重排页，页数与前态零漂移）；合计160页。
- 重盖章幂等：三件zip成员级DIFF=0（0901幂等铁证口径）。
- 盖章后回读断言全过（X缓存=start×2处、N=部分总数×2处、可见串regex形态、前段尾段=件型token）。

### 2.3 COM冒烟＋PDF逐页双断言
- COM开卷（Word 16.0，自建实例用完Quit）：三件开卷正常、页数=4/78/78、页眉页脚Range.Text=同串（PAGE域按start正确重算：B首页'第1页'、C首页'第79页'）。
- 导出PDF后**逐页双断言（全量160页，超规格抽3页要求）**：页眉节名=该页所属节标题＋页脚节名=同＋X=start+页序-1＋N=部分总页数＋页眉=页脚同串——**ALL PASS**。词典式跑头逐页验证（B p1'1.1'→p2'1.1.1'→p6'1.1.2'→p16'1.2'→p45'1.2.4'换节正确；C页脚X连续79→156）。
- 锚渲染像素检验（注记②）：三件全部锚行**本体bbox内部行暗像素=0**（1pt白字不可见）；锚bbox顶行单行seam暗行系既有设计元素（章标题底边框细线/节标题底纹带边缘恰落在段落边界——X2原件实测本有该pBdr，非锚产物）。
- 锚文字在PDF文字层在位（注记③复核——审计工具须列白名单）。

## 3. 需主会话裁决/备案项
1. **品牌前缀同部分一致性口径**（建议裁决）：规则§7只说「单行放不下时允许省略前缀」，未明确部分级一致。本次B（上卷）实测必须省、C（下卷）单件本可容——同部分页码跨卷连续、装订同本，形态应一致→已加`--no-brand`参数实现「部分内任一卷省则各卷省」。**Phase 1执行清单建议补一句：同部分各卷挂载时前缀形态一致（任一卷实测省前缀→其余卷--no-brand）**。
2. **COM Range.Text读STYLEREF显文档末锚**（备案）：无页面上下文时COM读页眉域结果取末锚，PDF渲染逐页正常——验收以PDF/§14为准，不影响印制；已备案。
3. X2衔接件仅1个节标题（2.8）→同串节名段全件恒'2.8 直线与圆锥曲线的位置关系'（该件组织现状全部挂靠2.8节，本次只挂载不改组织）。
4. 挂载改动面（供E执行归一化diff授权差异登记）：document.xml=锚段插入+sectPr三参数、styles.xml=锚样式、settings.xml=updateFields（本册已有）、页眉/页脚部件=同串重建——与口径J授权差异③④一致。
5. COM串宽实测中`Application.CentimetersToPoints`分发失败（本机怪癖）——已改点值直设＋PaperSize=wdPaperA4，落库为工具内注释。

## 4. 残留清查
- WINWORD进程：0（自建实例全部Quit，tasklist核实）；无.tmp残留；工具/__pycache__本任务pyc已删；PDF/脚本/记录全部收在工作区本子文件夹（勘察现状.py/独立断言.py/冒烟导PDF.py/PDF双断言.py/parts.json/盖章记录.md×2/盖章前指纹.json/PDF×3/PNG×3/挂载件×4）。产出文件夹原件零触碰（副本测试纪律）。

## 5. 版本预检落盘
Python 3.12.10；pywin32 OK；PyMuPDF 1.27.2.3；lxml OK；Word COM 16.0开卷冒烟+Quit OK。
