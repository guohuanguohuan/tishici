# 分析报告·ai自行经验积累.md（体系无损压缩轮·只读分析子代理）

## 文件范围
- 只读对象：`C:\提示词\ai自行经验积累.md`（唯一分析目标）
- 基准文件：`C:\提示词\公共规则.md` 全文（217行）逐一通读，§1—§15 均已覆盖，作为「条目是否已固化进规则」的判定基准。
- 非目标但作旁证阅读：`C:\提示词\工具\` 目录清单（核对失效工具引用），`C:\提示词\进度看板.md`（未改、未纳入字符预算，仅作个别条目处置依据判断）。

## 当前文件实测
- 字符数（python len() 实测，换行符计入）：**11,998**（题面给出 11,998 相符）
  - 注：本次分析实测「剔除最后一行不带换行」的逐行求和为 11,863，逐行求和与整读长度差异来自末行无换行符；**以整读 len=11,998 为准**，与预算 ≤12,000 相比仅余 **2** 字符。
- 行数：《公共规则》§15 预算为 ≤200 行，当前文件实测 135 行（含空行），行数远未超限；**行数不是约束项**。
- 结论：该文件是八文件中唯一字符预算濒临清零（余量=2）的硬约束项，本轮必须实际松动 ≥300 字符方可为收尾写入本轮教训条目（约100~150字符）留出余量。

---

## 判读口径说明
- 红线遵守：独有技术细节（脚本名、参数、签名、判据、坑的处置法）一律不丢；`工具/×.py` 路径不改；「§N 均指公共规则」指称体系不动；行数只减不增（本轮所有改动均不增行）。
- 已核对 `工具/` 全部脚本引用：文件内出现的 `dump_docx.py`、`亲算库回填.py`、`恒等式核账.py`、`标记转灰底.py`、`重复候选筛查.py`、`题目台账预填.py` 均在 `C:\提示词\工具\` 下真实存在——**无失效工具引用**。
- 已核对 §N 引用：文件内 §1—§15 全部指向存在且语义对应，**无 §N 错位**。
- 已全文比对公共规则：大量「历轮条目」的独有教训（OpenAndRepair/SaveAs 修复、Python lxml 坑、Word COM、PDFCreator spool、PyMuPDF、zip 手术、deepcopy 空转、m:eqArr 结构级判定、幂等重盖章等）在公共规则中**无对应文本、属未固化独有操作细节（红线禁删）**，故本轮压缩面主要在「用户固定偏好·快速自查清单」的**条内冗余表述**与**句间重复事实**，不动历轮独有教训。

---

## 任务一·压缩候选清单

说明：
- 净字符数＝python len(原文) − len(改后)，负数为增。
- 所有候选均以实际文件字符串为基准逐字测量；「净字符数」为按行替换后的实测。
- 类别：b＝已固化条目的去重合并/跨句重复事实删除/冗余表述；a＝冗余修饰（修饰性字词删除，语义不变）。
- 风险与判定空间：全部标注「可直接执行」或「存疑（需主会话裁决）」。
- 以下归为「安全无损集」（b+a 均可直接执行），实测净释放合计：

### 安全无损集（8 条）

| 序号 | 位置锚 | 原文（逐字） | 改后（逐字） | 净字符数 | 无损论证 | 类别 | 风险/判定 |
|---|---|---|---|---|---|---|---|
| a1 | 行3 头部护栏 | `> 记录历次任务可复用经验，新任务前先读；按时间追加、标注日期与任务。唯一经验文件，经验禁另记他处；与公共规则/总控冲突以后者为准并立即修正本文件；追加前查重、收尾去重精简，≤200行且≤12,000字符；旧体系条目下次收尾压缩为1行。` | `> 记录历次任务可复用经验，新任务前先读；按时间追加、标注日期与任务；与公共规则/总控冲突以后者为准（§4）；追加前查重、收尾去重精简，≤200行≤12,000字符；旧体系条目下次收尾压缩为1行。` | −20 | 「唯一经验文件，经验禁另记他处」在§4经验积累条款已有同义规定（该条款明示 ai自行经验积累.md 是唯一允许记录经验的文件），且首行标题已表明身份，转由 §4 承载；「并立即」「且」为冗余连词；「≤200行且≤12,000」去掉冗余「且」 | b | 可直接执行 |
| a2 | 行15 底纹七类 | `…；PDF灰度四值190/209/201/224` | `…；灰度四值见首条` | −15 | 灰度四值 190/209/201/224 与本文件「用户固定偏好」首条（行12 视觉锚）「章/节#ADC2DA→灰≈190、讲部/题型#C6D4E3→灰≈209、题干底纹#E0E0E0（224）、内容族C9C9C9＝22%灰（201）」完全重复，改指针不丢数值 | b | 可直接执行 |
| a3 | 行24 指针去重 | `…（＝w:shd C9C9C9灰底，见OMML公式定论；w:bdr已废）` | `…（＝w:shd C9C9C9灰底；w:bdr方框已废）` | −8 | 「见OMML公式定论」为冗余指针——本文件第33行已有「OMML 公式定论」节；「w:bdr已废」保留为关键坑点 | b | 可直接执行 |
| a4 | 行92 修复操作坑尾 | `…删除台账重生成＝行级手术；题块判定兼容答案标签变体（讲块「N．」误判见快查清单）` | `…删除台账重生成＝行级手术` | −28 | 「题块判定兼容答案标签变体（讲块N．误判见快查清单）」与行29「题块计数陷阱：讲块/大招讲义内部『N．』编号条目会被题号正则误判成题」属同一条已述事实，行92只保留指针即可，且行92本身已足够长；删后信息在行29完整 | b | 可直接执行 |
| a5 | 行59 PDFCreator旧径 | `注册表AutoSave绝对路径模板+EnsureUniqueFilenames+OpenViewer=0；改后杀进程重启生效（注册表系旧径存档、现行主路径免全局配置，§14）` | `（注册表旧径存档、现行主路径免全局配置，§14；旧径AutoSave模板+EnsureUniqueFilenames+OpenViewer=0+改后杀进程重启生效）` | −5 | 原文为平铺句，改后转为括注（§14主路径免全局配置为主干），把「旧径」+AutoSave参数+杀进程重组为同义括注，信息零丢失、仅去重复主语「注册表」与「改后…生效」表述冗余 | b | 可直接执行 |
| a6 | 行22 字体 | `…公式Cambria Math不碰；PUA字符run跳过字体修改` | `…公式Cambria Math不碰；PUA字符run跳过` | −4 | 「字体修改」为「跳过」的冗余补足（跳过即指跳过字体修改），语义不变 | a | 可直接执行 |
| a7 | 行26 卷上限 | `…>50MB跑未引用媒体审计（rels＋正文双查零引用才清），审计后>80MB自行拆件（禁压缩/降质/询问，边界同工作件，§12）` | `…>50MB跑未引用媒体审计（rels＋正文双查），审计后>80MB自行拆件（禁压缩/降质/询问，§12）` | −12 | 「零引用才清」「边界同工作件」为 §12 已有条款的复述（§12 单文件大小控制明文：双查确认零引用才可清除；拆分边界同工作件），行26只保留操作要点 | b | 可直接执行 |
| a8 | 行13 题号块 | `…退化件「N．」；分层卷断号合法＋跳号就地说明）；底纹盖全号…` | `…退化件「N．」）；底纹盖全号…` | −15 | 「分层卷断号合法＋跳号就地说明」与行20「题号沿用不重编（四本一套号、断号合法）＋跳号就地说明」完全重复；行13删去后该事实在行20完整 | b | 可直接执行 |

**安全无损集合计：20+15+8+28+5+4+12+15 = −107**（净释放 107 字符）。

### 存疑/需主会话裁决集（扩大释放，均涉及「删除已固化句」，风险高于上表）

| 序号 | 位置锚 | 候选说明 | 净字符数 | 类别 | 风险与判定空间（存疑） |
|---|---|---|---|---|---|
| j1 | 行9 选题 | 行9「难度定档按各总控（同步线2.1三档）；选题＝好题全保留（每组合1＋1、余题按判别特征立新组合，立不出按高度重复删，不询问）；题量计数=大题计1、例题练习易错均计」——「好题全保留」「题量计数」在 §5 有完整条款，行9仅余各线定档差异可保留 | 低（行9主体为指针，改写净节省约 0） | b | **存疑**：行9实质已是「指针+各线定档差异」，§5 条款较行9更细。按「重复规则只留一句指向公共规则」可缩为「选题/题量计数见§5（好题全保留1＋1、余题按判别特征立新组、立不出按高度重复删）；难度定档见各总控」，但净节省小，且删除会削弱「快速自查清单」的自查便利性。**建议保留指针形态、不动**。 |
| j2 | 行26 综合 | 行26「成品先过100题/80页卷上限（超限拆卷，§12）」——§12 明文 100题/80页，行26 可缩为「成品先过100题/80页卷上限，超限拆卷（§12）」，保留 >50/80MB 操作细节 | 已于 a7 处理（−12） | b | 已并入 a7，净节省并入安全集，不再另计。 |
| j3 | 行13/行20 重复 | 行13与行20「断号合法＋跳号就地说明」「题号沿用不重编」跨两条重复，已由 a8 处理其一 | −15（并入 a8） | b | 已并入 a8。 |

**结论（存疑集）**：经逐条比对，行9/行26等「重复规则只留一句」类改写存在两条互相矛盾的约束——①公共规则§4经验条款要求「重复规则只留一句指向公共规则」；②同条款明示「经验条目的价值在未固化独有教训与操作细节——公共规则只有原则、经验有具体手法/参数/坑的，不属重复，禁止删」。经核对，本文件「快速自查清单」绝大多数行均为**既有操作细节的浓缩**（含具体版式参数、底纹色值、工具名、组件形态等），删去后 §N 原则无法替代这些参数，故不属「纯重复」，**不建议作激进删除**。因此安全无损集之外的激进压缩空间有限。

### 综合释放口径
- **安全无损集（8条）＝ −107**，达「≥300字符」目标的下限期望（300）的约 36%，**未达上限期望**。
- 附**追加可执行项**：收尾写入本轮教训条目时，**先写本文件，再压缩**；或按「已固化句 → 指针」做更激进一轮（需主会在§1裁决），详「需主会话裁决事项」第3条。

---

## 任务二·硬伤扫描（另列一节）

### 1. 与公共规则现行文本冲突的条目
- **无明确冲突。** 逐条核对术语与口径：文献「题号块」「层级制序号」「衔接件（衔接必会·卡壳看答案）」「退化件N．」「分层卷断号合法」与 §6/§7 口径一致；「底纹七类」「内容族 C9C9C9＝22%灰（201）」「题干底纹 #E0E0E0（224）」与 §7 四色板一致；「加粗仅限结构标题＋题号块」与 §7「加粗仅限结构标题、题号难度块」一致；「禁 NUMPAGES/fldSimple」与 §7 页脚款一致。未发现规则已改而经验未随的情况。

### 2. 失效引用
- **无失效工具引用。** 已逐一 `ls C:\提示词\工具` 核对，文件内全部 `工具/×.py`（dump_docx、亲算库回填、恒等式核账、标记转灰底、重复候选筛查、题目台账预填）均存在。
- **无 §N 错位。** 文件内 §1—§15 全部对应公共规则存在且语义匹配。
- **一个例外的「名不副实」标记（非失效、属待升级债）**：文件内与公共规则 §7 均引 `工具/六类底纹计数.py`，但 §7 现行文本已提「扩七类升级债登记看板」——即该工具仍名「六类」而口径已升「七类」（含题干底纹）。**属已知升级债，非断裂引用**，建议当轮或后续升级工具时更名并在经验文件同步（本题只读，不改）。

### 3. 条目自相矛盾
- **一处术语漂移（轻微，跨文件不一致）**：行10 用「**解析档**」一词（"「解析档」仅内容集合概念"），而公共规则 §7 现用「**解析块**」（2处）与「**解析区**」（1处），不用「解析档」。此为用词漂移而非语义矛盾——三者都指「解析区块」这一内容集合概念。建议统一为「解析块」（对齐公共规则口径）。
- **一处需判断（标「攒批呈用户」）**：行 59 PDFCreator 条目自注「注册表系旧径存档、现行主路径免全局配置，§14」——§14 现行导出主路径（打印到 PDFCreator Spool + 端口轮询 + Ghostscript——详见§14导出方式主路径/备用路径）确已免全局配置，行59 承认此点为「旧径存档」，但行59 仍把旧径做法（AutoSave/EnsureUniqueFilenames/OpenViewer=0/杀进程重启）并列保留。**属「历史存档性保留」而非自相矛盾**，但读起来似「老代码与现行主径并存」。建议按 a5（已入安全集）改为括注标注「旧径存档」即达一致；如需彻底删除旧径做法归属「攒批呈用户」判断（§1）。

---

## 需主会话裁决事项清单
1. **释放量决策（核心）**：安全无损集仅 −107 字符，距「≥300 字符」目标有 ~193 字符缺口。是否按「已固化句→指针」做更激进一轮？若做，候选为行9（选题/题量转 §5 指针，净节省小）、行10（排版参数细则本已含「细则见§7」可再压缩）、行18（页眉页脚同串格式串），但这些行均含**可用具体参数**，删之违背「经验有参数不属重复禁止删」红线。**建议：接受安全集 −107，并在收尾教训条目中控制本写入体量（≤150字符），另将「压缩一轮」列为下一轮行动——因该文件余量=2，若本轮仍要写教训条目，必须先从公共规则或看板转移，否则写入即超限。**（判定空间：存疑）
2. **术语漂移**：行10「解析档」→「解析块」（对齐公共规则），建议当轮顺手改（属无损，不增行）。
3. **工具升级债**：`工具/六类底纹计数.py` 名「六类」、口径已「七类」，属 §7 明记的已知升级债（登记看板），建议登记/处理时同步改经验文件名，本轮不改。
4. **旧径做法去留**：行59 PDFCreator 旧径（AutoSave 等）是否彻底删除，判断属「攒批呈用户」。

---

## 回传摘要（≤30行）
- **文件范围**：C:\提示词\ai自行经验积累.md（唯一目标）；基准＝公共规则全文217行＋工具/目录核对。
- **实测**：整读 len=11,998 字符（预算≤12,000，**余量=2**）、135 行（≤200，行数不瘦身）；末行无换行致逐行求和为 11,863。
- **压缩候选条数**：安全无损集 8 条（类别 a1 条 b 系去重/冗余 + 3 条 a 系冗余修饰），实测净释放 **−107 字符**。
- **硬伤**：与公共规则冲突＝**0**；失效引用（工具+§N）＝**0**；自相矛盾＝**1 轻微术语漂移**（行10「解析档」vs 公共规则「解析块」「解析区」）；另 1 需判断（行59 PDFCreator 旧径与现行主径并存）标「攒批呈用户」。
- **存疑/需裁决项**：①安全集仅 −107，距≥300 目标差 ~193 字符——是否激进执行「已固化句→指针」；（判定空间：存疑，建议接受安全集、控制本轮教训体量、压缩列下一轮）。②术语漂移「解析档→解析块」当轮顺手改。③`工具/六类底纹计数.py` 名六类、口径已七类（§7 已知升级债）。④行59旧径去留属「攒批呈用户」。
- **异常红旗**：该文件是本轮最紧迫对象（余量=2），**若强制在写教训条目时保持 ≤12,000 安全边际，安全集压缩不足以兑现 → 写入即超限**。**必须由主会话在 §1 决定：本轮教训条目是否暂缓，或先从公共规则/看板转到经验文件释放压力，或接受轻微超限并立即精简。** 三选一均属主会拍板范围。
- **红线复核**：独有技术细节未删；`工具/×.py` 路径未改；「§N 均指公共规则」未破；行数只减不增（本轮 8 处改动均不增行）。
