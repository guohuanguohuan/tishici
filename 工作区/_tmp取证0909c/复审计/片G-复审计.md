# 片G（六图下置集成）轮·独立复审计报告

- 复审计人：独立代理（未参与本轮制作）
- 日期：2026-09-10
- 对象：`C:/提示词/工作区/字替对照-0909/variantF/main.pdf`（声称 7 页 A4 双栏，md5 `f80297ffeeb872438943da5bdc5cebc1`）
- 方法：全部结论出自本次实跑（复跑输出存 `工作区/_tmp取证0909c/复审计/`：`复测_测v4.txt`、`复测_权威.txt`、`复测_顶格.txt`、`探针_独立.py` 及其输出记录）；未采信任何未复测的中间结果。
- 纪律遵守：除本目录内输出与本报告外未写任何文件；未用 git；未碰 附则/总控/进度看板/variantF 源件。

---

## A. `_测v4断言.py` 重跑

| 项 | 声称（run5-测v4-全接线.txt） | 实测（复测_测v4.txt） | 判定 |
|---|---|---|---|
| exit 码 | 0 | 0（`EXIT=0`） | 对上 |
| ✓ 条数 | 51 | 51（`grep -c '^ ✓'`=51，逐条清点） | 对上 |
| ◎ 登记条数 | 42 | 42（`grep -c '^ ◎ 登记'`=42） | 对上 |
| ✗ 条数 | 0 | 0（`grep -c '✗'`=0） | 对上 |
| 末行 | `断言结果： 全部通过` | 同文，行98 | 对上 |

逐行 diff（`diff --strip-trailing-cr` run5 vs 复测）：**第 1–98 行逐字节一致**；唯一差异在第 99 行——run5 以 `echo "exit=$?"` 追加小写 `exit=0`，本次以 `echo "EXIT=$?"` 追加 `EXIT=0`，两者皆为本机追加的退出码标记（均为 0），非脚本输出差异。✓/◎ 内容无一处漂移（含 ⑱/⑱-2/⑱-3/N6/⑮ 等片G 新门全绿）。

注：`_测v4断言.py` mtime 15:47:45，晚于构建 13:06:54——属任务书预告的「工具再晚，不算越界」；且本次用**当前版本**工具复跑仍逐字复现 run5，等于以最新工具复核了 13:06 的 PDF。

## B. `断言顶格.py` 重跑

| 项 | 声称（run3-顶格.txt） | 实测（复测_顶格.txt） | 判定 |
|---|---|---|---|
| 输出三行 | (a) 无／(b) 无；签名行 128（[答案]14；违规0）｜表线 64（左框线16；违规0，花形豁免0）｜位图 0（违规0）｜矢量图 6（违规0）；通过 | 与 run3 逐字符一致 | 对上 |
| exit 码 | （run3 未记录） | 0 | 对上（补充实测） |

## C. `片G/权威实测.py` 重跑 + 逐行 diff

| 项 | 声称（run4-权威实测.txt） | 实测（复测_权威.txt） | 判定 |
|---|---|---|---|
| 内容 | 六图权威实测表 158 行 | `diff` 结果：仅多出本次追加的末行 `EXIT=0`；**前 158 行与 run4 逐字节一致**（diff 唯一输出 `158a159 > EXIT=0`） | 对上 |
| exit 码 | 未记录 | 0 | 补充实测 |

关键列复跑值与 run4 全同：六图线框 82.17×19.30／31.62×58.00／31.92×32.09／66.12×66.08／52.79×23.33／46.57×29.72 mm；Δ线框 +0.01/-0.01、-0.00/-0.04、-0.00/+0.08、+0.05/+0.05、+0.00/+0.00、+1.29/+0.82（g5 无锁定不变量路径）；正文相交 0×6；落位 p2c2/p3c2/p4c1/p5c1/p6c1/p6c2；标签数 21/6/9/8/6/6 件内=素材。

注：run4 声称的「连跑两次逐字节一致」系其内部过程，本次以**一次独立全新运行**即与 run4 逐字节一致，确定性结论获独立佐证（见「未能验证项」1）。

## D. 独立抽测（自写探针 `复审计/探针_独立.py`，pymupdf，未引用现成脚本中间量）

**① 页数与 md5**：页数=7（get_page_count），md5=`f80297ffeeb872438943da5bdc5cebc1`（hashlib 自算，273585 字节）——与声称一致。**对上**。

**② 六图在场与居中**（自写 get_drawings 暗色矢量墨聚类，栏心=59.20mm/150.80mm，图墨中心x−栏心x）：

| 片段 | 声称落位 | 实测落位 | 声称 栏心偏(线框基) | 实测 栏心偏 | 差 | 判定 |
|---|---|---|---|---|---|---|
| g6-triple | p2c2 | p2c2 | +0.32 | **+0.29** | 0.03 | 对上 |
| g1-prism | p3c2 | p3c2 | -0.06 | **-0.07** | 0.01 | 对上 |
| g2-cubeE | p4c1 | p4c1 | +0.77 | **+0.76** | 0.01 | 对上 |
| g3-cube6 | p5c1 | p5c1 | -0.16 | **-0.20** | 0.04 | 对上 |
| g4-dihedral | p6c1 | p6c1 | -1.20 | **-1.23** | 0.03 | 对上 |
| g5-fold | p6c2 | p6c2 | -0.25 | **-0.26** | 0.01 | 对上 |

**6/6 对上**，最大差 0.04mm（≈600dpi 单像素 0.0425mm，抗锯齿级）。口径备注：实测墨宽 81.91/31.58/31.58/65.54/52.60/46.22mm，普遍比 run4 线框小 0.03–0.63mm——run4 口径含线宽外放 0.8pt＋1px 膨胀（其【六】已自登记为固有正余量），方向一致、量级相符；g6 起初三子图分离且其下方 y[134.80,137.06] 三笔 0.277pt 黑短线被本探针 ≥10mm 高度门漏收，并集后高 19.16mm 与 run4 线框 19.30mm 相符（0.14mm），已归因、非矛盾。另：**PDF 全文档 raster 图像对象 = 0**（get_images 逐页求和），对象级独立佐证「位图 0」。

**③ main.log 三 0 ＋ Underfull 登记**（`grep -c` 直查 `variantF/main.log`，mtime 13:06:54 与 main.pdf 同批）：
- `^!`（error 行）= **0**；`Overfull` = **0**；`Missing character` = **0** —— 三 0 属实。
- `Underfull` = **6** —— 「underfull 6 处」登记属实。

## E. 范围核验

**mtime（本轮正文源 vs main.pdf 构建 13:06:54）**：
- `main.pdf`＝`main.log`＝13:06:54（同一次编译）。
- `body.tex` 13:06:47、`chapterhead.tex` 13:06:47（< 构建时刻 ✓）、`postproc_daoxue.py` 13:05:59 ✓、`qp-blocks.tex` 12:00:33、`qp-headfoot.tex`/`qp-fonts.tex` 11:18、`qp-titles.tex`/`qp-layout.tex` 11:14、`sec.tex` 09-09 21:25 —— **无任何正文源晚于构建时刻**。
- 工具类（不算越界）：`_测v4断言.py` 15:47:45（任务书预告）、`断言顶格.py` 13:08:16（构建后 84 秒的纯工具改动；其对 13:06 PDF 复跑通过，见 B）。

**figs/ 六片段**（恰 6 个 .tikz，全部早于构建）：`g1-prism.tikz`(11:57)、`g2-cubeE.tikz`(11:57)、`g3-cube6.tikz`(11:58)、`g4-dihedral.tikz`(11:58)、`g5-fold.tikz`(11:59)、`g6-triple.tikz`(12:00)。

**接线与残留**：`body.tex` 中 `\input{figs/}` 恰 6 处，行号 **L76 g6-triple／L152 g1-prism／L170 g2-cubeE／L250 g3-cube6／L305 g4-dihedral／L337 g5-fold**，与声称完全一致；置宽 `\linewidth`/41.1mm/42.0mm/`\linewidth`/66.3mm/54.7mm 同 ⑱ 声称；`body.tex` 内 `includegraphics`=0、`minipage`=0、`.png` 引用=0。全部 .tex 无 `.rst` 引用。`main.tex` 实际入件链＝qp-fonts/qp-layout/qp-parts/qp-headfoot/qp-titles/qp-blocks＋chapterhead＋body。**对上**。

备注（非违规，登记）：`sec.tex` 存有 9 条 `\includegraphics{media/media/*.png}`，但 `sec.tex` 不被任何文件 `\input`（死文件），不构成「被引用的位图残留」；如后续复用该文件需先清理。

---

## 放行结论：**对上，放行**

- A／B／C 三项复跑全部复现声称计数且内容逐字节一致（A 的 51✓/42◎/0✗/exit=0；B 的 128/14/64/16/0/6/违规0；C 的 158 行全同）；D 的三项独立抽测（md5+页数、六图落位与栏心偏 6/6、log 三 0＋Underfull=6）全部与声称吻合，且对象级位图=0；E 范围核验无越界、无位图/rst 被引用。
- 差异清单：无实质差异。仅两处「非内容性」说明：①A 末行标记大小写（run5 `exit=0` vs 本次 `EXIT=0`，均为追加的退出码标记，值同为 0）；②本次 C 输出多一行自加的 `EXIT=0`。均不触及脚本输出内容。
- 登记 2 条：`sec.tex` 死文件含 9 条位图引用（未被引用，建议下轮清理或明确弃用）；`断言顶格.py` mtime 晚于构建 84 秒（工具改动，已用其对 13:06 PDF 复跑通过自证无碍）。

## 未能验证项

1. run4 所称「同脚本连跑两次逐字节一致」的**过程本身**无法复现（只能见到其一次的留档）；本次以独立单跑与 run4 逐字节一致，等效佐证其确定性，但两次连跑的原始证据不在留档中。
2. 「本轮开工时刻」未给出精确值；以任务书给的 main.pdf 构建 13:06（实测 13:06:54）为基准执行了「正文源不晚于构建」判据，判据满足；若开工时刻另有定义，结论不受影响（所有正文源 ≤13:06:47）。
