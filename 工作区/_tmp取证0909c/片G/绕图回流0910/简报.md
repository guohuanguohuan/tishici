# 片G·绕图回流0911 简报（variantF 导学件）

任务＝恢复「绕图回流」（文本切分法＝图旁前缀窄栏＋图底以下余段通栏），按片E口径给 image1/image2 启用
`SIDE_DEF['split']`，逐图核验余段，**g2 若题干块太短、无余段可切→保持并排形态并逐图登记（不许硬造余段）**。
结论：**切分机制与断言支已恢复到位；启用图数＝0（g1/g2 实测均无图底以下余段可切，键以 `None` 入位并逐图登记）；
版面零扰动（body.tex 逐字节复现交付态）。**

## 一、改了什么（全部在链内，未动 tex 源与宏包）

`variantF/postproc_daoxue.py`

1. `SIDE_DEF`：`'image1.png'`／`'image2.png'` 各加 `split=None`（键显式入位＝复测登记态，非「键缺失」），
   注释带逐图 600dpi 复测数字；注释块补 split 键语义＋绕图回流0911 复测登记。
2. 新增两台账 `SIDE_ARMED`（真值切分点＝启用切分）／`SIDE_NO_REST`（键在位＝None＝复测无余段）。
3. `side_row` 切分支（片E 0909c 代码原样复用，几何一行未改）：`if split:` 命中→`SIDE_ARMED.append`；
   新增 `elif 'split' in cfg:`→`SIDE_NO_REST.append`；3c-A 日志改三态（切分余段／键在位＝None／无 split 键）。
4. 新增 `side_split_note(name)`，两处 `3-图` 发射日志由「段全在图旁，不绕图回流」改为逐图键态实读
   （不再写死结论，未来加真值键日志自动改口）。
5. 计数由**硬钉 `n_split == 0`** 改为**逐图标定驱动**：`n_split == n_split_arm` 且
   `n_split_arm + n_split_none == n_side == 2`（每个 side 图必须落进两台账之一＝逐图登记完备性）；
   第 8 步日志与 8c 台账同步改写。
6. 文件头 docstring 增「绕图回流0911」段（逐图数字＋启用 0 理由＋探针实证结果）。

`variantF/_测v4断言.py`：⑱-3 由两支门改**三支门**（其余门一律不回退）

- 下置支（g3–g6）：旁行=0，逐字不动。
- **并排无余段支**（本轮 g1/g2 实走）：旧门（旁行≥1 且整行在盒左）＋新正证「末旁行 600dpi 真墨底 ≤ 图真墨底＋0.5mm」
  ——此门系「不许硬造余段」的机器反向锁：真有行越图底而未切，该支即红，逼出补真值 split 键。
- **并排有余段支**（片E 照片E口径原样恢复）：识别＝片E `row_rest` 判据（side 行下一元素不以 `\bindp/\tjdnr…` 段首宏起）；
  门＝图真墨底以下首个「自栏左起」（行内最左 x0 ≤ 栏左+5pt）视觉行右缘距栏右 ≤5mm 判通栏；
  视觉行按 y0 差 <3.0pt 合并（H2 口径，防图旁行尾碎片误判）；零侵入复用 ⑱-2 `K18['nhit']` 不重算。
- 旧值逐条 `reg()` 登记：⑱-3 口径登记（绕图回流0911 三支分立·旧→新）＋⑱-3 沿革登记（承回退轮/并排恢复旧档原文）。

## 二、逐图余段实测（600dpi 墨级；量具 `_probe_余段.py`→`量_余段探针-基线.txt`；单位 mm 自页顶，栏左 108.8／栏右 192.8）

| 图 | 落位 | 图真墨矩形 | 末旁行真墨底 | 段末行 vs 图墨底 | 图底以下首行 | 判定 |
|---|---|---|---|---|---|---|
| g1 image1（探二【详解】首块） | p3c2 | y[66.21,114.43]，墨高 48.22，右缘 1.90 | 83.01（bbox 底 83.75） | **−31.42mm**（bbox 口径 −30.68） | 「故答案为：b−a−c」＝`\bindp` 独立通栏段，右缘距栏右 **55.34mm**（自然短行，非窄栏约束） | 旁行 3 行全在图旁⇒**无余段可切** |
| g2 image2（探三题干） | p3c2 | y[216.77,248.10]，墨高 31.33，右缘 5.04 | 246.07 | **−2.02mm＝贴线态** | 「[分析] 结合空间向量的线性运算列方程…」通栏段，右缘距栏右 **0.14mm ≤5mm**（通栏正证） | 旁行 6 视觉行（PDF 文本行 5）全在图旁⇒**无余段可切** |

body.tex 结构互证（`_probe_body.py`，176 元素）：image1 side 行＝elem 55、下一元素＝`\bindp 故答案为…`；
image2 side 行＝elem 64、下一元素＝`\bindp [分析]…`⇒按片E `row_rest` 判据＝无余段元素，与墨级实测一致。

**贴线态预警（g2）**：末旁行真墨底距图真墨底仅 2.02mm——题干文字再增一行即越图底，届时该处出现真余段，
须给 image2 补真值 split 键（本轮 ⑱-3 无余段支的反向锁会当场报红提示，无需人工盯）。

## 三、探针实证（临时改配置→重编译→复测→撤键复原，取证 `探针-硬造0911/`）

1. **g1 加真值 split 键**（切分点＝`\nobreak\(\overrightarrow{A_{1}B}`）→ **链内侧自锁**：postproc R2
   「则\(' 禁断绑定计数 23→22 硬断言当场失败（切分把绑定域劈成两段），body.tex 不落盘
   （`探针1-image1加键-postproc-log.txt`）。
2. **g2 加真值 split 键**（切分点＝「，若」，postproc 可过）→ **切分支命中**：`3c-B` 日志＝前缀 173 字窄栏＋余段 162 字通栏；
   body.tex 元素 176→177（elem 65＝独立余段段）；重编译 7 页／Overfull 0；**⑱-3 有余段支命中**：
   g2（并排·有余段）旁行 5→3、**余段首行右缘距栏右 0.00mm ≤5mm＝通栏正证**、侵入 0；
   ⑱-2 并排三窗本轮仍过门（顶差 −0.23／墨缝 7.83／盒缝 7.46／右缘 5.04）；
   **但「拉伸异常行＝0」门当场 ✗2 处**（p3 y705.8pt 「，|若」「，|则」空档 17.75pt）——硬造把「，」逼到行首，
   xeCJK 标点禁则撑出字间大空档，全件断言 0✗→1✗（`断言-探针-image2加键.txt`、`探针目验_g2带-硬造余段.png`
   目验：图旁只剩 3 行（⑱-3 读数；bbox 级 4 行）、末旁行 bbox 底距图盒底 14.57mm＝图右下方约 14mm 空白洞、
   通栏行以「，」起头）。
3. 撤键复原：`split=None`→重跑 postproc→body.tex md5 复现 **ce89dbf7b44d40bbb675699197f4d9c8**（＝交付态基线）。

⇒ 机制通路完好可用（有余段即切、切后即通栏），而硬造余段必破另一门——启用与否只能由真余段驱动。

## 四、复验链（终验，全部在撤键复原后重跑）

| 环节 | 结果 |
|---|---|
| postproc 幂等 | 连跑两遍 body.tex md5 均＝ `ce89dbf7b44d40bbb675699197f4d9c8`（＝接手基线，**逐字节零扰动**）；探针撤键复原后同值 |
| xelatex ×2 | rc 0/0；`Output written on main.pdf (7 pages).`；Overfull **0**／`^! ` 错误行 **0**／Missing character **0** |
| 页数 | **7→7**（本轮零版面扰动，body.tex 逐字节未变，页数不可能变；无 7→8 风险） |
| `_测v4断言.py` | **51 ✓／46 ◎／0 ✗**，rc 0，「断言结果： 全部通过」（基线 51✓/45◎/0✗→◎+1＝⑱-3 沿革登记另立） |
| `断言顶格.py` | 通过（签名行 125／[答案]14／表线 64／位图×6 认领 6/6；违规 0）——脚本零改动 |
| `render.py` | 7 张 150dpi（page1–7.png） |
| 400dpi 裁片目验 | `目验_g1带.png`／`目验_g2带.png`（p3 右栏，g1 带 58–128mm／g2 带 206–262mm）已亲看：图旁窄栏文字在场且整行在盒左、图底以下首行通栏、无触碰、无空洞；与接手基线目验一致 |

终验文件指纹（备查）：`body.tex` ce89dbf7b44d40bbb675699197f4d9c8（＝基线，零扰动）｜`postproc_daoxue.py`
2cfb4e4ce94feda18258b4316db5f5f8｜`_测v4断言.py` 1c60983bce507cf7d62db08c4d3c9a6f｜`main.pdf` 33e4f0d75ff135a1ff08cee0704e2e6e
（注：main.pdf 每次 xelatex 因 PDF 时间戳/ID 不复现同 md5，故「零扰动」证据链＝body.tex 逐字节同值＋页数同值＋
⑱-2/⑱-3 几何读数逐字同基线（g1 顶−0.40 墨缝5.63 盒缝5.59 右缘1.90 旁行3 侵入0／g2 顶−0.23 墨缝7.83 盒缝7.46
右缘5.04 旁行5 侵入0），不以 main.pdf md5 为据）。

## 五、交付态与遗留

- 交付态：g1/g2 保持「图右文左」并排（`split=None` 入位＋逐图登记），切分通道与 ⑱-3 split 支在位待用；
  版面与接手时逐字节相同。
- 遗留（不阻塞）：①g2 贴线态 2.02mm——题干增行即须补真值 split 键（反向锁会报红）；②`SIDE_DEF` 里
  image4/image5（探八/探九）旧标定 split 真值仍在位，但两图现按 g3–g6 同式下置，不经 `side_row`，
  故不入台账、不参与本轮计数（片E 备用标定，保留不动）。
- 纪律：只动 `variantF/postproc_daoxue.py`、`variantF/_测v4断言.py` 与本产物目录；备份
  `*.bak_绕图0910` 接手时已在；未用 git；未碰 附则/总控/看板/其他样张。
