# 并排恢复0910 · 简报 —— g1/g2 由「图下置居中」回归「图右文左」minipage 并排（位图时代重做）

- **日期**：2026-09-10｜**依据**：用户令「并排回归；位图时代重做」（旧几何档 3c 机制与标定保留复用，图源＝回退轮原位位图）
- **件**＝`工作区/字替对照-0909/variantF/`｜**改动全落生成链**（`postproc_daoxue.py` 发射，非手改 body.tex）
- **改前备份**：`postproc_daoxue.py.bak_并排0910`｜`body.tex.bak_并排0910`｜`_测v4断言.py.bak_并排0910`｜`断言顶格.py.bak_并排0910`（后者本轮**零改动**，仅按纪律预备案）
- **交付态 md5**：`body.tex` `ce89dbf7b44d40bbb675699197f4d9c8`｜`chapterhead.tex` `aed098e01fe045ecd74e6182dd6f599c`（**与句末标点0910 轮同值＝章首未动**）｜`main.pdf` `fc62f8cfcf6357a1c281bc475fad1d5b`（终验重编版，见§三末行）
- **禁碰区未动**：附则／总控／进度看板／其他件；无 git 操作。

---

## 一、恢复范围与保持下置的理由

| 图 | 内容 | 本轮形制 | 取用/盒宽 | 理由 |
|---|---|---|---|---|
| g1 `image1.png` | 探二例1【详解】图（直三棱柱） | **并排（side）** | 盒 28.800mm | 回退轮反解取用宽 28.8 落 TW-01（26.5–32）档内，直取入并排 |
| g2 `image2.png` | 探三例1 题干图（正方体＋E） | **并排（side）** | 盒 32.000mm | 反解 32.7 超 TW-01 上限 0.7mm → 按处置序首档「缩放反解」**缩至 32.0 入档**（标签等效字号 ≈7.35pt ≥ 7.0pt 下限，**不另设例外**） |
| g3–g6 | 探六/八/九题干·详解图＋条目3 三联图 | 保持**下置居中** | 54.1／47.5／42.5／84.0mm | 逐图皆 ＞TW-01 上限 32mm，缩至档内则图内标注失读（三联图缩并排即失读） |

- **不恢复绕图回流**（用户另议）：片E 的 `SIDE_DEF['split']` 切分机制（image4/image5 键）继续备用不动；g1/g2 两段均**段全在图旁**，本就无需切分。
- 图位与源归属不变：g1 挂【详解】首块文字旁、g2 挂◆探究点三 标签行下题干位（`STEMFIG`/`DETAILFIG` 分档走 `FIG[name][0]=='side'` 判定，下置支路原样保留给 g3–g6）。

## 二、并排几何标定（side_row 墨缘反解，非硬凑常数）

发射形制（`body.tex` L150／L168，⑱ tex 门逐图钉）：

```
\noindent\begin{minipage}[t]{文栏mm}\raggedright 段落文字\end{minipage}\hspace{胶mm}
\begin{minipage}[t]{盒mm}\centering\raisebox{\dimexpr-\height+H\relax}[\dimexpr H\relax][\dimexpr\height-H\relax]
{\includegraphics[width=盒mm,alt={@@@…}]{media/media/图.png}}\end{minipage}\par
```

| 图 | 文栏 T | 盒间胶 G | 盒宽 | raisebox H | 反解式 |
|---|---|---|---|---|---|
| g1 | 48.000mm | 5.342mm | 28.800mm | 3.332mm（c0 3.04） | G ＝ 栏宽 84 − r_edge 1.90 ＋ 盒右留白 − T − 盒宽；H ＝ c0 ＋ 盒上留白 ＋ 0.25 |
| g2 | 46.368mm | 0.925mm | 32.000mm | 3.545mm | 同上，r_edge 目标 5.00（例外，见§四-1） |

**实测读数（两口径并列，登记互差）**

| 图 | 顶差 | 墨缝 | 盒缝 | 右缘 | 带内文行 | 侵入 |
|---|---|---|---|---|---|---|
| g1 门读数（⑱-2，位图原生墨 bbox 同比映射＋行 600dpi 真墨） | **−0.40** | **5.63** | **5.59** | **1.90** | 3 | 0 |
| g2 门读数 | **−0.23** | **7.83** | **7.46** | **5.04** | 5 | 0 |
| g1 标定环读数（`量_并排缝.py`，渲染像素墨 600dpi/阈值128） | −0.25 | 5.84 | 5.63 | 2.13 | 3 | — |
| g2 标定环读数 | −0.25 | 7.87 | 7.50 | 5.05 | 4 | — |

- 两读差 ≤0.3mm，成因＝判据墨基不同（渲染反锯齿边缘 vs 原生像素 bbox 映射）；**验收以门读数为准**，标定环仅作迭代量具（`量_并排缝-终态.txt`）。
- 全部门窗：顶差 ∈[−1,+1]（目标 −0.25 档）／墨缝 ∈[5.2,8.2]／盒缝 ∈[4.7,8.2]／右缘逐图窗（g1 [1.6,3.6]、g2 [4.5,5.5]）——**四项全过**。
- T=48（g1）系标定所得：使数学式断行改簇（L1「…则A₁B＝」／L2「CB−CA₁=CB−CA−CC₁＝」≈45mm 满栏），墨缝由出窗回落窗内。

## 三、复跑链（终态，原始输出在本目录 `复跑0*.txt`）

| 环节 | 结果 |
|---|---|
| `python postproc_daoxue.py` | exit 0；内部断言（含 R2 裸()`1、\hfill` 右挂 0、kongwei 9）全过；8 步计数改口＝**图文并排 2/2 组、minipage 总 4/4、位图引用 6/6、TikZ 入件 0/0、\resizebox 0/0**；**重跑幂等**（连跑两遍 `body.tex`/`chapterhead.tex` md5 不变） |
| `xelatex main.tex` ×2 | **三 0**：error `! ` **0**／Overfull **0**／Missing character **0**；**页数 7**（未增页）；Underfull **6→5**——消失者＝旧探三题干**内联段**（badness 5802，旧 L168），该段现入 `\raggedright` 窄栏不再报；余 5 处与基线**同位同 badness**（行号 −2 位移＝g1 由「【详解】段＋下置图行」两元素并为并排一行，body.tex 401→399 行／元素 177→176），登记不设门 |
| `python _测v4断言.py` | exit 0，**✗0**（**✓51／◎45**；改前基线 ✓51／◎43，`基线-测v4-并排前.txt`）＝◎净增 2 系本轮新增两条口径登记（⑱-2 并排恢复、⑪ 并排恢复） |
| `python 断言顶格.py` | **通过**（脚本零改动）：签名行 125（[答案]14；违规 0）｜表线 64（左框线 16；违规 0）｜**位图×6 尺寸认领 6/6、越栏 0、压字 0** |
| `python render.py` | 清空重渲 7 张 png（1241×1754@150dpi） |

> **终验重跑链**（消「源新于件」时间戳歧义）：§二标定后为补注释又跑一遍 postproc（`body.tex` md5 **不变**＝纯注释零输出影响）→ 重编 `xelatex`×2（`复跑06`/`复跑07`，三 0／Underfull 5／7 页）→ `_测v4断言.py`（`复跑09`，✓51／◎45／✗0）→ `断言顶格.py`（`复跑10`，通过）→ `render.py` 重渲；`复跑01`–`复跑05` 为同链前次运行，`main.pdf` 因每编必含新 ID 而 md5 变更（`fc62f8cf…`），版面读数逐字同前（`量_并排缝-终态.txt` 复量 g1 5.84／g2 7.87 墨缝不变）。

## 四、判据改动与例外登记（断言侧四处，均旧值→新值留痕于 `reg()`）

1. **g2 右缘超通用档＝例外**：通用档 [1.6,3.6] 实测 5.04（超 1.44mm）。栏预算恒等式 **墨缝＋右缘 ＝ 84 − 图墨宽 31.33 − 文墨右缘 39.8 ＝ 12.87**，题干 `\raggedright` 合法断点仅 39.8／47.3／49.6 三档（47.3 触 xeCJK 行首标点令、49.6 压盒缝为负、「ABCD-」连字符断行违正方体名完整性〔探六 `\penalty100` 先例〕，皆弃），文短 6.5mm ⇒ **缝窗与右缘窗不可兼得**——取缝窗优先（用户「观感脱钩」判据），改立**逐图右缘窗** `REDGE_W`。
2. **⑱-2 零侵入外扩窗分形制**（假阳修正）：下置支逐字沿用声明盒外扩 0.5mm（四图读数不变）；并排支改依**图真墨矩形**外扩 0.5mm——声明盒底含 PNG 内白边（g2 盒底比真墨底低 0.93pt），旧口径把 g2 下邻 `[分析]` 通栏行判为「侵入 1 行」；实测该行真墨顶距声明盒底 0.33mm、**距图真墨底 0.66mm**，400dpi 目验无触碰（`目验_g2带.png`）。修后并排两支侵入 **0/0**。
3. **⑱-3 由「六图旁行=0」改按形制分支**：下置四图沿用旁行=0（实测 g6/g3/g4/g5 全 0）；并排两图反转为**旁行 ≥1 且整行在图盒左缘之左**（实测 g1 旁行 3／g2 旁行 5，越盒行 0）；「栏末图下邻免检」实例归零（g2 不再下置），机制保留。
4. **⑪ 题侧标签隙计数 23/0 → 22/1**：探三题干回图旁 minipage ⇒ 该◆标签行末无水平后隙＝换行态 1（p3）、内联 22；前隙恒 23（`\hspace` 定值，与题干形态无关），TJ-03 2.2mm 口径不变。
5. 承位图台账的门（**未改**、复验绿）：⑱ tex 形制（下置 4 行逐字节同式＋并排 2 行逐图钉）、N6 六图台账（位图恰 6、原生 px 互证、阅读序、落位 `FIG_AT` 逐图钉——g1/g2 仍 p3c2）、⑮ 五例1图归属（二/三/六/八/九 不变）。

## 五、目验存证（本目录）

| 片 | 结论 |
|---|---|
| `目验_g1带.png`（p3 右栏 y175–345，400dpi） | 【详解】三行居左窄栏、直三棱柱居右；图底以下「故答案为：b−a−c」恢复通栏；图旁文字与图墨无触碰 |
| `目验_g2带.png`（p3 右栏 y600–730，400dpi） | ◆标签行通栏、题干六行居左窄栏、正方体居右；`[分析]` 起通栏，图 A/B 标签与下行墨隙清晰（实测 0.66mm） |
| `png/page3.png`（150dpi 全页） | 两条并排带同页共存、无出血、无孤行；页脚页码 3 正常 |
| `png/page4.png` | 纯文字页，改版后未受影响（探四/探五区正常续排） |

## 六、遗留与风险

1. **绕图回流（图底以下文字窄栏→通栏的切分版）仍退场**——用户「另议」；`SIDE_DEF['split']` 与 `side_row` 切分支代码原样保留，恢复＝给 image1/image2 加 split 键，几何三窗不回退。
2. **g3–g6 保持下置**：若用户要求「六图全并排」，须先解「取用宽 ＞32mm 即出 TW-01」这一硬约束（缩档→图内标注失读），非改代码可绕。
3. **N6「图内字 ≥7pt 等效」断言仍为 SKIP 登记**（沿回退轮口径，需逐图文字像素高折算，另任务）。
4. g2 右缘例外窗 [4.5,5.5] 系**逐图钉**，若后续改文栏宽使断点跳到 47.3/49.6 档，该窗与缝窗需一并复标。
5. `进度看板.md` 登记（拟「四十三、g1/g2 并排恢复闭环」）留主脑落笔，本简报§一～§五即其素材。
