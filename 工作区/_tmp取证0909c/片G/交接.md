# 片G（六图下置集成）交接 · 第 5 次续跑落盘

时间：0910 中午。当前 PDF：`variantF\main.pdf` **7 页 / 三0 全 0（error 0／overfull 0／underfull 0）**，
md5 `5c4c9e85ce53d25c7f2ed14b05b5d4a5`（基线 H3 为 33dc5db8…）。**页数未增（7→7）**——与上轮摘要
「预计 9–11 页」的推测不符，原因：并排绕图基线里 `raisebox` 声明盒已把后续文字推到图底之下，图高本已
占用纵向空间；改下置只是把「图旁窄栏＋通栏余段」换成「通栏整段＋图独立行」，并省下窄栏断行的多行，
净增被抵消（此结论须在最终报告作显著说明并给实测佐证）。

## 一、已完成（均已实测核验）

1. **六片段入件**：`variantF\figs\g1-prism / g2-cubeE / g3-cube6 / g4-dihedral / g5-fold / g6-triple.tikz`
   （上一轮已写并自检：零空行、零 `\head{`／`\includegraphics`／`\documentclass`／`\begin{document}`）。
2. **导言宏就位**：`qp-blocks.tex` L34-42 加 `\qldrop/\qldsub/\qlabs/\qlab`＋`\definecolor{cubegray}`（纯增量，
   已实测不破坏编译）。
3. **生成器集成完成（10 处 Edit，`postproc_daoxue.py`）**，`python postproc_daoxue.py` **exit 0**：
   - 第 3 步节头＋`FIG`：六图全改 `('under','')`；
   - 新增 **3d 段**：`TIKZ`（六图→片段路径＋置宽 41.1/42.0/84.0/66.3/54.7/84.0）、`STEMFIG={3,6,8}`、
     `DETAILFIG={2,9}`、`under_center_row()`（发 `\par\vspace{1.9mm}\penalty10000\noindent\makebox[\linewidth][c]{\resizebox{盒}{!}{\input{figs/x.tikz}}}\par\vspace{-1.0mm}\penalty10000`）；
   - `reflow()`：`under` 分支插在 `assert w` 之前（顺序要紧）；
   - 例1 发射循环：`consume_side`/`side_row`/`detail_tok` 三路并排逻辑整体退场，改「题干图＝◆标签行下独立行、
     详解图＝【详解】首块文字下独立行」，`extract_fig` 仍用于摘除旧图 token；
   - **R2 空位口径适配**：探八题干裸 `()`（side_row 退场后不再被右挂消费）→ 就地转 `(\kongwei)` 并并入
     R2② `是\nobreak(\kongwei)` 禁断；`n_shi` 断言 3→4、`n_hang` 1→0、`n_kw8` 语义改「残留 0」、
     kongwei 总数维持 9；6a-F log 文案同步改写；
   - **条目3 尾距挂载点**：`body[-1]` → 「末**文字** `\bindp` 段」（排除含 `\input{figs/` 的图段）；
   - 第 8 步：新增 `n_under`/`n_gref` 计数；总断言 `n_side==5→0`、`n_split==2→0`、加 `n_under==6`、`n_gref==0`；
     第 8 步 log「图文绕图回流」行改写为下置台账；
   - 备份：`postproc_daoxue.py.bak_片G集成0910` 等在位（五件齐全）。
4. **body.tex 成品核验**：`\includegraphics` **0** 处；`\input{figs/` **6** 处（行 76=g6 三联、152=g1、
   170=g2、250=g3、305=g4、337=g5），六处形制一致（`\bindp` 前缀＋1.9mm 前胶＋makebox 居中＋resizebox＋−1.0mm 尾胶）。
5. **编译核验**：xelatex 两遍 exit 0，`main.log` error/overfull/underfull/vbox/缺字/警告 **全 0**。
6. **图真入件核验（矢量口径探针）**：`_tmp取证0909c\片G\diagprobe.py`（非轴对齐墨迹聚簇）命中 **恰 6 簇**，
   逐页 p2c2 / p3c2 / p4c1 / p5c1 / p6c1 / p6c2，栏心偏差 −0.07／+0.76／−0.20／−1.23／−0.26mm（居中达标档），
   p2c2 一簇 dev=+29.28mm 且尺寸 24.15×15.24 → **疑为三联图 g6 的局部簇**（三联含三段小图，簇被 6mm 间隙
   断开），须复核（见下「未完成」1）。

## 二、未完成（精确续做指令）

1. **三联图簇复核**：`diagprobe.py` 的 gap 参数（现 6mm）对 g6 过小，致 84×22mm 整图裂成多簇；把 gap 提到
   10mm 或改「同 y 带内 x 向间隙 <8mm 即并簇」，确认 g6 成一簇且 w≈84.0、dev≤1.2mm。
2. **六图终版逐图实测表**（尺寸/居中/前距/下距）：以 g6 修复后的 diagprobe 为基础，扩为逐图打印
   `置宽目标 vs 实测墨宽 mm`、`栏心偏差`、`上邻文字末行底→图墨顶`、`图墨底→下邻文字首行顶`。
3. **双断言适配（当前状态：`断言顶格.py` exit 1，`_测v4断言.py` 未跑）**：
   - `断言顶格.py`：docstring L21-33 旧口径改写；L80 `n_fig` 保留但换矢量口径；L134-141 (e) 段
     改「六图矢量墨区簇 ∈ 栏带内」；L146 `ok = viol==0 and not fail and n_fig==0 and n_vec==6 and n_ans==14 and n_leftedge>=6`。
     （实测现值：签名行 128／[答案]14／表线 74（左框线 16，违规 0）／图 0 → 只 (e) 口径破，其余维持。）
   - `_测v4断言.py`：⑱（L583-596）、⑱-2（L598-666）、⑱-3（L668-750）、⑮（L1813-1835）、N6（L1096-1132）
     五处按已定 D 方案改写；⑭（L1794-1811）与 ⑤（L310-323）跑后按实测校准；任何门值改动逐条 `reg()` 登记旧→新。
     脚本大（126KB），Edit 前必须先用 Bash 里的 python 行切片读原文（`Read` 工具带 `line_offset` 在本机
     恒报 schema 错，禁再用）。
4. **postproc 剩余文书**：8c 台账文案（L1207-1216）改写为「片G 0910 六图下置」；文件头 docstring 追加
   「片G 0910 六图下置集成轮」。
5. **维持项抽点复测**：字距 +0.10em／标点半角／栏宽 84.0／拉伸异常行=0／判断题序号隙／条目号隙／＝∥墨隙／
   数学式档／页码出血／栏线 188／句末 `.`。
6. **取证**：全页 150dpi png ＋ 六处「终版裁片 vs 源位图 `media\media\imageN.png`（三联 `sub3_B_4.png`）同尺对照」
   ＋ 所在整块上下文片，落 `_tmp取证0909c\片G\`。
7. **最终报告**：素材盘点/逐图集成口径、改动文件＋行号、三0 对比、页数（7→7，须显著说明）、六图实测、
   维持项复测表、断言适配登记、裁片路径、遗留风险（旧位图仅取消引用未删；`alt={@@@…}` 溯源标记随
   token 下置消失，断言无依赖）。

## 三、红线（不变）

只动 `variantF\`；临时产物只落 `_tmp取证0909c\片G\`；**禁 git**；禁碰 附则/总控/进度看板；
429 后 sleep 90–120s 重试 ≥3 次，命令分批、长输出重定向落盘后小段读。
