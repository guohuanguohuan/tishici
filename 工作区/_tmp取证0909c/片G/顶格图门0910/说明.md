# 顶格图门 0910 —— `断言顶格.py` (e) 段 矢量口径 → 位图口径

件：`工作区/字替对照-0909/variantF/断言顶格.py`（改前备份 `断言顶格.py.bak_顶格图门0910`，同目录）
产物目录：本目录。禁 git 未用；只动该一个脚本＋本产物目录；附则/总控/看板/其他件零改动。

## 1. 改动（仅 (e) 图门＋其语义注＋打印文案；(a)(b)(c)(d) 判据零改动）

| 处 | 改前（片G 矢量口径） | 改后（位图口径） |
|---|---|---|
| (e) 图源 | `page.get_images(full=True)`＋`get_image_rects` 计位图，另以 `_vec_clusters` 矢量墨聚簇计 `n_vec` | `page.get_image_info(xrefs=True)` 逐页归集 raster 图矩形，按原生像素尺寸认领 g1~g6（`FIG_PX`） |
| (e) 违规 | 越栏（出栏带）＋矢量簇越栏，两类计数 | 零容忍两类：**越栏**（`n_viol_e`，x0≥栏左−1 ∧ x1≤栏右+1）＝0；**压字**（`n_press`，图矩形 ∩ 任一正文行 bbox）＝0 |
| ok 式 | `n_fig==0 and n_vec==6` | `n_fig==6`（＋viol 全 0 ∧ `n_ans==14` ∧ `n_leftedge>=6` 不变） |
| 文案 | `｜位图 N（违规 x）｜矢量图 N（违规 x）` | `｜位图×6（尺寸认领 6/6；越栏 0，压字 0）`；docstring「片G (e) 换矢量口径」段整段换「回退轮 (e) 换回位图口径」段 |

`_vec_clusters`/`in_fig` 保留：现只服务 (d)（排除图内竖棱），位图时代实测 0 簇、`in_fig` 恒不触发，(d) 行为逐字节不变。
`n_vec`/`n_viol_vec` 标识符已全数退场（grep `n_vec`＝0 命中）。

## 2. 复验（同一 `main.pdf`，mtime 2026-09-10 21:03，句末轮终态件）

- 改前（跑 `.bak`）→ `改前输出-片G矢量口径门.txt`：**exit 1／未通过**
  `签名行 125（[答案]14；违规 0）｜表线 64（左框线 16；违规 0，豁免 0）｜位图 6（违规 0）｜矢量图 0（违规 0）`
- 改后（跑件）→ `改后输出-位图口径门.txt`：**exit 0／通过**
  `签名行 125（[答案]14；违规 0）｜表线 64（左框线 16；违规 0，豁免 0）｜位图×6（尺寸认领 6/6；越栏 0，压字 0）`
- **零计数漂移**：签名 125、[答案] 14、表线 64、左框线 16、违规 0 两侧全等——无一处硬凑。

## 3. 与既有记录比对

- `句末标点0910/复跑06-顶格断言.txt`＝改前件·终态件：`位图 6／矢量图 0／未通过`——与本轮 `改前输出` 逐字符一致（125 系该轮已解释的**收紧**：p4/p5「2．」分数分母＋句末点、p7「D．」答案字母＋句末点并组脱落，已采纳）。
- `句末标点0910/复跑06b-顶格断言-改前基线A-B.txt`＝A/B 另一侧：`签名 128、其余同、未通过`——**改前基线件与改后终态件同为「未通过」，违规两侧均 0**，坐实「口径未跟图源，非内容回归」。
- `回退轮0910/实测位图门值.txt`：PDF 位图对象数＝6、六图「侵入」（正文行 600dpi 真墨 ∩ 图矩形外扩 0.5mm）＝0——本门 bbox 级（不外扩）判压字 0，与墨级 0 互证；六图置宽 84.0/28.8/32.7/54.1/47.5/42.5mm 与像素认领（1408×374…1798×1350）逐图对上 `N6 位图台账`。
- `回退轮0910/断言-改后.txt`：`_测v4断言.py` ⑱/⑱-2/⑱-3/N6/⑮ 已位图口径——顶格脚本自此与主断言同口径。

## 4. 咬合反证（门会红，非空判）

`_探针-图门咬合.py`（只读件、内存内改两行后 exec；件字节不动）→ `探针输出-咬合反证.txt`：

- T1 图矩形 y1 拉长 60pt：压字 6、越栏 0 → **未通过 exit 1**（逐图报图号＋相交行数＋首行文字）
- T2 图矩形右移 30pt：越栏 1、压字 0 → **未通过 exit 1**
- T3 抹去 g1-prism 一件：`位图×5` → **未通过 exit 1**（`n_fig==6` 生效）

## 5. 「片G 矢量口径」残留清单（grep `vec|矢量|tikz|n_fig|n_vec`，本任务未动）

脚本内 17 处命中，逐条归类：

1. `断言顶格.py:213` (d) 门注「片G 0910：矢量图内竖棱（正方体竖棱等）不算表线——保 64 口径可比」——片G 文案残留，行为不变（0 簇）。
2. `断言顶格.py:95–164` 矢量墨聚簇内核 `_diag_items`/`_vec_clusters`（＋挂载点 `:170–175` `vcs`/`in_fig`、`:212–213` (d) 排除）——实测 0 簇，事实死码，保留供 (d) 防御；仿 `回退轮0910` 对 `_测v4断言.py` 内核「保留停用」处置，可下轮统一定夺。
3. `断言顶格.py:32–34` v4.4 历史注「图构成＝5 并排 side 0.56＋1 居中 60mm」「预期：图 6（5 并排 side…）」——并排时代旧史（现 body.tex minipage 0、\input{figs 0）。
4. `断言顶格.py:23–24` 片E 0909c 注「图旁前缀仍为 minipage 并排（本断言口径不变）」——同上旧史，已不成立。
5. `断言顶格.py:4–5` v4.2 结构复查①「题干图 minipage 并排行（0.56／0.40）…图仍在栏带内进 (e)」——同上旧史（(c) 段口径本身不受影响）。
6. `断言顶格.py:89–92` `FIG_PX` 以原生像素尺寸钉死六图——仅佐证不硬门；换图源（重采样/换档）须同步该表，已在注释登记。
7. 件内图源侧残留（非本脚本，`figs/README-退役.md` 已注）：`figs/*.tikz`×6 留档在盘不删；`qp-blocks.tex:32–42` `\usepackage{tikz}`＋素材宏 `\qldrop/\qldsub/\qlabs/\qlab`＋`cubegray` 色仍载（tikz 本体为 `\huadia` 花形所需，不可撤；素材宏/色不入页）。
