# 残留清理 0911 —— variantF 双断言脚本 7 条已列报残留处置

- **日期**：2026-09-11｜**件**＝`工作区/字替对照-0909/variantF/`（只动 `断言顶格.py`＋`_测v4断言.py` 两脚本＋本产物目录）
- **依据**：`顶格图门0910/说明.md` §5 残留清单＋任务列报 7 条｜**改前备份**：两脚本 `.bak_残留清理0911`（同目录）
- **禁 git 未用**；附则/总控/看板/件内其他文件零改动；`qp-blocks.tex`（`\huadia` 素材宏/tikz）未动。

## 一、逐条处置

| # | 处（改前行号） | 处置 | 依据与做法 |
|---|---|---|---|
| 1 | `断言顶格.py:213` (d) 段「片G 0910：矢量图内竖棱…不算表线」文案注 | **删**（随 :212 `if in_fig(r): continue` 一并删除） | 位图时代 0 簇→该分支恒不触发；注所述「片G 矢量」口径已随图源回退失效。grep 证 `in_fig` 全文仅 :172/:175/:212 三处（全属被删内核），无其他调用。 |
| 2 | `断言顶格.py:95–164`＋`170–175` 矢量墨聚簇内核（`_diag_items`/`_vec_clusters`＋挂载 `vcs`/`in_fig`） | **删**，原位落 4 行折叠注（:98–101） | 删前 grep 证 `_vec_clusters`/`_diag_items`/`in_fig`/`vcs` 除内核自身与 (d) 挂载外全文 0 调用；件内无脚本 import/exec 本件（`_探针-图门咬合.py` 仅 replace (e) 段位图行 `r = pymupdf.Rect(info['bbox'])`，该锚点仍在、未破坏）。现 grep 三命中全在折叠注/docstring，非代码。`qp-blocks.tex` 宏不在此件、未动。 |
| 3 | `断言顶格.py:32–34` v4.4「图构成＝5 并排 side 0.56＋1 居中 60mm」＋预期行 | **改**：标注「旧史——图源回退后不适用」，预期行改「图 6（位图）」并署「残留清理 0911 更正口径」 | body.tex 实证：`\includegraphics`×6／`resizebox` 0／`input{figs` 0；并排恢复0910 之 g1/g2 系文左图右新 minipage（28.8/32.0mm 图盒），非 v4.4 side 0.56 旧形。SIG/[答案] 行等仍真部分原样保留。 |
| 4 | `断言顶格.py:23–24` 片E「图旁前缀仍为 minipage 并排（本断言口径不变）」 | **改**：标注「旧史（图源回退后不适用——题干图并排已退场…）」 | 同上实证；⑪ 登记亦证「探二/探九题干由并排 minipage 改内联」。片E 余段通栏独立段描述仍真，保留。 |
| 5 | `断言顶格.py:4–5` v4.2① 题干图 minipage 并排行（0.56/0.40） | **改**：句首标「【旧史（图源回退后不适用）】…已退场」 | 同上实证；「题干行非签名行不进 (c)」判据本身不受影响，照旧登记。 |
| 6 | `断言顶格.py:89–92` `FIG_PX` 像素尺寸钉死 | **留**，注内补「像素尺寸为钉死值——换图源（重采样/换档）须同步本表」 | 按列报要求；`FIG_PX` 仅佐证不硬门（认领 6/6 打印用），行为零改动。 |
| 7 | `_测v4断言.py`「线框法内核·保留停用」段（:206–287＋:781–1121） | **折叠删除死码主体，保留活码基础定义**（见下明细） | ast 调用图分析＋逐名 grep（详见 §二）；两段各落折叠节注登记。 |

### 第 7 条明细（`_测v4断言.py`，2540→2149 行）

- **删（死码，grep/ast 证全文 0 调用或仅被死码调用）**：
  - :206–287 片G 矢量图簇：`_VEC_CACHE`/`_diag_rects`/`vec_clusters`（`vec_clusters` ref=0）；
  - :781–821 内核专属常数：`KDPI/KTHR`（:963/:972 死码内）、`GAP_CLUSTER/YTOL/XGAP/MINLONG`（仅 `clusters` 用）、`LONG_MM/SEED_MM`（仅 `strokes` 用）、`REPLAY_PAD/MASK_DIL`（仅 `_replay`）、`LAB_R/LAB_PAD/LAB_MAXW/LAB_MAXH`（仅 `measure_fig`）、`MIN_PIX/COLC`（仅 `comps_rect`/`sub_ink`）、`DELTA_OK/ISO_OK/SHAPE_OK/K_SRC/K_MAP`（ref=0）；
  - :824–1121 内核函数链：`strokes`/`clusters`/`_dashstr`/`_gray`/`_replay`/`ink_mask`/`comps_rect`/`sub_ink`（ref=0 或仅被 `measure_fig` 调）＋`measure_fig`/`fig_lock`（均 ref=0）。`scipy.ndimage` 为函数内局部导入，随死码退场。
- **留（活码，位图门沿用）**：`import numpy as _np`（`line_ink600`/拉伸异常行等位图门 :1148 起沿用）；`BODY_SIZE/BODY_MINW`（`is_body` 依赖）；`text_lines`/`is_body`（位图 ⑱-2/⑱-3 门调用）；`in_fig`（**锚位图台账 `FIG_ROWS` 非矢量簇**，:2416 防御调用仍在——与 `断言顶格.py` 的 `in_fig` 不同物，未删）。
- **改**：docstring 沿革行「旧矢量线框内核保留停用」→ 补「残留清理0911 已折叠删除＝死码，见节注」；:206–212 与 :781–791 两处节注换为折叠登记注（含原文指引 `.bak_残留清理0911`）。
- **不碰**：`reg()` 登记串内「片G 旧门：…」等历史文字（口径沿革登记，非代码）；:1124 起六图位图几何门全段；⑪/⑮ 等各活门。

## 二、复验（硬性，原始输出在本目录）

| 项 | 改前（跑 `.bak` 前基线） | 改后（跑件） | 判定 |
|---|---|---|---|
| `python 断言顶格.py` | exit 0／通过：签名行 **125**（[答案]14；违规 0）｜表线 **64**（左框线 16；违规 0，豁免 0）｜位图 **×6**（认领 6/6；越栏 **0**，压字 **0**） | **输出与改前逐字节一致**（diff 空），exit 0／通过 | **零漂移** ✓ |
| `python _测v4断言.py` | exit 0，**✓51／◎45／✗0** | exit 0，**✓51／◎45／✗0**；与改前全文唯一差异＝既有 `SyntaxWarning: invalid escape sequence '\m'` 的行号 1429→1038（上方删行所致位移，警告本身改前即存在，非本轮引入、非计数） | **计数零漂移** ✓ |
| `py_compile` 两件 | — | 双 OK | 语法完好 ✓ |
| 被删名残留 | — | 非注释行 grep（`vec_clusters/_diag_rects/_VEC_CACHE/measure_fig/fig_lock/strokes/_dashstr/_gray/_replay/ink_mask/comps_rect/sub_ink/KDPI/KTHR/LAB_R/COLC/K_SRC/K_MAP/DELTA_OK` 等）＝**0 命中**；`text_lines/is_body/BODY_*/in_fig` 命中均为保留活码 | 死码删净 ✓ |
| 探针咬合锚点 | — | `断言顶格.py` 内 `        r = pymupdf.Rect(info['bbox'])` 恰 1 处 | `_探针-图门咬合.py` 锚点未破坏 ✓ |

`xelatex` 未重跑（脚本注释/死码改动不触件，body.tex/main.pdf mtime 未变）。

## 三、边界与移交

1. 既有 `SyntaxWarning`（`_测v4断言.py` reg 串 `\makebox` 的 `\m`，现 :1038）系历史存在，本轮未修（不在 7 条列报内）；如需消除把串内 `\m` 写成 `\\m` 即可，字符串值不变。
2. 改前全文见两 `.bak_残留清理0911`；本目录存改前/改后原始输出四份。
