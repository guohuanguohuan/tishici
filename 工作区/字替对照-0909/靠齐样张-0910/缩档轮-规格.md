# 缩档轮·规格（2026-09-10 用户批准；并排恢复并轨）

> 目的：销「图太大」观感账（全品基准总表 L47 未销项）＋恢复符合全品的并排形制。用户令：判定以字号为准；并排缝不得再「门绿观感远」。

## 一、两级判据

- **主判据＝图内字母标签字号**：目标 ≈**7.5pt**（正文 10.5pt 的 70–75%），下限 7.0pt；配线宽 ≈0.21mm（附则/全品零件库.md TW-05）。
- **校验判据＝外廓**：落全品档——题侧 26.5–32mm／题下居中 27.7–36mm（TW-01/02）；并排每张 18–24mm（TW-03）。
- 冲突处置序：**缩放反解 → 仍超则收紧图内几何（TikZ 坐标压缩）→ 再超逐图登记例外**；硬闸＝不超栏宽 84.0mm。

## 二、执行序

1. **测量**（agent-51 在跑，只读）：逐图「字号 min/中位/max、线框/含标外廓、笔画线宽」实测＋按 7.5pt/7.0pt 反解新置宽＋逐图处置（A 直接缩放／B 需收紧几何／C 例外），落 `靠齐样张-0910/缩档测量/`。
2. **改件**：按测量表改 `variantF/body.tex` 六行 `\resizebox` 置宽；同步改 `_测v4断言.py` 的 ⑱ tex 门 `FIG_W`／`BOX_MM` 与 ⑱-2 相关窗值，逐条 `reg()` 登记旧→新。
3. **重跑**：`权威实测.py`＋`_测v4断言.py` 全量（目标：三 0、0 ✗）。
4. **并排恢复（并轨第二步，三项）**：
   - ① **紧 bbox**：每片段加/收紧 `\path[use as bounding box]`，画布锁到墨缘（g5 现无锁定，一并补）；
   - ② **墨缘定胶**：并排盒间胶按「图墨缘−文墨缘」反解到全品档（最小缝 ≈6.7mm）；
   - ③ **盒缝门回归**：断言改测「盒缝」（图盒左缘−文右缘），与观感同尺（旧「墨缝过门、盒缝观感远」的脱钩问题销账）。
5. **验收**：编译三 0；实测复验标签字号 ≈7.5pt＋外廓落档；并排图逐图目验缝宽；主脑逐页目验；复审计按例。

## 三、分臂（分臂对照制）

- 本件（步骤 2–3 的改件与重跑）交 **glm 臂**执行；**qwen 臂**继续现有样张收尾——两臂各出真货、不跑重复任务；回来后同尺比 KPI（三 0 率／耗时／一次过率／目验质量）。
- 步骤 4 视步骤 1–3 结果决定随行或另起小轮。

## 四、红线

- 只动 `variantF/` 指定行＋对应断言门值；改前留 `.bak_缩档0910`；禁 git（收口提交由主脑统一）；临时产物只落 `靠齐样张-0910/缩档测量/`。
