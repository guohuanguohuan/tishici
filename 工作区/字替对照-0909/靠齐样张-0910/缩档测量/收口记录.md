# 缩档轮收口 0910·收口记录（两件）

执行臂：LaTeX/构建链收口轮（前轮改件已完成，本轮只收口）。件＝`工作区/字替对照-0909/variantF/`。
基线（改前）md5：`body.tex 0c4d0cfc8a27369bc5158fa5c77eb58f`｜`chapterhead.tex 75b5b78b8cfa033713197db1c7559555`｜`main.pdf 7c382048848a4ffa53037cda5bc23e8e`（与简报登记值一致）。
改前备份：`variantF/postproc_daoxue.py.bak_缩档收口0910`、`variantF/body.tex.bak_缩档收口0910`。

---

## 件 1　生成器与产物同步（防「重跑即冲销」）——完成，diff 为空

**改动**：`postproc_daoxue.py` TIKZ 置宽表（原 L155–160）四值改钉＋配套注释（表头注、FIG 行注、3d-A 日志行、8/8c 台账文案）。g1 41.1／g2 42.0 不动。

| 片段 | 改前 | 改后 |
|---|---|---|
| g6-triple（sub3_B_4.png） | 84.0（→实发 `\linewidth`） | **80.8** |
| g3-cube6（image3.png） | 84.0（→实发 `\linewidth`） | **36.3** |
| g4-dihedral（image4.png） | 66.3 | **30.5** |
| g5-fold（image5.png） | 54.7 | **28.2** |

改前/后表片段全文：本目录 `收口-T1-TIKZ表-改前.txt`／`收口-T1-TIKZ表-改后.txt`。

**重跑验证**：改钉后 `python postproc_daoxue.py` →
- `body.tex` diff vs 改前快照＝**空输出**，md5 双证同值 `0c4d0cfc…` → **与现盘逐字节一致，重跑不再冲销**；
- `chapterhead.tex` md5 `75b5b78b…` 不变；
- `postproc_daoxue_log.txt` diff 共 8 处行级变化，全部为预期：3d-A 六行「置宽 \linewidth/66.3/54.7 → 80.8/36.3/30.5/28.2」字符串＋第 8/8c 两行陈旧口径文案更正（「源尺寸档」→「缩档轮 0910 改钉档」、台账置宽串）。无任何功能差异。
- 8c/8 文案同步后再次重跑，body.tex／chapterhead.tex md5 不变。

证据：`收口-T1-diff结果.txt`（md5＋空 diff 实录＋log 全量 diff）、`_收口-log改前.txt`（改前日志快照）。

---

## 件 2　收口唯一 ✗（⑦ 条目缝第 5 对 p2c2 条目3→4：2.54，越窗下沿 0.16mm）——完成，走试法二

**病灶**（改前探针实测）：p2c2 上末行「称为向量 a→ 在平面 β 上的投影向量．」β 降部墨底 y162.14（带21）→ 条目4 号行「4.」带顶 y164.68（带22），缝 2.54（窗 2.7–3.6）。

### 试法一（任务书首选候选：g6 图行尾胶 −1.0→−0.8mm 逐图覆写，经生成器实现）——已试，回退

- 实测（`_收口-试法一-测v4.txt`）：缝 2.54→**2.71**（回窗）；g6 下距 3.13→3.34；表底缝#3 4.10→4.07；其余读数全同改前。探针明细：上末行 y底 162.14→162.34（+0.20，刚体传播），号行带顶 164.68→**165.06（+0.38）**——号行额外多移 +0.18，证该缝带 glueguard 弹性再分配（也是改前 2.71→2.54 漂移的机制）。
- **弃因（两条，任一即弃）**：
  1. 破 ⑱ tex 形制门：`FIG_LINE` 正则把六行图行「**逐字节同式**（含 `\par\vspace{-1.0mm}\penalty10000` 尾胶）」钉死，g6 异形即「图行 5/6」新 ✗。改门即放宽标准，违「保窗严格、修几何」。
  2. 2.71 距窗下沿仅 **0.01mm（无安全垫）**——简报点名的旧读数病理复发。
- 回退：撤 −0.8 覆写、恢复 −1.0 字面，重跑 postproc 后 body.tex md5 复归 `0c4d0cfc…`（任务 1 态逐字节复现）。

### 试法二（采用：该处条目尾距逐点 0.35→0.80mm，＋0.45）

- **原则核对**：胶加在**被测缝内部**（改胶点＝缝自身），缝内刚体增量不受弹性再分配侵蚀（任凭 ±0.2 弹性，读数仍落 2.8–3.2，恒 ≥2.70）；逐点微调不触 `qp-blocks.tex` 全局档（全局 +0.45 会把第 2 对 3.56 顶出上沿 3.6）。
- **实施**：`postproc_daoxue.py` 5e 段尾挂载点 `body[_tail[-1]] += r'\tiaomutail'` → `body[_tail[-1]] += r'\par\addvspace{0.80mm}'`（展开形与 `\tiaomutail`＝`\par\addvspace{0.35mm}` 同构，仅值不同；qp-blocks 一字未动）。log 5e 段同步登记。
- **body.tex diff**＝仅 L80 一行（`\tiaomutail` → `\par\addvspace{0.80mm}`），实录见 `收口-T2-bodydiff.txt`。

### 最终读数（逐处对，`收口-复验-测v4.txt`）

| 项 | 改前 | 改后 | 窗 | 判 |
|---|---|---|---|---|
| ⑦ 条目缝 ×5 | 2.88 3.56 3.05 3.05 **2.54** | 2.88 3.56 3.05 3.05 **3.05** | 2.7–3.6 | **✓**（第 5 对垫 0.35mm，落全品带 3.06±0.2 内；前四对逐字不动） |
| ⑦ 探究点缝 ×9 | 4.57×7／4.06×2 | 同改前 | 3.3–4.8 | ✓ 不回退 |
| ⑦ 知识点缝 ×2 | 2.88 4.23 | 同改前 | 2.7–5.8 | ✓ |
| ⑦ 表底缝 ×3 | 4.01 4.10 4.10 | 4.01 4.10 **3.99** | 3.41–4.41 | ✓（#3 −0.11，垫 0.58） |
| ⑦ 解析→◆ | 4.23 | 同改前 | — | ✓ |
| ⑱-2 g6 前/下距 | 2.96 / 3.13 | 2.96 / **3.13（不变）** | 1.99–4.44 / 2.04–3.85 | ✓（改胶点在图下方，图及其上不动） |
| ⑱-2 g1–g5 逐图 | 前 4.19/3.77/2.62/3.01/2.24、下 2.29/3.60/3.09/2.75/2.41 | 全同改前 | 同窗 | ✓（W/Δ/偏/越/标/虚 全部逐字同） |
| N4b p2 行净空 | B3.77/3.43/3.43 | **3.81/3.47/3.47**（+0.04） | 2.9–4.3 | ✓ |
| ⑤a p2c2 利用率 | 97% | 98% | ≥70% | ✓ |
| ◎「⑦ 条目缝低值登记」 | 在（读低 2.54） | **退场**（无 <2.7 值） | — | 设计内 |

断言输出与改件基线逐行 diff 共 **6 处**（⑤a×1、⑦✓行×1、⑦✗行消除、N4b×1、⑦口径 reg 读数更新、结算行），零出窗、零回退。

---

## 复验（必做项，全绿）

1. `xelatex main.tex` ×2：**error 0／Overfull 0／Missing char 0**；页数 **7**（`main.pdf` md5 `6bd05cadfbff242ee850d946b539701b`）。
2. `python -X utf8 _测v4断言.py`：**✗ 0**（**✓ 51／◎ 44**；改前 ✓50／◎45／✗1——⑦转绿 +1 ✓、「⑦ 条目缝低值登记」退场 −1 ◎）。**断言文件 `_测v4断言.py` 本轮一字未动**（保窗严格之义，0 ✗ 全靠几何修正达成）。
3. 权威实测（`_权威实测-缩档臂0910.py` 驱动，口径源原样）：输出与改件基线 `缩档后-权威实测.txt` **逐字节一致**——簇 6/6｜落位（页,栏）六图逐图未位移｜有锁定五图 |Δ线框| max 0.13mm（门 0.50）｜g5 不变量全绿｜正文相交 0｜虚线保真率 1.000。
4. 三判据复核（`_复核-三判据0910.py`）：输出与 `缩档后-三判据复核.txt` **逐字节一致**——①字母字号 6/6 到档（max 偏差 0.01pt）；③有效线宽 6/6 到档（0.2067–0.2102mm）；②含标外廓 3/6 到档＋g1/g2/g6 三例外登记（照登项，未回退）。
5. `render.py`：`variantF/png/` 7 页重渲完成（150dpi，page1–7.png，缩档后新版供目验）。

## 纪律核对

- 改动文件（均改前留 `.bak_缩档收口0910`）：`variantF/postproc_daoxue.py`、`variantF/body.tex`（生成器再生成件，非手改）。
- 再生成／构建产物：`chapterhead.tex`（逐字节同前）、`postproc_daoxue_log.txt`、`main.pdf/main.log/main.aux`、`png/page1–7.png`、`_收口x1.log/_收口x2.log`（本轮编译日志，沿 `_缩档-x1/x2.log` 例）。
- 未动：`qp-blocks.tex` 等全部 qp-*.tex、`figs/*.tikz`、`_测v4断言.py`、口径源 `权威实测.py`、`sec.tex`、`附则/`、各总控、`进度看板.md`、`靠齐样张-0910/` 其他子目录。未用 git。

## 证据文件清单（本目录）

`收口记录.md`（本件）｜`收口-T1-TIKZ表-改前.txt`／`收口-T1-TIKZ表-改后.txt`｜`收口-T1-diff结果.txt`｜`_收口-log改前.txt`｜`收口-T2-bodydiff.txt`｜`_收口-试法一-测v4.txt`｜`收口-复验-测v4.txt`｜`收口-复验-权威实测.txt`｜`收口-复验-三判据.txt`

## 遗留（纯文案，不影响任何门）

`_测v4断言.py` ⑦ 条目缝口径 reg 的静态沿革文案仍写「两端点 2.71…窗 2.7–3.4」（片B 时代史述），与现行窗 2.7–3.6 及新读数（两端点已非 2.71/3.05 分布）不完全对应；读数部分系自动生成、实时正确。按「断言文件不动」原则本轮未改文案，下轮如顺手可做纯文案更正。
