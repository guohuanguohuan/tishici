# figs/ 六件 `.tikz` 退役留档（回退轮 0910）

- **性质**：AI（片G 0910 轮）按原图位图重绘的**三维几何 TikZ 矢量片段**——三棱柱、正方体系四件、二面角、折叠、投影三联。
- **退役令**：2026-09-10 用户二次令「**禁止画三维图；以前 AI 画过的一律不要；AI 只能画二维图**」。
- **处置**：`body.tex` 六行图引用已全部回退为**原图位图**（`media/media/image1…5.png`、`sub3_B_4.png`，
  即从源 Word 挖出的原始图片）；`\input{figs/…}` 与 `\resizebox` 在 body.tex 已归零（⑱ tex 形制门实测）。
- **留档**：本目录六件 `g1-prism…g6-triple.tikz`（含各自 `.bak_缩档0910`）**保留在盘、不删除**，仅作片G 矢量化轮的工作留痕，
  **不再被任何交付件引用**。`工作区/_tmp片G素材/` 内对应六件 standalone 素材（`img1_tikz.tex`、`image2_重绘.tex`、
  `tikz_tan6.tex`、`片G-image4-二面角.tikz/-standalone.tex`、`fig-image5-fold*.tex`、`sub3_投影三联.tikz/-standalone.tex`）
  同样保留在盘、全部引用清零。
- **断言侧**：依赖矢量线框口径的图门（⑱-2 线框几何、⑱-3 含标框判据、N6 矢量簇台账、⑮ 矢量簇归属）已改**位图口径**；
  原线框法内核（`_测v4断言.py`「权威线框口径内核」节）**保留但停用**——图源回退后不适用。
- **依赖宏**：`qp-blocks.tex` 中为六片段配的 `\qldrop/\qldsub/\qlabs/\qlab` 宏与 `cubegray` 色定义暂保留未删
  （片段不再 `\input`，不入页；③ 灰档门已实测 0x40 退档）。
- **详情**：`工作区/字替对照-0909/回退轮0910/简报.md`（三值表、断言改造、页数分析）。
