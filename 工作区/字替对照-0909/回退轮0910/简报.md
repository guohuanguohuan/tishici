# 回退轮0910·简报 —— AI 三维图全面回退为原图位图

- **日期**：2026-09-10｜**性质**：受控回退（用户二次令「禁画三维图；以前 AI 画过的一律不要；AI 只能画二维图」）
- **改件**（全部改前留 `.bak_回退0910`）：
  `variantF/body.tex`｜`variantF/_测v4断言.py`｜`variantF/figs/README-退役.md`（新增）｜
  `靠齐样张-0910/答案册/body.tex`（去图）｜`回退轮0910/`（本轮全部产物）
- **禁碰区未动**：附则/总控/看板；无 git 操作。

---

## 一、清点

详见 `清点.md`。须处置 8 处：variantF 六片段＋body.tex 六引用行＋答案册内联立方体；
`答案册式样-草案0909/demo` 与答案册立方体**同源同图**（逐行 diff 仅 C1—D1 虚/实一线之差）——不在只动范围，登记；
合法二维件（丝带/★/叉/平行号/卷首标/\huadia）12 处全部不动。

## 二、三值表（六图反解宽｜上限｜取用宽）

标签高＝PIL 原生像素连通域「字母主簇中位」（法＝`量标签高.py`，候选框目视核验 `候选框-*.png`/`标签montage-*.png`）；
反解宽＝位图宽px×(2.65mm÷标签高px)；上限＝min(84.0mm, px宽÷59.06cm×10)。

| 图 | 位图（px） | 标签px（依据） | 反解宽 | 上限 | 取用宽 | 片G 旧档 |
|---|---|---|---|---|---|---|
| g6-triple | sub3_B_4.png 1408×374 | 28（字母单字簇中位） | 133.26mm | 84.00（栏宽截断） | **84.0mm** | 80.8 |
| g1-prism | image1.png 691×1159 | 63.5（主字母 58–69） | 28.84mm | 117.01 | **28.8mm** | 41.1 |
| g2-cubeE | image2.png 764×764 | 62（主字母 61–64） | 32.65mm | 129.36 | **32.7mm** | 42.0 |
| g3-cube6 | image3.png 521×496 | 25.5（主字母 25–26） | 54.14mm | 84.00 | **54.1mm** | 36.3 |
| g4-dihedral | image4.png 788×424 | 44（六字母 44–46） | 47.46mm | 133.42 | **47.5mm** | 30.5 |
| g5-fold | image5.png 1798×1350 | 112（六字母 110–116） | 42.54mm | 304.44 | **42.5mm** | 28.2 |

对应关系已逐张目视核对（六图与 `对照位图-g*.png` 下半原位图逐一吻合；g6 三子图 ①② 与正文「如图①②③」互证）。
取用宽下等效墨密度：g1 610dpi／g2 594／g3 245／g4 422／g5 1075／g6 426——最低 g3 亦 >150dpi 自然档，无降质。

## 三、改件（body.tex 六行）

`\input{figs/x.tikz}` → `\includegraphics[width=<取用宽>mm]{media/media/<图>.png}`；
下置骨架六行逐字节同式（`\bindp \par\vspace{1.9mm}\penalty10000\noindent\makebox[\linewidth][c]{…}\par\vspace{-1.0mm}\penalty10000`，
模板法验证 6/6 MATCH，只差宽与路径）；`\input{figs/`=0、`\resizebox`=0。graphicx 原已装载（qp-layout.tex:46）。

## 四、断言改造（`_测v4断言.py`，改前基线 45✓/6✗ → 改后 **51✓/43◎/0✗**）

- **⑱ tex 形制门**：改核位图新形制——六行 `\includegraphics{media/media/…}` 同式＋置宽逐图钉（84.0/28.8/32.7/54.1/47.5/42.5mm）；
  `\input{figs/` 归零、`\resizebox` 归零、并排四要素继续归零。
- **共用台账**：矢量簇聚簇（vec_clusters）→ `get_image_info(xrefs=True)` 位图矩形＋原生 px 尺寸片段互证（FIG_PX）。
- **⑱-2 位图几何门**（先实测再定窗，实测±0.25mm 安全垫；实测源 `实测位图门值.txt`）：
  落位逐图钉｜置宽 ±0.5mm（实测偏差 0.00）｜栏心偏 ±0.25（实测 max 0.005）｜
  前距墨级 [2.04,3.73]（实测 2.29–3.48）｜下距墨级 [2.06,3.89]（实测 2.31–3.64；g2 栏末图免检）｜
  零侵入（含标签·墨级）＝正文行 600dpi 真墨 ∩ 图矩形外扩 0.5mm == 0（实测六图 0）。
  旧线框门值（前距 1.99–4.44／下距 2.04–3.85／栏心偏 ±1.45／Δ线框 ≤0.50／越盒 ≤0.30／标签数==素材／虚线保真 ≥0.99）逐条 `reg()` 登记；
  **原线框法内核保留但停用**（加注「图源回退后不适用」；text_lines/is_body/ink_mask 等基础定义仍被位图门沿用）。
- **⑱-3**：图旁零文字（正文行与位图矩形垂直重叠>1pt==0，实测 0×6）＋上邻在场＋下邻在场**或栏末**（g2 一处免检登记）。
- **N6**：位图恰 6＋px 互证＋阅读序＋落位逐图钉（g2 旧 p4c1→新 p3c2，余五图未位移）。
- **⑮**：位图矩形台账逐图归属——g1→二／g2→三／g3→六／g4→八／g5→九 全不变。
- **③ 灰档**（连带项）：片G 增第 7 档 cubegray 0x40（矢量灰棱上浮）随回退退役——白名单回位图时代 6 档 {76,77,119,122,189,221}，旧档登记。
- **in_fig**（⑪-3 印答线门）：判据改锚位图矩形台账（防御保留），实测计数不受影响。

## 五、页数与分页（如实报）

**7 页保住**（xelatex×2：0 error／0 Overfull／0 Missing character；Underfull 6＝与改前基线 `_缩档-x2.log`/`_收口x2.log` 同数同位，无新增）。
六图置宽变化→图高净变化约 **+7.7mm**（g1 −20.6／g2 −9.3／g3 +16.9／g4 +9.2／g5 +10.7／g6 +0.8），
由 p3–p6 双栏余量吸收；唯一落位推移＝**g2 由 p4c1 上移至 p3c2 栏末**（图缩小 42.0→32.7mm 后 p3 右栏可容图至栏底，
其 [分析] 起文字流 p4c1；图墨底距版心下际 3.5pt）——属正常两栏回流，非挤压硬凑，⑱-2/⑱-3 按「栏末图」口径免检下距/下邻并登记。

## 六、答案册图处置

`靠齐样张-0910/答案册/body.tex` 题 11（正方体 ABCD-A₁B₁C₁D₁、E 为 BB₁ 中点求 AE·BD）内联 AI 三维立方体：
查 `variantF/media/media/`（9 张原位图逐一目视比对，image2 系「E∈A₁C₁」另一题图）与 `工作区/_tmp片G素材/`
（全部为六图派生件，md5 比对无该题源图）——**均无该题原位图**；且草案简报 §288 自证该题为式样草案**新拟演示题**、
TikZ 立方体即其原创配图（无更早位图可回）。→ **按令去图**（宁可去图不留 AI 三维）：
删 `\ansfig{…tikzpicture…}` 26 行（留注释登记缘由），详解首「如图，」二字随之删（去图后不悬空，登记）。
重建：xelatex×2 三 0＋Underfull 0、**恰 2 页保住**、check.py 退出码 0（J3 图文重叠 0）、render.py 重渲 png 两张目视确认无图。
**后续项登记**：`答案册式样-草案0909/demo/body.tex:40-64` 仍含同源 AI 立方体（式样定稿基线，不在本轮只动范围——是否同样处置请主脑定夺）。

## 七、退役留档

- `variantF/figs/`：六件 `.tikz`＋六件 `.bak_缩档0910` **保留在盘不删**；新增 `README-退役.md`（AI 三维重绘、0910 用户令退役、回用原位图、所引断言已停用）。
- `工作区/_tmp片G素材/`：六件 standalone/素材 tex＋编译产物保留在盘。
- 引用清零核查：全库 `*.tex` 对 `figs/*.tikz` 的引用＝0（交付链）；唯一残引＝`_tmp取证0909c/片G/线审0910/我方探针.tex`
  （历史取证探针，非交付链，文件在盘不断链，不动）；`_测v4断言.py` 内 K_MAP/fig_lock 为**停用内核备查路径**（图门断言除外口径）。

## 八、复验产物清单（本目录）

`xelatex1.log`/`xelatex2.log`（variantF 两遍原始输出）｜`断言-改前基线.txt`（45✓/6✗）｜`断言-改后.txt`（**51✓/43◎/0✗**，exit 0）｜
`实测位图门值.py`/`.txt`（定窗实测）｜`量标签高.py`＋`标签montage-*.png`＋`候选框-*.png`（三值表取证）｜
`render.py.txt`（7 页 png 重渲）｜`对照位图-g1…g6.png`（六张件内渲染 vs 原位图同尺并排，逐张目视吻合）｜
`答案册-check.py.txt`（去图后自检＋重渲记录）｜`补丁-断言位图口径.py`（断言改造脚本，锚点断言式）｜`清点.md`｜本简报。
