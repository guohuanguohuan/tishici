# 回退轮0910·清点（AI 三维图在交付/样张链上的出现处）

清点日期：2026-09-10｜口径：全库 grep `tikzpicture`／`\input{figs/`／`.tikz` 引用，逐处目视分类。

## 一、须回退的 AI 三维图（本轮处置对象）

| # | 位置 | 内容 | 处置 |
|---|---|---|---|
| 1 | `variantF/figs/g1-prism.tikz` | 直三棱柱 ABC-A₁B₁C₁（AI 三维重绘） | 退役留档（盘上不删） |
| 2 | `variantF/figs/g2-cubeE.tikz` | 正方体＋E∈A₁C₁（AI 三维重绘） | 同上 |
| 3 | `variantF/figs/g3-cube6.tikz` | 正方体六线（AI 三维重绘） | 同上 |
| 4 | `variantF/figs/g4-dihedral.tikz` | 二面角双平面（AI 三维重绘） | 同上 |
| 5 | `variantF/figs/g5-fold.tikz` | 折叠四面体（AI 三维重绘） | 同上 |
| 6 | `variantF/figs/g6-triple.tikz` | 投影三联（AI 三维重绘） | 同上 |
| 7 | `variantF/body.tex` 六行（原 76/152/170/250/305/337 行） | `\resizebox{宽}{!}{\input{figs/x.tikz}}` 图引用 | 改 `\includegraphics[width=宽mm]{media/media/图.png}`，回用原位图 |
| 8 | `靠齐样张-0910/答案册/body.tex:98-122` | 内联 tikzpicture 立方体（正方体 ABCD-A₁B₁C₁D₁、E 为 BB₁ 中点，AI 三维） | **去图**（源素材查无原位图，见简报§六） |

## 二、同源同图核查（`答案册式样-草案0909/`）

- `答案册式样-草案0909/demo/body.tex:40-64` 与上表第 8 项**同源同图**（逐行 diff 仅一处不同：
  `(C1)--(D1)` 草案虚线→样张实线）；草案简报 §288 自注「带图解答例（题 5，**TikZ 正方体**）」——
  该题为式样草案**新拟演示题**，立方体即其原创配图，**不存在更早的原图位图**。
- 草案 demo 属式样定稿基线（只读引用件），不在本轮「只动」范围——**未动，登记为后续项**（若草案链也要执行禁令，需主脑另行发令）。

## 三、合法二维件（不动）

`靠齐样张-0910/` 其余 `tikzpicture` 逐处目视分类：

| 位置 | 用途 | 分类 |
|---|---|---|
| `导学增量/qp-fonts.tex:71-74`／`测评卷/qp-fonts.tex:71-74`／`练习线/qp-fonts.tex:71-74` | `\cha` 叉字符号（行内 2D 字形） | 合法二维·不动 |
| 同上三件 `:149-152` | `\parallel` 平行符号（行内 2D 字形） | 合法二维·不动 |
| `测评卷/main.tex:66-78` | `\sishou` 页角丝带（平面对色块＋白线＋45° 文字） | 合法二维·不动 |
| `练习线/main.tex:55-58` | `\juanmark` 卷首标记 | 合法二维·不动 |
| `练习线/main.tex:89-92` | `\lxstar` ★五角星 | 合法二维·不动 |

另：`variantF` 件内 `\cha`/`\parallel`/`\huadia`（菱形）同属合法二维，未动。

## 四、素材侧留档件（保留在盘不删）

- `工作区/_tmp片G素材/` 六件 standalone/素材：`img1_tikz.tex`、`image2_重绘.tex`、`tikz_tan6.tex`、
  `片G-image4-二面角.tikz`＋`-standalone.tex`、`fig-image5-fold.tex`＋`-body.tex`、`sub3_投影三联.tikz`＋`-standalone.tex`
  （及各自编译产物 pdf/log/aux）——保留，引用清零（断言停用内核的 K_MAP 备查路径除外）。
- `variantF/figs/*.tikz.bak_缩档0910` 六件——保留。

## 五、历史取证引用（不属交付/样张链，不动）

- `工作区/_tmp取证0909c/片G/线审0910/我方探针.tex:12-17`：以绝对路径 `\input` 六件 figs/*.tikz——
  0909c 取证轮探针（历史留痕件），非交付链；figs 文件在盘未删，该探针仍可编译，不构成断链。

## 六、依赖宏登记（未动范围）

- `variantF/qp-blocks.tex`：`\qldrop/\qldsub/\qlabs/\qlab` 宏、`\definecolor{cubegray}`、`\usetikzlibrary{shapes.geometric}`
  系片G 为六片段配的导言——片段退役后不再入页；本目录不在「只动」范围，**未动**（README-退役.md 已注记）。

**合计：须处置 8 处（六片段＋body 六行＋答案册立方体）；合法二维不动＝靠齐样张 9 处（\cha×3＋\parallel×3＋丝带＋卷首标＋★）＋variantF 3 处（\cha/\parallel/\huadia）；同源登记 1 处（草案 demo）；素材留档 6 组；历史探针 1 处。**
