# qp-m3 两钉·出战重现报告（M3 toolchain 实修轮）

日期：2026-09-14｜执行：M3 toolchain 实修代理｜母本：`工作区/_tmpM3两钉预研0913/负测/patch/qp-m3.sty`（md5 `7c3930362be8a0a2bdf21bbf8ac16573`）
落钉三副本（同 md5 四侧全等）：`M3-第2章量产0913/成卷/toolchain/qp-m3.sty`＝`工作区/_tmpM3toolchain0913/qp-m3.sty`＝`M3-第2章量产0913/成卷/toolchain/冒烟0914/qp-m3.sty`（前版 `c8a5b7e0…` 已整替，钉版行见 `toolchain钉档.md` §四）
红线自查：零 git（全程未运行 git）；写前核 mode＝auto；写入仅三副本 sty＋钉档＋冒烟探针＋本目录 `_tmpM3两钉实修0914/`；题面库/定稿层/M2·P1 换装正件全程只读（副本树仅沙箱复编对照，未落一笔）。
本文所有截图在 `截图/`，全部由本机只读渲染自产；对照件复编在 `复编-*/`（新 sty 本地挂载，cwd＝沙箱，xelatex 每档两遍取末遍）。

---

## 〇 一句话结论

两钉落盘后全部病灶按预期病愈：**①速查表行高 1.7mm 恢复**（EH 0.0→4.83694pt、表盒高 22.18758→27.26637pt，测评卷 p3 速查表区三面对勘旧病面行高塌缩、钉后面与原印面像素全同）；**②\parallel TikZ 矢量框回归**（滚A p1 十框位逐框坐标与原印面**全等**、旧病面 0 框、最小件 5 处 ∥＝10 框形貌 5.5×10.17pt）；**③三栏括线不再横穿**（ansblock 内 `\linewidth` 1086.90pt→341.43306pt＝120mm 栏宽，钉③照方案维持件面 `\jpcol` 主落点、sty 观望位不动）；**④M2 副本树测评本三件对照件复编 7 页全部 0.0000% 像素零差＝换装硬门达成**（钉前 0.3552%/0.0743%+0.3046%/0.2875% 三处病页全归零，且件面补钉已证明冗余——纯对照件即归零）；**⑤冒烟四编三零全绿**（页数 4/3、ANSKEY 27/27 与钉前基线逐项恒等）＋两常驻探针绿线全过＋题面库 21 片对号门 strict exit 0。

## 一 最小用例双档编译（旧病复现→钉后病愈，13 断言门全过）

双档＝`负测/orig/`（旧 sty `c8a5b7e0…` 本地挂载）vs `负测/patch/`（钉版 sty `7c393036…` 本地挂载），各面 xelatex 编译、log 层读数；一键复跑 `python _负测门.py` → **13 断言全 PASS，exit 0**（0914 实修轮复跑，钉后终检再跑一次读数同）。

| 断言面 | 旧病档（orig） | 病愈档（patch） |
|---|---|---|
| `\meaning\parallel` @begindocument/end 段 | `the character ∥`（unicode-math 后手覆盖残留） | `macro:->\mathrel {\begin {tikzpicture}…`（TikZ 复位） |
| `\meaning\parallel` @文档体 | `the character ∥` | `macro:->\mathrel {\begin {tikzpicture}…`（保持） |
| `\extrarowheight` | `0.0pt` | `4.83694pt`（＝1.7mm） |
| 2 行 3 线表盒高 TAB-HT | `22.18758pt` | `27.26637pt`（Δ=+5.07879pt＝1.05×1.7mm，与试迁移位 −5.1pt 同量） |
| ansblock 内 `\linewidth`（钉③件面补钉对照） | `1086.90pt`（382mm 页宽跨三栏） | `341.43306pt`（＝120.0mm 栏宽）；vtop 外仍 1086.90pt 不受扰 |
| 机理对照（mech/mech2/six） | ：105 钩确执行但被覆盖＝∥；sty 内移位不救＝∥；六宏 top-level 殿后存活 | —（谱系证据，见补丁方案 §一） |

## 二 钉①速查表行高恢复 1.7mm（读数＋截图）

- 读数：EH-VALUE `0.0pt → 4.83694pt`；TAB-HT `22.18758pt → 27.26637pt`（§一表）；整页级证据＝§五测评卷 p3 像素差 `0.3552% → 0.0000%`。
- 截图：`截图/速查表-测评卷p3-三面.png`（220dpi 同区裁剪）——A 原印面（M2 成卷件基线）｜B ctl-旧sty＝行高塌缩病面（表体明显压扁）｜C ctl-新sty＝钉后与 A 全同。

## 三 钉②\parallel TikZ 矢量框回归（对照滚A p1 十框位）

- **十框位逐框比对**（pymupdf 矢量层，滤 5.5×10.17pt 填充笔画框）：

| 面 | 框数 | 逐框坐标 (y0,x0,w,h) |
|---|---|---|
| 原印面（M2 滚A main.pdf p1） | 10 | (139.0,1034.6) (139.0,1037.7)｜(285.6,447.3) (285.6,450.5) (285.6,477.8) (285.6,480.9) (285.6,521.3) (285.6,524.4)｜(303.3,668.5) (303.3,671.6)，均 5.5×10.17pt |
| ctl-旧sty（旧病，试迁 §四载 10→0 案） | **0** | —（符号退化为 unicode-math 文本 ∥） |
| **ctl-新sty（钉后复编）** | **10** | **与原印面逐框全等 ✓**（与该页像素 0.0000% 互验） |

- 最小件：`最小-平行框.tex`（5 处 `\parallel`，10.5pt 件型字号，钉版 sty 本地挂载）→ 印面 **10 框、形貌集合 {(5.5, 10.17)}** 与滚A p1 十框同参；log `PAR-MIN-BODY: macro:->\mathrel {\begin {tikzpicture}…`。
- 截图：`截图/平行框-滚Ap1-三面.png`（band1 y≈139、band2 y≈286 两病害行）——B 面（旧病）`∥` 变体为细直双竖线、C 面（钉后）恢复 TikZ 双平行四边形矢量形，与 A 面同形同位。

## 四 钉③三栏括线不再横穿（照方案观望位，sty 不动）

- 结论照补丁方案 §四执行：主落点＝**件面 `\jpcol` 补 `\setlength\linewidth{\jpcolw}\setlength\columnwidth{\jpcolw}` 2 行**；sty 侧观望位（`\ansruledopen/:537`、`\ansruledclose/:540`、`\ansgrayopen/:531`、`\tailfill/:579,588` 改取宽）不采纳、登记待裁（灰底模零声明高 `\vtop` 对照档未跑，采纳前须先过）。
- 最小件双档读数：BAD（`\jpcol` 只设 `\hsize`）ansblock 内 `\linewidth`＝**1086.90pt（跨三栏旧病）**；GOOD（件面补 2 行）＝**341.43306pt（＝120mm 栏宽）**、vtop 外不受扰。
- 截图：`截图/括线-最小件-双面.png`——左 BAD 括线横穿整页宽、右 GOOD 括线恰一栏宽。
- 试迁已证该件面补钉为像素归零必要件（`_摘linewidth补钉.py` 反例 1082.8pt 墨宽案）；本轮 sty 未改此条，钉③不在 sty 钉版 md5 语义内。

## 五 M2 副本树测评本·对照件复编 → 0.0000%（换装硬门口径）

复编件＝副本树三件 `main-ctl.tex`（只换装不迁移、**无任何件面补钉**）原样拷入 `复编-*/`，本地挂载钉版 sty，两遍连编全 exit 0；比对＝`工具/视觉回归.py` 比对（150dpi 逐像素 tol=0，基线＝M2 原印面，基线目录拷贝至沙箱、只读源未动）。

| 件 | 页数 | 钉前基线（试迁 §四） | **钉后复编逐页差异** | 判定 |
|---|---|---|---|---|
| 测评卷 ctl | 3 | 0.0000% / 0.0000% / **0.3552%**（p3 速查表病页） | **0.0000% ×3** | 无差异 exit 0 |
| 滚动卷A ctl | 2 | **0.0743%**（p1 ∥ 病页）/ **0.3046%**（p2 速查表病页） | **0.0000% ×2** | 无差异 exit 0 |
| 滚动卷B ctl | 2 | 0.0000% / **0.2875%**（p2 速查表病页） | **0.0000% ×2** | 无差异 exit 0 |

- **7/7 页 0.0000%＝换装硬门达成**；四处病页全归零，且纯对照件（零件面补钉）即达标——sty 落钉后件面 fixprobe/parprobe 补钉**冗余但幂等**，探针件可退役（试迁 §六.3 落地项，待裁）。
- 副本树三件目录内的旧 sty 拷贝（`c8a5b7e0…`）本轮**未动**（只读纪律）；后续量产取 sty 一律以钉档 §四钉版 md5 为准。

## 六 冒烟全套复跑（四编三零）＋两常驻探针（`成卷/toolchain/冒烟0914/`）

- 四编（dummy-true/dummy-false 各两遍，钉版 sty 本地挂载）：**error=0／Overfull=0／Missing character=0** 双档四 log 全零；页数 **4/3** 与钉前基线恒等（无增页）；M3-ANSKEY **27/27** 两档恒等。读数 `冒烟0914/_读数.py` 可复跑。
- 常驻探针两件（新增，M2 两案各一，注释内写明绿线与跑法）：
  - `探针-速查表.tex`（钉①）：两遍连编 log `M3-EH-VALUE: 4.83694pt`＋`M3-TAB-HT: 27.26637pt` 全绿；
  - `探针-平行框.tex`（钉②）：两遍连编 log `M3-PAR-BEGINDOCEND:`／`M3-PAR-BODY:` 两阶段 `\meaning` 均为 `macro:->\mathrel {\begin {tikzpicture}…` 全绿；印面含 1 处 ∥＝2 矢量框可作 PDF 层复核。
- 双档复跑全绿：四编＋双探针 8 编译面 exit 0、绿线断言无一挂红。

## 七 题面库冻结片·对号门 strict 复跑（内容口径重核）

`python 对号门.py --strict`（题面库 21 片，逐片三集合相等＋逐键/整文件 sha256 重核＋期望值/多选门）：**21 片 552 键全集断言相等零缺漏 ✓，exit 0**。题面层零漂移——两钉只涉 toolchain sty 与件面补钉，题面库/定稿层零写入，本轮重核为旁证。

## 八 钉后终检与遗留

- 负测门终检（钉后复跑）：13/13 PASS、exit 0（读数见 §一，与本轮首跑逐字同）。
- 遗留登记（不在本轮写域，待裁）：①钉③ sty 观望位采纳与否（先补灰底对照档）；②fixprobe/parprobe 探针件退役；③门谱扩容两项（换装对照件逐页像素零差门、负测门三断言机器化入冒烟）；④[D]/[E] 门对两钉仍盲（试迁 §八之补），以本报告 §五像素门＋冒烟探针兜底。
