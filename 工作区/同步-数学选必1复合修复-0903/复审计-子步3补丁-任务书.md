# 复审计任务书·子步3三工具补丁（异族臂=opencode；§13「脚本变更本身列高危」当场复测）

身份：独立复审计代理（未参与原任务、异族臂）。权限＝复测类：被检对象只读（工具三件、六讲练件成品、I1/I2、报告与dryrun留痕）；可写＝你当前目录（报告/临时输出）。禁改成品与工具；git禁。

对象：子步3期间打的三处工具补丁（改前快照在 工作区/同步-数学选必1复合修复-0903/子步3/）：
①底纹去除器.py「表内归集补丁」（改前＝子步3/底纹去除器_改前.py）
②六类底纹计数.py（改前＝子步3/六类底纹计数_改前.py）
③首页断言集执行器.py（改前＝子步3/首页断言集执行器_改前.py）

复测项（同一脚本同环境重跑；python可用）：
1. **回归等价**：①补丁后底纹去除器对X1、I1跑 --audit-only——输出与 子步2/dryrun/X1_audit.txt、I1_audit.txt 逐行全等（执行者称全等——复证）。
2. **牙齿验证**：补丁后底纹去除器对现行B件 --dry-run——表内挂点归保留（不减）、复制带入芯片计入剥除、A5分区交并PASS；与 子步3/dryrun2/B_dryrun2.txt 对照数字逐等。
3. **作用域核对**：三件工具 改前快照 vs 现行（python difflib或文本diff）——改动恰为声明面（①表内归集；②jlp双值口径已在子步2审计过——本轮只核子步3是否再动；③锚点v2/断言口径），无未声明行为变更。
4. **断言器复跑**：首页断言集执行器.py 以锚点v2（子步3/锚点映射表v2.json）对 工作区…/pdf/ 十件PDF跑断言①②——10/10复现（H锚点截短口径照工具现行逻辑）。

输出：报告写入当前目录 复审计-子步3补丁-报告.md（逐项实测数字＋PASS/FAIL＋差异清单）；完成后≤12行摘要。被检对象前后哈希自证。
