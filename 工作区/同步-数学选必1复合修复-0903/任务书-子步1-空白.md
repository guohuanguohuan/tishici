# 任务书·子步1：空白缺陷修复（k3执行；2026-09-03子步0实测后定稿）

**子步0实测输入（施工面修正）**：断言① FAIL 6件＝X1/I1/B/X2/I2/E（带头部区件——首页空白实证）；PASS 4件＝C/F/G/H（后续卷首页有正文）——**施工面＝6件**，4件只跑回归断言。断言②：B/E导航表落双栏正文区（tblW=10200缇 vs 栏宽4890缇、5列含节内题号列）；I1/I2图例行通栏PASS。断言⑤ 10/10 PASS。锚点映射表v1＝工作区…/锚点映射表v1.json（已登记附则）；首页件获取＝全件PDF第1页裁剪（本环境Word忽略PrintOut Range/From/To/Pages——三写法复测在案，等价主路径管线产物）；阴性对照FAIL在案。

身份：任务执行子代理（⓪复合修复轮·子步1·空白）。权限＝执行类：可写＝工具/（断言器微调）＋工作区/同步-数学选必1复合修复-0903/；可改＝六件成品docx（X1/I1/B/X2/I2/E——唯一授权结构变更＝头部要素整体位移）；C/F/G/H只读（仅跑回归断言）；禁git、禁改规则/台账/看板/附则（附则回填行由主会话执行——你出报告）。

## 修复纪律（附则《双栏首页断言》修复纪律①-⑦全文适用）
① 假设集按成本序排查、命中即回填附则（回填由主会话执行、你只出报告）：H1段落级显式分页（w:br type="page"／pageBreakBefore／栏break／表格keepNext＋cantSplit／widowControl）→H2全宽表落栏（仅讲练件附加缺陷候选——衔接2无表仍现缺陷；最小复现件＝衔接2）→H3两节docGrid不等→H4 sectPr落位（表后缺承载段）→H5页眉页脚/titlePg不对称→全部落空＝「渲染例外」报告主会话走⑦出口判定树（用户三选一，禁自行处置）。
② 落点修正与触发属性消除并列；验收门＝断言集（结果层），不得以落点修正当修复完成。
③ 本子步文本流零变更：段落与run内容＋顺序零变更（归一化diff=0）；唯一授权结构变更＝头部要素整体位移（分节符落点＝头部要素全部之后），逐件附「位移前后段号」对账表。
⑤ 禁止增删内容或改页边距绕过。
⑥ 首页断言PDF获取＝Word PrintOut From=1 To=1送PDFCreator主路径链路（§14现行文本含120秒单件校准）；断言用PDF禁ExportAsFixedFormat。
⑦ 出口判定树须用户裁决——命中即停等报告主会话。

## 断言集执行（工具/首页断言集执行器.py——子步0建成）
- 断言①②⑤逐件跑（①②在首页单页PDF；⑤在全件PDF）；断言②无该要素的件逐要素登记「不适用原因」。
- 阴性对照复跑（衔接2缺陷态.pdf判FAIL）→十件修复后断言全过（附则④：首件通过件＝阳性对照件，登记条件＝断言通过＋人工看渲染首页双确认——主会话执行人工确认）。
- 修复每件后立即复跑断言（fail-closed拒落盘通过标记）。

## 上游断言回跑（qwen M9）
本子步改动sectPr/分节符落点/段落结构后：复跑断言①②落盘；表格落双栏区检查（断言②表形态）随跑。

## 修复次序与验收（6件施工）
1. 最小复现件X2先行：假设集H1~H5按成本序排查（每项证据落盘：属性定位＋前后值）；命中根因→定修法→修X2→断言①②⑤复跑全过→登记为阳性对照件候选（人工双确认由主会话执行）。
2. 其余5件（X1/I1/B/X2已毕/E）同法修复：B/E含导航表位移（导航表迁入头部单栏区——断言②）；I1/I2图例行已PASS不动。
3. 4件后续卷（C/F/G/H）回归断言①②⑤（应维持PASS——若修复引发回归即回修）。
4. 全六件文本流归一化diff=0实测（零字符增删）；位移对账表（前后段号）逐件落盘。
5. 修复后首页件重出（管线重跑受影响件）＋断言全过＋断言器fail-closed标记在案。

## PDFCreator链路纪律（本轮已修）
打印脚本spawn前从进程环境删GS_LIB（工具/逐页巡检管线.py已内建env.pop——复用该管线出件，勿另写打印代码）；单件120秒无.pdf先查spool（.PS在场＝渲染挂起）按§14处置；残留引擎只杀本会话时段自产的。

## 交付物
报告-子步1.md：6件逐件（根因行〔H1-H5证据〕｜修法｜位移对账表｜断言①②⑤结果｜diff=0实测）＋4件回归结果＋假设集总回填行＋阳性对照件候选路径。
