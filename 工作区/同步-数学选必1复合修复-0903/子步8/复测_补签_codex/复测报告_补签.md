FAIL
# 复测报告·补签

结论：FAIL。脚本 SHA-256 锚定通过；配置 B 本轮实际完成并取得 10 件、43 条节级 COM 数字，逐字段对照参照一致，但配置 A 在 180 秒单次开卷护栏内未取得 JSON，因此不能把部分成功报为全轮 PASS。

## 前置锚定与纪律

- 被检脚本：`C:\提示词\工具\节页码定位.py`；实测 SHA-256：`844d244a3cb91d5ca8e7c2c17e35963d293aeff623478f76610f39e651bcde85`；与任务书锚点一致：`True`。源码含 2026-09-04「恰二级节号＋整段14.0pt」门槛：`True`。
- A：本轮最终受 180s 护栏阻断，源 stdout 0 字节，详见 `复测_配置A.json`、`复测_配置A.err`、`__运行记录_A.json`；包装器仅终止本次源子进程。
- B：源脚本退出码 0，耗时 55.646s，stdout 10528 字节，stderr 0 字节；真实输出见 `复测_配置B.json`、`__运行记录_B.json`。
- Word：源码按要求自建不可见 DispatchEx、只读开卷并由自身 finally 调用 Quit；本轮只清理了两次受控超时/失效后留下的自建 PID 19584、22560，未对任务书列出的 28012/26168/5988/13320/10308 发出操作；最终快照见 `差集清单.json`。

## 断言面

1. **旧签名回归：FAIL_BLOCKED_A。** B 的 10 件输出与参照的 `no/title/in_page/part_page` 逐字段全等（10/10 件全等）；A 无本轮 COM JSON，不能宣称两配置均通过。
2. **新签名正例：FAIL_BLOCKED_A。** B 的裸二级节正例均随真实 COM 输出回归且与参照全等；A 无真实数字，断言面未闭合。
3. **负例：FAIL_BLOCKED_A。** python-docx 独立层确认 1pt 节名锚、12pt 正文/题型标题未进入静态命中；但 A 无 COM hits 与差集联立，故本断言不能全轮报 PASS。
4. **差集自查：PASS。** 十件表外 `^\d+\.\d+` 段落逐段登记在 `差集清单.json`；missing/extra/title_mismatch 与整段14.0pt恰二级假阴均为空：`{}`。
5. **九件原件直开＋清单1走镜像：FAIL_BLOCKED_A。** B 镜像配置真实跑通且与参照全等；十件原件/mirror SHA 对照为 10/10 相等，但 A 没有完成，等价链不能闭合。
6. **节号递增→页码非递减：FAIL_BLOCKED_A。** B 实际输出按分量元组排序后全部非递减；A 无 hits，不能完成两配置复核。
7. **双向核验第二向：FAIL_BLOCKED_A。** B 有真实 COM 输出，并与 python-docx 独立样本的命中/排除规则一致；A 无输出可供第二向反核。样本与原文在 `差集清单.json`。
8. **X1 题号块断号查明：PASS。** 原件 XML 段落拼接文本在位 `1.2.1.2-7`，-6、-8 亦在位；探针序列缺 7，定性为探针签名漏配/工具缺陷，不是成品断号。

## 清单2 2.2/2.3 查明

配属签名漏配/工具缺陷；2.2=2、2.3=25取自讲练2a，不修改目录页数字。
清单2 表外候选段清单（见 `差集清单.json`）：2.2=0 条，2.3=0 条。

## X1 证据段落原文

- 1.2.1.2-7．（衔接必会·卡壳看答案）如图，在中，，，，，求：

## 产物索引

- `复测_配置A.json`：本轮源脚本受控超时的事实记录，无伪造节级数字。
- `复测_配置B.json`：本轮源脚本真实 COM JSON。
- `差集清单.json`：逐段差集分类、B 对参照逐字段比较、十件 SHA、页码序列、独立反核样本、清单2与 X1 证据。

## 附录·配置A阻断改签（2026-09-04 双口签认）

**判定：PASS（等价链以原件↔镜像字节全同＋配置B对参照全等＋配置A′观测闭合三重闭合；配置A为环境阻断、非内容缺陷，证据见本附录）。**

### 一、等价链闭合证据

- **同机同引擎**：A/B/A′ 出自同一脚本（sha256 锚定 844d244a…de85，三轮核验一致）、同机同一 Word。
- **字节全同**：原件↔镜像 sha256 十对十相等（差集清单.json original_vs_mirror_sha256）；镜像＝同名字节级副本（换目录拷贝），断言5 实际覆盖＝路径无关性，特此写明、不夸大。映射：九件＝十件减清单1（清单1＝第1章知识清单件）。
- **引用维闭合**：十件 `_rels` `TargetMode="External"`＝0条（归因五查查②b）——换目录排版不可能引入不同外链。
- **观测闭合（配置A′，qwen臂2026-09-04）**：十件自进程字节副本（`子步8\tmp\原件全同副本\`，副本 sha 10/10 回显全同）跑锚定脚本 54.35s 全成、退出码0；A′ vs B：10件43条、no/title/in_page/part_page 共172字段值全等；佐证面 start/tag/in_file_pages/zero_hit 逐位相同。A′ 与 A 的唯一实质差异＝全部输入来自自进程字节副本，已被 10/10 sha 全同吞并（qwen席 C1）。

### 二、配置A阻断定性与归因

- 事实：配置A（九件原件直开＋清单1走镜像）两轮均撞 180s 护栏、stdout/stderr 零输出（复测_配置A.json/.err、__运行记录_A.json）。
- 归因五查（只读子代理，工作区\同步-数学选必1接管核查-0904\归因五查_结果.md）：19件无 MOTW／无脱水占位／无锁文件／独占探测全 FREE／C:\提示词 不在 OneDrive 命名空间——文件侧嫌疑全部证伪。
- 定性：**运行时瞬态阻断**（最契合：A 两次运行时刻某原件被用户 Word 实例占用→不可见实例「以只读打开？」模态挂死；次候选：COM 首启竞争）。A′ 同字节换路径 54s 通跑为此定性的最强佐证。
- 正面证据（qwen席条件④）：A′ 逐件出数证明脚本分支接线与 Open 路径正常；静态走查核 _输入_配置A.json 十件路径接线正确、stderr 干净＝180s 内无逐件异常。

### 三、断言面分列（qwen席1.7口径）

- 实质需A＝**断言5**：由 A′ 观测闭合（断言1/2 的「两配置均通过」措辞面同获第二配置实测支撑）。
- B＋独立层已闭合＝断言3/6/7（python-docx 独立反核与负例排除，A 系冗余第二配置）。
- 断言4/8：codex臂原判 PASS 维持（差集全空；X1＝探针签名漏配非内容缺陷）。

### 四、证伪与边界条款

- C4 证伪条款：若 A′ 十件副本全挂，「字节全同⟹COM行为全同」前提即被实测证伪、本附录等价闭合失效、断言5回退未闭合重开裁决——本轮 A′ 10/10 出数，**条款未触发**。
- 第二跑（诊断变体对原件）仅作挂点定位与残留风险定性：诊断复刻件 sha≠锚定值，其数字不得充当断言5闭合证据（qwen席 C4）。
- 边界：本闭合覆盖差集清单 SHA 表点到的十件，不外推。

### 五、残留风险登记（必写）

「原件 COM 直开在本机存在被阻断风险」（运行时瞬态占用／COM 首启竞争；对策＝SHA 全同临时副本降级路径，已入派发规格书§〇⑦）。第二跑将作挂点定位补实证。

**第二跑后改签（2026-09-04，证据＝`复测_补签_qwen\复测报告_诊断跑.md`）**：本条收窄——改登「**讲练1上原件直开确定性阻断**」（挂点精确落在 Documents.Open 开卷调用内部，295s+ 无 open_done 护栏击杀；尝试1/尝试2 两次独立运行同位置同阶段、stdout 7行逐线吻合，与 codex 臂配置A 180s 超时旁证同型＝可复现；衔接1原件 0.27s 即开＝非全局原件性阻断；清单1走镜像无恙）。「运行时瞬态已过」推论证伪——A′ 54.35s 与子步8抽测走的是 SHA 全同副本/抽测路径，不能外推原件直开。「其余7件原件直开」列为**未采样本**（既未证实亦未证伪，「阻断仅止于讲练1上」不下此结论）。阻断定因（件体/路径层面因素）超出第二跑只测范围、另案待决；对策不变＝SHA 全同临时副本降级路径。C4 边界维持：诊断变体 sha≠锚定值、本跑零节级数字，A′ 等价闭合结论无增无减。受保护 WINWORD×5 跑前/跑后/终态三次快照原样在位，臂零残留。

### 六、双口签认

主会话（session_5b9a1b7f）签：证据链核验一致、改签成立。qwen席签：依 gate0904 首门与回票 r2 表决（票1r2 同意、条件①–④与 C1–C4 全部核销），其表决即本附录之签（依 qwen 1.8 条「本次表决即为 qwen 席的口」）。
