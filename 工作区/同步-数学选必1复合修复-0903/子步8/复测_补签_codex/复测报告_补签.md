FAIL
# 复测报告·补签

结论：FAIL。被检脚本 SHA-256 锚定通过，但配置 A、B 均在 `DispatchEx('Word.Application')` 启动点返回 0x80070520，未取得任何新批量测量 JSON；依任务书不能把参照输出冒充复测结果。

## 前置锚定与纪律

- 脚本：`C:\提示词\工具\节页码定位.py`；实测 SHA-256：`844d244a3cb91d5ca8e7c2c17e35963d293aeff623478f76610f39e651bcde85`；期望值一致：`True`。
- 只读输入：十件成品、mirror、参照 JSON、盖章记录均未写入；未执行 git。
- COM 启动异常：A/B 均记录于 `复测_配置A.err`、`复测_配置B.err`，错误为“指定的登录会话不存在（0x80070520）”；没有开卷/Repaginate/Close 阶段可测。
- WINWORD 快照：A 前后均为任务书列出的 PID 5988、10308、13320、26168、28012（见 `复测_辅助.py` 运行快照）；未杀进程、未连接现存实例。

## 断言面

1. **旧签名回归：BLOCKED/FAIL。** 源脚本 A/B 均未能启动 COM，不能逐字段取得 `no/title/in_page/part_page` 复测值；空 stdout 已保留为阻断事实，结构化占位见 `复测_配置A.json`、`复测_配置B.json`。
2. **新签名正例：BLOCKED/FAIL。** 同上，未取得新签名的 Word 页码结果；独立 python-docx 静态命中与参照编号对照见 `差集清单.json`，不替代脚本复测。
3. **负例：BLOCKED/FAIL。** 需要源脚本的实际 hits 与差集联立；COM 启动失败，不能宣称断言面通过。
4. **差集自查：PASS（静态规则层）。** python-docx 独立枚举十件表外段落；详细逐段清单在 `差集清单.json`。
   - 参照编号与静态规则对照：missing={}；extra={}；title_mismatch={}。
   - 整段 14.0pt 且恰二级节号落入静态差集：{}。
   - 清单2 2.2/2.3：静态枚举与参照缺失项核查详见 `差集清单.json` 的 `clean_list_2_missing_check`；不把漏配定性为合法空。
5. **九件原件直开＋清单1镜像：BLOCKED/FAIL。** 配置 A 输入已按十件绝对路径准备，配置 B 使用外部 `parts_mirror.json` 原样；十件原件/mirror SHA 对照见 `差集清单.json`。由于 COM 启动失败，未能完成两配置 sections 全等闭环。
6. **节号递增→页码非递减：BLOCKED/FAIL。** 脚本没有产生 hits，无法对本轮输出按分量元组检查 `part_page`；参照输出的独立控制检查记录在 `差集清单.json`。
7. **双向核验第二向：BLOCKED/FAIL。** 已完成 python-docx 文本/字号规则层静态复算并记录，但没有本轮脚本输出可供逐条反核；不能冒报 PASS。
8. **X1 题号块断号：PASS（判别单步）。** 衔接1原件 `word/document.xml` 的段落拼接文本中在位 `1.2.1.2-7`，且 -6、-8 在位；探针序列确实缺 7，故定性为探针签名漏配/工具缺陷，不是成品断号。证据段落原文与 XML 片段已落在 `差集清单.json`，未使用旧 `tmp\A1_parts\X1` 档。

## 清单2 2.2/2.3 查明

清单2表外未发现 2.2/2.3 候选节标题段；结合参照中 2.2=2、2.3=25 来自讲练2a，定性为清单件配属签名漏配/工具缺陷，不修改目录页数字。

## X1 证据

- 段落原文：1.2.1.2-7．（衔接必会·卡壳看答案）如图，在中，，，，，求：

## 产物索引

- `复测_配置A.json` / `复测_配置B.json`：源脚本 stdout 为空的结构化阻断记录，不是伪造测量结果。
- `差集清单.json`：十件表外数字段落逐段分类、参照编号静态对照、原件/mirror SHA、清单2查明、X1 XML 证据。
- `复测_配置A.err` / `复测_配置B.err`：两次源脚本原始 stderr 捕获。
