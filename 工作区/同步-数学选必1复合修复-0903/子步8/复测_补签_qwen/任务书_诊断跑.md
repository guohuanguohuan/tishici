# 复测任务书·第二跑（诊断变体对原件——残留风险定性）　qwen臂　2026-09-04

你是「人教B版选必1 ⓪版式复合修复轮」复测臂（qwen臂/qwen族——用户令Ⅵ起臂池你独存）。本任务＝qwen席票1r2 批准的第二跑：诊断变体对九件原件＋清单1镜像重跑，用途＝「原件COM直开阻断」残留风险定性（挂点定位），**不作断言5闭合证据**（C4：诊断复刻件 sha≠锚定值 844d244a…de85，其数字只供挂点定位——等价闭合已由配置A′观测完成，本跑结果不改变该结论）。

## 〇、纪律

同前：只测不修、禁git、禁问用户、禁停等。COM 自建 DispatchEx 不可见实例、ReadOnly、用完 Quit；**禁动现存 WINWORD×5（28012/26168/5988/13320/10308）、禁全局杀进程**；残留按 PID 差分只清自己。可写落点＝`复测_补签_qwen\` 与 `子步8\tmp\`（产物新名、不覆写既有件）。总墙钟 ≤25 分钟（C2）；包装器护栏 300s、最多 2 次尝试；到线报在途与已完成面。

## 一、建诊断变体（不动锚定件）

复制 `C:\提示词\工具\节页码定位.py` → `子步8\tmp\诊断变体_节页码定位.py`，**仅**在主循环每件处理前后各加一行带 flush 的进度打印（形如 `print(f'[diag] open_start {name}', flush=True)` / `open_done` / `scan_done`）；其余逻辑一字不改。报告须附变体与原件的 diff 摘要（证明只加打印）。

## 二、对原配置 A 跑诊断变体

`python "C:\提示词\工作区\同步-数学选必1复合修复-0903\子步8\tmp\诊断变体_节页码定位.py" @"C:\提示词\工作区\同步-数学选必1复合修复-0903\子步8\复测_补签_codex\_输入_配置A.json" --record "C:\提示词\工作区\同步-数学选必1复合修复-0903\子步7\盖章记录_子步7.md" --json`

- 输出与逐件进度日志（包装器捕获 stdout 全量）落 tmp 新名（如 `复测_诊断跑_输出.json`、`复测_诊断跑_日志.txt`、`运行记录_诊断跑_qwen.json`）。
- 判定口径：全成＝「原件直开现时可通」（与 A′ 54s、子步8抽测3件一次成互证瞬态已过）；挂死＝日志最后一条 `open_start` 所指件＝挂点件，登记件名与时刻。

## 三、产物

`复测_补签_qwen\复测报告_诊断跑.md`（首行结论＝通/挂于何件/在途；逐件开卷耗时表；WINWORD 差分核对；变体 diff 摘要；对「原件COM直开阻断」风险的定性更新）＋ tmp 产物全件留档。
