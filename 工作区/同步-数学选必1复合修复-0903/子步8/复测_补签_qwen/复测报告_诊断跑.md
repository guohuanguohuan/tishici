**挂——第3件《讲练1上》挂于 Documents.Open 开卷阶段（open_start 16:25:27.411，295s 无 open_done，护栏击杀）；前2件（含原件衔接1）直开即通；后7件在途未及。**

# 复测报告·第二跑（诊断变体对原件·残留风险定性）　qwen臂　2026-09-04

qwen席票1r2批准之第二跑（任务书《任务书_诊断跑.md》）。用途＝「原件COM直开阻断」残留风险定性（挂点定位）；**按C4不作断言5闭合证据**——本跑数字只供挂点定位，配置A′等价闭合结论不因本跑改变。总墙钟：开工≈16:12、本报告落盘≈16:33，限额25分钟内。尝试配额2/2用满（第1次因本包装器缺陷作废存照，第2次为有效判定跑）。

## 一、诊断变体（不动锚定件）与 diff 摘要

复制件＝`子步8\tmp\诊断变体_节页码定位.py`：先 `cp` 逐字节复制＋`cmp` 全同验证（BYTE-IDENTICAL-COPY-OK），再仅插入进度打印。与原件 unified diff（全文存 `tmp\诊断变体_diff_qwen.txt`）：

- **+3行 / −0行**（added_lines=3, removed_lines=0），其余318行与原件逐字节同（318→321行）；
- 3行均为 `print(..., flush=True)` 进度打印，无任何逻辑、导入、签名改动；变体经 `ast.parse` 可解析核验（AST-OK）；
- 插放位置：主循环每件 `scan_doc` 调用前 `[diag] open_start {name}`、返回后 `[diag] scan_done {name}`；`scan_doc` 内 `Documents.Open(...)` 返回后一行 `[diag] open_done {basename}`——此一行按任务书示例标记（open_start/open_done/scan_done 三标记）放置，为区分「挂开卷/挂扫描」所必需，仍系纯打印；
- 锚定件 `C:\提示词\工具\节页码定位.py` 未触碰（cp源只读）。

## 二、两次尝试如实记录

**命令**（两次相同，包装器数组传参未改口径）：`python "...\tmp\诊断变体_节页码定位.py" @"...\复测_补签_codex\_输入_配置A.json" --record "...\子步7\盖章记录_子步7.md" --json`（十件 start 均 null，由盖章记录补齐）。

- **尝试1（作废存照，`tmp\运行记录_诊断跑_qwen_尝试1缺陷存照.json`）**：300s 护栏触发；本臂包装器缺陷——`threading.Thread(args=(...))` 单元素元组漏写尾逗号，reader 线程把捕获流迭代当了位置参数（TypeError），子进程 stdout 全丢、日志/输出两件皆空，**逐件证据不可用**。仅存旁证：子进程死前 stdout 到达过 7 行（`payload/diag` 计数外的线程层观测）。WINWORD 差分：自建 22760 已按差分击杀，受保护五实例未动。
- **尝试2（有效判定，`tmp\运行记录_诊断跑_qwen.json`）**：16:25:22.409 起跑，300s 护栏 16:30:22 触发，全程 308.81s，timed_out=true、源退出码无（击杀）、diag 7 行、JSON 输出面 0 行（`复测_诊断跑_输出.json` 0字节——未走到输出阶段）、stderr 0 字节。diag 7 行与尝试1的 stdout 7 行旁证**逐线吻合**（同一模式：两件走满三标记＋第三件 open_start）。

## 三、逐件开卷耗时表（日志逐行到达时刻，`tmp\复测_诊断跑_日志.txt`）

| 序 | 件名（短名） | 来源 | open_start | open 耗时 | scan 耗时 | 件总耗时 | 状态 |
|---|---|---|---|---|---|---|---|
| 1 | 衔接1 | 原件 | 16:25:24.198 | 0.27s | 1.13s | 1.40s | 通 |
| 2 | 清单1 | 子步7 mirror | 16:25:25.598 | 0.15s | 1.66s | 1.81s | 通 |
| 3 | 讲练1上 | 原件 | **16:25:27.411** | **∞（295s+ 无 open_done）** | — | — | **挂（开卷）** |
| 4–10 | 讲练1下/衔接2/清单2/讲练2a/2b/2c/2d | 原件 | — | — | — | — | 在途未及（单会话顺序处理，挂点件不跳采） |

判定口径（任务书§2）：挂＝日志最后一条 `open_start` 所指件＝挂点件——**挂点件＝讲练1上，登记时刻＝2026-09-04 16:25:27.411（击杀于 16:30:22，挂时≥295s）**；且 `open_done` 从未出现 → 挂点精确落在 `Documents.Open` 开卷调用内部，非扫描阶段。「全成＝原件直开现时可通」**未成立**。

## 四、WINWORD 差分核对

- 受保护名单 {28012, 26168, 5988, 13320, 10308}：跑前、跑后、终态（16:31+）三次快照均**原样在位、无一被触碰**；全程无全局杀进程（仅按差分名单逐 PID taskkill）。
- 尝试1：before=5 → after=6（+22760＝本臂 DispatchEx 自建）→ 差分击杀；尝试2：before=5 → after=6（+16148，起于 16:25:22＝本臂自建）→ 差分击杀；终态快照 WINWORD 恰＝受保护五实例，本臂零残留。

## 五、对「原件COM直开阻断」风险的定性更新

1. **风险未过、非瞬态已过**：原件直开在讲练1上仍可确定性挂死（300s 无响应）。与 A′ 54.35s、子步8抽测3件一次成的「瞬态已过」推论相抵——那些证据走的是 SHA 全同副本/抽测路径，不能外推到本件原件直开。
2. **由「不明阻断」收窄为「单件级阻断」**：衔接1（原件）0.27s 即开——阻断非全局原件性；清单1（mirror）0.15s——镜像路径无恙；挂点件讲练1上在尝试1/尝试2两次独立运行中同位置、同阶段（7条stdout逐线吻合）、且与 codex 臂配置A 180s 超时旁证同型——**可复现，指向该文件实体/路径层面因素**（如件体本身、网络标记/占用锁等），非 Word 会话级或机器级随机现象。
3. **覆盖面如实声明（在途面）**：其余7件原件本跑未及（挂点件之后未处理），其直开风险**既未证实亦未证伪**；「阻断仅止于讲练1上」不可下此结论。
4. **C4 边界重申**：本跑零节级数字（未走到输出面），对断言5闭合面**无增无减**；配置A′闭合维持原判。残留风险登记建议：由「原件COM直开（时好时坏）」改登为「讲练1上原件直开确定性阻断（已定位、待另行定因——超出本跑只测范围，交票面决）」，并把「其余讲练原件直开」列为未采样本。

## 六、产物清单（全留档，均新名、零覆写既有件）

- 正式报告：`复测_补签_qwen\复测报告_诊断跑.md`（本件）
- `子步8\tmp\`：`诊断变体_节页码定位.py`、`诊断变体_diff_qwen.txt`、`包装器_qwen_诊断跑.py`、`复测_诊断跑_日志.txt`（7行全量）、`复测_诊断跑_输出.json`（0字节，如实留）、`复测_诊断跑_stderr_qwen.txt`（0字节）、`运行记录_诊断跑_qwen.json`（尝试2）、`运行记录_诊断跑_qwen_尝试1缺陷存照.json`
