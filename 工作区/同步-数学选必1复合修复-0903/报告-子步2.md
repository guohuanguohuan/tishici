# 报告·子步2：底纹减法（k3执行；T2确认点停等中）

日期：2026-09-04　执行：k3子代理（选必1⓪版式复合修复轮·子步2·底纹减法）
文件范围：可写＝工具/（新建 底纹去除器.py）＋工作区/同步-数学选必1复合修复-0903/子步2/（一次性脚本与取证）；可改六件成品本轮**未动**（T3待确认）；git＝只读 show/log/diff（任务书代跑③明示HEAD对照所需，零写操作、零提交）；规则/台账/看板/附则未改。
状态：**代跑节✓＋T1✓＋T2✓ 已完成并停等确认；T3/T4/T5 未执行**（执行边界：主会话逐行确认 dry-run 数字前 T3 禁执行）。

## 代跑节（子步1复审计qwen臂四项臂力阻塞之上游回跑；结论全绿）

### 代跑① 断言①②十件现存PDF复跑（沙箱法，未覆盖子步1产物）
- 产物：子步2/代跑/断言复跑/（锚点映射表v1.json副本＋首页PDF/＋pdf/＋首页断言{报告.md,结果.json}）。
- 结果：断言① **10/10 PASS**，锚点y值逐项＝报告-子步1§五（X1@113〔更正后条目号式锚点生效〕 I1@155 B@531 C@255 X2@567 I2@115 E@678 F@276 G@318 H@316）；断言②逐行全等（B/E导航表tblW=10200缇落头部单栏区＋PDF表框宽510.0对基准510.3±2、B/E统计行x0=42.5、I1/I2图例行×2 x0=42.5、余要素逐件「不适用」登记）；断言⑤ 10/10 PASS；**阴性对照判FAIL（块数=3）**；blocked全false（fail-closed零触发）。
- 程序化对照：新JSON vs 子步1 首页断言结果.json——a1/a2/a5/阴性对照逐件逐行全等（对照脚本输出在子步2/代跑/）。
- **与报告-子步1数字完全一致，断言面无回潮。**

### 代跑② 六件文末sectPr w:type=continuous在位XML抽验（子步2/代跑/sectPr抽验_子步2.py → sectPr抽验结果.json）
- 六件全 PASS：文末（body级）sectPr 恰1处＝body末子元素，含 `<w:type w:val="continuous"/>` 且位于 pgSz 之前；正文节 cols num=2 space=425 sep=1；段落级sectPr全局恰1处（头部节）type=continuous＋cols=1＋hdr/ftr引用各1＋start=1。
- X2深查：头部节sectPr载于文内标题段（body idx0）；无导航表（本件无章首导航表，登记）；正文自 idx1「2.8 直线与圆锥曲线的位置关系」起排 ✓。
- B深查：头部区序列＝标题段(0)→全件统计行(1)→**导航表tbl(2，头部单栏区内)**→零字符承载段(3，sectPr；spacing line=20 exact＋sz=2＋零文本实测✓)→正文自 idx4「1.1 空间向量及其运算」起排 ✓（导航表落区与承载段落点同位移对账表）。
- 登记观察（非本轮修复面）：E件文末sectPr子元素序 [type,cols,docGrid,pgSz,pgMar] 非规范序——git 18af5f6 修复前态实测为 [cols,docGrid,pgSz,pgMar]，系双栏改制工具旧产物（修复前即然）；w:type在位且断言①②⑤全过、渲染正常，其余五件为规范序 [type,pgSz,pgMar,cols,docGrid]。建议随子步4-6或后续触碰轮顺手正序，请主会话定夺。

### 代跑③ X2/B文本流diff复测（基线订正＋授权差异清单口径）
- **基线订正**：派发语「HEAD~1」以派发时栈计（彼时HEAD=b1f043d＝子步1修复提交）；现 HEAD=181ced1（复审计收尾提交）→HEAD~1=b1f043d＝修复后态。取修复前态 **18af5f6**（＝派发语时义的真HEAD~1）为基线，证据：git diff --stat 18af5f6..b1f043d 恰＝六件成品（字节+12~+29），181ced1 不碰成品。
- 产物：子步2/代跑/diff_HEAD基线_子步2.py → diff_HEAD基线实测.json（复用子步1 diff_文本流.py 同口径；基线件 X2/B_修复前_18af5f6.docx 留档）。
- **X2 PASS**：zip成员清单一致、document.xml以外成员逐字节恒等、w:t/m:t文档序字符流全等（5426=5426）、八项计数全等；字节18354→18371（+17）。
- **B PASS**：主流（剔导航表）全等（55277=55277）＋导航表子树流全等；容器同上；计数唯一差＝w:p 1129→1130（授权新增零字符承载段）；字节6164005→6164034（+29）。
- 现件 sha256前12：X2 1135b7307567／B 9753a17b99e8——与复审计D5主会话代跑值全等 ✓（六件现状哈希另见 dryrun 报告§三）。
- A4口径注记（复审计增补）：位移正确性由 sectPr抽验（代跑②）＋位移对账表联合坐实，流diff管字符守恒——两面均绿。

## T1 底纹去除器（工具债案6，先建后用）

- 新建成库：**工具/底纹去除器.py**（执行/dry-run/audit-only 三模式；fail-closed——11项作用域断言任一不过即拒写并退出码3）。
- 口径与剥除/保留清单：见 报告-子步2-dryrun.md §一（附则甲案改文后现行文本逐条映射；正则与 六类底纹计数.py 同源常量并注「改动须两处同步」）。
- 断言面：A1剥除fill精确={C9C9C9}｜A2白名单四族恒等（段级四色/run级/OMML/他色零变化）｜A3文字流全等＋元素计数恒等＋shd减数=剥除数｜A4条目区芯片同run MANUAL门｜A5 run级与OMML分区各自不交并。
- 自建自验（冒烟）：B dry-run 全断言✓ → **G复制件实执行**（子步2/冒烟/）：容器成员清单一致、仅 word/document.xml 变更、zip完好；减后件跑 六类底纹计数.py 旧签名留痕＝题号块0/芯片挂灰0/答案值run0/OMML余10（＝条目区保留，与dry-run模拟值逐等）/条目号run1保留/题干底纹118段恒等/标题6+52恒等——旧恒等式失效率CHECK属预期（T4签名改造对象）；audit-only复核减后fill分布＝{run C9×1（条目号lead）、OMML C9×10（条目区）、段级6/52/118}，与dry-run预测全等。

## T2 十件 dry-run 分布预演（确认点）

- **全量落盘：报告-子步2-dryrun.md**（六件各15行分类表＝90行≥42行要求；四件零变化对照＋fill恒等＋十件sha256基线；与六类底纹计数.py交叉核验逐件闭合）。
- 六件总判定全 PASS；剥除合计 run 4109＋OMML 1817（与A5逐件闭合）；保留四族恒等。
- 需主会话知悉/裁决项（dryrun报告§五逐行）：①讲部方法讲解区灰底列入剥除的依据确认；②导航表表头tcPr（B5/E5）与四值校验「仅此来源」字面的口径确认（T4白名单登记方案）；③E/F/H覆盖缺口系减前既有登记。

## T3/T4/T5（未执行——待主会话确认 T2 后连续执行）

- T3执行预案＝dryrun报告§六；T4四件签名改造（六类底纹计数.py⑦类题干化＋①类双值＋四类废止列；灰度四值校验.py 201三源口径＋冒烟；块标签芯片.py/答案值分型改标.py减法口径）与T5图例减后形态登记，均已就绪待行。

## 残留清查（§4⑨）

- 本轮未启 Word COM／PDFCreator（纯 XML 作业），无实例残留；spool 目录未触碰。工作区新增文件均在 子步2/ 内（探针、抽验、diff基线件、冒烟件、dryrun报告），收尾随子文件夹清点处置。
