# 跨脑复审计制（附则；外移自公共规则§13 L180，2026-09-06 用户拍板外移降预算；同拍板两处改写已落注：复审计/商议体不限模型族、「与执行者恒异族」前提废止）

> 原语境注记：文中「§1/§2/§13」「K1」内指照旧（K1 块驻附则《多脑调用登记》）；原「公共规则§13 资格门先归因后顺延」等引用改指本附则。公共规则§13 原位留指针。

独立复审计：整体验收（阶段7）不得只采信本轮自检记录的数字——必须随机抽自检记录≥3项实测重测（多智能体模式下由未参与原任务的审计代理执行——**跨脑复审计制**：复审计者与任务执行者不为同一代理、模型族不设限〔2026-09-06 用户拍板废止「恒异族」前提〕；族别判读按**通道前缀优先于厂商前缀**匹配实测model原始串——kimi-k*/kimi-for-coding＝kimi族（k3属之）、qwen*＝qwen族、gpt-*与codex-*＝gpt族（codex臂）、opencode通道模型ID（opencode/、github-copilot/、google/、openrouter/前缀封闭枚举——多模型一臂、臂内任一在册模型可执行）＝opencode族，清单未列且前缀无法归族＝族别未定视同与执行者同族不予放行、新增族别须另案登记；**臂池＝kimi-code 子智能体独存（2026-09-06 用户令：禁用其他厂商 CLI，qwen CLI/codex/opencode 臂全体退役留痕、族别判读枚举转历史）**，复审计/复测恒派未参与原任务之独立子智能体实例（当轮模型＝config.toml [secondary_model] 绑定值、思考档钉死 max，梯队照附则《主脑协审制》ZH-1/ZH-6）；指针制随单脑废止；复审计臂不可用＝fail-closed不得凭复审计放行、当场报用户、无顺延臂；臂资格门＝能以同一脚本同环境执行复测；**资格门先归因后顺延**：臂故障必须先把错误信息三分归因——派发配置类（权限档/沙箱/派发目录权限件缺失/参数缺失/路径解析）先修配置原地重试同臂、环境类（上游502/CLI bug/瞬时网络）可换用法重试同臂，修复实证无效或确属臂能力类（模型能力不足/凭证失效/工具面缺失不可配置）才记skip（2026-09-06 外部 CLI 退役后独存臂、无顺延臂，即入复审计臂不可用处置）；修复尝试与证据记商议记录与K1；仍不满足记skip（2026-09-06 外部 CLI 退役后无顺延臂），复审计臂不可用＝fail-closed不得凭复审计放行、当场报用户；凭证双证一致＝主会话派发记录model入参＋复审计会话原始model串回显、K1只存原始串不存推导族标——定义驻§13）（候选：页脚域形态与缓存、⑧格式继承清点、统计行/导航表恒等式、标签计数、图形态零锚定扫描、**目标值实测——本轮改动追求的目标值本身抽样实测（例同§2验收铁律）、内容抽检——随机抽2个含公式条目重验公式自洽（量纲/符号/系数），防逻辑亲检虚报**）并落盘复核值；另做**同册横向一致性diff**：本册各件的页脚域形态、styles.xml docDefaults（rPrDefault/pPrDefault）、件标识体系、标签体系必须一致，件间差异即缺陷信号、逐差异定位处置。复核与diff结果并入过程对账；抽验重测不合格即整轮自检记录作废重跑；**高危清单当场复测**——删文件、台账覆盖、跨多件改动、正文公式逻辑修正、恒等式/命名体系改动、规则文件行数或行号变动、任何计数改写——命中者任务完成当场由未参与之代理（复测类权限——含全量读；唯一可写落点＝复审计报告与K1凭证路径，脚本临时输出写其工作区临时目录用完即删）重测，不等阶段7；复测须复用同一脚本（脚本变更本身列高危）并核断言面⊇改动面（git diff清单对比脚本覆盖清单，防空真通过）；单会话模式：高危项本会话脚本重跑＋换角度亲算复核后落盘，数字类目（页数/计数/哈希/底纹/域形态）复审计只认脚本实测重跑数字、亲算类按重新亲算核验落盘；**抽样配方**——3项＝1项高危（无则补随机）＋1项自检耗时最短＋1项真随机（种子＝本轮基线SHA256前8位 mod 候选数、数字落盘可复算）；本条对制定轮自身生效：落盘前由跨脑复审计轮值当值臂当场复测（表决≠复测——复测核的是落笔与共识一致性，当值臂虽参与表决不构成参与复测）（验证集＝新词族分布＋行数恒等＋SHA256前后对账＋git diff恰预期笔数）。
