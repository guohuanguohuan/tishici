# ⑩轮规格书：漏读治理——拆段＋留痕标记＋摘要映射＋规则lint（2026-09-06 用户拍板六条建议之①②④，③⑤主会话已落笔）

> 依据：2026-09-06 用户对提示词体系诊断六条建议拍板「接受建议」。本规格书授权范围即 ZH-7 所称「主脑逐次明确授权到件到段」——子代理仅可在本文件列明之文件与操作内动笔，**语义零改动**（纯格式重排＋标记），超出即停手回票。

## 任务A：拆段＋留痕标记＋摘要映射（单代理，顺序执行 A1→A2→A3）

### A1 拆段（7 件 15 行，行 >1000 字符者）
文件与超长行数（执行时以脚本实测复核）：公共规则.md×8、附则/多机协作与工具纪律.md×2、附则/多脑调用登记.md×1、附则/跨脑复审计制.md×1、附则/页面与页码细则.md×1、附则/PDF导出链.md×1、高中同步总控.md×1。
- 拆法：句界（。；）或款界（①②③、**款名**）断行；行内并列项转 markdown 子行（缩进两空格＋- 或保持原项目符号）；断点后单行 ≤1000 字符。
- **禁改字、删字、增字**——允许的新增仅三种：换行符、行首 markdown 缩进/项目符号、A2 的「▽留痕：」前缀。
- 每行拆成几行、断点位置，逐行落盘映射表（旧行号→新行号区间）。

### A2 留痕标记（全库规则件：公共规则.md、附则/*.md、四件总控、00总纲.md）
- 判定词集：留痕｜废止｜作废｜不再适用｜历史保留｜转历史｜出脑｜退役。**「覆盖」不在词集**（覆盖门等是现役机制）。
- 命中且语义＝「该物已停用/被取代、仅存档备查」的分句，句首加「▽留痕：」。同句新旧并存（部分现行部分留痕）只标留痕分句。
- 歧义句不猜不标，列清单（文件＋行号＋原句）入终报回主会话裁决。
- **幂等防护**：句首已有「▽」者跳过（与 XJ-1c「无▽前缀」判定对齐，二次执行不重复加注）。
- 加前缀后单行若越过 1000 字符，按 A1 拆法同步再拆。
- 多脑调用登记 K1 的 CLI 时代旧文段（2026-09-06 退役段已宣告「留痕不删」者）属明确命中。

### A3 摘要映射（公共规则·目录摘要.md）
- 按 A1 映射表机械重算全部条目行号（L 号），条目概括文字不变；头部「源文件」行的字节数/行数按拆段后实况更新（**行数沿用摘要现行 split 口径**——含文件尾空行，写明 N 值，与覆盖断言同口径）；生成日期行追加⑩轮注记。
- 例外：L19/L20/L21 三条概括因今日语义改动（索引精读默认制＋举证门）需刷新概括文字——新文由主会话提供（见附言一）。
- 重跑行号覆盖断言：条目行号并集＝1..新行数 无空洞。

### A 组验收断言
- C1 守恒断言（脚本）：基线＝主会话已落盘的 `工作区/体系-漏读治理-0906/⑩_改前快照/`（含今日③⑤改动后的工作区实况；**勿用 git HEAD**——其未含③⑤，必假失败）。双侧同法归一后逐字符全等：剥换行符、行首缩进、新增行首项目符号、「▽留痕：」前缀、摘要行号数字（旧侧同法剥）；**全程保持原 EOL（CRLF）不动**。许可改动仅两类并须单独 diff 展示：摘要 L19/L20/L21 概括刷新＋头部「源文件/生成日期」行更新。
- C2 拆段后全库规则件无 >1000 字符行。
- C3 留痕标记清单全落盘（每处：文件｜新行号｜原句起止各 15 字）；歧义清单单独列。
- 报告落 `工作区/体系-漏读治理-0906/⑩_终报A.md`，过程脚本与映射表同目录。

## 任务B：规则lint 工具（另一代理，与A并行）
- 新建 `工具/规则lint.py`，检查面与阈值照 `附则/规则巡检.md` XJ-1a~1e 逐字实现（先读该附则）。白名单机制内置于工具头部常量（初值：公共规则·目录摘要.md 与 附则/规则巡检.md 的词集枚举行——其 XJ-1c 命中系词集定义自身，常驻豁免）。
- 测试用副本与合成样例（自建临时目录造含各类违规的样例文件验证检出率），**不在生产规则件上练手**；正式首跑不在本任务范围（复测阶段跑）。
- 验收：合成样例每类违规至少 1 检出走查通过；对当前库跑一次 dry-run 输出报告（此时 A 组未落地的超长行等命中属预期，照实输出）。
- 报告落 `工作区/体系-漏读治理-0906/⑩_终报B.md`。

## 纪律短版（两代理同）
①中途禁停等空转；②不向用户提问，裁决项写报告；③禁 git；④只动本规格书列明文件；⑤已知坑：文本文件比对禁裸 md5 跨 git/盘上比（EOL 假信号），用同口径读字节比；写文件失败重试（同步盘瞬时锁）；规则件是心脏——每一笔下刀前想清楚「这算不算语义改动」，算就停手回票。⑥报告按 ZH-8⑥/公共规则 L20⑥ 举证门：附已读文件与条款清单＋关键条款复述。

## 附言一：摘要 L19/L20/L21 三条新概括
- L19｜§1｜多智能体模式｜默认生效；主会话五件事；执行一律子代理；子代理＝平台原生（[secondary_model] 绑定）；阅读默认索引精读制
- L20｜§1｜多智能体派发纪律｜索引＋条款ID精读＋纪律短版五句＋⑥通读举证门（无举证视同未读）
- L21｜§1｜主会话上下文与调度纪律｜摘要回传/规格书先行/抽验不重做/互斥切分/代理粒度/⑥阅读制（默认索引精读、涉面宽升级全文通读）/⑦分片轮换
