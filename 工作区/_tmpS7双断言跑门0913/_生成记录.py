# -*- coding: utf-8 -*-
"""S7 双断言记录生成器：_S7跑门读数.json → 成卷/S7双断言记录.md（逐件逐门表）。"""
import json
import io
import sys

sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')

SRC = r'C:\提示词\工作区\_tmpS7双断言跑门0913\_S7跑门读数.json'
DST = r'C:\提示词\工作区\M2-第1章量产0911\成卷\S7双断言记录.md'

res = json.load(open(SRC, encoding='utf-8'))
G = '\u2713'
R = '\u2717R'

L = []
A = L.append
A('# S7 双断言链·全件跑门记录（M2 第1章）')
A('')
A('- 日期：2026-09-13　｜　跑门器：`工作区/_tmpS7双断言跑门0913/S7双断言跑门.py`（只读跑门，零写盘；逐门读数 json 同目录 `_S7跑门读数.json`，控制台留档 `_S7跑门控制台0913c.txt`）')
A('- 射程：33 件＝源件 29（导学 12／练习 10／拓展 2／测评卷＋滚A／滚B 3／答案册 1／装配册目录页 1）＋装配四本 4（导学本 44／练习本 38／测评本 7／答案本 13）。')
A('- 判据谱系：源谱 `_测v4断言.py`（v4.4 54 门）＋`断言顶格.py`（顶格五查）；A4 双栏族窗口逐字承袭 v4 L1 校准值；卷件/答案册/配页/装配按「同禁则扩门」在场＋族内一致性降格（排产单 §五-A1、S2就绪报告 §四-2 授权，P2 登记）。')
A('')
A('## 一、总判')
A('')
n_green = sum(1 for v in res.values() if v['n_red'] == 0)
n_red_it = sum(1 for v in res.values() if v['n_red'] > 0)
n_red_g = sum(v['n_red'] for v in res.values())
A(f'**全件 33 项：全绿 {n_green} 项；红 {n_red_it} 项（红门合计 {n_red_g}），两项皆真红（内容/快照缺陷，非门伪阳），零现场修，即录即报。**')
A('')
A('| 件 | 族 | 门数 | 红 | 判 |')
A('|---|---|---|---|---|')
for it, v in res.items():
    tag = '全绿' if v['n_red'] == 0 else f"红{v['n_red']}（{'、'.join(c['gate'].split('（')[0] for c in v['gates'] if not c['ok'])}）"
    A(f"| {it} | {v['family']} | {len(v['gates'])} | {v['n_red']} | {tag} |")
A('')
A('## 二、逐件逐门读数表')
A('')
for it, v in res.items():
    tag = '全绿' if v['n_red'] == 0 else '红%d' % v['n_red']
    A(f"### {it}（{v['family']}）—— {tag}")
    A('')
    A('| 门 | 读数 | 判 |')
    A('|---|---|---|')
    for c in v['gates']:
        gate = c['gate'].replace('|', '∣')
        det = c['detail'].replace('|', '∣').replace('\n', ' ')
        A(f"| {gate} | {det} | {'✓' if c['ok'] else '**✗R**'} |")
    A('')
A('## 三、红项专节（定位＋初步归因；均未现场修）')
A('')
A('### 红1　导学件/章末-本章总结提升 ⑤ 奇偶交替（偶页缺「人教B」串）')
A('')
A('- 读数：✗p2偶,p4偶,p6偶；p2/p4/p6 页脚实印「高中数学 选择性必修第一册**(人教A 版)**」。')
A('- 定位：`成卷/导学件/章末-本章总结提升/main.pdf`（快照）。')
A('- 归因：**源侧快照陈旧**。main.tex 2026-09-12 15:02:46 勘正（撤销批6 误换的「(人教A版)」册名行）晚于 main.pdf 14:53:13，勘正后未复编译；装配侧副本（`装配/导学本.pdf` 起39 六页）已印「(人教B 版)」正确。')
A('- 实证：源侧 main.tex/main.pdf 逐页 diff＝除页脚页码（1~6 vs 39~44）与 A/B 版串外正文零差异。')
A('- 处置建议：主会话裁示后对源件复跑 xelatex×2 并重装配（属写操作，本链不动）。')
A('')
A('### 红2　答案册 ⑬ 全角括号残留（开合总数 212≠211）')
A('')
A('- 读数：全角（ 212 ／ 全角） 211，差 1；栈配对孤（ 1 处：`变式1B（√24`。')
A('- 定位：`成卷/答案册/body.tex:342`＝`\\ansline{探究二}{例1 A（\\(30^{\\circ}\\)；变式1 B（\\(\\dfrac{\\sqrt{2}}{4}\\)}`——末尾以 **`\\)`（数学闭定界符，不印字）** 收束，缺全角右括号 `）`；姊妹行 `例1 A（\\(30^{\\circ}\\)）` 带全角 `）` 正常。')
A('- 实证：600dpi 渲染（探针-答案册p3zoom.png）目验 `变式1 B（√2/4` 后无任何括号字形；非提取假象。')
A('- 归因：组装链（S5 组装器/组装body.py）注入该值时把收尾全角 `）` 误写作 `\\)`；同页 212 源字面（ 与 pdf（ 212 相等，唯合括号差 1。')
A('- 处置建议：body.tex:342 变式1 B 值尾 `\\)` 改 `\\)）`（或 `）`），过钉值门后复编译答案册并重装配；属内容写操作，本链不动。')
A('')
A('## 四、装配刷新读数（承 交接-20260913 §二.3 resume 第1步）')
A('')
A('- 复跑 `工作区/M2-第1章量产0911/_tmp装配装订0912/装配四本.py`：**28 副本全 err=0 ovr=0 mch=0 OK**；四本合并 **导学本 44／练习本 38／测评本 7／答案本 13＝102 页**（报告：同目录 `_S7重跑装配报告0913.txt`）。')
A('- G7 口径：装配副本答案册 `\\setcounter{page}` 改写 90（`_tmpS7双断言跑门0913/_装配补G7答案本.py`，与原轮同款注释；**原件 `成卷/答案册/body.tex` 未动**），副本 xelatex×2 三0、重并 `装配/答案本.pdf`（13 页＋1 书签）。')
A('- 终验页脚：导学 1~44／练习 45~82／测评 83~89／答案 90~102 **全连续 ✓**；书签 28/28 ✓（终验页脚.py，脚本末行文案硬编码「1-103」系旧轮文案，断言逻辑按各本起页自适应，以输出为准）。')
A('- 装配四本跑门（本链 ⑩门级）：导学本/练习本/测评本/答案本 除 ①「三0＝装配报告引用」②「页数登记」替代门外全绿；页脚跨本连续（全局页码 1/45/83/90 起、零填充格式各按本式）✓。')
A('')
A('## 五、附属读数（成卷/答案册/ 下四门，均只读）')
A('')
A('| 门 | 命令 | 读数 | 判 |')
A('|---|---|---|---|')
A('| 钉值门·正测 | `python 钉值门.py` | 生效门 **16** 项（编号至钉-18 含空号；历史 16→17、任务口径 18，**以实跑为准录 16**）全过零漂移 | ✓ |')
A('| 钉值门·负测 | 同上（自带负测例） | 灌旧值 **16/16 真拦** | ✓ |')
A('| 钉值门·strict | `python 钉值门.py --strict 值快照.json` | 值快照 **633 键**逐门断言全过 | ✓ |')
A('| 对号断言 | `python check_pairs.py --拓159 --strict` | 四本侧键集 **631**（拓159档）｜答案册侧 pair 标记 **631**；缺 0 多 0，集合相等零缺漏 | ✓ |')
A('| 值一致性 | `python _s7值一致性.py` | pair 631＝比对 631（json 633 含 2 非键）；body.tex↔值快照全键一致；V 表含新值 10 项（练04-2/练04-10/练04-13/练04-15(3)/练06-9/课05预习填空/课02判5/拓-001/拓-053/课06评5） | ✓ |')
A('')
A('## 六、门窗口径承继与本轮定窗登记（P2）')
A('')
A('1. **逐字承袭（未动）**：A4 双栏族 ④⑤④窗（块 26.2×7.8、底 11±0.5、数字 10.5~11.5、近 2.5~3.4、小字 5.0~5.45、FZSSJW/NotoSansSC）、⑧行距 18.0±0.5、⑩栏左±3.5pt、⑭右挂 ink 0.75~1.35／bbox −0.8~−0.5、⑮花形组窗——均为 v4 L1 校准值。')
A('2. **A 族定窗（预定→实测定窗）**：页脚块宽 13.8→**31.0**（13 页一律 31.0×7.8 底 11.08）；行距 18.0→**6.25**（主峰 ×143，次峰 18.0×32）。练习件/拓展册同款块 **31.0×7.8**、页码三位零填充（`001`）。')
A('3. **⑦ 栏线扩门**：v4 长度窗（非末 0.5／末 0.22 版心高）在 M2 误伤（课01 p1 跨栏元素断线 61.9+79.1mm、拓下 p13 末页 51.0mm），改「逐页在场＋0.4±0.05pt 色 189＋覆盖 ≥0.15 版心＋芯 170-210」，段高读数登记。')
A('4. **⑨ 定义内豁免**：衔接节 `\\lxopt`/`\\subl` 定义体内 `\\hangindent`×3＝件型设计（同 J 族 hang=4 登记口径）；正文裸用仍须 0。')
A('5. **⑬ 判据改域**：v4 字面账（body（−zhentib）只成立于定义体在 main.tex 的三件式域；M2 宏定义在 qp-blocks.tex、拓展册 fancyfoot「（上）」定义行字面多次渲染，字面账先天不齐。改「**全册全角开合总数相等**＋D 族判断槽恰＝zhentib 数＋J 族 pdf 槽对＝`\\kw` 用数」；栈配对/源字面降为读数。该判据即位即捕获红2 真缺字。')
A('6. **⑫ 墨级压字口径**：bbox 相交仅初筛；对相交图在内存副本影删（`delete_image`，不落盘）后 600dpi 重渲染得文字自墨，与位图原生暗墨投影掩码（变换逆映射采样）2px 邻域求交，重叠方计压字。缘由＝M2「解答书写区」浅灰水印（184~233 灰，暗阈无墨）行盒与图 bbox 相交而墨不触，矩形/墨盒级判法在本域假阳。位图期望账＝图源内嵌位图总数（多栅格 PDF 图与 `\\fig` 类宏包装图按用量展开）。')
A('7. **⑥ 普查判据修复**：HTML 灰档判据原写 `v[0::2]==v[1::2]`（对 A6A6A6 类恒假，v4 域靠白名单掩盖），改 `v[0:2]==v[2:4]==v[4:6]`；补内联 `\\color[HTML]{..}` 普查。册目录页 toc 灰 99/144/157/176/199 随之入档。')
A('8. **装配本扩门**：①编译三0＝装配报告 28 副本 OK 引用（无独立 log）；②页数＝登记值；页码/奇偶按全局号（G7）；位图账/源字面账归 29 源件门。授权源＝排产单 §五-A1＋S2就绪报告 §四-2（P2 登记）。')
A('')
A('## 七、红线自证')
A('')
A('- 本链零写盘（跑门器只读源件/快照；`delete_image` 影删仅在内存副本；探针 png 全落 `工作区/_tmpS7双断言跑门0913/`）。')
A('- 成卷内容零改动：拓-057 对号档、课09、钉值门门表未动；答案册原件 body.tex 未动（G7 改写仅在装配副本）。')
A('- git：未使用。')
A('')

open(DST, 'w', encoding='utf-8').write('\n'.join(L))
print('written', DST, len(L), 'lines')
