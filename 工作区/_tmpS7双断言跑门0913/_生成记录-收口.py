# -*- coding: utf-8 -*-
"""S7 双断言记录生成器·收口轮：_S7跑门读数.json → 成卷/S7双断言记录.md。
承 _生成记录.py（首轮）骨架；总判/红项专节/装配刷新/附属读数按 0913 收口轮口径改写，
两红案的新读数自 json 现取（不硬抄首轮文本）。"""
import json
import io
import sys

sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')

SRC = r'C:\提示词\工作区\_tmpS7双断言跑门0913\_S7跑门读数.json'
DST = r'C:\提示词\工作区\M2-第1章量产0911\成卷\S7双断言记录.md'

res = json.load(open(SRC, encoding='utf-8'))
n_green = sum(1 for v in res.values() if v['n_red'] == 0)
n_red_it = sum(1 for v in res.values() if v['n_red'] > 0)
n_red_g = sum(v['n_red'] for v in res.values())


def detail(item, gate_head):
    for c in res[item]['gates']:
        if c['gate'].startswith(gate_head):
            return c['detail']
    return '(门缺)'


d5_zm = detail('导学件/章末-本章总结提升', '⑤')
d13_da = detail('答案册', '⑬')
d5_da = detail('装配/导学本', '⑤')

L = []
A = L.append
A('# S7 双断言链·全件跑门记录（M2 第1章）')
A('')
A('- 日期：2026-09-13（**收口轮复跑**）　｜　跑门器：`工作区/_tmpS7双断言跑门0913/S7双断言跑门.py`（只读跑门，零写盘；逐门读数 json 同目录 `_S7跑门读数.json`，控制台留档 `_S7跑门控制台0913d.txt`；首轮 31 绿 2 红留档 `_S7跑门控制台0913c.txt`）')
A('- 射程：33 件＝源件 29（导学 12／练习 10／拓展 2／测评卷＋滚A／滚B 3／答案册 1／装配册目录页 1）＋装配四本 4（导学本 44／练习本 38／测评本 7／答案本 13）。')
A('- 判据谱系：源谱 `_测v4断言.py`（v4.4 54 门）＋`断言顶格.py`（顶格五查）；A4 双栏族窗口逐字承袭 v4 L1 校准值；卷件/答案册/配页/装配按「同禁则扩门」在场＋族内一致性降格（排产单 §五-A1、S2就绪报告 §四-2 授权，P2 登记）。')
A('')
A('## 一、总判')
A('')
A(f'**33 件全绿 0 红（2026-09-13 复跑）**——全件 {len(res)} 项：全绿 {n_green} 项、红 {n_red_it} 项（红门合计 {n_red_g}）。'
  '首轮两红项（红1 章末 ⑤ 奇偶交替／红2 答案册 ⑬ 全角括号）修净后本轮**复跑转绿**，其余 31 件读数与首轮同态＝**零回归**。')
A('')
A('- **零回归证据**：首轮控制台 `_S7跑门控制台0913c.txt` ↔ 本轮 `_S7跑门控制台0913d.txt` 门级读数行共 **482 行**，逐行 diff 仅 **2 行**变动（＝章末 ⑤、答案册 ⑬ 两门由红转绿），余 **480 行读数逐字同值**。')
A('')
A('| 件 | 族 | 门数 | 红 | 判 |')
A('|---|---|---|---|---|')
for it, v in res.items():
    tag = '全绿' if v['n_red'] == 0 else f"红{v['n_red']}（{'、'.join(c['gate'].split('（')[0] for c in v['gates'] if not c['ok'])}）"
    if it in ('导学件/章末-本章总结提升', '答案册'):
        tag += '｜原红项，本轮转绿'
    A(f"| {it} | {v['family']} | {len(v['gates'])} | {v['n_red']} | {tag} |")
A('')
A('## 二、逐件逐门读数表')
A('')
for it, v in res.items():
    tail = '（原红项·本轮转绿）' if it in ('导学件/章末-本章总结提升', '答案册') else ''
    tag = '全绿' if v['n_red'] == 0 else '红%d' % v['n_red']
    A(f"### {it}（{v['family']}）—— {tag}{tail}")
    A('')
    A('| 门 | 读数 | 判 |')
    A('|---|---|---|')
    for c in v['gates']:
        gate = c['gate'].replace('|', '∣')
        det = c['detail'].replace('|', '∣').replace('\n', ' ')
        A(f"| {gate} | {det} | {'✓' if c['ok'] else '**✗R**'} |")
    A('')
A('## 三、红项专节（两案已修净＋本轮复跑转绿＋证据指针）')
A('')
A('### 红1　导学件/章末-本章总结提升 ⑤ 奇偶交替（偶页缺「人教B」串）—— 已修净')
A('')
A('- 首轮读数：✗p2偶,p4偶,p6偶；p2/p4/p6 页脚实印「高中数学 选择性必修第一册**(人教A 版)**」。')
A('- 定性（不变）：**源侧快照陈旧**，非内容错——main.tex 2026-09-12 15:02:46 已勘正（撤销批6 误换册名行），勘正后未复编译；装配侧副本当时已印正确 B 版。')
A('- 修净：对源件复跑 xelatex×2（`成卷/导学件/章末-本章总结提升/main.pdf` 2026-09-13 09:50 再生，三0、6 页不变）。')
A(f'- 复跑转绿：本轮 ⑤ 门读数＝`{d5_zm}`（零违规）；装配侧同件（`装配/导学本.pdf` 起 39 六页）⑤ 门读数＝`{d5_da}`。')
A('- 证据指针：源件 p2/p4/p6 页脚抽读＝「…选择性必修第一册(人教B 版)」（本轮实跑，见 §四 装配刷新旁证）；快照路径 `成卷/导学件/章末-本章总结提升/main.pdf`。')
A('')
A('### 红2　答案册 ⑬ 全角括号残留（开合总数 212≠211）—— 已修净')
A('')
A('- 首轮读数：全角（ 212／全角） 211 差 1；栈配对孤（ 1 处 `变式1B（√24`。')
A('- 定性（不变）：键 `导-课时06-探2-变式1` 值尾以 **`\\)`（数学闭定界符，不印字）** 收束，缺全角 `）`——缺字在值源注入环节，非 body 手改。')
A('- 修净：**源头根治**于组装器 `成卷/答案册/组装S5.py:168`（该值改 `B（\\(\\dfrac{\\sqrt{2}}{4}\\)）`，注释留 0913 修红2 注记），`body.tex` 由组装器再生（2026-09-13 09:49）＋答案册 xelatex×2 复编译；**body.tex 未手改**（唯一值源口径守住）。')
A(f'- 复跑转绿：本轮 ⑬ 门读数＝`{d13_da}`（开合相等、错位 0）。')
A('- 证据指针：源字面账（raw，含注释行）**（ 233 ＝ ） 233**、剥注释后 **212 ＝ 212**（body.tex 现读）｜pdf 账 **（ 212 ＝ ） 212**（main.pdf 13 页，页数 13＝13 不变）｜值快照 `导-课时06-探2-变式1` 现值＝`B（…）`（全角收尾）｜600dpi 渲染实证＝`工作区/_tmpS7双断言跑门0913/探针-答案册p3zoom-修红2.png`（p3 `变式1 B（√2/4）` 右括号字形在场）。')
A('')
A('## 四、装配刷新读数（收口轮·承 装配记录.md §六「双轨各自重编」口径）')
A('')
A('- 复跑 `工作区/M2-第1章量产0911/_tmp装配装订0912/装配四本.py`：**28 副本全 err=0 ovr=0 mch=0 OK**（报告同目录 `_S7重跑装配报告0913.txt` 已随本轮刷新）。')
A('- 页数表（合并后实读）：')
A('')
A('| 本 | 页数 | 全局页码区间 | 书签 |')
A('|---|---|---|---|')
A('| 导学本 | 44 | 1~44 | 12 |')
A('| 练习本 | 38 | 45~82 | 12 |')
A('| 测评本 | 7 | 83~89 | 3 |')
A('| 答案本 | 13 | 90~102 | 1 |')
A('| 合计 | **102** | **1~102 连续** | **28** |')
A('')
A('- G7 双轨：装配副本答案册 `body.tex:6` 重置位改写 90（`_tmpS7双断言跑门0913/_装配补G7答案本.py`，原轮同款注释），副本 xelatex×2 三0、重并 `装配/答案本.pdf`；**独立轨原件 `成卷/答案册/body.tex` 起页 1 未动**（两轨同源、差异仅该行）。')
A('- **重点读数**：答案本副本已含**新 body.tex**——`装配/答案本/答案册/body.tex` 头注「生成：2026-09-13｜键数：631」、全角括号 raw 账 （233＝）233、红2 修线（`探究二` 行 `变式1 B（…）` 值尾全角右括号）已在印件源中在场＝修后值随快照入印件。')
A('- 终验页脚（逐页）：导学 1~44／练习 45~82／测评 83~89／答案 90~102 **全连续 ✓**（`_tmp装配装订0912/终验页脚.py`；脚本末行文案硬编码「1-103」系旧轮文案，断言按各本起页自适应，以逐本输出为准）。')
A('- 书签断言：12/12/3/1＝**28 条**挂齐（顶层＝件名；`册目录页.pdf` 2 页无书签＝配页件设计态）。')
A('- 装配四本跑门（本链门级）：四本全绿，① 三0＝装配报告 28 副本 OK 引用、② 页数＝登记值、页码/奇偶按全局号（G7）。')
A('')
A('## 五、附属读数（成卷/答案册/ 下四门，本轮复跑，均只读）')
A('')
A('| 门 | 命令 | 读数 | 判 |')
A('|---|---|---|---|')
A('| 钉值门·正测 | `python 钉值门.py` | 生效门 **16** 项（编号至钉-18 含空号）全过零漂移 | ✓ |')
A('| 钉值门·负测 | 同上（自带负测例） | 灌旧值 **16/16 真拦** | ✓ |')
A('| 钉值门·strict | `python 钉值门.py --strict 值快照.json` | 值快照 **633 键**逐门断言全过 | ✓ |')
A('| 对号断言 | `python check_pairs.py --拓159 --strict` | 四本侧键集 **631**（拓159档）｜答案册侧 pair 标记 **631**；缺 0 多 0，集合相等零缺漏 | ✓ |')
A('| 值一致性 | `python _s7值一致性.py` | pair **631**＝比对 **631**（json 633 含 2 非键）；body.tex↔值快照全键一致；V 表含新值 10 项全在场（练04-2/练04-10/练04-13/练04-15(3)/练06-9/课05预习填空/课02判5/拓-001/拓-053/课06评5） | ✓ |')
A('')
A('## 六、门窗口径承继与定窗登记（P2·未随本轮变动）')
A('')
A('1. **逐字承袭（未动）**：A4 双栏族 ④⑤窗（块 26.2×7.8、底 11±0.5、数字 10.5~11.5、近 2.5~3.4、小字 5.0~5.45、FZSSJW/NotoSansSC）、⑧行距 18.0±0.5、⑩栏左±3.5pt、⑭右挂 ink 0.75~1.35／bbox −0.8~−0.5、⑮花形组窗——均为 v4 L1 校准值。')
A('2. **A 族定窗（预定→实测定窗）**：页脚块宽 13.8→**31.0**（13 页一律 31.0×7.8 底 11.08）；行距 18.0→**6.25**（主峰 ×143，次峰 18.0×32）。练习件/拓展册同款块 **31.0×7.8**、页码三位零填充（`001`）。')
A('3. **⑦ 栏线扩门**：v4 长度窗（非末 0.5／末 0.22 版心高）在 M2 误伤（课01 p1 跨栏元素断线 61.9+79.1mm、拓下 p13 末页 51.0mm），改「逐页在场＋0.4±0.05pt 色 189＋覆盖 ≥0.15 版心＋芯 170-210」，段高读数登记。')
A('4. **⑨ 定义内豁免**：衔接节 `\\lxopt`/`\\subl` 定义体内 `\\hangindent`×3＝件型设计（同 J 族 hang=4 登记口径）；正文裸用仍须 0。')
A('5. **⑬ 判据改域**：v4 字面账（body（−zhentib）只成立于定义体在 main.tex 的三件式域；M2 宏定义在 qp-blocks.tex、拓展册 fancyfoot「（上）」定义行字面多次渲染，字面账先天不齐。改「**全册全角开合总数相等**＋D 族判断槽恰＝zhentib 数＋J 族 pdf 槽对＝`\\kw` 用数」；栈配对/源字面降为读数。该判据即位即捕获红2 真缺字，红2 修后仍由同一判据验绿（非改门放行）。')
A('6. **⑫ 墨级压字口径**：bbox 相交仅初筛；对相交图在内存副本影删（`delete_image`，不落盘）后 600dpi 重渲染得文字自墨，与位图原生暗墨投影掩码（变换逆映射采样）2px 邻域求交，重叠方计压字。缘由＝M2「解答书写区」浅灰水印（184~233 灰，暗阈无墨）行盒与图 bbox 相交而墨不触，矩形/墨盒级判法在本域假阳。位图期望账＝图源内嵌位图总数（多栅格 PDF 图与 `\\fig` 类宏包装图按用量展开）。')
A('7. **⑥ 普查判据修复**：HTML 灰档判据原写 `v[0::2]==v[1::2]`（对 A6A6A6 类恒假，v4 域靠白名单掩盖），改 `v[0:2]==v[2:4]==v[4:6]`；补内联 `\\color[HTML]{..}` 普查。册目录页 toc 灰 99/144/157/176/199 随之入档。')
A('8. **装配本扩门**：①编译三0＝装配报告 28 副本 OK 引用（无独立 log）；②页数＝登记值；页码/奇偶按全局号（G7）；位图账/源字面账归 29 源件门。授权源＝排产单 §五-A1＋S2就绪报告 §四-2（P2 登记）。')
A('')
A('## 七、红线自证')
A('')
A('- 本轮写动清单（全在授权面内）：①装配快照刷新——`成卷/装配/` 四本 PDF＋28 装配副本（含 `答案本/答案册/body.tex` 起页位改写 90）；②装配报告 `_tmp装配装订0912/_S7重跑装配报告0913.txt`（刷新）；③本记录 `成卷/S7双断言记录.md`；④跑门控制台/读数 json/探针图（`工作区/_tmpS7双断言跑门0913/`）。')
A('- 成卷内容件零改动：拓-057 对号档、课09、`钉值门.py` 门表未动；答案册原件 `body.tex` 起页 1 未动（G7 改写仅在装配副本）；红2 修在 `组装S5.py` 值源（上一轮已落，本轮未再动）。')
A('- 跑门器与四门附属件均只读（`delete_image` 影删仅在内存副本）。')
A('- git：未使用。')
A('')

open(DST, 'w', encoding='utf-8').write('\n'.join(L))
print('written', DST, len(L), 'lines')
