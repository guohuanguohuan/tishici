# M3 抽答器（纯答案册·三档导出第三档）冒烟报告 — 2026-09-14

> 执行：M3 toolchain 装配层代理｜写前核 mode＝auto｜红线执行：零 git（全程未运行任何 git 命令）；
> 写入仅 `工具/答案抽册器.py`＋`工作区/_tmpM3抽答器0914/`；成卷正件（main.tex／值台账／sty／PNG 域）
> 全程只读零落笔；qp-m3.sty 未动（四侧 md5 锁 `7c3930362be8a0a2bdf21bbf8ac16573` 复核一致）。

## 一、脚本口径（`工具/答案抽册器.py`，613 行）

- **输入**＝成卷 tex 件目录（含 main.tex＋qp-m3.sty 本地挂载副本）。
- **抽取**＝照 `% ans:<键>` 源层锚＋`\begin{ansblock}[键]` 编译层锚，逐块花括号平衡取参
  `\ansitem{印面号}{值}`／`\ansnote{详解}{文}`（逐字保真，跨行安全）。
- **产出**＝纯答案册（册头照源件标题宏＋`【纯答案册】题面不重印·印面号与正册同号同序`标签行
  ＋逐题 ansblock：题号＋[答案]＋[详解]，**题面零重印**；全线括线 `\ansblockgrayfalse` 承练习件口径；
  `\tailfill[30mm]` 定高档，理由见 §四）。单源 `ansbook.tex`＋双壳：
  `ansbook-true`＝详解印本／`ansbook-false`＝纯值速查本（`\ansbookdetail` 件面开关，sty 零改）。
- **册序**＝`--order zhengce`（默认）正册装配序＝印面号升序（同序同号，与正册 log ANSKEY 序逐位全等）；
  `--order canonical` 可切冻结 manifest 键序。两模式均断言键集合≡canonical（题面库冻结 manifest
  本体优先，件manifest 次之）。
- **符号路由**＝▱NSC 子块／−TNR 补钉照母版体例 §二逐字随册；编译 TinyTeX xelatex 双档×两遍，
  cwd＝册目录。
- **默认落位**＝`工作区/_tmpM3抽答器0914/<件型>-<片名>/`（`--out` 可改）；读数落盘
  `抽册读数.json`；PNG 留证 `png-true/`／`png-false/`（150dpi 全页）。

## 二、两册冒烟读数（2026-09-14）

| 项 | 导学件 课时01-坐标法 | 练习件 课时01-坐标法 |
|---|---|---|
| 源件 main.tex md5 | `54eaf641b3a5737881b282e32ad619ae` | `e7fb9af42e5d6e62412187893d78b691` |
| 键数（守恒） | 23（G5＋E16＋T2） | 16（E1~E16） |
| canonical 来源 | 题面库冻结 manifest | 题面库冻结 manifest（E 键子集序） |
| 三零（双档×两遍） | 4 编全 rc=0，err/Over/Under/Miss＝0/0/0/0 | 同左 4 编全零 |
| M3-ANSKEY | 23 键×4 编全同，序≡册tex序≡正册log序（逐位） | 16 键×4 编全同，序≡册tex序≡正册log序 |
| 页数 | true **3**／false（纯值）**2** | true **3**／false **1** |
| PDF[答案] | 23／23，号序≡正册true PDF（逐位）＋1..23 连号 | 16／16，号序≡正册true PDF＋1..16 连号 |
| PDF[详解] | true 23／false 0 | true 16／false 0 |
| 逐键值印面在册 | 23/23 全中（两档，空白归一＋NFKC 折形） | 16/16 全中（两档） |
| 题面泄漏 | **0**（题面库题面行≥12字run，正册答案块体豁免；两档） | **0**（同口径；两档） |
| ansbook.tex md5 | `4d29f7e5242117b7850f28111fc96f47` | `ac08097369f5496ba8d81b626668861e` |
| PDF md5（true/false） | `07a7b168…06e05`／`d6854a2c…eb55b` | `e1de80a9…f7639045`／`843ab834…d4ae10` |

**页级目验自证（PNG 已逐页目验）**：册头章名/节名照源件＋【纯答案册】标签行；题号 1..N 连号全；
[答案] 值与正册逐字（含 T1/T2 `⟺`、⩽、▱、√2 数学形）；题面零泄漏（册内只有号＋答案＋详解）；
纯值档详解零印；尾块恰 1。留证＝两册 `png-true/`／`png-false/` 全页 PNG（已目验 5 页代表页：
导 true p1/p2/p3＋练 true p1＋练 false p1）。

## 三、门读数（抽册件四门，两册全绿）

| 门 | 断言面 | 导学件 | 练习件 |
|---|---|---|---|
| 守恒门 | 源锚数＝源块数＝册锚数＝册块数＝canonical 键数（抽册守恒：源键数＝册内块数） | 23＝23＝23＝23＝23 绿 | 16×5 绿 |
| 对号门 | 册log ANSKEY 序≡册tex序（两档两遍）；集合≡canonical 双向diff；两档恒等；zhengce 模式加验册log序≡正册log序逐位 | 全绿（23 键） | 全绿（16 键） |
| 钉值门 | 册值≡**正册件内值逐字全等（硬断言）**≡值台账 items.值tex 逐字；册详解≡正册详解逐字；册印面号≡值台账印面号 | 23/23×4 腿全等 | 16/16×4 腿全等 |
| PDF 门 | [答案]计数＝N；号序≡正册true号序逐位（同序同号印面证）；true[详解]＝N／false＝0；逐键值印面在册；题面泄漏＝0；页数 true≥false | 全绿 | 全绿 |
| 三零门 | error/Overfull/Underfull/Missing character 全零＋exit=0（双档×两遍） | 4/4 全零 | 4/4 全零 |

机读全量读数＝两册目录 `抽册读数.json`（逐项 38／38 条判定，红 0）。

## 四、过程纪要（Overfull 归因，探针件在 练习件册目录 `_probe_*.py`）

1. 首编练习件 true 档出 `Overfull \vbox (12.99207pt too high) while \output is active`（页2，双遍同值）。
2. 归因探针（分段留证）：去任一尾块（E8/E14/E15/E16）即消→末内容页平衡病；`\multicolundershoot/
   \multicolovershoot=14pt`、`\raggedbottom`、`multicols[10mm]` 均无效；`\columnbreak` 有效但系
   钉档 §三.3 禁常态逃生口；**无尾块＝零溢**→病灶锁定 `\tailfill` 无参（fill 胶）末页平衡。
3. 处置照钉档 §三.3 既有裁定（「无参在平衡栏呈内容尾随框，贴栏底须定高参」）：册面改发
   `\tailfill[30mm]` 定高档——两册复跑四门全绿（导学件同步换定高复验仍全绿），sty 零改。
4. 抽取器两处修红（均有过程件留证后覆写）：`extract_arg` 花括号深度自 1 起（原 0 起致
   `\sqrt{4^{2}}` 类嵌套早闭／无嵌套体吞源——曾致册体混入源件碎块）；源行 CRLF `\r` 剥除。

## 五、遗留与边界

- 册序默认＝正册装配序（同序同号正册向）；若产线欲照 canonical 键序排列，`--order canonical`
  一键可切（对号/钉值/守恒门同绿，册log序≡正册log序 一项按模式自然豁免）。
- 题面泄漏口径＝题面库题面行 TeX 剥离后 ≥12 字连续段（NFKC＋空白归一，正册答案块体已有者豁免）；
  纯数学碎片（如 `\sqrt` 渲染形）不在承重面。
- 值台账缺件时钉值台账腿自动降级（正册件内值硬断言腿恒在）；本次两册台账均在，四腿全跑。
- 抽册器对其他片（课时02~19／衔接节）未冒烟；结构解析已按件面通用锚制实现，复用时先跑本门谱。

—— 报告终（生成：M3 toolchain 装配层代理｜工具/答案抽册器.py）
