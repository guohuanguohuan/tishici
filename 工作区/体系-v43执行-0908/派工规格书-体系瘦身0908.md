# 体系瘦身手术·规格书 v2（2026-09-08，用户点名「太庞大」；体系改进轮①；含商议席五点修订）

基线＝`a6ee541`（已推送，回滚基线）。本轮**只动体系文件**：公共规则.md、四总控、ai自行经验积累.md、公共规则·目录摘要.md；其他文件发现问题一律登记进度看板欠账、本轮不动。计量单位统一**字符数**（中文一字≈3字节，禁用 KB/字节混报）。

## 一、手术总法：行内抽脂·保行号·语义零变更

公共规则锚点体系＝行号（L12/L14/L21/L100/L118 等被全库高密度引用——附则 13 件、看板、台账均以 L 号引之），故：
1. **行数不变**（公共规则 227 行、高中同步总控 146 行）——只缩行内文字，不删行、不并行、不改行序。
2. **每行内容三分**：①现行有效规范句＝一字不动保留；②历史/沿革/留痕/已废止旧口径/解释性注记＝**全文抽走**迁往留痕档案（不丢一字）；③抽走处在原行留短指针「〔沿革→留痕档案L{N}〕」。
3. **语义零变更红线（优先级＞体量目标）**：凡拿不准属①还是②的句，一律按①保留；禁改写、禁概括、禁「优化表述」——只搬不改。**未达标落盘实测值＋按①留清单呈报，禁为达标扩抽**。
4. **判据示例/检测签名归①**（如 L71 检测签名示例、L63 难度判定例）——②类「案例复述」措辞不得误伤规范本体。
5. **接缝补位**：②类常居括号内，抽走后指针连同其专属括号/标点补位，残留空括号、重复标点的清理不视作改①。
6. 留痕档案＝`附则/留痕档案-<源文件名>.md`，按 L 号/行号归档，块头标来源行与抽离日期。

## 二、分片（互斥文件范围，一片一代理）

- **A 片**：`公共规则.md`（227行/≈49K字符 → 参考档 28–32K 字符，红线优先）＋新建 `附则/留痕档案-公共规则.md`。
- **B 片**：`高中同步总控.md`（146行/≈21K字符 → 参考档 ≤12K 字符）＋新建 `附则/留痕档案-高中同步总控.md`；顺带只读体检 `大学数学总控.md/二轮复习总控.md/强基竞赛总控.md`（无②类内容则登记「无需动」不改）。
- **C 片**：`ai自行经验积累.md`（141行/12,728字符＞自限12,000）——**先 grep 断言全库无指向本文件的行号级入站引用**（有则同样保行数、指针行占位；无则条目制外移）：早期条目（09-03 前）冷档外移 `工作区/看板档案/经验冷档-0908.md`（沿用 08-29 组退役→冷档先例），原处留指针；近轮（09-03 起）条目全保留；降至 ≤12,000 字符。
- **D 片**（**A/B/C 全毕后串行**）：`公共规则·目录摘要.md`（292行/≈10K字符 → 参考档 ≤6K 字符）按 A 后公共规则逐行核对重生成；**终态**全库引用一致性 grep（`L\d+` 引用行号未移位＋被引条款仍在该行；**豁免工作区/历史交接件**，命中非体系文件→登记看板欠账本轮不动）。

## 三、验收尺子（体检轮硬指标）

1. 体量前后实测对比落盘（每文件 行/字符 两数）；
2. **行数不变断言**：公共规则 227 行、高中同步总控 146 行改后逐字核；
3. **重组恒等断言（机械全量，脚本执行）**：逐行断言「基线原行 ≡ 现行剩余＋指针＋档案块」逐字可重组（基线取 `git show a6ee541:<文件>`）——取代抽样比对；
4. 全库 `L\d+` 引用一致性 grep（操作性定义：引用号 ≤ 目标文件行数 且 被引条款仍在该行；豁免工作区历史件）；
5. **跑现货 `工具/规则lint.py`**（XJ-1a~1e 超长行/断链/留痕缺标/占位符/日期；规则文件大改轮完成后必跑——附则《规则巡检》XJ-1；报错级不修不放行，警告级登记看板工具债区）；
6. 两道体检（旧词族 grep＋正文↔证据句对照）＋未参与修订的独立审计代理全库通读。

## 四、纪律

主会话不亲自改；各片代理落盘前后实测数字＋抽走块清单（L号｜字符数｜去向锚点）；看板超预算 6,666/6,000 旧账行禁动；禁 git（主会话统一收尾）；禁动非体系文件；中途禁停等空转、需决策事项写入落盘报告交主会话。
