% ============================================================
% 全品式双栏样张 v2 · LaTeX 轨（人教B版选必1 §1.1.1 空间向量及其运算）
% 编译：xelatex main_v2.tex（两遍）
% 相对 v1（main.tex）的变更（逐项见 实验报告-latex轨-v2.md）：
%   1. twocolumn → multicol：节/小节标题通栏居中，仅正文双栏（对齐全品 p06/p08）；
%      栏线颜色用 multicol 原生 \columnseprulecolor，弃 etoolbox \patchcmd 补丁
%   2. 表格弃 booktabs 三线制 → 全框 0.4pt 细线 ＋ 表头行白底黑体加粗居中（v2.1 弃灰底，
%      colortbl 随 \rowcolor 一并移除）；数据格左对齐 10.5pt；\tabcolsep 3pt 紧凑内边距；列宽定长
%   3. 图内容感知分档：立体线框 30mm／组合图 45mm，86mm 禁用；正文图独立成块、题号右缘 7mm 起排
%   4. 逻辑绑定：\glueguard 组标题/题号防孤悬（multicol 下禁用 \Needspace，见 108 行区）；
%      题干→选项/①链/尾句 \penalty10000；
%      \clubpenalty/\widowpenalty 10000；xeCJK CheckSingle 防孤字行
%   5. 结构：\zhishi（◆ 知识点N 12pt 黑体）插入讲部；\zutit 挂「探究点N｜」编号；
%      \ansbox \fboxsep=1pt；标题上下留白加大（垂直节奏 > 题间距 6pt）
% 版式规格：A4 四边 15mm；双栏 columnsep=7.5mm 栏线 0.14mm 浅灰；
%   正文 10.5pt/18pt；章标题 22pt 黑体居中+通栏 0.4pt 横线；
%   节 18pt / 小节 15pt 黑体居中（通栏）；知识点 12pt 黑体◆；题型组 12pt 加粗+左 2.25pt 黑竖条；
%   题号悬挂 7mm（题号列恒空：图/表/答案行从题号右缘 7mm 起排）；题块间距 6pt；答案行一行式+答案值浅灰底（\fboxsep 1pt）；
%   表格全框 0.4pt＋表头白底黑体加粗；图 30/45mm 档；挖空 15mm；
%   页眉 9pt 灰字节名；页脚 6.5pt 灰字章名+31×7.8mm DDDDDD 灰块页码
% ============================================================
\documentclass[fontset=none]{ctexart}

% ---------- 字体 ----------
\setmainfont{Times New Roman}
\setCJKmainfont[BoldFont=SimHei]{SimSun}   % 宋体正文，加粗映射黑体（无伪粗告警）
\setCJKfamilyfont{hei}{SimHei}
\newcommand{\heiti}{\CJKfamily{hei}}
% 带圈序号/几何符号/◆ 等路由给中文字体（Times 无这些字形）
\xeCJKDeclareCharClass{CJK}{"2460->"24FF, "2200->"22FF, "25A0->"25FF}
\xeCJKsetup{CheckSingle=true}               % 检测并消除汉字单字孤行

% ---------- 宏包 ----------
\usepackage[a4paper,margin=15mm,headheight=12pt,headsep=2mm,footskip=6mm]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{multirow,array,calc}
\usepackage{multicol}
\usepackage{needspace}
\usepackage{fancyhdr}
\usepackage{indentfirst}

% ---------- 颜色 ----------
\definecolor{rulegray}{HTML}{BFBFBF}   % 栏线浅灰
\definecolor{headgray}{HTML}{808080}   % 页眉灰字
\definecolor{footgray}{HTML}{808080}   % 页脚灰字
\definecolor{ansgray}{HTML}{C7C7C7}    % 答案值浅灰底
\definecolor{pnumbg}{HTML}{DDDDDD}     % 页码灰块

% ---------- 版面 ----------
\setlength{\columnsep}{7.5mm}
\setlength{\columnseprule}{0.14mm}
\renewcommand{\columnseprulecolor}{\color{rulegray}}  % multicol 原生支持
\setlength{\multicolsep}{8pt}
\renewcommand{\normalsize}{\fontsize{10.5pt}{18pt}\selectfont}
\normalsize
\setlength{\parindent}{2em}
% 孤行寡行治理 + 断行韧性（数学长式允许在 =、· 处断行，消 overfull）
\clubpenalty=10000
\widowpenalty=10000
\displaywidowpenalty=10000
\tolerance=9999
\emergencystretch=8em   % 序言预设；实际生效处在 multicols 内（multicol 进入时会把 \emergencystretch 重置为其默认 8pt，见 129 行区）
\binoppenalty=100
\relpenalty=100
\raggedbottom   % 章首通栏区/浮动表放不下时整体下移，避免压页脚
% 浮动参数放宽：大表贴栏顶，正文跟排
\setcounter{topnumber}{4}
\setcounter{totalnumber}{4}
\setcounter{bottomnumber}{2}
\renewcommand{\topfraction}{0.95}
\renewcommand{\textfraction}{0.05}
\renewcommand{\floatpagefraction}{0.85}

% ---------- 表格默认（全框细线＋紧凑内边距） ----------
\setlength{\arrayrulewidth}{0.4pt}
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.3}
\renewcommand{\multirowsetup}{\centering}   % multirow {=} 单元格内水平居中

% ---------- 页眉页脚 ----------
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancyhead[C]{\fontsize{9pt}{11pt}\selectfont\color{headgray}1.1\ 空间向量及其运算}
\fancyfoot[C]{%
  \begin{tabular}[b]{@{}c@{}}
  \fontsize{6.5pt}{8pt}\selectfont\color{footgray}第1章\ 空间向量与立体几何\ ·\ 讲练件（上）\\[1.2pt]
  {\setlength{\fboxsep}{0pt}\colorbox{pnumbg}{\makebox[31mm][c]{\rule[-2.6mm]{0pt}{7.8mm}\fontsize{10.5pt}{12pt}\selectfont\bfseries\thepage}}}
  \end{tabular}}

% ---------- 自定义标题命令 ----------
% 章标题：22pt 黑体居中＋下方通栏 0.4pt 横线（multicol 外通栏区）
\newcommand{\zhangtitle}[1]{%
  {\centering\fontsize{22pt}{30pt}\selectfont\heiti #1\par}%
  \vspace{3pt}\noindent\rule{\textwidth}{0.4pt}\vspace{8pt}\par}
% 统计行（通栏，无缩进）
\newcommand{\statsline}[1]{{\noindent\fontsize{10.5pt}{18pt}\selectfont #1\par}}
% 节标题：18pt 黑体通栏居中；上下留白大于题间距（垂直节奏）
\newcommand{\jietitle}[1]{\par\Needspace*{3\baselineskip}\addvspace{14pt}%
  {\centering\fontsize{18pt}{26pt}\selectfont\heiti #1\par}\addvspace{9pt}}
% 小节标题：15pt 黑体通栏居中
\newcommand{\xiaojietitle}[1]{\par\Needspace*{3\baselineskip}\addvspace{10pt}%
  {\centering\fontsize{15pt}{22pt}\selectfont\heiti #1\par}\addvspace{6pt}}
% multicol 安全的防孤悬 guard：无 \break 的“软”断点（负伸缩自闭合，剩余够则不触发）。
% multicol 下禁用一切含 \break/\pagebreak 的 \Needspace——\penalty-10000 在 multicol
% 整页收集机制下是页级断点，会把整页提前收断、跳过第二栏（tracingpages 实证：
% p5 采纳断点 t=197.61 plus 1.0fil b=0 p=-10000，即 \Needspace 的 \vfil\break 路径 → 空栏页）
\newcommand{\glueguard}[1]{\vskip 0pt plus #1\baselineskip\penalty-100\vskip 0pt plus -#1\baselineskip}
% 知识点块标题（v2 新增）：◆ 知识点N　题名，黑体 12pt 左对齐（对齐全品导学案 p08）
\newcommand{\zhishi}[2]{\par\glueguard{4}\addvspace{12pt}%
  \noindent{\fontsize{12pt}{17pt}\selectfont\bfseries ◆\ 知识点#1\quad #2}\par\addvspace{3pt}}
% 题型组（探究点）标题：12pt 宋体加粗左对齐＋左侧 2.25pt 黑竖条；与统计段/首题绑定防孤悬
% （原 \Needspace{6\baselineskip} 已删：单变量实验实证其 \vfil\break 即 p5 空栏页根因，
%  9 页→8 页；防孤悬由 \glueguard 降级承担，best effort）
\newcommand{\zutit}[1]{\par\glueguard{6}\addvspace{8pt}%
  \noindent\fontsize{12pt}{17pt}\selectfont\bfseries
  \raisebox{-1.6pt}{\rule{2.25pt}{13pt}}\hspace{4pt}#1\par\addvspace{2pt}}
% 题号悬挂：题号顶格，后续行内收 7mm；题前 6pt 题块间距；与题干首段绑定
\newcommand{\timu}[2]{\par\glueguard{4}\addvspace{6pt}%
  \noindent\hangindent=7mm\hangafter=1\textbf{#1}#2\par}
% 【】标签：黑体加粗无底纹
\newcommand{\biaoqian}[1]{{\heiti #1}}
% 答案值浅灰底（\fboxsep 1pt）
\newcommand{\ansbox}[1]{{\setlength{\fboxsep}{1pt}\colorbox{ansgray}{#1}}}
% 挖空留白线
\newcommand{\kongbai}{\underline{\hspace*{15mm}}}

\begin{document}
% 章首＋节标题＋小节标题＋本节统计行：全部 multicol 外通栏
\input{chapterhead_v2.tex}
% 正文双栏（栏距 7.5mm、栏线 0.14mm 浅灰）
\begin{multicols}{2}
% multicol 进入时会把 \emergencystretch 重置为其默认 8pt（\typeout 实测），导致
% 1.1.1.6-5 详解 C 巨型公式在 = 处断行后的松行 badness=10000 不可行→29.68pt overfull；
% 重设为 8em 后断行可行，该段转为 3 条松行（Underfull 10000 ×2 登记），overfull 清零
\emergencystretch=8em
\input{body_v2.tex}
\end{multicols}
\end{document}
