# 商议记录（三脑共识门）——dsh日费上限轮
日期：2026-09-03　单位：体系修订轮（公共规则L12短指针＋附则新文件＋看板K1）　轮次：1轮往返，三方绝对同意
来源：用户明示拍板——「每天使用deepseekv4flashvision不能超过5元」。

## 三方门（1轮）
kimi同意（附建议：熔断事件登记触发轨、术语「日费上限」四字钉死——均纳）；qwen附M1-M8硬条件（全采纳零驳回，其结论明示全部落地即绝对同意）：M1折算式与token语义写死一处（cached系input子集、未命中＝input−cached、量纲元/百万token）；M2单价只引不内联（数值唯一载体＝K1，带查证日期与来源、季度复查、历史条目不追溯）；M3一次召集成本＝全程所有请求token并集（含多轮嵌套）；M4按召集发起时刻归日、跨00:00不回改不计新日；M5追加额度判据与①类点名同构（独立明示句含金额或「不设限」、语境暗示不构成、逐字入K1、无金额＝仅放行本次一次）；M6控制流复用「到席不能」降级路径不造第二套（单次预估即超日额→停等三选一：追加/拆多次/常任三脑承担；熔断首行报告必含「今日已用X元/上限5元」；只约束dsh不约束常任门与主会话）；M7预检基准＝近3次实测最大值（无历史→试探性召集上限降1次）、判据含20%余量、越额允许但K1记标记；M8取数未验证前保守预估照常计入、确认不可得→日累计4次等同熔断、记账预检熔断主体一律主会话。
qwen另指出L13终止线（上轮自锁「此后分流附则文件」）——本轮遵行：**L12只放短指针句（含上限值与附则指针），全部细则分流附则新文件自立锚点**（附则/目录系L13终止线机制首次启用；qwen原建议放本轮工作区子文件夹，主会话改置根级附则/目录——理由：工作区子文件夹有收尾清理风险、附则系常设细则文件，偏离已在记录注明）。

## 执行
新文件 附则/dsh日费细则.md（五节：折算式/成本归属与归日/预检/熔断与控制流/账本载体）；L12按需成员段尾插短指针（含5元上限值＋附则路径＋熔断视同到席不能＋只约束dsh）；看板K1块扩「日费上限账本」字段。

## 体检（全过）
217行恒等（L12行内短追加）；L12＝1073字符（<2000预算）；「日费上限」词族＝公共规则×1＋看板×1＋附则标题×1＋本记录——无越界；单价数值未出现在任何规则正文（唯一数值载体待首次查证落K1）；附则新文件＝自立锚点，8文件集与行数基线未受扰动。

## 锚点
基线HEAD=d99c460；git范围＝公共规则.md＋进度看板.md＋附则/dsh日费细则.md＋本文件夹。
