# ②-D 回写轮事故报告 — 上61 同步盘 T7 dry 两轮 COM 超时未出数（2026-09-06）

## 现象

②D_R2_05_回写回归.py 全链实跑中，④ 同步盘幂等段的 T7 dry：**上61 在 dry1、dry2 两轮各 2 次尝试（run_tool 内建重试）共 4×600s 全部超时**，未产出「节数…拟插符…当前页数」行 → 该件记「失败」，致 idem=False、两轮全等=False、SUMMARY ALLOK=False、exit 2。其余九件 dry1/dry2 均正常出数且拟插符 0（全程无一处拟插符非零告警）。

## 已排除与佐证（同轮同件其余面全绿）

| 面 | 上61 结果 |
|---|---|
| ① 回写 | OK（抗锁未触发） |
| ③ MD5 | 同步盘 71edb6e6… = 副本终态，PASS |
| ④ T9 dry×2（XML 面） | a/b/c 新挂全零，两轮 True |
| ⑤ oMath（zip/lxml 面） | 3251 = 锚，PASS |
| 副本侧 T7 dry×2（COM 面） | 同 MD5 文件当日 ②D_R2_幂等终验.md 全绿（拟插符 0） |

即：**断言从未测出非零**——这是测量面（COM 打开超时）失败，不是状态断言 FAIL。态本身由 MD5 全等＋副本侧同文件 COM 绿强支撑。

## 处置经过

- 脚本机制内杀 WINWORD 孤儿 4 个（pid 11196/2296/2116/10324），重试后仍超时；用户同步进程（Resilio/RealTimeSync 5 进程）全程未动。
- 全链自然跑完（⑤⑥ 段完成并落盘），**未补测、未额外重试**（遵「禁强行重试硬闯」），现场保持。

## 假设（未实证）

回写刚落盘 26 件 docx，同步客户端进入上传队列窗口；上61 为最大件之一，Word COM 打开与上传读写撞锁 → 反复超时。XML/zip 面工具不经过 Word，故无碍。

## 待裁决（二选一）

1. **稍后单件补测**：等上传队列空闲（同步客户端托盘无活动）后，仅对上61 跑一次 `节标题栏顶器.py <同步盘路径> --dry-run`，预期拟插符 0＋当前页数 65；绿则 ④ 段补齐为十件全绿。
2. **等价传导认定**：以「副本侧同 MD5 文件 T7 dry×2 全绿＋同步盘 MD5 逐件全等」认定 T7 幂等面十件 PASS，上61 补测挂 ②-E 前置项。

在裁决前，②-D 回写轮按「五段半绿＋上61 T7 dry 测量悬置」封账。
