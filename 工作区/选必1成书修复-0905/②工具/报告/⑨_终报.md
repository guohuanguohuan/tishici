# ⑨轮终报 — 计数器覆盖门 WJ 归一＋X1 三题覆盖诊断修复（单目标单文件，2026-09-06）

## 〇、一句话

债2（⑧轮 A3 漂移预案立项）已修复：`工具/六类底纹计数.py` 单文件两处代码改笔（:737 覆盖门加 U+2060 归一＋腿3 续值段补 OMML 挂点扫描）＋docstring 沿革注记，**B2 12/12 PASS**（X1 29/29、X2 13/13），10 件与⑧报告逐字节全等、X1/X2 漂移恰为许可集 2 行（覆盖行＋结论行）；X1 三题诊断结论＝**计数器侧四腿夹缝，非文档侧缺陷**，未触红旗（§四前提成立，B2 判定有效）。

## 一、对象与文件范围

- **唯一改动文件**：`工具/六类底纹计数.py`（git diff HEAD --numstat＝17/1，改点见§四）。
- **零改动核验**：`工具/extract_structure.py` numstat 31/10 与本轮改前基线逐位一致（⑧轮遗留未提交，本轮零触碰）；12 件 docx MD5 改前锚定/改后复核 12/12 对平（§六）；PDF/交付区零触碰（本轮全部写操作仅落 ②工具/ 与 报告/）；git 未做任何写操作（仅 status/diff 只读）。
- 本轮新增落盘（②工具/）：⑨_00_解剖X1三题.py＋_out、⑨_01_锚定与模拟.py＋_out、⑨_02_A3计数器12件与比对.py＋_out、⑨_03_B3_MD5复核.py；报告/：⑨_六类_*.txt×12（⑥/⑧报告并存未覆盖）、⑨_12件MD5锚定.json、⑨_B3_MD5复核.json、本终报。

## 二、B1 环境预检

Python 3.12.10＋lxml 6.1.3（照⑧轮 A1 同款口径）；12 件 docx 全部在位、无占用锁实证＝⑨轮 12 次计数器运行全部一次 rc=0（重试布防未触发）；MD5 锚定先行于全部计数器运行。

## 三、:736 门根因与 X1 三题值行形态解剖（⑨_00/⑨_01 探针留档）

1. **门根因（X1/X2 全量假阴性）**：:737 裸门 `t.startswith('【答案】')`，而衔接件【答案】行原文＝`【\u2060答\u2060案\u2060】`（标签字符间嵌 U+2060），裸匹配恒假——X1/X2 29/13 条无一可开（⑧轮 ⑧_00d 已实证），覆盖恒 0、结论 CHECK。同款前科＝⑥轮 :318 ANA 边界（当时已修、B 件 61/61）。修法＝门前 `t.replace(WJ, '')`（WJ='\u2060' 模块常量）。
2. **X1 三题（1.2.1.2-9／1.2.1.3-15／1.2.5.2-10）归一后仍不命中的真因**：三题块结构同构——题号段→空段→【答案】段（仅标签芯片 run 挂 C7C7C7）→**值段**→【知识点】段。值段原文归一后＝` ； `（裸空格＋全角分号×2，**无任何挂灰文本 run**），答案值全部为段内 **OMML 公式**：m:r/m:ctrlPr 挂点全挂 C7C7C7（1.2.1.2-9：m:r×3＋ctrlPr×1；1.2.1.3-15：m:r×5＋ctrlPr×2；1.2.5.2-10：m:r×3＋ctrlPr×1）。原四腿逐一检视：腿1（值行 run 灰底/深蓝）——【答案】段内无值 run，不中；腿2（【答案】段内 OMML）——OMML 在值段不在【答案】段，不中；腿3（续值段）——只扫 `els[cj].iter(q('r'))` **w:r 文本 run**，值段文本 run 全裸、OMML 不在其扫描范围，不中。即**腿2 与腿3 的扫描面拼不出「续值段 OMML 值」这一文档在用形态**＝计数器四腿夹缝。
3. **已覆盖题对照（证明形态分歧非孤例杜撰）**：X1 已覆盖 26 题中 20 题腿3 命中形态＝值段文本 run 挂 C7C7C7（如 1.2.1.1-1 值段 ` 3/7　`、1.2.1.2-6 值段 ` 4/5；16/9　`）；6 题腿2 命中＝OMML 值在【答案】段内。三题属第三形态（OMML 值在续值段），文档侧值在位、挂点正确（C7C7C7 计入 ③族 om_mr/om_ctrl 分桶——X1 桶 63/20 与三题挂点数吻合），**非文档缺陷、非值行缺失/错位**。
4. **SM 不翻转实证**：SM 两图例题块为单段（body el [3..4)/[4..5)），块内无【答案】段，门归一后仍无腿可开——⑨_01 模拟 0/2 保持，⑨报告与⑧逐字节全等实测复核（§五）。

## 四、改点清单（工具/六类底纹计数.py，2 处代码＋1 常量＋docstring）

1. **模块常量**（:210）：增 `WJ = '\u2060'`（注记指向⑥轮 :318 前科）。
2. **覆盖门**（:737）：`if not t.startswith('【答案】')` → `if not t.replace(WJ, '').startswith('【答案】')`。
3. **腿3 续值段**（:753-766 域）：w:r 文本 run 扫描后 `if not hit` 补 OMML m:r/ctrlPr 挂点扫描（与 :743-747【答案】段内 OMML 腿同构：namespace==M、tag∈('r','ctrlPr')、shd_fill==FILL_CONTENT 或 deep_blue）。
4. **docstring**：增「⑨轮覆盖门修（2026-09-06）」沿革段（门归一＋腿3 OMML＋覆盖语义不变声明）。

**覆盖语义论证（禁放宽条款合规性）**：修后语义仍为「答案值行（或跨段续值段）有内容标记族挂点（C7C7C7 灰底或深蓝任一在位）」——腿3 补扫描的正是计数器 ③族本就清点入册的 om_mr/om_ctrl 挂点，无「无答案行也算覆盖」「无挂点也算覆盖」类造假路径；门归一为既有 :318 同款归一纪律。未改动 jlp 分支、base_ok 断言集、kind 分型、分桶恒等式等任何其他语义。

## 五、B2 修后 12 件对照表（命令形态照⑧轮 A3：B..H 带 --jlp，其余自动判定）

| 件 | ⑧轮结论 | ⑨轮结论 | 与⑧比对 |
|---|---|---|---|
| B讲练1上/C讲练1下/E讲练92/F讲练90/G讲练68/H讲练89 | PASS×6 | PASS×6 | 逐字节全等 |
| I1清单1/I2清单2/TOC册目录页 | PASS×3 | PASS×3 | 逐字节全等 |
| SM使用说明 | PASS | PASS | 逐字节全等（覆盖行维持 0/2） |
| X1衔接1 | CHECK | **PASS** | 仅 2 行许可漂移（下） |
| X2衔接2 | CHECK | **PASS** | 仅 2 行许可漂移（下） |

- X1 漂移实测：覆盖行 `内容标记覆盖＝题块数 0/29（未覆盖题: [...12 样本]）` → `29/29`（样本后缀消失）；结论行 `CHECK 见上` → `PASS 四色板齐＋七类恒等式成立＋讲部/题型竖条恒等`。**答案值/需背run 46＋m:r 63＋ctrlPr 20、芯片 58=58、⑦行 38=38、分桶 Σ245=245✓ 等其余全部行逐字节不动**。
- X2 漂移实测：覆盖行 `0/13（未覆盖题: [...12 样本]）` → `13/13`；结论行 CHECK→PASS。**run 38＋m:r 62＋ctrlPr 6、芯片 26=26、⑦行 18=18、Σ150=150✓ 等逐字节不动**。
- 讲练族恒等式对平：⑦行 131/123/161/164/118/138、题量行真实值 61/79/92/90/68/89（随逐字节全等自动成立）。
- X1 修后命中腿分布（⑨_01 模拟＝实测一致）：腿3续值run ×20＋腿2值行OMML ×6＋**腿3续值OMML（新增腿）×3**——新增腿恰命中原三题、其余 26 题腿别零扰动。

## 六、B3 核验

1. **docx 二进制锚定**：改前 `⑨_12件MD5锚定.json`（12 件 MD5＋字节数）→ 改后复核 `⑨_B3_MD5复核.json`：**12/12 对平**（B 51122a7f…、C 773298d2…、E 8ea1e44f…、F ae58aac6…、G 86a10a74…、H 8bd978ef…、I1 9550a181…、I2 d94c22cb…、X1 a3498227…、X2 be32c259…、SM 89621f48…、TOC b5c2bbda…，全量见 json）。
2. **文本文件零改动（git diff HEAD --numstat，非裸 md5）**：改前基线＝仅 `工具/extract_structure.py` 31/10（⑧轮遗留未提交）；改后＝该行逐位不动＋新增 `工具/六类底纹计数.py` 17/1 一行。**本轮 delta 恰一件**。`git status --porcelain -- 工具/`＝上述两件 M（extract_structure.py 为⑧轮态，非本轮触碰）＋0 新增 tracked 改动。
3. PDF/交付区：本轮零写操作（全部落盘限 ②工具/ 及 报告/）。

## 七、留档清单

- 探针：⑨_00（三题＋对照题逐 run/逐 OMML 解剖）、⑨_01（MD5 锚定＋SM 解剖＋修后逻辑模拟 X1 29/29・X2 13/13・SM 0/2）。
- 断言：⑨_02（12 件运行＋与⑧逐行比对断言）、⑨_03（MD5 复核）；输出 ⑨_02_A3_out.txt、报告/⑨_六类_*.txt×12、报告/⑨_12件MD5锚定.json、报告/⑨_B3_MD5复核.json。

## 八、需主会话裁决/追认项

1. **无红旗**：X1 三题诊断为计数器侧四腿夹缝（§三.2/3），文档侧零缺陷，B2 判定有效，无需停手裁决。
2. **工具债台账**：⑥轮登记债2（SM 图例演示段色值 E0E0E0×1）维持顺延未触碰（⑨ SM 报告与⑧全等）；「题量锚兜底」债注记⑧轮已消，本轮无新增债。X1/X2 的⑧报告 CHECK 态已被⑨报告 PASS 取代（⑧报告按规留档不删）。
3. **git 提交**：`工具/六类底纹计数.py`（17/1）待主会话统一提交；注意 `工具/extract_structure.py`（⑧轮 31/10）仍未提交，两件同链可一并处置。
