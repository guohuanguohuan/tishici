# ⑨轮规格书：计数器覆盖门 WJ 归一＋X1 三题覆盖诊断（单目标单文件）

> 2026-09-06 立。前序：⑧轮已修 extract_structure 题量提取（12/12 中锚、复审计六项全过），回归浮出计数器侧潜在旧账一笔（⑧轮按 A3 漂移预案停手升级，主会话裁决立项本轮）。文档（12 件 docx/PDF/交付区）为交付定稿，**全程零触碰**。

## 一、对象与边界
- **只许改一个文件**：`工具/六类底纹计数.py`。extract_structure.py 本轮零改动（⑧轮已修、复测已封板）。
- 禁 git；报告落 `工作区/选必1成书修复-0905/②工具/报告/`，`⑨_` 前缀，不覆盖⑥/⑧报告。

## 二、现象与已实证线索
1. 计数器 `:736` 覆盖门以 `t.startswith('【答案】')` 判定题号值行，**未做 U+2060 归一**——同文件 ANA 边界已有归一前科（⑥轮 §三.6，WJ 归一后 B 件 61/61）。⑧轮实证：X2【答案】行原文＝【⁠答⁠案⁠】（标签字符间嵌 WJ），原文 startswith 假阴性。
2. ⑧轮模拟（门加归一后）：X2 覆盖 13/13 足额转 PASS；X1 仅 26/29——余 3 题（**1.2.1.2-9／1.2.1.3-15／1.2.5.2-10**）值行四腿不命中，须先解剖这 3 题详解区值行的实际文本形态（归一后全文引出比对），再对症修门。**禁凭猜修**。
3. 漂移面已知且许可：X1/X2 结论行 CHECK→PASS、覆盖行 0/0→29/29 与 0/13→13/13、SM 覆盖行维持⑧轮 0/2——此外一切数字不得动。

## 三、验收硬断言（逐项落盘实测数字）
- B1 环境预检：python/依赖可用、docx 无锁（照⑧轮 A1 同款）。
- B2 修后重跑 12 件（命令形态照⑧轮 A3：`python 工具/六类底纹计数.py <docx> 工作区/选必1成书修复-0905/②工具/报告/⑨_六类_<件代号>.txt`，讲练件族带 --jlp）：**12/12 PASS**；X1 覆盖 29/29、X2 13/13；与⑧轮报告逐件比对——除 §二.3 许可漂移行外逐字节全等（⑦行恒等式 131/123/161/164/118/138、芯片 58=58／26=26、题量行真实值等逐项对平）。**前提挂钩§四**：B2 以 X1 三题为计数器侧诚实可修为前提；若解剖指向文档侧、或需放宽门语义才能命中——按§四停手，以红旗报告替代 B2 判定，**禁放宽覆盖门语义凑 29/29**（如「无答案行也算覆盖」即造假 PASS）。
- B3 波及面：git status 工具/ 仅六类底纹计数.py 一件改动；文本文件零改动核验以 `git diff HEAD --numstat` 为准（或 EOL 归一后 md5）——**禁裸 md5 比对文本文件**（⑧轮复测 R4 勘误：core.autocrlf 环境下 git show 吐 LF、盘上 CRLF，裸比对必假阴性）；docx 二进制锚定不受此限——12 件 docx 改前取 MD5 锚定、改后复核对平（锚定 json 落报告目录）；PDF/交付区零触碰。
- B4 `报告/⑨_终报.md`：:736 门根因、X1 三题值行形态解剖（原文归一后引文＋不命中原因）、改点清单、12 件对照表、B2/B3 核验结果。

## 四、红旗处置
若 X1 三题诊断指向**文档侧**缺陷（如详解区值行真缺失/真错位）而非计数器口径——立即停手，零改动文档，写红旗报告回主会话裁决（文档是交付定稿，处置权属用户/主会话）。

## 五、交回主会话的报告（≤30 行结构化摘要）
文件范围／完成与未完成分界／B1-B4 实测数字／异常与红旗／需裁决项。
