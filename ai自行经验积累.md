# AI 自行经验积累

> 本文件记录历次任务中积累的可复用经验，执行新任务前请先阅读。按时间追加，标注日期与任务。本文件是唯一允许记录经验的文件，经验禁止另建到其他任何文件或文件夹；与《新的数学物理单元同步提示词》冲突时一律以总控提示词为准并立即修正本文件；追加前全文查重，每轮收尾去重合并精简，全文不超过 200 行；体系切换后旧体系条目下次收尾压缩为 1 行。2026-08-22 起体系为「简单/中档/冲刺」三卷＋落选题清单；旧两卷时代（稳分/冲刺）条目已按主题压缩归并，完整流水在 git 历史（本次精简提交的父提交）中可查。

---

## 用户固定偏好·快速自查清单
- [ ] 原文件不删除、不移动、不备份，始终留在原位置；改动只在桌面副本上动手；成品（三卷＋知识清单＋落选题清单）数学放 提示词/高中数学/高中数学同步、物理放 提示词/高中物理/高中物理同步
- [ ] 选题先与用户交流确认（三卷难度分配结果＋组合保留对照表＋全部删除清单），同意后才成卷；交流是硬闸门
- [ ] 每题保留「题目后紧跟答案」与全部内容；**任何内容一定不要丢失，任何转换/删除/清理操作后必须逐项核对到零差异**
- [ ] 三卷按总控 2.1 定位、2.3 选题最少化（各卷内部组合归组、每组合默认只留最典型1题、至多2题、无数字上限）；题量计数=大题计1、例题/练习题/易错题均计入
- [ ] 答案步骤：公式行内化＋短步骤横排；数学卷用数学符号（∵∴），物理卷除公式符号外用汉字（因为/所以）；段前段后0行、多倍行距1.25
- [ ] 选项与不同内容间加「；」；短字段横向合并；题间空1行（无答案文件空2行，大题后空6行）
- [ ] 页边距上下左右1.27cm、gutter=0、全A4；去分页/与下段同页/段中不分页；全左对齐（正文 jc、块级公式 m:jc 缺省居中必须显式 left）；页脚页码例外＝居中、五号、「第X页（共Y页）」（PAGE＋NUMPAGES 简单域）、首页显示、单一页脚定义、距底边1.27cm（footer=720缇）、页脚仅页码无页眉（总控 3.4 页脚页码规范）
- [ ] 全文字号五号（每个 w:r 显式 sz=szCs=21；pPr>rPr 段落标记字号也要归一，否则 PDF 残留12pt且行高变大）；加粗仅主标题/结构标题/【标签】/答案与必背（b+u thick）
- [ ] 字体：中文宋体、西文/数字 Times New Roman（显式 rFonts 防继承主题字体）；公式 Cambria Math 不碰；PUA 字符 run（Euclid Math Two 等数学字体）跳过字体修改
- [ ] 标点：题号/选项点全角「1．」「A．」；小问半角 (1)；中文叙述全角；公式框内与数字紧邻（坐标/区间/小数）保持半角
- [ ] 文字/图片等非公式内容一律不进公式框；数学符号和公式进公式框（OMML）；知识清单划线粒度=填空答案＋公式＋少量核心名词，叙述句不划
- [ ] core title=文件名去 .docx（否则 PDF 名带 .docx.pdf）、作者清空；文件名/文内标题/core title 三处一致且随题量变化同步更新
- [ ] **删除/清理必须精确列出目标文件名逐个删，禁止 notlike 等宽泛批量删除**（曾误删用户桌面7个无关PDF）
- [ ] 大循环套小循环；逻辑检查必须亲自处理禁止脚本代替；逐题亲算并登记台账，台账是选题交流与验收唯一依据
- [ ] 中间产物删除、自己打开的窗口关闭（用户开的不动）；可复用脚本只放 提示词/工具/，一次性脚本桌面用完删
- [ ] 任务开始前和每次推送前 git fetch 比对；远程领先先拉取合并复查再执行；push -f 仅用户明确同意重置历史时用

---

## 旧轮次精华·按主题归并（2026-08-15~08-21 两卷时代沉淀的通用技术，以组卷网源文件处理为背景）

### OMML 公式处理定论
- **下划线渲染实测定论（PDF管线）**：非分数 m:r 加 w:u(thick)→粗线可靠；分数内 m:r 加 u→被忽略（只加 b）；分数在 m:fPr>m:ctrlPr 加 b+u→分数下方粗线可靠；0.72pt「细线」是分数线本身勿误判为下划线失效；文本 w:r 加 u thick→可靠。验证用 PyMuPDF drawings 按高度分桶，并区分分数线（分子分母间）与下划线（字符下方）。
- oMathPara 块级公式缺省居中（OMML规范），无 ParaPr 必须插 `<m:oMathParaPr><m:jc m:val="left"/></m:oMathParaPr>`；混合段（段内有文字）的 oMathPara 转行内 oMath——lxml 法：deepcopy m:oMath→insert 到原位置→remove 外壳，保命名空间完整；纯公式段（详解推导）保留块级；python-docx API 直接转会破坏 XML 致 Word 打不开。
- lxml 命名空间必须用 Clark 记法（MC='{'+M+'}'，无花括号恒 None/False）——曾因此误判公式丢失、漏统计 m:r 下划线、险些「纠错」两道正确题。
- 文本线性化/dump 会压扁 m:d 分隔符（|绝对值|、{}集合）、根号、上下标（20/81→2081）；扁平文本判读公式缺分隔符时必须先查 XML，勿下结论。
- PUA 私有区字符（U+E000~F8FF，如⊊是 U+F0DC，run 指定 Euclid Math Two）：格式 pass 强改 rFonts 会渲染豆腐块，须跳过；dump 看不到属正常，验证查 XML。
- 公式框内中文分治：整段中文=误转，转回 w:r 文本（m:sty p/b/i→w:b/w:i，保留 u/b）；中文下标（R测、U总、I额）是变量符号不拆；「4．直线y=kx+m」序号混入公式也要拆出。
- **图片可能嵌在公式 run（m:r）里**（w:drawing 在 m:r 内、m:t 为空）：公式转文本时该 m:r 必须整体转普通 w:r 保留，绝不能删（曾16张图全丢、media 还在但引用没了）。
- 表格里带下划线的 OMML 公式渲染不可靠→整段转 Unicode 文本（m:sSup→基+上标、m:sSub→基+下标、m:f→a/b、m:d→|内容|、m:rad→√，跳过 *Pr）后加 u；大 oMath 可按顶层子元素抽取重组；答案空位填公式优先从源答案段 deepcopy（克隆优于重建），克隆源首子元素是 pPr 会索引偏移，必须按内容查找。

### 构建、拆卷与重编号坑
- 题块定位必须用「来源标签＋原题号」或内容关键词，不能只按显示题号：结构标题「6．题目」、讲条目「1．电源的作用」、跨run题号（'9'+'．关于…'）都会误判/漏判；题号 run 拆分只操作第一个非空 w:t，纯数字 run 须下一 run 以全角「．」开头才换号（防「1.2万块」→「28.2万块」类误改）；「0.X」数字被序号正则吃掉→入口正则加 (?!\d) 前瞻或限定全角「．」（全角点无小数风险不需 lookahead）。
- 短选项合并只收集【答案】段之前的选项区，否则误收详解里「B．…」行；段尾已带「；」的段（器材清单）不合并；合并后必查「；；」三种形态（单run内/相邻run/跨空run）。
- 短步骤横排合并方向：后段并入前段末尾（文字在前公式在后，反向合并成「公式；文字」乱序）；纯公式段并入前一个文字段（仅排除纯标签段）；详解区内新选项/小问边界（B．/（2）开头）不合并；前段末字符已是标点（，、．。；：）时不插「；」直接拼接；带图段落不合并（否则「图；文字」）。
- 删块必须先收集元素再统一删（逐个删因索引位移隔段误删，曾吞整题块）；跨文档移块会夹带块尾标题/※说明行，提取后剥离；cut 后重插锚点必须在文档树内（脱离元素 addnext 挂空丢失）；同锚点多块插入用游标。
- 重编号/插入定位必须基于内容关键词（固定段落索引在插入后偏移，曾全部错位+1）；跨文档移图：`dst.part.get_or_add_image(io.BytesIO(blob))`（python-docx 新版只收1参、返回 (rId,image) 取[0]）+ a:blip r:embed 重映射 + rels/media 三查。
- merge_2para 要处理 b 段 pPr（直接搬全部子元素会两段 pPr 共存）；选项分号补丁按渲染序遍历子元素（w:r/OMML）在选项起点 run 前插分号，只对 w:t 做 re.sub 会漏跨run/Tab前缀/公式后接 B． 情形。
- 改名脚本 replace 后必须判断 new==path 则跳过删除（不匹配时 save+remove 会删原文件，曾误删3卷靠备份恢复）；桌面脚本不要起标准库名（inspect.py 劫持 import 致诡异循环导入）。
- 新插入题块格式必须统一 set_para_fmt（spacing 300/0/0+jc left）+ set_run_fmt（sz 21+宋体/TNR），公式 run 不动；分号/新建 run 必须补 sz=21 否则 PDF 残留大字号。

### 校验与内容守恒
- **图片三查**：media 文件数==rels 引用数==文档 drawing/pict 引用数，且全部 media md5 ⊆ 源文件 md5 集（零新增零损坏）；题目图统计到下一题号行（答案/详解区的图算在题内，停在【答案】行会误报一片）；合法复用图 draw>media 属正常。
- **内容守恒不能只用全局文本 diff**（表格拼接顺序、合并单元格会误报）：段落级＋单元格级对比；norm 删分号（合并插入大量「；」）、剥题号、统一全半角与空白；填空类文件源文本按 _{2,} 拆片、每片（≥8字、剔元数据）须为成品子串；守恒提取源题块时排除结构标题段；白名单豁免批准纠错，禁止宽正则掩盖差异。
- 孤儿表格检查：列全部 w:tbl 核对前/后段归属（表格与原件完全一致时「未匹配」检查抓不到）；空段判定必须元素级查 w:drawing/w:pict（`'drawing' in p._p.xml` 命中 xmlns 声明误判、`not txt.strip()` 丢纯图段/纯公式段）。
- 知识点覆盖：源全部知识点 ⊆ 三卷并集；删除题的独特知识点必须由保留题实质覆盖（组卷网【知识点】标签丢失/残留都要查）。
- PDF 校验：PyMuPDF 查页数/A4(≈595×842)/每页 min x0≈36pt（奇偶页交替36/72=中 w:gutter≠0）；行内公式位于长行中部是正常左对齐勿误报居中；PDF 文本提取有字体映射伪影（引号字母→数字、「共」→「偱」），内容判断以 docx XML/渲染图 OCR 为准；视觉验收裁剪目标区域 200dpi 放大确认（72dpi 整页图喂视觉会误判「无下划线」）。
- A4 边距断言用 12.7（python-docx 返回毫米）；pgSz 直接写 XML（twips），python-docx setter 是 EMU 会改坏；页脚域（PAGE/NUMPAGES）格式化后必须重验渲染；组卷网物理页脚是嵌套域（fld begin×2 包 page 域）可能打印不渲染→删全部 runs 重建简单域，数学页脚简单域正常勿一刀切；页脚文字必须以完整 w:r 挂入段落否则 Word 拒开。

### PDFCreator / PDF 管线（总流程见总控 3.7，此处只记增量坑）
- 注册表 HKCU\Software\pdfforge\PDFCreator\Settings\ConversionProfiles\0：AutoSave.Enabled=1、FileNameTemplate=**绝对路径**模板（相对路径报 FilePath_InvalidRootedPath）、ExistingFileBehaviour=EnsureUniqueFilenames、OpenViewer/Preview Enabled=0；改注册表后必须杀进程全新启动才生效（进程只读一次配置）。
- 打印后立即杀 PDFCreator 保住 spool；6.3 当前版本 spool 直接出 .pdf（无 .PS），未压缩27MB级，用自带 Ghostscript 压缩；Git Bash 里 -dPDFSETTINGS=//prepress 双斜杠；-sOutputFile 用字面绝对路径（$变量、中文路径偶发不写文件，python subprocess 传参最稳）；GS rc=1 但 PDF 完整时以 PyMuPDF 验收为准。
- Word COM 能直接开中文路径 docx，但 VBS/打印链路中文路径易乱码→复制 ASCII 临时名再打；Word PrintOut 卡死=确认面板在等交互（配置未生效）；CLI /PrintFile 挂起不可靠、COM JobQueue 版本敏感；一次打印多个会卡死，逐个来。
- GS 重编码 PDF 文本层有字体子集 cmap 伪影，仅影响 get_text 不影响视觉；视觉结论以渲染图 OCR 为准。

### 其他技术坑
- Python `'' in 任意字符串` 恒 True，in 判断前先处理空串；qn() 不支持 'v:' 前缀（用完整 URI）；body.iterchildren() 会把 w:sectPr 当表格；脚注重序列化必须用保 nsmap 的 lxml（ElementTree 丢命名空间声明→Word 报「文件可能已损坏」）；修复后必须 Word COM 实际打开测试。
- 讲核心横幅图（92px 条图、仅小节标题文字、带原卷「一~十七」序号）按「输出全部教材化」删除并以题型标题文字替代（先只删 drawing 保留同段讲条目文字，否则讲条文丢失）；易错警示图（含知识点文字）、题目图、思维导图大图是真实教学内容保留勿当垃圾删。
- 混排源文件题块边界：详解尾段可能漏在题块 end 外，边界扩展到下一真实题开始前再用结构标题截断；超纲/挂靠判定必须开目标教材电子版逐级核对册→章→节，禁止凭记忆（具体章节锚点不记此处）。
- 结构标题挂靠到该节/题型第一道保留题前；某分节/题型全部题目被删则删该标题（勿留空标题）；标题插入选用现有标题段 deepcopy 改 runs；addprevious 连续调用为正序、addnext 连续调用逆序（后插的在前），插入后复查顺序。

## 2026-08-22 新三卷规则执行（第12章首例→第3章+第12章完整三轮）
### 第12章三卷首例（简单10/中档28/冲刺9+删5）
- 组卷网讲和练结构=讲知识区＋【例题】＋【练习题】＋【易错题小练习】；题目块/知识区精确到段，块尾隔一个【例题】标记=吞下知识区首段；知识区末尾表格（在段与段之间、无P索引）必须把 end 延伸到表后下一段（曾丢3个表）。
- deepcopy 克隆后 getparent() 恒为 None（用「is not None→remove」判断合并永远为假）——改用 remove 集合按索引剔除；知识标题重建只删文字 run 保留 drawing run（否则丢实验器材图）。
- 实验题元字段（【难度】【知识点】）不紧跟【答案】段——在答案区（【答案】至【详解】前）找元字段并入最后一个非元字段落；答案续段是裸答案列表（无【答案】前缀），按段索引补删。
### 第3章+第12章亲算台账与知识清单轮
- 知识清单空位 run 会被 Word 拆分（多相邻纯下划线 run「___」+「____」、混合 run「____发电等」）：同段多空**从后往前填**（前填会使后续 blank 索引偏移），填后全局删纯下划线 run＋re.sub(r'_{2,}','') 清理混合 run。
- 必背公式划线用位置状态机：定位「：/则」标记 run 后连续加 b+u 到停符（。/，/只）；公式与叙述同 run 时在首个「，」处 deepcopy 拆 run；公式汉字下标单字 run（外/内/总/机/热等）属公式要划；编号①②独立 run 跳过。
- 空位形态两种：纯下划线 run 直接替换；公式框内空位（m:t 含 ⇒____）不拆公式框，在 m:eqArr 行内剥离 _ 新建 m:nor 中文答案 m:r 插行末保留 cases 结构。
- 源文件存在条目复制未改的题（题干与上文完全相同）——是纠错线索：曾发现分类推广误作分步推广（题干改「n个步骤」、答案「+」改「×」，oMath 内 m:t 逐个 replace）。
### 第3章+第12章三卷成卷轮
- 题号正则全角/半角分治：全角「．」无小数风险不需 lookahead，半角「.」才用 (?!\d)——「4．12名选手」曾被误判为小数漏提取6题。
- 题号 run 拆分（「2」+「．电饭锅…」）renumber 先改数字 run 再给下一 run 补「．」；物理讲部分穿插在题块区间内（详解后无【例题】标记直接接「1．电源的总功率」讲条目）——clean_block 用 after_detail 状态机：过【详解】/故答案后遇「N．短标题行」「（1）器材说明」「一、/二、」则截断。
- 讲部分含图段落文本为空，`if not t: continue` 会丢图——必须 has_draw 元素级检查；构建只迁移选中题块的图（讲部分的图不迁移）可避免孤儿 media。
- 三卷构建脚本输出名用 split("（")[0] 或显式传文件名（split("·")[0] 会使三卷同名互相覆盖）；`from docx.oxml.ns import qn` 后脚本内 qn 作题号变量会遮蔽函数——用 `import qn as QN`。
- oMathPara→行内全量转换安全（lxml deepcopy 法，含纯公式段），Word/PDF 渲染正常；表格负缩进+超宽会使 PDF x0<36 暴露——收窄按 gridCol 等比、tblW 与 tcW 同步。

## 2026-08-22 git/同步盘生存手册（当日两轮损坏修复沉淀，3.1手册补充）
1. **对象损坏症状**：git show/log 报「object file … is empty / unable to read tree / bad object HEAD」但部分命令正常。同步盘会清零 objects 下已存在路径的 loose 对象（曾中招：e688134 的 tree、ad7dce8 的 commit）；`git hash-object -w` 写回后数秒内再次被清零——loose 修复路线在同盘无效。
2. **外科修复（远端完好时优先，历史全保留不用 push -f）**：浅克隆 `git -c http.version=HTTP/1.1 clone --depth 1` 到 /tmp（网络抖动重试2-3次）→ 把克隆的 pack+idx 整体拷入 `.git/objects/pack/`（全新文件名同步盘不抢）→ 删空对象文件 → `rm .git/index && git reset` 重建 cache-tree → fsck --full 全绿。
3. **修复后必须立即 `git repack -a -d`** 把全部 loose 对象打包——同步盘只清零已存在路径的 loose 文件，不碰新文件名的 pack；此后每次提交推送后也 repack 一次。
4. index 的 cache-tree 也指向坏对象，只修对象不重建索引 fsck 仍报「invalid sha1 pointer in cache-tree」；整仓 clone 大仓会 early EOF 断流。
5. **同步盘还会删同步文件夹里的新成品**：当日 8 份三卷/知识清单成品被陆续删除——发现成品消失先 `git status` 查 D 项，`git checkout -- .` 全部恢复；GitHub 是唯一可靠层，成品一落地尽快提交推送。
6. 并行会话已把本会话未提交改动带上云的确认法：`git show 对侧提交 --stat` 看文件清单含本会话文件 + `git show origin/main:文件 | diff - 本地文件` 一致即已在云端，无需重复提交。

### 第3章计数原理三卷重做轮（简单29/中档66/冲刺6+落选125，2026-08-22）
- **源文件最后一题末尾带body级sectPr**：extract_blocks 必须过滤题块中的 sectPr（`tagof(el)=='sectPr'`），否则 deepcopy 后插入成品中间导致 Word 报「文件可能已经损坏」——简单卷因 sectPr 恰在body末尾侥幸通过，中档/冲刺卷因后续还有内容而必崩。
- **python-docx新文档footer坑**：`section.footer.is_linked_to_previous = False` 必须在 Document() 创建后、body.append 内容**之前**调用，否则 footer part 的 rId 引用链断裂（KeyError/InvalidXmlError）；footer 内 PAGE 域用 lxml SubElement 手动构建 fldChar begin/instrText/end。
- **OMML公式内文本纠错**：`2<x≤9` 在 OMML 中被拆为 5 个 m:t（"2","<","x","≤","9"），`{3,4,5,6,7}` 拆为 9 个 m:t——文本替换不能只操作 w:t，必须遍历 m:t 逐个定位（如把第一个 `<` 改 `≤`、把 `3` 改 `2,3`）；先 `om.iter(MC+'t')` 拼接 full 判断完整公式再逐 t 修改。
- **图片迁移必须重映射 r:embed**：deepcopy 题块后 drawing 的 a:blip r:embed 仍指向源文件的 rId（源 rId5→image1.png），但目标 doc 的 rId5 可能指向 numbering 等非图片 part——必须用 `doc.part.get_or_add_image(io.BytesIO(blob))` 注册到目标并重写 r:embed；源文件 rId→media 路径映射从 `word/_rels/document.xml.rels` 预提取。
- **超纲判定新坑**：二项式定理章中「等差/等比数列」「裂项相消」属选必三第五章（本章之后），题面含「成等差数列」或详解必须用等比前n项和公式（48项幂和无法直加）即超纲；但「二进制」「回文数」等情境仅用组合数不涉数列公式不算超纲。
- **B版教材 A(n,0)=1 约定**：B版选必二3.1.2明确「为了方便起见，也规定 Aₙ⁰=1」——排列数不等式 A(9,x)>6A(9,x-2) 中 x=2 合法（A(9,0)=1），原答案 {3,4,5,6,7} 漏 x=2，正确解集 {2,3,4,5,6,7}。

### 第3章计数原理三卷重做轮排版修复（2026-08-22）
- **原卷结构标题清洗**：源文件题块deepcopy会带入「题型一/二」「一、二、三」「（一）（二）」等结构标题，3.3要求全部删除替换为教材化标题——extract_blocks后必须过clean_block_titles过滤（正则匹配题型N/中文数字序号），否则成品残留原卷标题违规。
- **标签横向合并**：源文件【答案】【难度】【知识点】各自单独占行，3.4要求横向合并成一行——merge_label_blocks收集连续标签段把后续run搬到首段末尾（全角空格分隔）。坑：答案被拆成多段（【答案】(1)900 / (2)648 / (3)379）时续段不以【标签】开头会中断合并——需先merge_answer_continuations把续段并入【答案】段，再merge_label_blocks。
- **详解短句合并**：源文件详解每句话独立成段，3.4要求短句与前后合并——merge_detail_blocks从【分析】/【详解】开头段向后收集短段（总长<80字）合并；非标签短句向前合并到前段（前段末尾无标点加「；」）。坑：纯公式段（只有OMML无w:t）first_text返回空会误判——用para_text而非first_text判断，空文本无图段可继续合并。
- **源文件最后一题末尾带body级sectPr**：deepcopy后插入成品中间导致Word报损坏——extract_blocks必须过滤sectPr。

### 第3章排版修复第二轮（oMathPara转行内，2026-08-22）
- **oMathPara块级公式→行内oMath**：源文件公式用块级oMathPara（另起一行居中），3.4要求「公式行内化，不可另起一行」——convert_omathpara_to_inline把oMathPara外壳去掉只保留内含oMath插入原位置（deepcopy oMath→insert→remove外壳），四卷共转2266个。oMathPara=0为合规。
- **3.4新规则②详解合并须极谨慎**：只合并同一推理步骤内部的短句；不同推理步骤、分类讨论各分支、各小问解答一律独立成行——merge_detail_blocks的80字阈值过大会把不同步骤挤成一段（含多个「故选」「综上」即为违规），需在"；"+结论词处拆分回退。

### 第3章第三轮修复（标题穿插+自检记录，2026-08-22）
- **标题堆积/两段式装配**：build_volume先写全部标题再写全部题目的逻辑产生目录块（3.3明确禁止"先写完全部标题、再写完全部题目的两段式装配"）——标题必须穿插在题目流中（节标题→题型标题→该组合题目，逐组合依序写入）。修复：把堆积在开头的题型标题移到对应题型首题正前方，节标题移到该节第一个题型标题前。
- **落选题清单裸排判断**：落选题清单是分组标题格式（非题型标题），3.3"每道题挂在题型标题下"不适用——检查脚本需排除落选题清单的裸排判断。

### 任务C知识清单终检（第3章数学＋第12章物理，2026-08-22）
- 知识清单的解释性答案不能因“答案要加粗下划线”而整句机械划线；开放辨析题只保留“具体排法/不是数/所有排列的个数/一个数”等少量判别核心为粗下划线，完整叙述保持普通正文。
- 源DOCX启用奇偶页不同时，只重建 `section.footer` 会让偶数页残留旧“试卷第X页，共X页”；必须同时重建 footer、even_page_footer、first_page_footer，最终逐页渲染核对。

### 任务C教材节与图片挂靠补充（2026-08-22）
- 知识清单也必须全文教材化：删除源文件的单元目标、知识架构、数字分组等结构标题，改用目标教材的正式节号与节标题；标题层级到教材节为止，不增设题型标题。教材目录中没有源条目可挂靠的节，仍按正确顺序保留空标题，不补造知识。
- 思维导图和条目配图不能停留在章首或因重构而丢失：先插入所属教材节标题，再原样保留图片；**只查 media 数量/哈希会漏掉“图片文件仍在包内但正文引用已删”的假保留**，必须三查 `word/media` 文件数＝图片 relationship 数＝正文 a:blip/v:imagedata 引用数，并逐图确认前置教材节标题。本轮物理源11图、旧成品仅7个正文引用，靠三查恢复章首导图相关4图。
- 第12章源条目“电源的 U-I 图像”是教材12.3测量电池电动势和内阻的数据处理方法，文字、截距/斜率公式和配图应整体挂到12.3，不能留在12.2而让12.3悬空；同时知识清单文件名、文内标题、core title三处均须含3.5规定的“人教版必修3”版本册别前缀。

### 任务B第12章物理三卷重做轮（简10/中24/冲4+落选14，2026-08-22）
- **详解合并双保险**：被并入段只允许「纯公式段」或「量值段」（无中文、含=或数字）；中文引导句（「并联部分两端电压为」）绝不并入前段，否则吞掉下一步骤开头（「…4.5V并联部分两端电压为」教训）。且前置检查 `if not tp or not tq: continue` 会把纯公式段（无w:t）全挡掉——须改 `if not tq and not has_omml(q): continue`。
- **选项字母与点分属不同run**（「B」run+「．xxx」run）：tab→「；」替换必须跨run拼接渲染序文本再匹配，逐run判断恒不中；选项段合并应按「首段A+后续段字母严格递增」（允许「A；B行+C；D行」两行式），且序列后须紧邻【答案】段防误并详解区B．段。
- **合并搬runs必剥b段pPr（再犯）**：中间pPr还会被dump工具把 pPr>w:tabs>tab 制表位定义误渲染成\t——unify时统一删 pPr/w:tabs，段首缩进tab（首个内容run前）一并删。
- **横幅删除两坑**：wp:inline→getparent()×3 是 w:p 不是 w:r（永远删不中）；作用域写错（`if banner_idx is not None`未限定首元素）会把讲块全部小图删掉。正解=仅讲块首元素＋a:ext cy<100万EMU 双条件。
- **图片对账基线**：四卷去重md5数必须==源保留图数（96−14横幅−2封面−3源内重复=77）；数量对不上=误删/漏迁，比逐卷三查更快抓回（本次靠它抓回10张讲块内容图）。
- **易错题标记段大图≠横幅**：【易错题小练习】段内1276×434/512大图是「易错警示」知识点内容图（视觉识别确认），删文字留图挂到对应讲块（讲E/G）尾；92px扁条才是栏目横幅。
- **整体提交下的并行交互**（2026-08-23 起总控 3.1① 规定整体提交，git add -A 为标准做法）：并行会话的整体提交会把本会话工作区改动一起带上（旧规则时代案例 c28dce6）——发现被对侧提交先 `git show 对侧提交 --stat` 确认本会话文件在内即可，勿重复提交；refs/codex/* 坏ref `git update-ref -d` 报 broken 删不掉，直接 `rm -rf .git/refs/codex` 后 fetch 即恢复。
- 讲I「一、实验原理和方法…六、注意事项」是实验讲解正文组织标题（非「题型N/考点」类原卷栏目），讲部分原样保留；无简单题的节（12.3）保留悬空节标题+讲部分于正确知识位置。

### 第3章第四轮修复（标题体系重建，2026-08-22）
- **标题移动bug教训**：fix_structure.py"收集标题→重建children"的移动逻辑丢失了绝大多数标题（简单卷29个标题只剩2个）——移动/重排类修复后必须**全量计数核对**（节数+题型数与排列顺序定稿一致），只查"第一题前有无标题"会漏检。本轮靠人工通读dump发现。
- **lxml addprevious顺序**：`anchor.addprevious(x)`把x插到紧贴anchor处，**最后调用的最贴近锚点**——要实现"空行→节→题型→题"须按空行、节、题型的顺序依次调用；调用顺序写反会把题型排到节前面。
- **标题重插后必须再折叠空行**：题前原有1空行+新插1空行=双空行，插入完成后统一折叠连续空段至1。
- **body中间游离sectPr**：重建body时sectPr被挤到中间会把文档切成多节（首节页面设置失控）——保留含footerReference的sectPr移到body末尾，删除其余；迁移后复查sections=1、边距、footer引用三件套。
- **裸排检查正确姿势**：组合首题前必须有题型标题；同组第2题紧随第1题不加标题是3.3规定行为，不计裸排；落选题清单为分组标题制，裸排检查不适用。
- **落选题标注也要做锚点审计**：只核对“删除原因=题数”会漏掉125条原因全部堆在题目前的结构错；须逐题检查题号段的直接前一非空段就是该题“原位置＋删除原因”，并检查分组标题分别紧贴本组首条标注。原位置可用规范化题干对源DOCX逐题回溯，但“所属组合＋保留题号”必须来自选题台账，台账缺失时禁止用相似度猜写。

### 第3章验收轮（公式移位专项，2026-08-22）
- **行内公式移位是看不见的内容缺陷**：题干/详解的行内 oMath 被挪到段尾堆积（"设，当为何值时…⟦x>0⟧⟦x⟧⟦…⟧"），文字完整、公式齐全、grep/计数全过，只有逐段读 dump 才暴露——成品后必须做「文字-公式交错序列」对源审计。
- **审计法**：源窗口（1~4段拼接，normkey=去公式去标点去**题号**文字）匹配成品段，比对公式后邻文字锚点；同 key 多模板取错位最少者；相邻公式**不可合并计数**（合并会把堆尾多公式数成1个而漏检）；公式在段尾+锚文字在下一段首=合法行内化，非移位。
- **修复法（按模板重排段内节点）**：文字流不动，按源模板 t/m 序列重建 children；切分用双指针原文对齐（模板片全部字符含标点按序消费，S 标点不匹配则跳过）——规范化长度切分会吞掉片首/片尾标点（"【详解】"→"详解】"）。
- **坑五连**：①nrm 正则字符类 `[]` 未转义解析错，用字符判断 `''.join(ch for ch in s if ch not in STRIP)`；②oMathPara 需拆出内层 oMath 计数；③拆 run 插入 rel==0 必须 addprevious 不是 addnext；④omml 线性化不含 m:d 定界符，'g(x)'匹配要写'gx'；⑤同段多次 move 锚点必须全文唯一（'，'/'求'类通用锚会插到第一处）。
- **修复自验双基线**：段落文字流与 git HEAD 逐段 diff（原文级，非规范化——规范化会掩盖吞标点）+ 源模板 modstr 比对；两者清零才收。
- **行内化误报**：源"公式独立成段"被成品并入前段尾（"…电功率为⟦P=UI⟧"）是 3.4 规定行为；判别看公式 after 文字是否在下段首。
- **同步盘会重写成品质损格式**：被同步盘静默重写的文件特征=run被打碎、sz字号属性全剥、docx体积减半（文字/图片/加粗下划线幸存）——处置流程总控3.6已收录，此处留判定特征。
- **源文件自带 null 知识点**：教辅源数据有「【知识点】null、xxx、null」缺陷，处置规则总控3.2已收录；实现细节：null 常分散在多个 w:t，删后清残留顿号。
- **知识清单条目名≠结构标题**：规则总控任务C已收录（条目题名保留为条目首行）；实现细节：题名行格式=普通五号正文行（复制锚点段pPr+标准rPr，数学清单同款），插在条目内容首段前。

### 任务A2/B2/C2轮（选必1第2章平面解析几何+选必2第1章安培力洛伦兹力，2026-08-22）
- **A2题号连续性防假题号**：源文件题13点睛内"1．定义法：2．等价法：3．集合法："会误判为新题——提取用「期望题号==实际题号」推进（expect++只在校验通过后），比纯正则稳健；成品审计同理（B2简单卷讲块内编号条目"1．确定研究对象"会被审计误切题块，真题判定加「到下一题号段之间出现【答案】」前瞻）。
- **守恒审计规范函数必须保序**：hard_norm若分别拼w:t和m:t再串联，公式与文字顺序被打乱（标签合并后公式位置"看似移动"误报206题）——必须iter()单次遍历按文档序拼接；且要剥重编号后的题号、剥合法插入的「；」分隔run、剥run拆分差异（连续同类折叠：re.sub(r'(.)\1+',r'\1',s)对t/m序列折叠后比对，能检测公式移位且免疫run碎片化）。
- **答案token提取三坑（知识清单）**：①「（1）」子题标记被拆成'（','1','）'三个相邻run——先按文档序无分隔拼接连续文本run再按空白切分；②题22答案跨两段（（2）段在次段）——答案收集须延续到【难度】前所有段；③token前后均可粘连子题标记（"半径（2）"/"洛伦兹力18．"）——前后缀都剥；物理答案段内"N．"分段边界可在token中间（"通电导线2．磁场方向"），需循环匹配切割。
- **公式填空插入位置**：空位run为纯下划线且无前后文时split_run_at返回空锚点，`parent.insert(0)`会把公式插到段首——必须先记录原idx再按idx插入；跨run题号重编号用构建2A2.apply_renumber（"15"+"．平面…"拆分形态）。
- **组卷网答案冗余**：物理知识清单答案段"1．通电导线"对应无空位条目、"2．磁场方向 电流方向"对应单空位（磁场方向已在题面）、16题答案多一个公式token——token≠空位数时须逐题亲核覆写，多余token不填即可（勿强行对齐错位填入）。
- **旧命名成品覆盖判定**：第1章旧知识清单与新清单同源，norm前缀比对24块22直接命中+2块为旧文件块边界合并（内容均在）→确认覆盖删除；第2章旧稳分/冲刺卷含讲义内容（非本轮13文件基线，命中12/61、41/178）→不删保留。比对用KEEP字符集norm+前25字锚。
- **B2物理讲和练讲块锚点切分**：15个讲块全部位于【例题】标记前，锚点=讲块首句独特短语在题N块内定位切割（题33/34锚点易错，讲块实际在Q34后）；标记段"【图】【例题】"剥文字留图归讲块；简单卷讲块按知识顺序穿插（S0→P1→S1…S15），中档/冲刺纯题目。
- **本轮组合代表速查**：A2按33组合（简22/中31/冲14代表+7道实质差异第2题）；B2按21组合P1-P21（例题/练习题标签不构成组合维度）；超纲2题：A2参数方程（B版全套无此知识，逐册检索确认）、B2磁电式电流表C选项感应电动势（选必2第2章）。

## 2026-08-23 任务A1/B1/C1轮（数学选必1第1章132题选43＋物理第13章26题选18＋两章知识清单填空）
### 知识清单填空引擎定论
- 源结构是「空位在前、【答案】段在后」：必须缓冲空位w:t引用、遇答案段才回填，禁止正向顺序填
- 「（1）」「①②」分组标记常被拆成多个run（（＋1＋）三个w:t）：解析器必须先把整段文本拼接再split，逐run处理必漏检标记导致标记被当答案消费、全体错位
- 分隔符全集＝连续空格＋（N）＋带圈号①~⑩＋【难度/知识点/详解/解析】(截停)；「磁场 变化」类单空格双汉字答案用窄规则切分（两侧≤6纯汉字）
- 紧随答案段、以（N）开头的无空位段落可能是真续行答案（如「（4）特斯拉…」无任何标签）也可能是下一小问题干（如「（2）求直线和平面所成的角」）：用前瞻判定——下一段是元标签/【答案】/条目号才认续行；【图】占位文字段要跳过
- try_fill绝不能删除原run：一个run里可有多个空位t，删run会使后续空位脱离文档树（parent=None）；正解=只改本t文本为前缀＋addnext插入答案与后缀新节点
- OMML答案克隆后加粗下划线：m:r加w:rPr(b,u thick)、分数在m:fPr>m:ctrlPr加b+u；答案token含⟦M⟧占位deepcopy即可
### A1数学多源合并与成卷
- 六个docx合并成卷：块边界只认全角「N．」起始且长度≥8（[．.]含半角点会把「0.5」「45°.」等行误当题号，简单卷曾误检41个）
- 跨包图片迁移双路重映射：a:blip@r:embed与v:imagedata@r:id都要get_or_add_image重写；base取任一源骨架、清body留sectPr
- 结构标题清理pass放在格式化之后统一做（题型N/（一）/题组N/考点N），正则要避开自插的教材化标题与落选分组标题；点睛/方法点睛按内容原样保留不算栏目
- 冲刺卷0题合法：概念章（如电磁感应初步）经亲算无特别难题时不产出空文件，筛选清单注明理由
### C1知识清单教材化
- A版源节号必须映射B版结构（A版1.2基本定理→B版1.1.2等），条目挂靠先查目标教材PDF定位（如共面向量在B版书页13=1.1.2开头；投影向量在必修三第八章属前序知识不算超纲）
- 后续章节内容整节删除时capture范围排除sectPr；条目内混有本章说明段的可拆分保留并补条目名行
- 源缺陷纠错实例：答案oMath带重复前缀p=（题干已有⟦p=⟧）、之轴→z轴；修OMML内文本要遍历m:t定位，勿按整串replace
- **半章成品命名约定（用户2026-08-23确认）**：目标教材一章分两轮完成时（B版选必1第2章平面解析几何=2.1~2.4直线与圆+2.5~2.8圆锥曲线），成品名在章标题后加范围标签：「第2章 平面解析几何（直线与圆）·简单卷（23题）」；圆锥曲线轮用（圆锥曲线）标签；整章合并版不加标签。范围标签是章范围的语义限定，不属3.5禁止的「教材归类精修/final」类版本性后缀；改名须同步文件名/文内开头标题/core title三处。

## 2026-08-23 任务B/C轮（物理选必2第3章交变电流：简6/中18/冲7/落选14/知识清单22条目）
### 构建脚本框架沉淀
- 讲和练型源的讲块全部位于前题块尾部（题块区间[start_i,start_{i+1})含尾部讲块），提取须按锚点切走：CUTS={题号:讲块首段idx}；【例题】【练习题】【易错题小练习】标记段可能带内容图（易错警示图/自耦示意图）——剥标记文字留图，并把图移挂到其后第一道保留题前。
- 横幅判定=cy=381000EMU的满宽细条图（讲块标题条），只删drawing保留同段标题文字作讲块行；12700EMU圆点为装饰artifact整段删。
- build的items分支必须与装配清单里的kind一一对应——曾漏'KT'分支致三卷全缺题型标题（裸排违规），layout_check的题型标题计数是唯一能抓住它的自检项。
- merge_options必须以本块【答案】段为右边界（否则详解区A．B．C．D．解析行被误并）；merge_answer_continuations循环游标要j+=1且跳过已脱离树的元素，否则每块只并得第一个续行。
- 纯公式段并入目标段的禁判改为「^【(答案|难度|知识点)」或纯标签行——【详解】开头的混合段是合法合并目标；collapse需在inline_omathpara之后再跑一遍（omp转换会改变段落内容分布）；纯公式段（无w:t有oMath）不是空行，layout_check把纯公式段计成"双空行"属检查器误报。
- 跨run/m:t文本替换：源文件数字/符号常被拆成多节点（'转过30'+'0'、'U'+'3'+'2'），单w:t替换必漏——用按段落/oMath分组的跨节点_span替换器；OMML线性化检查脚本若缺sSup/sSub分支会把结构完好的公式误报为扁平缺陷（先开好lin再定缺陷）。
- 组卷网源OMML真实缺陷实例：U32/U22/U02/I22/L22ω（上标丢失成平文本）→修为Unicode U₃²/U₂²/U₀²/I₂²/L²ω；I22R线→I₂²R线、ΔP=I2r→ΔP=I²r、转过300→转过30°、"原线圈匝数为200匝"→副线圈（P42详解张冠李戴）。
- 知识清单元块删除用源索引硬删（一~八栏目8处+答案/难度/知识点/解析详解7区），但缓存DEL_ELS必须在任何插入之前做、实际删除放在节标题插入之后执行（插标题会使索引偏移）；条目重编号含无号条目名行（正弦式交变电流和电压/电流关系/解决…基本方法）补号后连续1~22。
- 填空填充：答案序列硬编码队列最稳（41空位逐项核对）；oMath答案克隆自答案元块（须在删除前deepcopy）或mk_math构建（sSub+m:r）；公式答案填入后统一math_bold_underline（m:r加w:rPr b/u、分数m:fPr>ctrlPr加b+u）。
- Word COM验证脚本的ps1必须带BOM（Set-Content -Encoding UTF8），write工具的无BOM UTF-8会让PS5.1按GBK读中文路径报假"文档损坏"。


## 2026-08-23 任务B2/C2轮（物理选必2第2章电磁感应：73题选56＋知识清单22条）
- **构建快照陷阱（两个bug教训）**：用 children[] 索引快照取题块时，任何「树内 addnext 插入/搬移」的新元素都不在快照里——补标签段、易错图挂讲块必须在副本层（els 列表 insert/append）操作或在 build 时显式附加 EXTRA 列表；树内搬移=静默丢失
- **纯公式段过滤bug**：题块清洗判空必须查 m:oMath（无 w:t 的 oMathPara 段不是空段），曾把整卷详解公式滤光；讲和练源「题号全角N．/讲条目半角N.」全半角直接区分题块与讲块边界，锚文本定位讲块起点最稳
- **图片对账口径**：表格内嵌图（w:tbl→tc→p→blip）在「body直系 w:p 扫描」口径下漏计——源侧计数必须全文档 iter；输出侧转投件按设计重复收录（源n=四卷n，转投+1）
- **知识图鉴横幅判定**：40px 扁条=「讲核心」类栏目标题图（删，同段文字保留）；554px 易错题标记图=易错警示知识点内容图（剥标记文字留图挂对应讲块尾）；1×1 信标删；序号小图（⑤/④字符图）保留
- **知识清单公式答案克隆带前导字母**：源答案「n⟦ΔΦ/Δt⟧」的 n 在 oMath 外的 w:t 里，只克隆 oMath 会丢 n/L——先扫描答案段 w:t+oMath 完整结构再克隆；条目21思维导图=答案图，贴空位处插入
- **前序章判定先例**：简易电动机题（电路并联/左手定则转向/电动机原理/能量转化）整题无感应知识=选必2第1章内容，删除转投第1章暂存；扼流圈选项给了「对交流有阻碍」前提时用2.4自感即可判=章内保留
- 标签补写先查源再补：源可能只缺【知识点】不缺【难度】（Q62）；补写值与恒等式审计口径：流比对时统一剥【难度】【知识点】标签段（标签完整性已单独验收）

## 2026-08-23 任务B/C轮（物理必修3第9章：66题选41+知识清单109空）
- **纯公式段（只有oMath、无任何w:t）会被"空段剔除"逻辑误杀**——判空必须同时查 oMath/drawing/pict；本轮曾静默丢122个独立成行公式，靠"源432 vs 输出444（差=Q59合法重复12）"的公式守恒对账抓回
- 讲块范围必须含引导句段（S7/S8漏掉intro导致泄漏进前一题块）；dump行号→body索引经表格处偏移不可靠，直接用元素索引扫描定界
- 组卷网知识清单答案令牌三坑：①「【答案】N．首答案」常粘连一块（<2空格），须先剥【答案】再按标记切分；②标记分治——全角．无小数风险不需前瞻、半角.才要(?!\d)（数字答案"24．0"跟全角标记后属正常剥离）；③续行答案段（以N．开头、无标签）是下一小题答案，须并入上一答案组消费
- 填空FIFO对账以「每答案段tok数 vs pend数」逐段打印定位漂移最快；终态恒等式：填空数==令牌消耗==残留0
- 一题同入落选题清单＋他章转投暂存时图片合法重复一份，md5图片审计需对该hash白名单

## 2026-08-23 知识清单查缺补全轮（物12/13/选必2-1，跨成品插入参考资料讲块）
- **已完成章的知识点查缺基线**：不能只对源知识清单文件（必然全命中）——要把该章全部参考资料的「讲区」逐块扫出条目名与成品条目名对比；数学大单元作业设计分节文件是纯题集无梳理区，物理讲和练讲区才是增量来源
- **body级sectPr位置因文件而异**（物12/13在末尾、选必2-1在第0子元素）：「插到sectPr前=文档末尾」的假设会 把全部新增插到开头——先探测 sectPr 索引再决定 addprevious 还是 body.append
- **编号改写必须跨run区间替换**：题号「1」+「．xxx」拆两 run 时 re.sub 只改首 run 静默失败；统一用 w:t 串联文本定位 token 区间→按重叠拆分各 t 改写（replace_text_span 一套函数服务重编号/降级/短语划线）
- **drop_start 与横幅剥离顺序坑**：丢标题段后下一段（含内容图）变成新首段被横幅规则误删（永磁体四联图案例）——横幅判定用实测尺寸 cx>3M 且 cy<45万 EMU 细长条，且只对「段内有标题型文字」的图生效
- **重编号连带效应**：条目号变化后【详解】/【解析】区引用旧编号（详解4．/解析13．）全部错位，需按区段映射同步改写；校验器对这类有意修改要放行（norm 时剥标记内编号）
- **图片审计口径**：历史成品可能自带孤儿引用（物13 rId18 media25/rels25/refs24），三查等式应放宽为「新旧差值一致＋无新增孤儿＋哈希⊆旧∪源」

## 2026-08-23 任务B/C轮（物理必修3第10章70题选59＋知识清单73空）
- 讲和练的栏目标记段可能夹带讲义正文（如「（2）将a-t图像…v-t图像。【例题】」「…切线斜率k＝⟦图⟧。【例题】」）——整段删除丢正文；正解=剥标记词后实质文本归当前讲块，纯图则走裸图决策
- 裸图归属状态机：连续裸图成组、由下一决定性元素定向；裸图+紧邻表格=新讲块（表格1/2前置图）；cur为讲块→回贴末尾，cur为题块→前插下一题/讲块
- 纯公式段并入三坑：_wt只统计w:t不算m:t；oMath藏在oMathPara内必须.//查找；排除仅限【答案】【难度】【知识点】三种标签开头（【详解】/「C．根据」类引导段允许并入）——排除过宽曾漏并38处
- 填空引擎补充：空位可在条目首段内（题干与空同段，收集范围必含首行）；答案token先按组标记（（N）/①②）两侧插空格再空白切分（解「负(2)」「一（3）法拉」粘连）；『106/1012』映射10⁶/10¹²；源答案缺项（E25缺「定片」）逐空亲核手工补
- 并行转投消费：运行中收到他章转入暂存→本轮即消费；亲算重判可推翻来方判定（「电势高低比较」只需9.4等势体结论→判前序章内容原路回投），落选清单随之扩员改名，台账目标章行备注待收转投
- 并行push被拒（cannot lock ref）＝他会话已推新提交：fetch+rebase（无冲突则秒过）+push；repack偶报rev too small不阻塞，稍后单跑即成