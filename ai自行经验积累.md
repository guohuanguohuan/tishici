# AI 自行经验积累

> 记录历次任务可复用经验，新任务前先读；按时间追加、标注日期与任务；与公共规则/总控冲突以后者为准（§4）；追加前查重、收尾去重精简，≤200行≤12,000字符；旧体系条目下次收尾压缩为1行。
> **护栏**：「历轮条目」只存指针与未固化独有教训；「工具/×.py」均在提示词/工具/ 下

## 用户固定偏好·快速自查清单
- [ ] 原文件不删不移不备份；改动只对副本做；中间产物入提示词/工作区/本轮子文件夹（收尾归位先于删除）；成品放提示词/高中数学|物理/…同步；可复用脚本只进提示词/工具/（§10）；提示词根＝C:\提示词（各机一致，迁移见§4）
- [ ] 每题保留「题目后紧跟答案」；**任何内容不得丢失，操作后逐项核对到零差异**
- [ ] 难度定档按各总控（同步线2.1三档）；选题＝好题全保留（每组合1＋1、余题按判别特征立新组合，立不出按高度重复删，不询问）；题量计数=大题计1、例题练习易错均计
- [ ] 排版（细则见§7）：段前段后0、去分页属性；边距1.5cm/850缇（页脚距边同值、页眉距顶283缇）/gutter0/A4；全部左对齐（页脚页码、封面、目录页、表格无豁免）；**字号全件小4号（24半点＝12pt）、行距统一line=410 atLeast**（「解析块」仅内容集合概念；导航表同参数）；字号例外唯一清单（§7自检⑧）＝标题梯子（章/讲32、节28、讲部/题型24均加粗）＋封面件＋册目录页降级节级行＋页眉页脚18半点＋节名锚段；行距例外＝页眉＋节名锚段
- [ ] 版面双栏制（内容件）：正文cols=2/425缇/栏间分隔线、每栏约8.6cm；头部单栏区（标题/统计行/导航表/图例）后进双栏；配页件四类（封面/使用说明/册目录页/部分封面）全文单栏；页眉页脚定义各仅一处、页码start落头部节sectPr
- [ ] 视觉锚：标题顶格（w:ind清零）＋整行两色底纹（章/节#ADC2DA→灰≈190、讲部/题型#C6D4E3→灰≈209）＋题干底纹#E0E0E0（224）；内容族C9C9C9＝22%灰（201）；章标题底边框通栏细线（w:pBdr bottom）；标签横排合并（【难度】删、难度入题号块）；节标题与统计行并一行、枚举/小问短行必并；题间无空行（题号底纹分隔）
- [ ] 题号块「题型号-节内序号．（档位·提分线·卡壳看答案）」加粗（层级制序号节内连续跨题型累进、条目号维持「节号-序号．」两形并存；其余线「N．」；衔接件「（衔接必会·卡壳看答案）」同形；退化件「N．」）；底纹盖全号、run数＝题量；加粗仅结构标题＋题号块；存量件随首跑/回扫轮升级（改版前按现版式校验注明「待改版」）
- [ ] 块标签：行内【×】栏目标签一律同款底纹不加粗、只盖【×】防连片（答案/知识点/分析/详解/点睛/编注/大招指引/题后反思/温馨提醒及讲部定义/结论等栏目标签；学史【易错】【了解】豁免）；并行解法标记挂芯片；详解序号①②与选项字母不挂
- [ ] 底纹七类分开计数：内容标记/题号难度块/标题整行底纹（章·节·讲部·题型）/块标签（含行内标签）/条目号/条目第一子层/题干底纹（带题件题干段铺#E0E0E0：题号块起至【答案】标签行前全部段、图段豁免、解析白底；段数＝题干段清点数）；灰度四值见首条
- [ ] 答案值统一灰底（深蓝#1F4E79废止）：文字型run级C9C9C9、公式型/混合型按OMML挂法（见OMML公式定论）；恒等式＝灰底run数＋挂灰oMath块数＝答案值数（无答案值条目/作答/退化真题卷除外）；需背同款
- [ ] 配图嵌入型独立段制（零锚定、wp:anchor禁项）：阅读性配图所在段只含该图（左对齐、随全件行距、紧随引用文字段），两小图（各≥3cm且字≥6.5pt）可并排；图标级小图（显示高≤1.5cm）行内嵌保留；图不挂标题段、150dpi禁超自然尺寸、单图高≤9cm、显示宽≤栏宽约8.6cm全图无例外、逐张登记；定尺寸＝主体字符（占比最大字号档）视觉高≈9pt、上限12pt超即缩，最小字符仅保≥6.5pt不驱动放大；跌破者进差图重绘候选≤10张/单位/轮（超限按§1处置加额）
- [ ] 页码＝部分独立页码制：各部分独立从1（同章同类型件为一个部分、讲练多卷跨卷连续、后件start=前件累计+1）；页眉页脚同串9pt左对齐单行「羿郭工作室·册名 第X章 章名·件型（共N页）·本n/共M本　节号节名　第X页」——X=PAGE复杂域唯一自动数、N=部分总页数COM实测写死、禁NUMPAGES/fldSimple（存量拆域＝删begin/instrText/separate/end域组、留separate与end间缓存run）；节名由STYLEREF“节名锚”域抓取（节标题前置1pt白#FFFFFF非隐藏锚段；隐藏文字形态域不认）；WPS缓存风险→PDF逐页双断言「页眉/页脚节名＝该页所属节」
- [ ] 装订＝分本制：每部分一物理本、册级配页件随第一本、单本>400页沿件边界二次拆分、不另配封面；装订组合方案A整册/B件型抽订/C分层抽订；二轮粒度=模块/专题、强基=子线、大学=科
- [ ] 分层三卷派生：每章自定稿讲练件派生简单卷（学习本：全章讲部全量复制＋导航表）/中档卷/冲刺卷（进阶题本）——题号沿用不重编（四本一套号、断号合法）＋跳号就地说明（不属创作层不计题量）；三卷各自成部分独立页码、各一物理本；顺位=定稿且过样册门；不派生面=实验卷/衔接件/知识清单。部分封面每部分一张＋分层卷每卷一张（不计页、无页眉页脚、主题图TikZ图内零文字）；册目录页分层卷只列件级行；使用说明页增分层用法小节
- [ ] 知识清单件＝〔基〕/〔进〕标记（不挂底纹）＋每条【编注】句＋对比辨析（只向后比、每节≤2条）＋图例行两段制（两个独立段、禁手动w:br；使用说明页图例区同步两行）；讲部挖空＝灰底答案嵌入原位、禁「＿＿空位线＋灰底答案」双标记并列；绘图/补绘＝创作层⑥（模板库先建后用、差图触碰轮顺手、图内禁中文说明——标注仅字母数字、≤10张/单位/轮超限按§1处置）
- [ ] 字体：中文宋体、西文TNR（显式rFonts防主题继承）、公式Cambria Math不碰；PUA字符run跳过
- [ ] 标点：题号选项点全角「1．」「A．」，小问半角(1)；公式框内与数字紧邻保持半角
- [ ] 文字图片不进公式框；知识清单标记粒度=填空答案＋公式＋少量核心名词，叙述句不铺（＝w:shd C9C9C9灰底；w:bdr方框已废）
- [ ] core title=文件名去扩展名、作者清空；文件名/文内标题/core title三处一致随题量同步更新（分卷照§8分卷例外）
- [ ] 成品先过100题/80页卷上限（超限拆卷，§12），再量单文件：>50MB跑未引用媒体审计（rels＋正文双查），审计后>80MB自行拆件（禁压缩/降质/询问，§12）
- [ ] 大docx先工具/dump_docx.py --index 出题块索引、--slice 按元素区间定点读取，禁止无目的全量dump
- [ ] 题目台账机械列用工具/题目台账预填.py预填骨架、判定列亲自填（§3）
- [ ] 查重先工具/重复候选筛查.py预筛再亲算判定（禁照搬相似度）；恒等式计数用工具/恒等式核账.py、亲算库回填用工具/亲算库回填.py（判定亲自）；题块计数陷阱：讲块/大招讲义内部「N．」编号条目会被题号正则误判成题，题量一律按「题型标题后首个含【答案】的块」计数
- [ ] **删除必须精确列文件名逐个删，禁止宽泛批量删除**；逻辑检查亲自处理禁脚本代替；逐题亲算登记台账
- [ ] 任务前与每次推送前 git fetch 比对；远程领先先rebase复查；push -f 仅用户同意时用

## OMML 公式定论
- 标记方案＝浅灰底纹w:shd(clear,auto,C9C9C9＝22%灰，薄纸防透印)：文字run/m:r/结构ctrlPr的w:rPr内挂shd，分数整块覆盖、相邻块视觉连续；**w:bdr方框已废**（字符边框内边距机制固定0~1.3pt，w:space两链均无效，箭头/上标/分数高字形必然贴线压字）；转换工具＝工具/标记转灰底.py；验证=PDF两链fill块计数恒等＋灰度201（旧值A6A6A6=166、D9D9D9=217勿混用）＋框线0
- oMathPara缺省居中须插oMathParaPr jc left；转行内=lxml deepcopy oMath→insert→remove外壳（python-docx直接转会坏）；行内化安全全量可做
- lxml命名空间用Clark记法；文本线性化压扁m:d分隔符/上下标，判读前先查XML；PUA字符(Euclid Math Two)格式pass须跳过
- 公式框内中文分治：整段中文=误转回w:r；中文下标是变量符号不拆；图片可能嵌在m:r内（m:t为空），该m:r整体转普通run绝不能删
- 表格内OMML渲染不可靠→整段转Unicode文本后按标记方案加浅灰底纹w:shd（C9C9C9）；答案空位填公式优先从源deepcopy（克隆优于重建）
- 文本纠错遍历m:t逐个定位（`2<x≤9`拆5个m:t），勿按整串replace

## 构建与重编号坑
- 题块定位用「来源标签+原题号」或期望题号推进法；跨run题号（'2'+'．xxx'）先改数字run再补「．」；全角「．」无小数风险不需lookahead，半角「.」才要(?!\d)
- 跨卷重编号SEQ装配通法（分层三卷等通用）：SEQ映射按「题型标题→题号」实配＋装配后题号连续性断言——同卷双题组合跨卷易错位
- 选项合并按首段A+字母严格递增收集且紧邻【答案】；「；；」三形态必查；合并方向=后段并入前段末尾；带图段落不合并
- 删块先收集再统一删；cut后锚点必须在文档树内；跨包图片迁移双路重映射a:blip@r:embed与v:imagedata@r:id＋rels/media三查
- 改名脚本new==path则跳过；临时脚本勿起标准库名；源文件末题带body级sectPr必须过滤否则Word报损坏；footer时序=is_linked_to_previous=False须在body.append前
- 新插内容统一set_para_fmt+set_run_fmt；分号/新建run补sz（现行按归一制全件24半点）；构建输出名勿用split截断

## 校验与守恒
- 图片三查：media数==rels引用==正文drawing/pict引用，md5全集⊆源；题目图统计到下一题号行；只查media会漏"包内在、引用已删"
- 孤儿图引块级清点法（比±2段窗口准）：题块/讲块级清点「含『如图/图所示』且块内0图」即疑似缺图——回源核图后跨包补插；「图在2段外」批量误报自动消除
- 守恒对比段落级+单元格级；norm删分号剥题号统一全半角空白；hard_norm必须iter()单次遍历按文档序拼t/m（分开拼会乱序误报）；run碎片化用连续同类折叠比对
- 知识点覆盖：源全部知识点⊆成品并集；PDF校验PyMuPDF页数/A4/min x0≈43pt；文本提取有字体伪影，内容判断以XML/OCR为准；视觉裁剪200dpi放大确认
- 页脚域格式化后重验渲染；组卷网嵌套域可能不打印→删runs重建复杂域（fldChar+instrText，§7禁fldSimple）；奇偶页不同须重建footer+even_page_footer+first_page_footer
- 交错序列审计：成品后与源逐段比对全部含公式段的文字-公式顺序（normkey匹配+错位最少模板）；相邻公式不可合并计数；修复按源模板重建children双指针对齐；m:d定界符/括号/减号字形差线性化不可见，掉位判定须对照源交错模板
- 行内公式移位只有逐段读dump才暴露——文字完整公式齐全计数全过也会中招；审计是发现静默删改唯一手段

## PDFCreator管线增量
- （注册表旧径存档、现行主路径免全局配置，§14；旧径AutoSave模板+EnsureUniqueFilenames+OpenViewer=0+改后杀进程重启生效）；打印后立即杀保spool；6.3直接出.pdf用GS压缩；Word COM能开中文路径但VBS链路复制ASCII临时名；多件连打卡死、逐个来；多会话期间禁全局杀进程

## 其他技术坑
- Python `'' in s`恒True；qn()不支持v:前缀；脚注重序列化用保nsmap的lxml；修后Word COM实测打开
- 横幅图判定=仅讲块首元素+a:ext cy<100万EMU双条件；易错警示大图是教学内容保留；讲I实验组织标题原样保留；横幅处置按§5图像保真条款执行
- 混排源题块边界扩到下一真实题前再用结构标题截断；超纲判定必须开教材PDF逐级核对禁止凭记忆
- 结构标题挂靠该节/题型第一道保留题前；某节全删则删标题；标题插用现有段deepcopy改runs；addprevious连调正序/addnext逆序；重插后折叠双空行；body游离sectPr切多节须归位
- 裸排检查：组合第2题紧随第1题不加标题是合规；标题移动类修复后必须全量计数核对
- 同步盘三宗罪：清零loose对象（修复=浅克隆拷pack+idx→删空对象→rm index&&git reset→fsck全绿→repack）；删新落地成品（git status查D按路径checkout恢复，GitHub唯一可靠层）；静默重写成品质损（run碎裂sz全剥体积减半——比对document.xml后按路径checkout）
- 并行会话把未提交改动带上云：git show 对侧提交--stat确认即直接推送勿重复提交；refs/codex坏ref直接rm -rf .git/refs/codex
- 组卷网讲练结构=讲知识区+【例题】+【练习题】；冲刺卷0题合法；半章命名范围标签；后续章节内容整节删除capture排除sectPr；六docx块边界认全角「N．」起始且长度≥8；填空引擎空位在前答案在后回填；连续裸图成组由下一决定性元素定向；电动势=12.2超纲判据；任务C无源载体方案（旧命名成品作基线）；同包克隆免重映射；选项合并索引位移坑；reprefix跨run形态；半偏法讲块部分删除
- 教材PDF自带文本层PyMuPDF逐页提取txt可全文检索；生成型docx脚本防转义三原则（@frac/<<>>/分块cat）；从零生成按五项自检+postcheck收口
- 实验卷装配成熟管线（同包克隆+跨包双路重映射+题号重编+复制保真diff）；页脚重建zip手术模板（footer_fix.py已退役——由部分独立页码制＋册级连续页码.py取代，勿按件内自洽重建页脚；zip手术手法仍适用）；大commit推不上分块push
- 续跑接管先找工作区本轮子文件夹定位分界；题号正则只认全角「．」；三卷装配要点（游离sectPr归位/选项正序合并/emf-wmf兜底/空段清理公式感知）分见「构建与重编号坑」「选项合并」条；页脚旧成品须按现行规范重建
- drawing检测必须元素级find；oMath可为段级直接子元素；复合条目（N．名：正文）前插名行；块定义后全量缝隙排查（横幅图落块外即三处皆失）；修复轮体检范围自扩（同轮产物同病全查）；守恒审计LCP前缀法；dump线性化不识别sSubSup须实查oMath子元素
- 恢复基线取git HEAD非同步盘现行版；media重编号对账按md5；工作区副本遇占用先重读；Word COM用pywin32

- 工具选择序：文件操作专用工具优先shell；中文手术一律python一脚本完；grep仅字面量计数（通配长行卡死）；git bash；断言先于写盘提交
## 历轮条目（指针＋独有教训；§N均指公共规则）
- 2026-08-25 物理11/12章实测/落选清单转删除台账/第1章重审整册——已固化§2/§3/§5/§7/§9；教训：
- 交接/RESTORE清单须逐题复核（起点非终稿）：恢复题集合与删除台账＋提取块三方交叉核对；extract块区间会误含相邻题——逐块扫「块内多题号」＋相邻块重叠，修复=截尾到次题号前；块内嵌入题扫描必做
- 标签/克隆坑：run拆分标签正则只换一半（清后续纯数字run）；克隆模型段须iter()找w:t（findall直接子级找不到→克隆整段重复）；模型段选错→克隆后重写全部w:t
- 空行折叠图/公式感知：drawing-only段ptext=''被当空段删——is_empty_p查blip/imagedata/oMath/drawing/pict；终检三误报＝lineRule multiple与auto同值陷阱、表格空单元格被空行检查误计（只查body直接子级）、whitespace-only run非内容字号不计
- Word「文件可能已损坏」（跨包大量追加累积发作，与docPr重复/zip重名无关）→OpenAndRepair+SaveAs(FileFormat=16)；SaveAs2 FileFormat=17是PDF（误用成伪docx）；修后media同blob去重
- 子代理批量亲算：输出纯JSON数组＋dup_existing跨池查重；临时dump监督删除
- 判定先例：大招素材归属以文件夹为准；叉乘/极点极线/仿射按超纲③删讲部（有课标内解法保留改解析）；圆锥曲线准线等后续概念题面出现即超纲③；纯解三角形转投必修4第9章
- API：get_or_add_image(io.BytesIO(blob))[0]→rId；emf/wmf兜底Part(PackURI,content_type,blob,package)+relate_to；COM须Documents.Open大写O、PrintOut前ActivePrinter='PDFCreator'、只传Background；新建docx默认Letter须XML改A4；单页件导PDF用ExportAsFixedFormat(path,17)；git查中文路径`git -c core.quotepath=false ls-files`
- 衔接培优讲义双区结构：题目区典题无答案＋文末解答区；重复题干段（图与题干区MD5全同）去重、解析图保留；典题补【答案】【知识点】；四心文件答案内联
- 大招docx切块两坑：年份开头题号被(?!\d)误杀；漏检题藏讲区/相邻题块——全文扫描^\d+．与【典例交叉核对题块集
- 纠错器用「值+上下文」规则（first/last/after_minus/after_pm）别用节点序号；签名须唯一（「整理得」误命中前段）；√塞m:t文本（√6无上划线可接受）
- 方框废案扫描坑：按属性值分桶（显式关闭值混计误报）；iter全树≠findall直接子级、验证用原始正则计数
- 批量升级与脚本复用：体检扫描→批量手术（纠错→噪音剥离→标记转换→验证对账→开卷→PDF代表件）；模块级stdout重包被GC关buffer、模块状态存原始模板重建、lxml用copy.deepcopy；副本同步用「线性化字符Counter差=既定增量」验证
- 填空亲核按「框内run文本+段落全文」批量dump对照标准结论
- 修复操作坑：索引序号按body全子元素计（含表格）——段落列表索引自表格后全错位；重编号按注册题首元素集合做；提取块块尾带源结构标题——插入按^\d+(\.\d+)+跳过；疑似源错先查OMML结构级（结构化dump f/rad/d/SUP定位，见下eqArr条）；Word首段可能空段——标题改写按「讲练件（」定位；PyMuPDF数字汉字间插空格——匹配正则容忍空白；删除台账重生成＝行级手术
- 2026-08-26 公式掉位全库清剿/样册反馈改版＋讲部守恒全库复审/工具链标记切换——已固化§1/§2/§5/§6/§7/§9；独有教训：
- 题号底纹run拆分：「N．」常拆成数字run＋句点run——「前缀消费」跨run拆分＋题号序列门控（1..N递进区分题号与讲部条目）；单run匹配命中0
- 大树copy.deepcopy(doc)病态空转——卷生成改「根open_tag截取+逐元素tostring缓存+字符串拼装」；zipfile句柄close后再os.replace（未close=拒绝访问）；新落文件瞬时锁重试10次×6-8秒
- lxml坑：body.iter()产出Comment节点（tag非str）QName抛ValueError——跳过非str tag；行内数学必须包m:oMath外壳（裸m:f/m:r插段级=Word拒开且OpenAndRepair救不回）；独立块级公式判定＝段内除pPr/oMathPara外无内容且无图；并入=deepcopy oMath到前文字段end（跳空段、遇标题/表格停）；表格内oMathPara保留
- 页脚纯文本抽取会混入instrText内容——缓存校验regex须容忍
- 答案标签变体定式：【标准答案】【详细解析】替换为规范名；【解析】同行含其他标签＝答案位改名、否则改【详解】；无答案大招题在块内首个标签段前插「【答案】值　」（值亲算、mfrag构造）；双【知识点】同run合并
- 修复轮验证三件套：check_release/word_check/pdf_export（spool快照法）
- nav_split.py：贪心≤100题定卷数（4卷起节号区间）；卷起点上溯父节标题；导航表仅卷一
- 视觉模型对精细渲染的描述可能互相矛盾——此类判定走几何/像素测量（框线-墨迹间隙扫描法）不可目测
- XML细节：子层倒退检测特征＝全角（k≥2）行后紧跟全角（1）行且中间无题名行（【分析】/【详解】各自起算、半角小问不属违规）；跨包追加后rels须插在</Relationships>内（直加文件尾→Word报损坏），lxml良构校验＋开卷实测落盘前必过；克隆横幅段改docPr id防重复；wpg组合图形包着的图Word可能不渲染——判定须PDF渲染过目
- 2026-08-27 成品截图反馈/选必1整册多智能体轮——已固化§1/§2/§5（存量债见看板）；独有教训：
- docGrid行网格（仅document.xml sectPr）：type=lines不关则行距收益被15.6pt网格吞掉——须关；紧凑化五件套已收编工具/（五杠杆改版/短行合并/回退/零字符核验/COM页数/公共库.py）
- E类空白选项槽（三签名不触发）：选项内容整块丢失而题号与【答案】在位——选项行骨架短于同卷同类且附近无公式碎片，回源clone恢复；C类「图形顶替实体名」同族观察维度
- 单向阈值型规则在条件消失后无回退路径＝体系性缺陷模式；主观判断条款必须配分档定义与量化门
- 2026-08-27~28 整册修复轮——已固化§7/§11/§13；独有教训：
- m:eqArr线性化伪迹：m:d包裹eqArr＝左括号条件组——线性化判「粘连/掉位」必须OMML结构级复核定案，禁按dump直接改；判定缺陷必查OMML结构级＋w:drawing元素级双维（公式图片在位而扫描器只扫w:t＋oMath即漏）
- 「先内容后页码」熬到最后一件微修后才盖（中间轮盖章返工即作废）；PAGE域缓存不随sectPr start自动更新——册级连续页码.py flush_page_cache()刷为start实测值
- 零字符对账形态：逐件出「w:t/m:t流差异逐项对应登记清单」（差异字符数↔已登记修复项一一对应）；授权纠错以「全件唯一差异位＋坐标」作证
- 签名法永远追着缺陷构造走——词表外对象逃签，题干通读抽扫不可替代；扫描器增签建议已备案工具债
- 2026-08-29 回扫轮/成书形态回扫——已固化§4/§7；独有教训：
- 容器回写核验法：md5全变而内容无恙——核验一律XML内容级比对（逐部件SHA256/diff）勿依赖md5；sha256异常先定性（git blob恒等＋结构复核全绿＝容器重写、良性）
- 换行质量须加w:br（文本换行）全量计数；删w:br可致页数−1——须重走盖章与目录页码列
- 跨run分裂标签易漏计（脚本计数＜实测）——同类缺陷统一处置须跨件全量扫描
- 题块吞尾：「题号块起至下一题号块/标题」式讲部识别必配「题块尾部讲解群」扫描（末个【×】芯片起段后的独立图段/无公式讲解散文/标题式短行/(N)①起段），命中回源核对归属后标题化或搬移
- 图像判反必须PDF渲染过目＋源图md5对照，禁凭尺寸/位置族推断；判反＝互换处置＋原报告追加书面更正节（勿改写历史）
- 工作副本被误覆盖还原后必须重跑全部当轮修复项再收口（漏重跑＝未闭环＋数字失实）
- push「Everything up-to-date」≠远端已收到——git ls-remote核远端行或fetch比对HEAD后才可报完成
- 2026-08-30~31 四拍板执行轮＋三条错误教训——已固化§2③（N21）/§6/§10（N18）/§13；独有教训：
- 修复工具不得夹带职责外副作用——升级/复用工具时审计其全部既有语句与后继拍板兼容性
- 派发语中的装订序/结构序逐字对总控任务E原文；册目录页页码倒挂＝装订序二次校验
- 幂等重盖章法：页数零漂移时以「盖章前后全zip成员逐字节DIFF=0」为幂等铁证——语义级零改动看zip成员级、非容器md5（仅系重打包时间戳）
- 样式挂载：清单/衔接件无标记真教材节须亲判节层补挂；新建样式basedOn须指向该件真实Normal styleId（字面量悬空即样式链断）；契约缺口须干净基线全断言回归
- 空位三签名对行内公式图片盲区：段内oMath=0不必然缺式——先查段内w:drawing再定性（v3扫描器已计图件为公式对象）
- 源书图内数学错误先例：可对media图像像素级最小修复（多源互证定错→像素手术→纠错同步反查零扩散→document.xml零改动→登记）
- 【编注】禁未经查证的出处自称：「教材定义：」类须过教材PDF检索才可写、查无即删改并复评〔基〕/〔进〕；标记变更须重算恒等式并全库反查复制件
- 2026-09-01 版式改版回扫轮（欠账A）、十项改制拍板轮——已固化§2③/§7；独有教训：
- 工具三坑：lxml代理id()入豁免集、GC后地址回收致复核断言随机假失败（持活列表）；题号门控硬编码起=1不适配续卷（--qstart参数化）；工具ROOT硬编码旧路径致扫描空转假成功（相对__file__推导＋启动首行自查）
- 2026-09-01~02 A'改制轮（十项改制口径重排）——已固化§7；独有教训：
- OMML内显式w:sz剥除交docDefaults继承；oMath内常无显式sz（「ctrlPr 18/21」计数多系id()伪影/pPr误归——以m:oMath子树字节级实测为准）
- 多代理并行改同一工具文件必冲突（六类底纹计数.py两代理同修）——工具文件应互斥派发
- COM长任务失活的孤儿进程按「锁定文件属本工作区＋启动时刻=失活批次」归属判定后可清
- 2026-09-02 A''体系修订轮（双栏改制拍板20项落库）——已固化§2/§5/§6/§7/§11/§14/§15；独有教训：逐字写死的成品文案入规则前必须按版面宽度预检（61字图例行单行必折行等5笔用户点名）——已落库§2读者视角观感门＋§15固定文案先见实物＋43条全量复评
- 2026-09-02 弹窗分轨修订轮——固化§1两轨制；教训：机制废止类体检并扫同义措辞
- 2026-09-02 压缩轮——释出仅期望非配额、释不出实报不判返工；压缩主力＝删重抄公共规则段

- 2026-09-02 A''成品轮——已固化§7；独有教训：
- lxml新建Element须带nsmap（否则ns0:前缀、正则类工具全盲；混合件可文本级归一验xmlns同URI）；heredoc补丁链会把组引用写成字节进源码——补丁后必扫<0x20
- anchor→inline三坑：父是w:drawing非w:r、inline须包drawing外壳、子元素白名单分ns（wp四子＋a:graphic）、宿主空drawing/空段必清；头部节sectPr勿挂节名锚段；盖章域写值致临界件repaginate（收敛第2轮页数±1属预期）
- Word残留实例累积锁COM导出——清本会话时段winword后秒过；PDF超时按§14降级勿死等

- 2026-09-04 选必1⓪轮接管续跑——已固化§13「资格门先归因后顺延」＋K1臂执行类调用形态；教训：臂故障先归因三分（派发配置类/环境类/能力类）——配置类（qwen headless缺--yolo致shell/edit被deny、codex workspace-write沙箱令牌挡COM报0x80070520）修配置原地重试同臂即可复活，误判「臂不可用」直接顺延＝白烧臂（两起被用户点名）；复测/执行类调用形态以K1实证登记为准（qwen --yolo、codex danger-full-access、opencode权限件bash=allow/edit=deny）；任务书字母码须全册同一套（混用m_*.json码与自定义码致F3冲突）、批量配置相对路径按配置文件目录解析（裸件名配方全灭——绝对路径）、节号排序键用分量元组不用字典序（防2.10排2.2前）
- 2026-09-04Ⅳ 多脑制废止（只留codex CLI单脑）：派发目录dispatch0904\{kimi,qwen,codex,opencode}为中立目录防项目记忆泄入；codex投票read-only/执行danger-full-access（沙箱挡COM教训）；被停臂的COM实例随进程消失时先验PID差分清场
- 2026-09-04 选必1⓪轮子步11——故障先修纪律入附则（附则/故障先修纪律.md，FX-1..5，用户直令）；教训：PDFCreator自动出件全轮零命中、每件白等600s——根因＝常驻进程继承用户级GS_LIB（TinyTeX 10.07.1）与自带GS 10.05.1 gs_init错配死锁；净环境重启PDFCreator即修复（自动出件12.3s，~50倍）；固定耗时与对象规模无关＝兜底常态化的故障信号；spawn前清GS_LIB只护打印环节护不住常驻进程
- 2026-09-05 选必1⓪轮修订3回环——①页脚距边=下边距(850缇)时页脚整行压正文、每件白丢约1页容量：零占位判据=w:footer+页脚行高(≈266缇)≤下边距，选必1 C/H改567缇页数即回本(62→60/71→70)、总账406不变——例外机制入附则《页脚零占位例外》+册级连续页码.py footer_twips按件覆盖；②同步盘目录(高中数学同步)原件路径COM Open间歇卡死(零CPU、无锁文件、无MOTW、rename可)而字节相同的中立目录副本秒开——COM开卷类工具(盖章/探针)走镜像副本+回拷，逐页巡检管线先拷副本故免疫；③巡检必配PDF文本层越界字自动扫描：I1 p002数学斜体缺字形4处系扫描捕获、巡检目检漏检——凡TNR-Italic承载U+1D4xx即缺字形(真缺集)，U+2080系上下标TNR有字形不属缺陷；④盖章回拷会重打包docx→mtime晚于PDF失真：P3④「盖章晚于内容改动」改断言现行XML哈希==重盖前快照(sha1前12位逐member:document/settings/header/footer N.xml)——X1/X2/E/G实证字节零变动旧PDF持续有效，mtime强制仅施加于重导件；⑤题号随题型重构跨轮漂移(1.2.1.2-2→1.2.1.3-2)：跨轮引用题勘误须带题面特征签名定位(坐标串全件唯一命中)，禁按旧题号硬找
