# AI 自行经验积累

> 本文件记录历次任务中积累的可复用经验，执行新任务前请先阅读。按时间倒序追加，标注日期与任务。本文件是唯一允许记录经验的文件，经验禁止另建到其他任何文件或文件夹；与《新的数学物理单元同步提示词》冲突时一律以总控提示词为准并立即修正本文件；追加前全文查重，每轮收尾去重合并精简，全文不超过 800 行。

---

## 用户固定偏好·快速自查清单
- [ ] 原文件不删除、不移动、不备份，始终留在原位置；改动只在桌面副本上动手；成品（三卷＋知识清单）数学放 提示词/高中数学/高中数学同步、物理放 提示词/高中物理/高中物理同步
- [ ] 选题先与用户交流确认（三卷难度分配结果＋全部删除清单，同意后才成卷）
- [ ] 每题保留「题目后紧跟答案」与全部内容；新编号+来源标签可追溯
- [ ] 简单卷（讲部分+简单题；数学≤40题、物理≤20题）/ 中档卷 / 冲刺卷（只收特别难题，难题定义见总控3.2）
- [ ] 选项与不同内容间加「；」；短字段横向合并；题间空1行（无答案文件空2行）
- [ ] 页边距上下左右1.27cm；去分页/与下段同页/段中不分页；删多余空白
- [ ] 文件名序号=内标题序号；文件名与内标题含真实题数
- [ ] 视觉检查仅用 PDFCreator 导出 PDF；配置：桌面自动保存、文档标题命名、不弹框、不开查看器
- [ ] 所有内容必须左对齐：正文jc、页脚jc、块级公式m:jc（缺省居中！）、图片后文字换行、缩进清零
- [ ] 文字、图片等非公式内容一律不进公式框（文字归文字、公式归公式、图片归图片，表格/题号/空段/标注/下划线等各归其位）；数学符号和数学公式进公式框（Word自带微软公式OMML）；**任何内容一定不要丢失，任何转换/删除/清理操作后必须逐项核对，反复检查到零差异为止**
- [ ] 全文字号五号（每个w:r显式sz=szCs=21，公式m:r字号也归一、内容与Cambria Math字体不碰）；加粗仅主标题/结构标题/【标签】；pPr>rPr的段落标记字号也要归一（否则PDF残留12pt且行高变大）
- [ ] 字体：中文=宋体、西文/数字=Times New Roman（每个w:r显式rFonts，防继承主题字体）；纸张一律A4（直接写pgSz XML，单位twips，python-docx setter是EMU会改坏）
- [ ] 标点：题号/选项点全角「1．」「A．」；小问统一半角(1)；中文叙述标点全角（GB/T 15834）；公式框内与数字紧邻（坐标/区间/小数）保持半角；清理､；选择题空括号（　　）内全角空格保留
- [ ] core title=文件名去掉.docx（否则PDF名带.docx.pdf）；作者清空；文件名改动加简短说明
- [ ] **删除/清理必须精确列出目标文件名逐个删，禁止notlike等宽泛批量删除**（曾误删用户桌面7个无关PDF，6个靠微信FileStorage找回）
- [ ] 执行模式：**大循环套小循环**——先按逻辑顺序列任务清单，每个小任务=「完成→检查→发现问题→修复→再检查→直到该小任务零问题」的完整循环；全部小任务完成后整体检查，发现问题再列新清单修复复查，直到整体零问题
- [ ] 检查答案步骤排版与全部内容逻辑；实质性错误可改、纯格式可直接规范
- [ ] 中间产物删除、自己打开的窗口关闭（用户开的不动）、经验积累更新
- [ ] 可复用脚本只放 提示词/工具/（唯一允许代码的文件夹），优先复用已有脚本；一次性脚本放桌面用完删；经验文件引用工具写「工具/脚本名」完整路径
- [ ] 任务开始前和每次推送前都先 git fetch 比对：远程领先先拉取合并复查，再执行任务/再推送；禁止未比对强推（git push -f 仅在用户同意重置历史时用）

---

## 2026-08-15 全五号统一修订轮：6成品全面复查+格式统一（数学稳分47/冲刺35、物理稳分37/冲刺12、物理知识清单25、数学概况12）

### 用户新确认的排版规范（此后沿用）
- 全五号+加粗层级、宋体/TNR、A4、1.25行距（line=300 auto已统一）、标点方案（见清单）、core title去.docx、作者清空；数学第3章概况文件补「第3章」前缀（每文件前面要有第几章）。
- 修订引擎 dsh_tools/revise.py（可复用，路径argv传入）：stream_edit字符流规则 + fix_runs（rFonts/sz按OOXML rPr顺序插入ins_ordered）+ fix_pmark + pgSz纸张 + core属性。复核脚本：mega_check（排版全套）、run_check（u/b）、final_scan（标点残留）、szfont_check、pdf_batch/pdf_uline2。

### 本轮踩坑（务必记住）
1. **Python '' in 任意字符串 恒True**：elif nxt in PUNC_NEXT 在nxt==''也成立→段尾句点被整批误删。任何 in 判断前先处理空串。
2. **小问转换守卫用错坐标系**：用流位置(m.start(),m.end())和(ti,ci)键比较，撞同值键会误跳过（冲刺卷[70]（2）漏转）→守卫用目标键(seg[pos][1],seg[pos][2])。
3. **PDF残留12pt的真相=段落标记字号**：正文所有w:r都21但PDF还有12pt加粗span→分区标题段pPr>rPr的sz=24（段落标记高度影响行高）→fix_pmark归一。
4. **下划线在PDF渲染成细矩形**（get_drawings items 're'，height<2.5,width>8），不是线段('l')——验证下划线数用细矩形计数；docx层u数不变=无损。
5. **公式线性化**：M+'r'（无花括号）永不匹配——用qn('m:r')/tagof()；m:t必须按文档顺序提取，w:t先拼m:t后拼会把公式甩到段尾，读起来像"文本丢失"，曾据此误判Q19答案错误（实际题干问的是√(m²+n²)的范围，3±2√2正确）——下结论前先看doc-order原文。
6. **误删用户文件（重大教训）**：清理桌面PDF用宽泛删除，误删7个无关PDF（教师职称/会议期刊目录）。恢复：6个从微信FileStorage同名找回；「绩效考核通知」「CSCD(2023-2024)」磁盘无副本、回收站无记录、无管理员权限无法跑数据恢复→需用户从聊天记录重下。**删除只准逐个列文件名**。
7. run_code的JS数组join写Python偶发"Expected ','"解析失败（flaky，重试即可）；最稳=Python源全ASCII（中文用chr()/\uXXXX）+路径经pwsh argv传入；run_code内不能用import（用pwsh跑python）。
8. PDFCreator默认配置（HKCU\Software\pdfforge\PDFCreator\Settings\ConversionProfiles\0）：AutoSave.Enabled=True、FileNameTemplate=<Title>、TargetDirectory=<Desktop>、SkipPrintDialog=True、ShowProgress=False、OpenViewer.Enabled=False、Preview.Enabled=False（本轮关掉，否则弹预览窗）、ExistingFileBehaviour=EnsureUniqueFilenames；同名输出会加_2，重打前先删旧PDF。
9. 6文件复查全部通过：mega NONE、题号连续、两卷查重NONE、u/b完好（清单80处、概况57处全thick+b）、PDF全A4全五号（稳分17/冲刺21/物稳33/物冲14/清单5/概况5页）、下划线细矩形数与旧版一致。
10. 覆盖核查口径：源文件知识点集合-两卷并集≠缺失——被删题（同知识点重复题）的知识点由保留题实质覆盖（名称可能不同），逐题映射由前轮确认；本轮只复核「两卷来源交集=0」。

---

## 2026-08-15 检查4个成品卷+2个知识清单文件（B版标题+结构标题挂靠+分区加粗）

### 检查结论
- 数学第2章稳分卷47题/冲刺卷35题：选题（两卷82题无重复）、题号连续、来源标签、与源文件保真（低相似度6处均为合理规范化/纠错：mod10原5答案改『证明见解析』+详解补证明、mod13原37详解③补整点无穷证明等）、页边距720、行距标题300/正文240、页脚『试卷第N页』、无分页控制、空行分布、答案标签、选项分号、OMML公式——全过。
- 发现2问题→用户确认后修复：①结构标题跳号（模块2缺（三）（四）（五）、模块3缺（二）（五）（七）、模块5缺（一）（二）（四）（五）等）；②标题体系按人教B版口径改『平面解析几何·直线及其方程、圆及其方程』。
- 物理第11章稳分卷37/冲刺卷12：61=37+12+12集合正确、讲部分保留、分区标题无空区、答案标签、来源标签（例题/练习题/易错题小练习｜原N题）——全过；4题缺【知识点】与源文件一致（源文件本来就无），不算错；分区标题两卷粗细不一致→统一加粗（用户确认）。
- 物理知识清单（规范完善2）（25题）：80处u全部thick+b、无u缺b（b无u仅标题段）、必背内容划线完整——通过。
- 数学圆锥曲线知识清单（规范完善2）（12题）：59处+表格全部thick+b、表格公式上标正确、内容完整——通过。

### 本次新踩坑/经验
1. **结构标题挂靠修复模式**：跳号标题处理——①原位挂靠（插入位置与源顺序一致）不加说明；②非原位挂靠加『（自模块N…之间挂靠于此）』；③知识点题目在另一卷的加说明行『※ 题型X：题见另一卷题号』。插入实现：deepcopy现有标题段XML→清runs→makeelement造新run（bold+szCs21）；说明行用正文段模板（不粗体line240）。anchor._p.addprevious连续调用为正序；after时连续addnext会逆序（后插的在前）——插入后必须复查顺序。
2. **章节号按教材口径**：人教B版选必1目录：第一章空间向量与立体几何；第二章平面解析几何（2.1坐标法/2.2直线及其方程/2.3圆及其方程/2.4曲线与方程/2.5-2.7椭圆双曲线抛物线/2.8直线与圆锥曲线）。人教A版『第2章直线与圆的方程』→B版口径标题『平面解析几何·直线及其方程、圆及其方程』（用户确认）。物理人教2019必修3：第十一章电路及其应用（电源和电流/导体的电阻/实验导体电阻率的测量/串联并联/实验练习使用多用电表）——知识清单八节与教材节次对应。
3. **run_code写Python脚本的JS解析坑**：code参数用String.raw模板或数组join写中文Python时，偶发『Unterminated template』『A numeric separator is only allowed between two digits』『Expected ','』解析错误（原因不明）。最稳：Python脚本按行做成JS字符串数组join('\n')，一次写一段，失败就拆分重试。
4. **表格公式误判**：成品表格『c²=a2+b2』线性化像丢上标，实为vertAlign=superscript的run（渲染正确）——linear_text拼接假象，判读前必须先dump单元格XML；段内m:sSup的『F』『1』是基+下标结构。
5. **PDF文本提取**：标题数字与汉字间会插空格（『第2 章』），PDF检查字符串要容错（查子串）。
6. 挂靠后验证清单：标题序列无跳号（每模块编号连续或缺失处有※说明行）、每个标题下直接接题、空行分布不变、题数/图片数/media数与修改前一致、Word COM能打开、PDFCreator打印验证。
### 追加：『所有内容必须左对齐』全面处理
- 检查范围（6个文件）：数学稳分卷/冲刺卷、物理稳分卷/冲刺卷、物理知识清单、数学圆锥曲线知识清单。除 w:pPr/jc 外还要查三处易漏：
1. **块级公式 m:oMathPara 的 m:jc 缺省=居中**（OMML规范）！无 oMathParaPr 的独立公式段默认居中（如数学知识清单『11．抛物线标准方程的几种形式』整段在 m:t 里、Word COM 读 Alignment=0 却是居中渲染——极隐蔽）。修复：遍历所有 m:oMathPara（正文+表格单元格），无 ParaPr 则插入 `<m:oMathParaPr><m:jc m:val="left"/></m:oMathParaPr>`（insert(0)），有则补/改 jc=left。
2. **页脚段落 jc=center**（试卷第N页居中）——遍历 section 的 footer/first_page_footer/even_page_footer 段落改 left。
3. **『图片在段首+标题文字同行』的讲核心条目**（文字被图挤到图右侧 x0=319）：在图片run后第一个文字run的 w:t 前插入 `<w:br/>`，文字换行到图下顶格。判定：段首（除pPr）run 含 w:drawing 且后续有非空文字run。
4. 段落 w:ind 缩进也要清零（本次全0，检查确认即可）。
- **验证方法**：jc_check（w:jc分布）+ mjc_check2（oMathPara jc状态含noParaPr）+ center_check（fitz块 mid≈页面中心297.5 且 x0>60 的疑点）。行内公式渲染块（私有区字符如 '\x01''∴'）和表格单元格内公式（x0=列左边界）是正常块，非居中。
- **PDFCreator同名文件自动加 _2/_3**：多轮打印后桌面易混淆——检查最新版先看时间戳，最后清理旧PDF重打得干净文件名。
- **Word PrintOut 与 ExportAsFixedFormat 渲染一致**（本怀疑过PDFCreator，实测Word导出同样结果——公式默认居中是docx本身的OMML行为）。
### 追加：文字与公式分离（用户规则）
- **用户确认规则**：文字不要放进公式框；数学符号和数学公式放进公式框，公式形式用Word自带的微软公式（OMML）。
- **历史缺陷**：数学知识清单（规范完善2）曾把整段标题/标签/叙述文字误转成 oMathPara（『11．抛物线标准方程的几种形式』『图形』『焦点』『p的几何意义』等+必背公式片段），274处『无数学结构』的oMathPara——文字进了公式框，字体变公式体、渲染居中、编辑不便。
- **修复**：无数学结构（m:f/sSup/sSub/rad/d/nary等）的 oMathPara 整段转回 w:r 文本run：m:r→w:r，m:sty p/b/i/bi→w:b/w:i，w:rPr 的 w:u(thick)/w:b 保留——转换后583处下划线全部保留且仍加粗，变量斜体保留，PDF零居中块。**有数学结构的oMathPara（真分数/上下标）保留公式框不动**。
- **判定经验**：数学符号/变量（单个字母A、B、ρ、Ω、ΔABC等）在公式框里是正常组卷网格式，**不转**；整段中文文字在公式框里才是误转，转回文本。
- **行内公式里的中文也要查**：块级oMathPara转完还要扫行内m:oMath——『4．直线y=kx+m』（序号文字混在公式里）、『a=−1或2』（连接词『或』混在公式里）也要拆出：连续中文m:r段→w:r文本（前面紧邻的纯数字/标点序号段并入文字），其余m:r段→保留m:oMath。**中文下标（R测、U总、I额、R内、R左/右等）是变量符号的一部分，保留公式框不拆**。
- **扫描正则**：`re.findall(r'[\u4e00-\u9fff]+', m:t全文)` 找公式框内中文，再人工判断是『下标符号』还是『误入文字』。
- **重大教训：图片可能嵌在公式run（m:r）里！** 组卷网源文件的表格图形单元格＝w:p 内 m:oMathPara＞m:oMath＞m:r（w:rPr 后有 w:drawing、m:t 为空或空白）。公式转文本时若把『m:t 为空的 m:r』跳过再整体删除 oMathPara，**图片随公式框一起被删**（**注意别把图弄没了**）（16张图全丢、media文件还在但引用没了）。**转换前必须检查：m:r 含 w:drawing/w:pict 时，把该 m:r 整体转成普通 w:r（deepcopy 子元素含图）再保留，绝不能删除**。转换后必做『图片三查』：media数、drawing/pict引用数、逐表图形单元格核对——与备份版一一对应。
- **表头标签重复**（源文件固有）：抛物线性质表第2行第1列重复『标准方程』（该行是p的几何意义说明行）→ 清空该单元格。
- **块级公式（oMathPara）强制独立成行**：组卷网源文件的答案/详解公式多为块级公式——即使插在文字run之间也会换行显示（『【答案】（1）［换行］公式』、选项『A．［换行］公式』）。修复：**混合段（段内有文字）里的 oMathPara 转行内 oMath**（把 oMathPara 内部的 m:oMath 逐个移出替换掉 Para 外壳）；**纯公式段（段内无文字，如详解推导步骤）保留块级**（高考标准：每步推导一行）。4个卷共转1807个公式，答案行/选项行恢复紧凑同行。
- **改名脚本必须校验 replace 是否命中**：`path.replace('（左对齐）.docx', ...)` 对『（分区标题加粗、左对齐）.docx』不匹配（前有『加粗、』），new==path 时 doc.save(new) 后 os.remove(path) 会把原文件删掉！**每次 replace 后必须判断 new==path 则跳过删除，改名后立即列目录确认文件存在**（本次误删物理稳分卷+数学两卷，靠备份恢复重做）。

- **答案步骤排版规则（用户确认）**：①公式行内化+短步骤横排、逻辑不丢、左对齐竖着写；②数学卷答案用数学符号（因为→∵、所以→∴），物理卷除公式符号外用汉字（因为/所以）；③段前段后0行（w:spacing before/after=0）、多倍行距1.25倍（w:line=300 lineRule=auto）。
- **步骤合并逻辑（务必照此顺序）**：先 merge_formula=纯公式段（无w:t有公式）合并到**前一个非空非粗体段**（前段允许『【详解】（1）…』这种带文字段，只排除整段≤10字的纯标签段如『【详解】』）；再 merge_short=相邻短段合并加『；』（排除【标签/题号行/选项行/粗体标题/『（1）』小问段，合并后≤70字）。**注意合并方向：文字在前公式在后**——若把『（1）』开头段错误排除，公式段会与后面的文字段反向合并成『公式；文字』乱序。效果：物理冲刺卷题1详解6段→4段（【答案】公式同行、推导每步文字+公式一行、短步骤；横排）。
- **调试坑：linear_text 拼 m:t 必须用 MC='{'+M+'}' 花括号**——`el.tag == M+'t'`（无花括号）永远 False，dump 显示公式『丢失』实为提取 bug；判断内容先看 XML 再下结论。

- **『任何内容一定不要丢失，反复检查』落地方法**：①图片三查=media数==rels引用数==文档drawing/pict引用数，且全部media的md5⊆源文件md5集合（零新增零损坏）；②文本零丢失=**段落级**逐段对比（doc.paragraphs 的 w:t+m:t 序列）+**单元格级**逐格对比（图数+文本签名），不能只用全局文本diff——表格段落与正文拼接顺序、合并单元格去重会让全局diff误报『文本丢失』（实测 ratio=0.9931 的diff全是误报，段落级/单元格级全一致）；③差异项必须逐条人工确认属于预期修改（如『或』『4．直线』换位、标题加序号），非预期的一律修复。

---

---
## 2026-08-16 追加：第11章稳分卷/冲刺卷成品检查（发现并修复2处残留，选题未变）

### 任务
- 输入：已生成的稳分卷37题+冲刺卷12题（选题方案此前已确认：冲刺=原7/27/35/42/47/48/49/52/56/59/60/61，删除=原2/5/6/21/26/33/37/43/46/51/55/58），本次只做全面检查+修复2处残留。

### 检查方法沉淀（可复用）
1. **题目块分割不要用『lookahead找【答案】』启发式**——知识讲解条目（『1．电源的作用』）会误判为题。用论文里的【来源：模块｜原N题】标签直接映射原题块，归一化（去空白）比对即可；论文未匹配条目应只剩：标题行、来源标签题号行、合并的答案行（答案+难度+知识点）、合并的选项行（；分隔）、已修复的笔误。
2. **孤儿表格检查（本次核心收获）**：列出文档全部 w:tbl 及『前一段/后一段』上下文核对归属。被删题6（电动汽车铭牌）的6×2表格残留在【练习题】标记与题4之间——因表格文本与原件完全一致，普通『未匹配』检查抓不到，必须做归属检查。修复：删表格+其后多余空段（保证标题后直接接题）。
3. **文件开头多余空段检查**：首段应为标题段；稳分卷标题前有2个空段（冲刺卷没有）。**正文空段判定用 is_blank（含 drawing/pict/oMath 检查）**——纯公式段/图片段不算空。
4. **图片三查**：①媒体文件数==rels图片引用数（无孤儿无断链）；②论文所有 word/media 的 md5 ⊆ 原文件 md5 集合（无新增/损坏）；③逐题图数核对=用题干前30字匹配原题块，统计从题号行到**下一题号行**的全部图片（答案/详解区的图算在题目内，不能停在【答案】行——否则误报一片）。
5. **知识点覆盖**：原文件全部【知识点】集合 ⊆ 两卷并集（零缺失）；删除题的知识点必须被保留题覆盖（同知识点+同解法判定）。
6. **脚本坑**：body.iterchildren() 会把末尾 w:sectPr 当表格处理——必须显式判断 child.tag==qn('w:tbl')（python-docx 不同版本行为有差异，可能先能跑通后报 CT_SectPr 无 tr_lst）。
7. **PDF验证**：Word COM 逐个打印（一次多个会卡死），桌面轮询新PDF；PyMuPDF 检查：标题、【来源】标签数=题数、无孤儿内容、文本边界≈36pt（页脚/公式墨迹越界属正常）；PDF文本中『1.5×103』等行首数字会污染题号正则，题号连续性以 docx 为准。
8. 修复后：稳分卷33页PDF、冲刺卷15页PDF；修复前版本备份在「原文件备份_修复孤儿表格前_20260816」。

---

## 2026-08-16 追加：下划线不明显 → 答案加粗 + 粗下划线（thick）
- 用户反馈『下划线似乎不太明显』，提供4个方案（ask_user_question）→ 用户选择**答案加粗 + 粗下划线（Recommended）**。
- 实现：对文档中所有带 w:u 的 run（w:r 和 m:r 都要）做两件事：①rPr 中追加 <w:b/>；②w:u 的 val 从 single 改为 thick。
- 物理文件155处、数学文件585处全部转换；用 Word COM 打开正常。
- **像素验证加粗效果**：渲染 clip 区域放大20x → PIL 灰度 → 统计每行 >100px 连续黑色段 → 按连续行分组测线厚度：物理『正极』答案下划线厚度 0.5pt→1.2pt（约2倍），数学标准方程公式下划线 1.35pt 且 126.6pt 长线覆盖整个公式——粗下划线渲染确认。
- 注意：w:u val='thick' 在 Word 渲染中确实是粗线（PDFCreator 打印也正常）；如还要更明显可考虑红色 uColor（w:u color=FF0000）但高考标准格式一般不用彩色。

---

## 2026-08-16 任务：第11章物理知识清单 + 圆锥曲线章节概况 检查完善（两个文件）

### 任务模式
- 输入两个已填答案的文件：物理《第11章电路及其应用知识清单（题目+答案）（完成）（共25题）》和数学《1 章节整体概况-圆锥曲线的方程（共12题）题目填入答案》。用户要求全面检查：答案下划线保留、**高考必背内容全部加下划线**、排版紧凑（短字段横向合并、按长度动态合并）、分号规范、core title 与文件名一致、页边距720、行距240、题间1空行、PDFCreator验证、备份原文件、改文件名加说明。
- 处理顺序：一个文件彻底完成再处理下一个；每个文件先 pandoc 转 md 分析→python-docx 检查结构→修改→验证→PDF 打印→像素级下划线验证。

### 物理文件修改清单（第11章知识清单）
1. **core title 修正**：源文件是『…（共25题）（共25题）』重复 + 与文件名不一致 → 改为与文件名一致（PDFCreator按文档标题命名）。
2. **内标题**『（知识点，题目）』→『（知识点，题目+答案）』（与文件名呼应；注意『题目）』是独立run，要匹配 '题目）' 精确替换）。
3. **必背内容补下划线**：4题公式 I=q/t、11(2)公式 R=ρL/S（m:r 加 w:u——单独数学元素时渲染可靠）；12(2)①『电阻温度计』、②『减小』『热敏电阻』（原文件②没挖空但属必背结论，与①③④对称补划）；5题换算关系 1A＝10³mA＝10⁶μA 整体（含1A＝、mA＝、μA框架）、10题 1kΩ=10³Ω；1MΩ=10⁶Ω 整体（含kΩ、MΩ单位名）；**拆run时用 deepcopy 三段式（前段/答案段u/后段）**。
4. **表格行3列2并联电阻公式**：原为 OMML（m:f 分数+m:r+），m:r 的『＋』『＋⋯＋』下划线被 Word 抑制不渲染（经验印证）→ **整段公式转为纯文本 w:r（『1/R1+1/R2+⋯+1/Rn』，R 斜体、下标用 vertAlign）后下划线全部可靠渲染**。转文本时锚点用『，即』run，追加『1/R＝』文本（不要重复『即』！本次踩坑：先写成『，即1/R＝』导致『即，即』重复）。
5. 合并 28+29 段（10题单位+换算合并为一段，中间加『；』）；合并 62+63 段（16题三参量合并）；删除 18题号后的多余空段（标题后直接接题）；表格行距 300→240；90段『（1）测量直流电压』补『：』（与其他子标题统一）。

### 数学文件修改清单（圆锥曲线章节概况）
1. core title 与文件名一致；0标题后加空段、图后『三、单元知识梳理』前加空段（标题前1空行规则）。
2. **表格中带下划线的 OMML 公式全部转 Unicode 文本**（m:r 下划线不可靠）：标准方程 x²/a²+y²/b²=1(a>b>0)、焦距 |F₁F₂|=2c、离心率 e=c/a(0<e<1)、范围、Δ>0/Δ=0/Δ<0、渐近线、顶点 A₁A₂ 等 32 处 → 用递归 OMML→文本转换器（m:r→m:t、m:sSup→基+Unicode上标²³、m:sSub→基+Unicode下标₁₂、m:f→a/b、m:d→|内容|、m:rad→√内容，**跳过 *Pr**）。
3. 转换后 split_italic 拆段（拉丁字母斜体+u，其余仅u）；**c²=、F₁、B₁B₂ 等 m:r 也要处理**——统一『给单元格所有 w:r+m:r 加 u』再重新跑转换器（先加u→转文本两步走）。
4. 表6范围英文逗号','→中文'，'（与列1、3一致）。
5. 段15集合定义 P={M|||MF₁|-|MF₂||=2a,0<2a<|F₁F₂|}：转文本后检查多余空格（『₂|| =2』→『₂||=2』，原 m:t 自带空格）。

### 本轮新踩坑
1. **m:t 父元素判断**：inspect 脚本里 M+'r' 忘记加花括号（'http://…math'+'r'）→ 永远 False，所有元素被误判为 w:r——正确写法 MC+'r' 其中 MC='{'+M+'}'。
2. **『c²=』这类题干公式在表格里是 oMath 的 m:r**（不是 w:r），add_u_wr 找不到 → 要遍历 m:r 用 add_u_mr；之后必须重新跑『带u的oMath→文本』转换器（分两遍：先加u，再转文本）。
3. **PDF 文本提取公式是私有区字形乱码**（⁵⁶⁷ 等）——不是内容丢失，OMML 渲染的字形映射；验证公式用像素级 get_drawings/渲染图分析（长水平线=下划线），不用文本比对。
4. **像素验证下划线**：渲染 clip 区域（Matrix 放大12-16x）→ PIL 灰度 → 逐行找连续黑色段（>25-40px 视缩放）→ 转回 PDF 坐标。分数线（m:f 横线）与下划线区分：分数线在分子分母中间，下划线在字符下方。**文本形式公式的下划线渲染成整条长线（如 x²/a²+y²/b²=1 整行 126pt 长线），OMML m:r 只有零碎短线**。
5. 数学表格『焦点 F₁(-c,0)』中 F₁ 是 m:r 无u、(-c,0) 是 w:r 有u（混合结构）——补 F₁ 时按元素类型分别处理。
6. PDFCreator 同名文件自动加 _2（EnsureUniqueFilenames）——清理旧 PDF 后重打可得干净文件名；物理文件最终重打印了3次（修改→删除→重打）。

### 校验清单（两文件全过）
core title=文件名、内标题序号/题数=文件名、页边距720×4、行距240（含表格）、无keepNext/pageBreakBefore/keepLines、无连续空段、题间1空行（标题后直接接题）、题号连续（物理主序1-18+子编号=25条、数学1-12）、必背公式全下划线（PDF像素验证）、Word COM 打开正常、图片OCR无隐藏条件、备份文件夹、原文件保留、中间文件已删。

---
## 2026-08-16 任务：第11章电路及其应用讲和练拆分（61题→稳分37+冲刺12+删除12）

### 选题方案（用户确认）
- 61题全部通读后：冲刺12题（原7氢原子等效电流/27管线中空/35组装多用电表/42多用电表综合/47伏安法+黑盒/48双安法/49双伏法+改装/52 LED/56伏阻法/59电桥测长/60量角器电桥/61双刀双掷电桥0.4压轴）；删除12题（原2/5/6/21/26/33/37/43/46/51/55/58，均为同知识点+同解法）；稳分37题。
- 结构标题：原文件为【例题】【练习题】【易错题小练习】三区；冲刺卷按题目所属模块在分区变化处插标题（练习题3题→例题1题→练习题1→例题2→练习题2→例题1→练习题2）。
- **空区标记处理**：某分区内题目全部被删时（如题48/49所在的【例题】区），该区标记段也要删除，否则出现无题空标题。

### 构建关键坑（本次踩坑，务必记住）
1. **短选项合并误收详解行（严重）**：cands收集『^[A-F][．.]开头且≤20字』段，会误收【详解】里的『B．当导体两端电压为1V时，R2的电阻为』行——当真实选项B>20字未进cands时，A选项与详解B行错误配对合并（稳分卷题11发生，表现为：选项A段多出『；B．…为』且详解缺B行、原选项B仍在）。**修复：合并只收集【答案】段之前的选项区**；构建后用扫描验证（合并段『；』后字母与下一段相同=误合并）。
2. **跨run双分号『；；』**：短选项合并时a尾append『；』run，若a原文末尾已有『；』（器材清单行尾分号），形成『；』run+『；』开头的run——修复：遍历段内w:t序列，前t以『；』结尾且后t以『；』开头则删后t开头『；』。器材清单行（A.电池组E…；）也会被短选项合并逻辑误合并（段尾分号不拦截）——合并条件应排除段尾『；』的段。
3. **get_or_add_image 返回元组**：(rId, image)，需取[0]。
4. **qn()不支持'v:'前缀**：docx的nsmap无v前缀，用完整URI '{urn:schemas-microsoft-com:vml}imagedata'。
5. **题块边界人工修正**：题56、58的块尾包含『无编号讲核心』（『（1）操作：如图甲所示…』图段、『（2）原理…』），自动边界（下一分区标记前）会误删讲核心——需人工核对被删题块的块end，硬编码修正。
6. **media清理后python-docx保存会保留孤儿**：先清理media再python-docx保存会恢复未引用part？——本次验证0孤儿（python-docx 1.2.0保存只写rels引用part），但保险起见：**media清理放最后一步**。
7. **PDF文本提取差异**：带引号字母『A』『B』『C』『D』在fitz提取时可能映射为『5』『6』『7』『8』（字体映射）——docx内容正确即可，不要据此改docx。

### 源文件笔误修复清单（第11章）
- 『根据电阻定律可动』→『可得』；『滑动变阻器应选最大最值较小』→『最大阻值』；『用故选填』→『故选填』；『将电阻挡，由低档』→『将电阻挡由低档』（删逗号）；『5.899#5.900』→『5.899##5.900』（统一##分隔）；题38详解『指针偏角不会超过13』是1/3公式（勿改）；题36(2)选项C/D内容错位（D=电流表分压、C=电压表分流，按答案BD交换为C=电流表分压、D=电压表分流）；讲核心『ρ＝RxS/l＝πd²U/4u』分母『u』→『Il』（与题39答案4Il一致）；题61『测量电阻Rx，的阻值』删逗号；题1详解『A C．』→『A、C．』。
- **核对公式用源文件XML**：fulltext线性化『0.120.314』实为『0.1²/0.314』（m:sSup+分式，结构正确勿改）。

### OCR检查（本章）
- 讲核心横幅『（讲核心）一、电源的理解』~『十七、电桥法测电阻』共17个（横幅图文字=小节标题），稳分卷全部保留。
- 易错警示图2个（电流定义q为绝对值之和；伏安特性曲线切线斜率≠电阻），与对应题目内容一致。
- 题目图（电池铭牌3.8V/3000mAh、IM-mA图像、LED图、1/U-R图、电阻箱图）均与题干信息一致，无隐藏条件。

---
## 2026-08-15 任务：第2章直线与圆的方程稳分卷/冲刺卷检查修复（第二遍：补多选题+格式修复）

### 本次检查范围与结论
- 成品：稳分卷（45→47题，补模块14多选题2题）、冲刺卷35题（修订）。检查项全部通过：题号连续、80个来源标签与源文件核对一致、两卷无重复、结构标题完整、空行规则、页边距1.27cm、页脚域、无分页控制、行距统一、数学逻辑抽查正确、图片OCR无隐藏条件。
- **选题确认流程**：检查后向用户报告4处覆盖空缺（模块14多选题区、模块2题型二、模块5平行线距离、模块6点与圆位置），用户只同意补**模块14多选题题10+题11**（其余3处不补，尊重决定，间接覆盖已有）。
- 补入的题10/11需做：题号行改『44．【来源：模块14 章节综合测试｜原题10】』格式、选项加分号（A．2；B．−2；C．√6；D．−√6）、【解析】→【分析】、pPr统一（去shd/jc、line=240、snapToGrid=0）、块间补空段。

### 本轮踩坑（务必记住）
1. **来源标签格式是「原题N」（原+题+数字），正则别写反**：`原题(\d+)】` 才对；写 `原(\d+)题` 永远匹配不上。
2. **ps1 脚本必须 UTF-8 BOM**（PowerShell 5.1 按 ANSI 读，中文路径全乱、Word报『文档损坏』假象——实为路径乱码打不开文件）。
3. **行内公式是 m:oMath（不是 m:oMathPara）**：提取段内文本/插入分号时 el_text 必须同时处理 m:oMath 和 m:oMathPara，否则公式文本为空、前缀匹配永远失败（题29分号3次插入都静默失败就是这个原因——**必须检查 insert 函数返回值**）。
4. **题号行第一个 run 是纯数字（『44』）**，不含句号；改题号用 `re.match(r'^%d$', tt.text)` 精确匹配；用 `^(\d{1,3})([．])` 会因缺句号匹配失败（fix3 用 `^(\d{1,3})` 又误伤题干中其它数字 run，把题9的『9』『4』『15』全改了）。
5. **按来源定位题号时 key 必须带模块上下文**：`'原题15】' in t` 会同时命中模块4的题15（稳分卷题9）——用 `'模块14 章节综合测试｜原题15】'` 完整串。
6. **重编号要在插入新题之前做**（新题 retag 为44/45后，renum(44→46) 会先命中新题）；或插入后按『模块名｜原题N』定位修改。
7. **extract_block 跳过源文件空段 → 题块之间会缺空行**（题44与45之间、题45与『三、填空题』标题之间）——插入序列里手动补 make_blank()；插入后必须重扫空行分布（题号前：标题后0空段、题间1空段）。
8. **插入 anchor 前已有空段时勿再插**（『二、多选题』标题前出现双空段）——插入前先看 anchor 前一段是否为空。
9. **多选题区易被遗漏**：章节综合测试的『二、多选题』区4题全未选=高考题型空缺，检查时必须核对每个题型分区都有题。
10. 行距统一规则落地：标题段（（一）/题型N/N、开头）line=300，正文+空段 line=240；源文件是360（1.5倍），成品统一更省纸（稳分卷18页→15页）。
11. 修复流程教训：**一个 bug 修完立即验证再进下一步**（题号错序、前缀丢失、双空段、缺空段都是构建脚本的累积 bug 链）；构建脚本重跑前从备份重建副本，不要在原副本上反复打补丁。

### 验证清单（本轮全过）
- 题号连续（1-47/1-35）、来源标签80个与源文件md核对相似度0.7-1.0、两卷零重复、选择题选项分号全检（跨段也检）、空行分布、行距分布（300标题/240正文）、页边距720twips、页脚『试卷第N页，共N页』、PDF文本含新题、OCR、Word COM 打开+打印成功。

---

## 2026-08-15 追加：第10章稳分卷/冲刺卷终检修复（补讲部分缺失+断图）
- **任务**：稳分卷42题+冲刺卷18题已生成，本次只做全面检查。发现并修复4类问题（输出 _补讲部分.docx）：
  1. 讲部分知识段遗漏（源文件讲部分位于被删题块尾，随块误删）→ 从源文件恢复『讲核心』横幅图与知识条目；
  2. 断图：冲刺卷缺3张图（题块切割时图在边界外）→ 跨文档补图（r:embed 重映射）；
  3. 表格残留：稳分卷出现源文件表格（应删）；
  4. 空段/分页控制残留：统一清理。
- **补讲部分方法**：从源文件 deepcopy 段落到目标位置；图片 add_picture 后段落被 append 到末尾，需 remove 原位置再用 addnext 移到锚点；**连续 addnext 会倒序**——每段以『上一段』为新锚点（prev = new_p）或一次性按顺序链。
- **断图修复**：dst.part.get_or_add_image(io.BytesIO(blob)) 返回新rId，deepcopy源图段XML后替换 a:blip 的 r:embed（VML图还要改 imagedata 的 o:relid），target._p.addnext(src_xml) 插入。
- 修复后稳分卷33页PDF、冲刺卷15页PDF。

---

## 2026-08-15 追加：稳分卷A级精简执行（49→42题）
- **用户确认执行A级精简**：删7题（新13/14/21/22/28/30/47，对应原15/16/26/27/36/38/66），49→42题。
- A级冗余判定标准：同知识点+同解法（情境不同不算不重复），本次4组：
  1. 等势面+轨迹分析（原15/16/17留17）；2. 电容器内部P点电势（原36/37/38留37）；3. 匀强电场等分法（原25/26/27留25）；4. ψ-x图像（原66/69留69）。
- 执行流程：备份49题版→复制为42题版→删除7题块→重编号1-42（来源标签保留原题号）→更新标题/core title→空行规范化→清理未引用media（5.1MB→3.9MB）→PDF验证（34页）→49题版移入备份文件夹。
- 冲刺卷18题用户确认『不动』（A级冗余为0，当初按解法去重选过）。
- 价值评估术语已存档：区分度/冗余度/解法矩阵/典型性/信息量/考频/陷阱密度；A级=明显重复必删，B级=解法相似可删，C级=0.85+送分题酌情删。

---

## 2026-08-15 追加要求：冲刺卷结构标题 + 图片OCR检查（第10章）
- **冲刺卷必须加原卷结构标题**：原卷有【例题】【练习题】【易错题小练习】分区标记（加粗纯文本段），冲刺卷按题目所属模块在分区变化处插入对应标题（同一分区连续多题共用一个标题），标题前空1行、标题后直接接题。用 lxml 构造加粗段（pPr>spacing line=240/jc=left + rPr>b/sz=24 + run）。
- **图片OCR检查**：安装 `pip install rapidocr_onnxruntime`（纯Python+onnxruntime，支持中文，无需tesseract）；从docx提取 word/media/*.png 用 RapidOCR 批量识别。发现两类有用文字图：
  1. 「讲核心 一、……十五、」小节横幅图——是讲部分的结构标题，**若位于被删除题块内会随块误删**（39题块/52题块），需恢复；
  2. 「易错警示」框图（image10/68/122）含完整知识点文字（电势符号、电容器电势、功能关系），稳分卷已保留。
- **题块边界陷阱**：删除题块时，块内末尾可能混入下一讲部分的横幅图/知识点文字（边界按『下一题号行』截断，但讲部分段在题号行之前）。构建后必须用OCR/文本扫描核对：所有讲核心横幅（14个）、易错警示图（3个）、知识点文字（如『做直线运动的条件』）是否都在。
- **恢复缺失段**：从原文件 deepcopy 段落 XML 到稳分卷；图片用 python-docx add_picture 插入（自动命名 imageN.png，内容哈希与原图一致）；**连续 addnext 插入多段会倒序**——必须每段以『上一段』为新锚点。
- **注意**：add_picture 后段落会被 append 到文档末尾，需 remove 原位置再用 addprevious/addnext 移动；定位锚点要按『下一段文本』判断，不能按文件名（新图已被重命名）。
- 文件名区分：输出文件命名已含『高考稳分卷/满分冲刺卷（共N题）』，与原文件明确区分；任何修改在副本上进行，原文件不动。

---
## 2026-08-15 任务：第10章静电场中的能量（讲和练→稳分卷49题+冲刺卷18题）

### 任务模式（物理讲和练拆分，用户确认流程）
- 选题确认后执行：稳分卷=原文件副本只删「高度重复题+冲刺题」，其余（讲+题）保留重编号；冲刺卷=冲刺题重编号成卷。本次70题→冲刺18+删除3(19,50,67与17/49/69重复)+稳分49。
- **文件名/内标题/核心title 三处一致**：`第10章 高考稳分卷-静电场中的能量（共49题）`、`第10章 满分冲刺卷-静电场中的能量（共18题）`；PDFCreator 按文档 title 命名。
- 来源标注格式：`N．【来源：模块名｜原M题】`（模块名=例题/练习题/易错题小练习，取自 blocks json 的 sec 字段）。

### 构建脚本要点（python-docx + lxml）
1. **删除题块后必须 doc.save 再重新 Document() 打开**，段落索引才有效（曾因未保存导致重扫失败）。
2. 答案合并【答案】+【难度】+短【知识点】：答案段可能含 oMathPara，**只能段尾 append run**（make_run 建 w:r>w:t），删除难度段/知识点段时用**元素引用**（diff_el.getparent().remove(diff_el)），不能靠索引（删除后索引偏移，kp_i 会指向错段——本次踩坑：kp_i 用索引导致 ValueError: Element is not a child）。
3. 合并规则：知识点≤20字或合并后总长≤60则全合并；否则答案+难度合并、知识点单独留段。长知识点单独段会排在【答案】段后（合法）。
4. 选项处理：w:tab（run内）→ 替换为『；』run（parent.addnext 后 remove）；短选项段（各≤20字且合计≤42）两两合并 A+B、C+D，合并时把 b 段子元素（除 pPr）append 到 a 段、删 b 段——**含图选项段不合并**。
5. 空行规范化：**先删全部空段（is_blank 检查 drawing/pict/oMath），再在题号段前、节标题前插空段**；随后**删除节标题后紧跟的空段**（『标题后直接接题』），修正后 PDF 39页。
6. 页边距 720twips=457200EMU（sect.top_margin=720*635）；行距 line=240 单倍、before/after=0；去 pageBreakBefore/keepNext/keepLines/手动分页符（w:br type=page）。
7. **冲刺卷会带上讲部分表格和横幅图**（表格在 body 中独立于段落、题块范围外）——构建后必须检查：删除所有 w:tbl（冲刺卷表格=0）；横幅图+知识点条目（如『做直线运动的条件』）在题块间残留需按文本定位删除。
8. **公式错误修复**：半圆环场强叠加 E'=√{(2×√2/2)E²+√2/2 E²} 应为 √{(2×√2/2·E)²+(√2/2·E)²}=√10/2 E——用 lxml 重建 m:sSup（e 含括号+分数+·E，sup=2），deepcopy 分数元素避免复用冲突。
9. 笔误修复清单（源文件固有）：『电子电子在』→『电子在』、『静土状态』→『静止状态』、『做做匀速直线运动』→『做匀速直线运动』、『正正比』→『成正比』、Q48详解第二个『C．』→『D．』。
10. **media 清理**：从原文件复制删段落后，未引用 media 文件仍留在包内（冲刺卷 141→25 个）——重打包 zip：收集 document.xml+header/footer 中使用的 r:embed/r:id，删除未引用的 word/media/* 和 rels 条目，文件 9.9MB→4.5MB。

### 验证流程（本次全过）
- 题号连续（正则 `^\d{1,2}[．]【来源`）、答案数=详解数=题数、图片数、空行分布、页脚『试卷第N页，共M页』（域在 PDF 自动更新）、边距（公式墨迹超几pt正常）。
- PDFCreator 配置确认：FileNameTemplate=<Title>、TargetDirectory=<Desktop>、SkipPrintDialog=True、ShowProgress=False、OpenViewer Enabled=0、AutoSave ExistingFileBehaviour=EnsureUniqueFilenames。

---

## 2026-08-15 任务：第2章直线与圆的方程稳分卷/冲刺卷检查修复

### 本次检查范围与结果
- 成品4件：稳分卷合一（45题）、稳分卷题目版、稳分卷答案版、冲刺卷（35题）。来源标注（模块+原题号）全部正确、两卷无重复题、内容与源文件保真（仅排版合并）。
- **发现并修复**：①文件名/内标题缺「第2章」前缀→统一加（含core title同步）；②稳分卷题33详解缺图、冲刺卷题14答案缺图→从源文件跨文档补图；③冲刺卷题21详解比源多一句补充说明→删除；④题目版多处分号错位/缺失（题4的A-B、C-D之间缺；题9/10/11/29/32/41的D选项前误加；）→元素级修复；⑤补页脚「试卷第N页，共N页」（参照第1章成品）；⑥**合一版无法被Word打开→根因是段299末尾<w:t>裸露在<w:p>下（未包在<w:r>内）**，非法XML结构，python-docx能读但Word拒绝。

### 关键坑（务必记住）
1. **<w:t> 必须包在 <w:r> 内**：合成文档时若直接 etree.SubElement(p._p, 'w:t') 会生成裸w:t，Word打开报『文件损坏』，python-docx/lxml/pandoc均不报错——**Word打不开优先检查裸元素**（遍历 p._p 直接子元素找 w:t）。
2. **Word COM 打开测试有间歇性假失败**：同一文件反复测试可能 ERR/OK 交替（Word实例/文件锁问题）——打不开先重试，并用『复制到C:/Temp再打开』排除路径锁；不能一次下结论文件损坏。
3. **不要用 zipfile 手动重打包 docx**（即使内容一致也打不开）——必须用 python-docx 打开→修改→save 的方式。
4. **定位损坏段落用二分法**：保留前半/后半段落分别保存测试，快速收敛到单段。
5. **跨文档补图**：dst.part.get_or_add_image(io.BytesIO(blob)) 返回新rId，deepcopy源图段XML后替换 a:blip 的 r:embed（VML图还要改 imagedata 的 o:relid），target._p.addnext(src_xml) 插入。
6. **core title 必须设置**：PDFCreator按文档标题命名；空title虽不影响打开，但PDF名会错。
7. **页脚构造**：试卷第{PAGE}页，共{SECTIONPAGES}页，fldChar begin/instrText/separate/t/end 结构照抄第1章成品XML。
8. 选项分号错位常见模式：D．；-2,32（分号误加在D后）、A．xxx；yyy 中分号放错位置——逐run修文本即可（选项内容是OMML公式时run是分离的：字母run+点run+公式+分号run）。
9. **结构标题（节/题型标题）补充经验**：
   - 用户要求成品（稳分卷合一版/冲刺卷）保留源文件的层级结构标题，如「（一）圆与圆的位置关系」「题型一 判断圆与圆的位置关系」「一、新定义试题」「一、单选题」——即『讲』的部分。
   - 归属映射状态机：节标题（一）出现时**清空整条结构链再append**（不能只清节标题，否则旧题型残留）；题型N/大类N、替换链中同层级的最后一个标题。题目归属=当前链。
   - **讲解条目误判为题号**：源文件混排「1．定义法：…」这类知识点讲解条目（无【答案】）——判断真实题目的方法：题号行后8段内出现【答案】/【详解】/【分析】/【知识点】/【难度】才算题目。
   - 插入格式：加粗+szCs 21+行距300+snapToGrid 0（与题目段一致）；**标题块前补一个空段**（若前段非空），块内标题之间不空行，标题后直接接题（题前不另加空段）。
   - **插入顺序bug**：多个标题连续插入时用正序 addprevious(anchor)（每次插到anchor前自然保持顺序），**不要reversed**（会倒序）。
   - 模块11（综合运用）等无结构标题的模块，题目直接连续排，不插标题。
   - 用pdf文本核对结构标题是否生效（打印前确认PDF时间戳/同名_2后缀——同名文件PDFCreator自动加_2，注意取最新版核对）。
10. **文件命名规则（用户强调）**：文件做任何更改，**文件名都要添加额外说明以与原文件区分**（如「（加结构标题）」「（题目）」「（答案）」），内标题与 core title 同步更新，PDFCreator 按文档标题命名输出同名PDF；改名/改内容前先备份当前版本到备份文件夹。
11. **图片文字识别（OCR）检查（用户强调）**：文档图片很可能含有用文字（题目条件、数据、坐标刻度、点标记），处理任何文档后应把 word/media 图片提取出来做 OCR 检查。
   - **Windows 自带 OCR 引擎可用**（无需安装）：PowerShell 加载 WinRT（Add-Type -AssemblyName System.Runtime.WindowsRuntime + [Windows.Media.Ocr.OcrEngine, Windows.Foundation, ContentType=WindowsRuntime] 等），支持 zh-Hans-CN；Await 辅助函数用 System.WindowsRuntimeSystemExtensions.AsTask 泛型方法（GetMethods 过滤 Name -eq 'AsTask' 且 IsGenericMethod）。
   - 小图（坐标图）先 PIL 放大2x（Image.LANCZOS）再识别，效果更好。
   - 本次稳分卷9图+冲刺卷14图全部OCR：仅3张识别出文字（稳分卷题9坐标图刻度 0/1/2、冲刺卷题2点标记、题29的『=4』），均为题干已有信息，无隐藏条件——数学几何图通常只有刻度/点标记，与题干一致；若有识别出题干没有的数据/条件，必须核对是否遗漏。
   - 图片归属题目：遍历段落 a:blip 的 r:embed → part.rels 查 media 文件名 → 对应当前题号。

---
## 2026-08-14 任务：物理讲和练→稳分卷/冲刺卷（9章逐章）

### 物理讲和练文件结构（知识图鉴·单元讲练测）
- 分区：【知识点】区（讲解+填空条目，**无答案**，不入选）＋【例题】区＋【易错题小练习】区（题目带【答案】【难度】【知识点】【详解】，入选对象）。
- **显示题号在分区之间重启**（多个『1．』『2．』）——选题与构建必须用『题号行位置序号』（ordinal），不能用显示题号做字典映射（会取错块）。构建脚本中 DUP/CHONG 列表一律用 ordinal（本次曾把显示题号当 ordinal 导致删错题、题数不对，务必复查映射）。
- 题块边界＝下一题号行或下一个【例题】/【易错题小练习】标记；块内空白段剔除。
- 答案含公式（分数答案如 kq²/2d²）——合并【答案】+【难度】时**保留原run/oMath只在段尾追加**（重写整段会丢公式，与数学卷同坑）。
- 校验『题间空行』时 is_blank 必须同时检查 oMath——纯公式段落会被误判为空行。
- 文件很大（4-24MB、图片近百张）：清单生成用脚本跑；子智能体逐文件全量分析容易超时，尽量给脚本化指令或干脆主智能体脚本自产清单。
- **提取方法（用户确认）**：稳分卷＝原讲和练副本，只删除「高度重复题」＋「已提取到冲刺卷的题」，其余（讲部分＋其余题目）全部保留，保留题目重新连续编号；冲刺卷＝冲刺题重编号成卷。稳分卷题数=原题数−冲刺数−重复数。
- **文件名与内标题必须带「第N章」前缀**（用户强调）：`第9章 高考稳分卷-静电场及其应用（共32题）.docx`。
- 输出文件名：`第N章 高考稳分卷-章名（共M题）.docx`、`第N章 满分冲刺卷-章名（共M题）.docx`。

---

## 2026-08-14 任务：第1章稳分卷拆分＋计数原理/物理题目文件补空行

### 大题判定经验（数学/物理通用）
- 自动检测：小问标记`[（(][0-9Ⅰ1i][）)]`＋小问后8字含 求/证/若/判断/计算/问/试 或 『求/证明：』在小问前（『求：（1）…』）或 多小问+题干含求/证明 或 答案含『证明见解析/见解析』。
- **必须人工复核**：填空题多空（(1)(2)+横线）、结论列举（(1)AC⊥BD…）、选择/判断题会被误判；多小问填空答案为数字 → 不算大题。最终清单与用户确认（ask_user_question）。
- 物理「精简版_知识点例题及易错题」文件：知识点讲解条目（『1．…的理解/方法/特点』）与真实题目（带数值、'如图所示'、选项）混排且题号重启——用**位置序号**标大题，不用显示题号。

---
## 2026-08-16（第二轮）6成品全面复查：数学两卷+物理两卷+两清单

### 本轮新工具（dsh_tools 已提交）
- fulltext.py（全文doc-order转储）、qdump.py（带sqrt/上下标渲染的题目转储，oMath白名单必须含oMath）、spacing_check.py（行距分布）、nd_check.py（难度提取）、extract_src.py（源题清单）、uchk.py（修正版u/b统计）、xfix/fix2/fix3（精准修复模板）。
- **u_texts.py/run_check.py 有bug**：数学run的t用 M+'t' 拼接（少'}')，m:r 的 u/b 全部漏统计——判断下划线用 uchk.py（w:r+m:r 都要，m:t 用 MC+'t'）。

### PDFCreator 6.3 踩坑（重要）
- 注册表：AutoSave 是嵌套子键 HKCU\Software\pdfforge\PDFCreator\Settings\AutoSave 和 ConversionProfiles\0\AutoSave（平铺值会被运行中的程序覆盖重置）。
- **AutoSave 的 TargetDirectory 在本机不生效**（日志 FilePath_InvalidRootedPath）→ 改用 CLI 最稳：PDFCreator.exe /PrintFile=<docx> /OutputFile=<pdf>（ASCII路径）。
- Word 打印：PowerShell COM PrintOut 参数绑定报错；VBS 可行但中文路径被 ANSI 误读 → docx 复制成 ASCII 临时名放 C:\Users\hello\Desktop 再打。
- push 偶发 SSL_ERROR_SYSCALL → 直接重试即可。

### 本轮内容核查结论（数学）
- 82题逐题验算全对；linearization 会丢根号/绝对值（rad/m:d 内容无符号），下结论前必看 OMML（Q2 -√2≤t≤√2、Q45 y=k|x+1| 曾被误判）。
- 源文件固有错误：模块13原1 C详解把 d1+d2=0 误写 d1=d2=0；修复=中点M在l上、方向不确定。
- 冲刺卷混入0.85-0.94题10道（Q1/2/5/8/10/12/15/26/27/28）→待用户定是否移回稳分卷；稳分卷缺口：方向向量（B版§2.2明确，两卷0题）、求圆一般方程（模块6原25-28全删）、几何意义最值（模块8原14/模块14原12/16全删）；稳分卷内Q5/Q9题型解法重复。

### 本轮内容核查结论（物理）
- 讲和练表格固有错误（人教公式应为U/Ig-Rg、IgRg/(I-Ig)、RxRV/(Rx+RV)）：电压表"U_g/I_g"、电流表"I_RR_R/(I−I_R)"、RV=U_g/I_g、外接法R₃RV/(R₁+RV)——已修；冲刺Q12(4)详解两处不等式方向写反（答案"大于"对）——已修。
- 讲条目序号"2。"、选项"G。"→全角点"．"；被删12题全部高度重复（同知识点+题型+解法）。
- 清单155处u（含m:r）全部thick+b；补：R=U/I、Rg/Ig/Ug及Ug=（现164处）；概况583处thick+b；概况表格重复标签（T4R1C2/R2C2"标准方程/图形"、T6R1C1应"p的几何意义"）已修。
- 两清单行距：清单240（上轮用户确认）、概况原无设置已改300——统一方案待用户定。
- 物理123张图OCR：电路图/表盘/铭牌标签，与题干一致，无隐藏条件。
---
## 2026-08-16（第三轮）用户确认后执行：数学两卷选题调整终版

### 终版方案（用户同意）
- 稳分卷 47→59题：删Q9(原15，与Q5重复)；冲刺卷移入10题（原45/49/16/35/5/8/10/12/24/30）；源补3题（模块2原17方向向量、模块6原26求圆一般方程、模块8原14几何意义最值）；补标题（题型三 求圆的一般方程、题型二/四 圆与圆交点/确定方程真实标题、（五）平行线距离真实标题）+ ※题型二 二元二次→冲刺卷6。
- 冲刺卷 35→25题：删10题移走；17个悬空标题删除；※引用全部更新为两卷新编号（交叉双向核对一致）。
- 两清单行距统一300；物理知识清单164处u thick+b；概况表格标签修正。

### 构建脚本坑（务必记住）
1. find_q按题号定位在混合文档中会撞号：源题块无来源标签，插入源原26时 find_q(A,17) 撞上先插入的源原17题号→错位到模块2。锚点必须用「来源标签+原题号」或题块内容特征定位。
2. 同锚点多块插入必须用游标（插入后紧跟上一块末尾），否则后插的跑到前插的前面。
3. 删块必须先收集元素再删：range内逐个remove会因列表位移隔段误删（曾吞掉Q10整块）。
4. 跨文档移块会夹带题块后的标题/※行（块边界=下一题号行，中间标题/※被带走）→ 提取后剥离尾部空行/标题/※；删除误入标题时按位置区分，精确匹配会双杀两卷同名标题。
5. cut后重插锚点必须在文档树内：在已脱离元素上addnext→其余段落挂空丢失。
6. 重编号run有前导空格时 str(n)+尾切片 会拼回原数字（' 1'→'11'）→ 用 index+切片替换。
7. 孤儿图片清理：按 document.xml 的 r:embed 过滤 rels+media 重写zip，恢复 media==rels==draw 三查一致。

### 终版验证结论
- 数学稳分59/冲刺25：dup=NONE、题号连续、来源标签完整、mega NONE（3个无标签源题不影响）、图片三查一致、字体五号、行距300、空行规范、※双向引用全部正确、PDF全五号A4无居中。
- 物理两卷+两清单：全过；清单行距已改300。
- 6文件已替换原位置（旧版备份至「原文件备份_第二轮…前_20260816」文件夹）。

---
## 2026-08-17 第3章圆锥曲线两卷第三轮减量与成品复核

### 本轮可复用经验
- 选题确认后再生成：最终稳分卷143题、冲刺卷22题；稳分卷只删高度重复题和已移入冲刺卷的题，讲解部分与其余题目原样保留；冲刺题必须同时满足综合/参数/动态/开放/最值/压轴中的高难特征，普通综合题仍归稳分卷。
- 题块定位必须使用「来源模块＋原题号」，不能只按显示题号；每题完成“选题—纠错—排版—来源/答案/图片核对”后再处理下一题，最后再做两卷交叉覆盖检查。
- Word 页脚 XML 坑：`w:r` 的子节点不能直接追加到 `w:p`；页脚文字必须以完整 `w:r` 节点挂入段落，否则 python-docx 可能能读而 Word 拒绝打开。格式化后必须做 Word COM 打开测试。
- PDFCreator 6.3 自动保存对含空格/中文的相对输出模板容易报 `FilePath_InvalidRootedPath`；视觉复核使用 PDFCreator CLI 的 `ProcessFile`，输入与输出均传绝对路径，检查实际 PDF 时间、页数和标题后再渲染查看。
- 全页视觉检查可先检查首/中/末页和所有含图页，再用 PDFCreator PDF 的全页缩略总览确认无空白页、截断、重叠；最终只保留成品 DOCX，PDF/PNG/Word导出输入均为中间文件。
- 每轮结束前固定复核：页边距720 twips、题号连续、来源标签数=题数、答案标签齐全、题间空行、图片 media/rels/drawing 三查、公式框内无中文误入、Word可打开；源文件只读，成品另名归位。
- 结构标题必须挂靠到该节/题型“第一道保留题”前：删掉前置题后，标题要随知识归属前移，不能全部堆在模块总标题下；若同一模块删去某题型的全部题目，该题型标题应删除，节标题挂到后续仍保留的相关题目前。修复后逐段检查“标题→来源原题号”的顺序。
- 重新插入结构标题后不要重复运行会改写选项文本的规范化脚本；否则已有选项末尾“；”可能被再次追加，造成“；；”。若只改标题，直接执行结构修复＋字体版式整理，再逐题与备份文本比对。

### 2026-08-17 第3章结构标题全量重新挂靠

- 用户指出标题堆在模块开头并非单个模块问题；应对两卷所有章节标题、模块标题、分节标题、题型标题统一扫描，按源文件同级标题边界计算题目范围，再挂到该范围内第一道保留题前。
- 章节标题必须按两卷各自的首个保留题定位；模块/分节/题型标题只在其范围仍有保留题时保留，整段没有保留题的标题删除。这样既避免空标题，又保持知识发展顺序和题目来源可追溯。
- 结构修复脚本必须先删除旧结构标题，再按“来源模块＋原题号”锚定题目插入；插入后只做空行与版式整理，不再运行会改写选项末尾标点的脚本。修复后核对“预期标题→首题来源”映射、题目文本与备份逐题一致。
- 本轮全量复核结果：稳分卷143题、冲刺卷22题，结构映射分别142处、32处；两卷均通过Word打开、PDFCreator导出、A4尺寸和逐页视觉检查。

## 2026-08-17 第6章计数原理两卷精简与排版

- 选题确认是硬闸门：先向用户提交稳分/冲刺题号及覆盖审计，得到明确同意后才能生成或改写成品。
- 混排数学文件中，`6．题目` 这类结构标题可能被误识别为第6题；真实题目应同时满足来源标签或题号后有【答案】等题块标记，不能只按显示题号定位。
- 文本转储会把 OMML 分数、根号、上下标压扁（如 `20/81` 变成 `2081`）；纠错和逻辑判断前必须检查原始 OMML，不能据扁平文本下结论。
- 过滤/合并 DOCX 后必须同时核对 `document.xml`、关系文件和 `word/media`：每个图片 `r:embed` 都要有对应关系和媒体，保留题图不能只看段落文字。
- 用户允许稳分卷题间一行空行；校验器应统计这类空段但不能把它误报为错误。公式图片、表格和 OMML 段也不能误判为空段。
- PDFCreator 6.3 的稳定流程是使用官方 COM 的 `cscript` 脚本加入 `PDFCreator.JobQueue`，绝对路径输出后再用 Poppler 渲染检查；PowerShell 直接调用 COM 可能触发类型库错误。视觉检查只以 PDFCreator 生成的 PDF 为准。
- 文件名、core title 和文档内标题要三处一致；不能只检查 `docProps/core.xml`，还要检查正文第一个非空段落确实是第N章标题，结构标题必须排在其后。
- 原文件必须只读保留；处理副本放桌面临时目录，成品另名回写，备份放源目录的日期文件夹；结束时删除 PDF、PNG、合并中间件等临时产物，只保留最终 DOCX和确有复用价值的脚本。

## 2026-08-18 第6章计数原理分层复核与格式修复

- 用户确认“人教A版第6章”后，版本口径不得被复核意见改写；只按用户批准的分层修复。
- 冲刺卷中标注综合但实际为常规计数的题应回稳分卷；参数、推广证明、最值/范围证明即使难度字段偏低，也要按题目实质保留冲刺卷。
- 跨文档移题必须复制 `document.xml.rels` 对应关系并重命名媒体资源；完成后做 `r:embed`、关系、媒体三查，再用PDFCreator实视。
- 重组冲刺卷时，先独立保留原首标题，再挂靠“模块→分节→题型”标题；标题、文件名、core title的序号和题数必须三处一致。
- 每轮修复后用来源模块＋原题号逐题比对文本守恒；仅对批准的标点、小问编号和题号做定点修改，避免公式和题目结构被重写。

## 2026-08-18 第3章圆锥曲线两卷第四轮重审终稿（稳分148题／冲刺17题）

- 选题确认仍是生成闸门：源题497题，删除高度重复332题；稳分148题、冲刺17题，三集合互斥且并集覆盖全部保留题。普通综合题归稳分，只有真正含参数/动态/开放探究/最值范围证明/原卷压轴且难度足够高的题进冲刺。
- 章节按人教B版第二章平面解析几何重排；结构标题按知识匹配挂靠到首道保留题，空标题删除，挪移标题保留原位说明与新位来源说明；题目与答案逐题相邻，来源标记只保留在说明行。
- 成品固定做四层核查：题号连续与答案块数、人工逻辑复核与题型/知识/难度覆盖、OMML行内公式/字体/边距/行距/图片三查、Word实际打开和PDFCreator视觉检查。脚本只负责计数、结构和格式，逻辑结论必须人工确认。
- **Word脚注命名空间坑**：用 `xml.etree.ElementTree` 重序列化含 `mc:Ignorable="w14 w15 wp14"` 的脚注，会丢掉未直接使用的命名空间声明，Word会报“文件可能已经损坏”，而 `python-docx` 仍可能读得出来。修复脚注必须用保留 `nsmap` 的 lxml，或以源脚注为模板，只更新页数字段缓存；修复后必须用 Word COM 实际打开测试。
- PDFCreator 6.3 最稳流程：Word仅生成中间PDF，最终视觉PDF必须再用 PDFCreator `processfile`；`/OutputFile` 使用带引号的 Windows 绝对路径（不要用相对模板，避免 `FilePath_InvalidRootedPath`），输出后核对页数、答案数、A4尺寸，再渲染首/中/末及含图页抽查。当前有效结果为稳分77页/148答案、冲刺17页/17答案。
- 临时 DOCX、Word中间PDF、PDFCreator输入输出、渲染PNG均只放桌面工作目录；最终只归位另名DOCX，原文件与日期备份保留。清理临时内容时必须逐个列出精确路径，禁止宽泛通配删除。

### 本轮紧凑终稿补充（稳分85题／冲刺10题）

- 选题确认是生成闸门：题单经用户确认后，构建脚本只允许使用该组 stable/sprint 数组；三组集合必须满足 `stable∩sprint=∅` 且总数=源题数。
- 混排源文件的题块登记可能把详解尾段漏在 `end_para` 外；题块边界应扩展到下一真实题开始前，再用结构标题截断。这样选中题的详解不丢，未选题的孤儿详解也不会流入冲刺卷。
- 内容守恒检查不能只比较全局字符串：按输出题号分段，比较源题块经同一组批准纠错/排版规则处理后的直接段落与表格序列；额外插入的结构标题单独排除，且不要用 `^3\.` 这类宽正则误删详解中的“3．/3.”小步骤。
- 本轮确认的实质纠错包括：M6Q11核对绝对值系数和后确认原答案ACD正确，仅把详解的“当r=4”改为“当r=6”；M6Q12末段相遇点A1误写为A4；M4Q13第（8）问原答案960经验算正确，不改为192。公式顺序、图片、表格均以OMML/XML逐项核对。
- 生成后固定做四层检查：题号/答案/详解计数，A4/720页边距/五号字体/行距，关系与图片三查，PDFCreator首中末页及全部含图页视觉核查；最后才归位原目录并清理中间件。
- 若PDFCreator队列有残留打印任务，单次 `NextJob` 可能捕获旧文档；应先结束本轮启动的PDFCreator进程，再将队列候选逐一导出，按PDF标题、页数和首页内容确认目标文件。

### 2026-08-18 第3章结构说明串联修正补充

- 多条“原位说明/来源”不能压在同一段；应按“完整句号+新说明类型”拆成独立段落；多个“题型”也应拆成独立标题，避免读者误把来源说明当题干。
- 题型编号按最终保留题重新连续编号。若某原题型没有任何保留题，删去空标题并保留一条“未保留对应题目、不设置空标题”的结构说明；不能把该标题挂到知识不匹配的题目前。
- 只改结构段落时，直接用 lxml 保留 `document.xml` 之外的 DOCX 部件；修改后必须重做题号/答案/公式/图片审计、Word 实际打开和 PDFCreator PDF 视觉复核。

### 2026-08-18 第6章第二轮结构与元数据复核补充

- 人教B版目录核对以本地《选择性必修2》教材为准：计数原理是第3章；文档属性的 subject 也必须写“选择性必修第二册第3章”，不能沿用输入文件的 A版册次。
- 部分冲刺题跨越未选小节时，删除空标题；保留小节改为本卷内连续编号，并在标题中注明原教材位置；有题的正式分节标题（如3.1.2）要补回，避免学生知识链断裂。
- 结构修复后要做两次内容守恒：新副本与结构修复前副本逐题比对，再按来源模块＋原题号与5份源文件逐题比对；已批准的人工纠错单独列入白名单，不能用宽泛正则掩盖差异。
- 本轮成品为稳分85题/38页、冲刺10题/8页；PDFCreator实际打开、打印、A4尺寸、全页缩略图和重点公式/图片页均通过。最终文件采用“结构连续修正版”另名归位，原文件和日期备份保留。
- 经验文件新增条目只记录可复用规则，不重复粘贴单题审计流水；历史项目的题量仅用于追溯，不能作为新项目的默认题量。

### 2026-08-18 第6章题型标题统一补充

- 具体题型标题不得保留“题型一、题型二”或“一、/（一）”等原序号，统一写成“正式教材节号 教材节标题：具体题型”；本章综合测试的题型统一写成“本章综合测试：单选题”等。
- 题型改名先按正式教材节建立上下文，再改标题；正式节标题保留，改名后没有直接题目的大类标题删除，避免空标题和层级误导。
- 纯结构标题改动也必须按题号分块做内容守恒，并复核首标题、文档属性标题、旧序号残留、空标题和PDFCreator视觉输出；成品另名时在文件名追加简短说明，原成品和日期备份保留。

### 2026-08-18 答案嵌入与知识清单处理补充

- 填空时按“逻辑空”计数：Word 可能把一条长下划线拆成多个相邻 `w:t`（如 `_______`、`_`、`__`），应先合并相邻纯下划线节点；答案若紧跟题干已有 `W＝`、`Q＝` 或 `T_{k+1}=`，只填右侧表达式，避免公式重复。
- 组卷网答案行可能跨多个段落，且答案行与下一小问交错；删除答案元数据时要在遇到新的小问正文或图片时立即结束“答案尾段”状态，不能用整段连续删除，否则会误删后续题目和图片。完成后必须做 `media`、`document.xml.rels`、`r:embed` 三查。
- 高考必背定义、定律、关系式和结论统一使用 `w:b` + `w:u val="thick"`；已有 OMML 只改其叶节点样式，保留分数、上下标和行内结构，不把公式扁平化为普通文本。`m:oMathPara` 若只是块级包装，转为行内 `m:oMath` 后再做 PDFCreator 视觉检查。
- 含中文路径的 DOCX 用 PDFCreator COM/cscript 视觉验收时，复制一份 ASCII 临时输入文件；PDFCreator 配置保持标题命名、自动保存、唯一文件名、不打开查看器和不弹确认框。临时 DOCX/PDF/PNG 只放桌面工作目录，最终只归位另名 DOCX。

### 2026-08-18 高中物理第12章知识清单教材化复核

- 2019人教版必修第三册第12章的题型归类按本地教材目录处理：电功/焦耳定律/电动机归12.1；电动势、闭合电路能量与欧姆定律、路端电压、U-I图像归12.2；能量守恒、方向性、能源应用归12.4；没有匹配题的12.3实验节不强行设置空正式标题。
- 知识清单答案应嵌回原空位，答案和高考必背定义、定律、公式、结论统一用粗体加粗下划线；OMML公式只改叶节点样式，分数、上下标和行内结构必须保留。
- 小问标记可能拆成“（、1、）后接正文”三个文本运行，规范化时要处理“）”与正文同一运行的情况，统一为半角`(1)`；不能只匹配单独的右括号节点。
- 完成后固定做：标题/文件名/core title一致性、教材节与题型覆盖、答案/元数据残留、A4与720 twips页边距、五号字体和1.25倍行距、oMathPara、粗体下划线、图片媒体/关系/绘图三查、Word COM打开和PDFCreator全页视觉检查。

### 2026-08-18 第12章知识清单复核完善（答案加粗填写+必背下划线重做；上一轮产出有内容丢失故从源文件重建）

- **OMML 下划线渲染实测（PDFCreator 6.3 为准）**：文本 run 加 w:u val=thick → 粗线 1.3pt 可靠；无分数的简单 OMML（m:r 序列）加 u → 粗线可靠；**分数 m:f 内 m:r 加 u → 只渲染细线 0.72pt（thick 被 Ghostscript 忽略）**；**分数在 m:fPr>m:ctrlPr>w:rPr 加 u(thick) → 分数下方渲染粗线 1.2pt 可靠**。故必背分数公式用 ctrlPr 加 u，分数内 m:r 只加 b。DOCX 层 w:u 数不能直接对应 PDF 下划线矩形数（连续 run 合并）；用 fitz 文本行坐标匹配矩形高度（粗>1.0/细<=1.0）验证。
- **答案嵌入实现**：先合并相邻纯下划线运行（w:t 全为 _）为一个逻辑空，替换时删除整组下划线运行、在原始位置正序插入答案 run（b+u thick）；3.6×10^6 用文本 3.6×10 + vertAlign=superscript 的 6；分数类答案从【答案】段 deepcopy 原 oMath（保证结构一致）再套样式。
- **多步骤处理一个 docx 必须每次从干净源文件副本重跑**（增量修改累积污染，例如答案重复插入——同一个脚本跑两轮就出“静电力静电力”）。完成后再做零残留检查（_、【、【难度】 = 0）。
- **删除答案/难度/知识点段用收集元素后统一删（引用），不能靠段落索引逐个删**（索引随删除位移，会漏删余段）。题号锚点、节标题锚点一律用文本定位（^数字．），不能用固定段落索引——插入 12.1 后 12.2/12.4 的索引已偏移，曾误插到题3/题7前。
- **桌面工作区的脚本不要起标准库名**（如 inspect.py），Python import inspect 会被劫持，docx/lxml 报诡异循环导入。
- 组卷网知识清单开头 [0][1][2] 段落是「思维导图/知识清单」标签横幅 + 整章思维导图大图（image1=3x3 占位、image2/4=650x50 标签、image3=1648x2332 思维导图），它们正常显示在 PDF 第 1 页（RapidOCR 验证识别出电功/电功率/焦耳定律/闭合电路/实验/能源等节点），**勿当垃圾删除**；图片三查=media 11 == rels 引用 11 == drawing 11 + md5 与源文件全同。
- 页脚「试卷第N页，共N页」是域（fldChar/instrText），格式化只改 jc=left + 字号 21，完成后重新 PDFCreator 确认页码域仍渲染。m:oMathPara 本文件为 0（公式全行内）。
- 本轮成品：第12章 电能 能量守恒定律知识清单（知识点，题目+答案）（共10题）（教材归类精修·答案加粗下划线·复核完善）.docx（5页 A4，下划线矩形 114，题号1-10连续，节标题 12.1/12.2/12.4，内容守恒+物理逻辑人工复核全通过）。

### 2026-08-18 第12章划线粒度修正（用户反馈：不要一整行下划线）

- **用户明确不满整句/整行粗下划线**（我曾把“电流做功的实质是……在做功。”等整句都加粗划线）。正确粒度=选择性划线：**只划 填空答案 + 公式/单位换算（含OMML）+ 少量核心必背术语**（焦耳定律/额定电压/额定电流/外电路/内电路/内阻/能量守恒定律/能量耗散/温室效应等），叙述性汉字长句一律不划。
- 实现（在成品上逆运算）：对整句划线过的 run 重新分类——①文本∈答案集合；②含ASCII字母/数字/希腊字母(Δ等)/数学符号（＝＋－×÷·/等）的公式run；③∈必背术语（若长run内含术语则按术语位置拆分出独立run只划术语段）→保留 b+u(thick)；其余中文叙述run移除 u 和 b；子标题段（^[（(]\d[）)] 且≤18字）整段去线；小问编号数字不划（如“(1)”中的“1”）。
- 公式汉字下标的单字run（U外/U内/P总/P热/P机/Q外/E初/E末/ΔE增减 的“外内总机热增减初末短”）应划（属公式一部分）；全角加号＋/减号－、希腊字母也需要识别为公式字符。
- 验证：PDF下划线矩形宽度分桶（15/30/60/120pt）——无120pt以上整行线，最长约105pt(短换算公式)；同时目测无长段连续线。

---

## 2026-08-18 第11章电路及其应用讲和练 教材化拆分（61题到稳分37+冲刺12+删除12）第二轮：全新标准重建

### 任务模式
用户重发第11章任务并用最新标准要求重建（与8/16旧版不同点：全部教材化、题型标题=“教材节号 教材节标题：具体题型”、不保留【例题】分区与来源标签、按教材顺序重排、按知识顺序重编号）。选题沿用已确认方案：冲刺=原7/27/35/42/47/48/49/52/56/59/60/61，删除=原2/5/6/21/26/33/37/43/46/51/55/58，稳分37题。

### 生产过程（本次新沉淀）
1. 教材化结构：按2019人教版必修三第十一章（11.1电源和电流/11.2导体的电阻/11.3实验：导体电阻率的测量/11.4串联电路并联电路/11.5实验：练习使用多用电表）重排。原文件顺序为 11.1到11.2到11.3(游标卡尺+实验原理)到11.4到11.3(伏安法)到11.5到11.3(差值/半偏/替代/电桥)——按教材重排成 11.1到11.2到11.3全部到11.4到11.5。电桥小节（题59/60/61全进冲刺）的讲核心随之整体移入冲刺卷（符合“若某题型只有特别难题，则该题型可只保留在冲刺卷中”）。
2. 讲核心横幅图处理：17个“讲核心|一、…十七、”底横幅图（image3/4/11/14/23/27/34/42/46/55/70/74/82/103/119/130/135）只含标题文字且带原卷“一~十七”序号——按“输出必须全部教材化、原序号删除”删除横幅图，用“11.2 导体的电阻：电阻定律”这类题型标题文字替代（信息不丢）。但注意横幅与讲条文同处一段（如“1．电源的作用”）：要先 strip_banner_img 只删 drawing 保留文字，否则讲条文丢失。
3. 易错警示图（image9/image20）是真实教学内容（“对电流定义不理解导致错误”等）必须保留；顶部组卷网广告横幅（image2）删除；题图（如题12的I-U图image16嵌在【例题】标记段内）也删除文字【例题】但保留图。
4. 题块定位用“正则 + 向下找【答案】”自动检出61/37/12题完全吻合；题块=题号行到下一题号行前；讲条目（1．电源的作用等）因无【答案】不被误判。

### PDFCreator打印失败排查经验（本轮大坑，务必记住）
- Word打印到PDFCreator卡死=弹了确认面板：PrintOut阻塞直到交互，agent环境无解。根因不是PDFCreator坏了，而是配置没生效（进程启动时读旧配置）。
- 修复序列：杀全部PDFCreator/WINWORD进程；HKCU...ConversionProfiles/0/AutoSave 设 Enabled=1 + FileNameTemplate=绝对路径模板（必须绝对路径，否则 FilePath_InvalidRootedPath）+ExistingFileBehaviour=EnsureUniqueFilenames；OpenViewer/Enabled=0（不自动打开查看器）；Preview/Enabled=0（避免预览Ghostscript转换再报错）；设置后再打印。改注册表后必须全新启动PDFCreator才生效。
- 产生56MB巨大中间PDF/转换3分钟 = PDFCreator自己profile输出无压缩；直接结果不可用。改用其自带Ghostscript直接渲染spool的.PS：gswin64c.exe -dNOPAUSE -dBATCH -dSAFER -sDEVICE=pdfwrite -dPDFSETTINGS=/prepress -sOutputFile=out.pdf spool\XXX.PS → 40页仅3.4MB、1分钟内。spool路径 %LOCALAPPDATA%\Temp\PDFCreator\spool。打印后立刻杀PDFCreator进程可保住.PS不被app吃掉（否则app转成56MB垃圾PDF）。这仍是“用PDFCreator导出”的管线（Word到PDFCreator打印机再到其Ghostscript引擎渲染）。
- -sOutputFile=$变量 偶发不写文件（引号/串行问题）→ 用字面绝对路径 + Set-Location 最稳。
- 注：PDFCreator CLI /PrintFile 会挂起（队列残留）不可靠；COM JobQueue cscript 对APP版本依赖强；最终采用“打印到杀进程保PS到自带GS渲染”最稳。

### 排版格式坑（本次新增）
- format_pass 误设 w:gutter=720 → 偶数页文字整体右移36pt（1英寸）变奇偶页交错边距！gutter 必须=0。检查法：PyMuPDF per-page min content x0 交替36/72=中招。
- 文字+块级公式混合段(oMathPara)强制公式另起行且居中 → 必须跑 dsh_tools/block2inline.py 把段内oMathPara转行内（混合段转、纯公式段保留）。转换后345+162处。检测“居中块”注意：行内公式位于长行中部且窄(x0约页中)是正常左对齐，不是居中（用span级再确认，勿误报）。
- 标签“X。”误转：题干/器材清单用半角“G.开关S、导线若干” → revise.py 会把前有空白/段首的“字母+.”误转成“字母+。”而非“字母+．”。修复：run开头 字母+。→．；跨run时独立run“B。”整段替换。而“故选C。”“E。若”“图中的A。”是合法句号（字母前为CJK）不能动；判定=字母前是空白/段首/；：。
- 讲条目“2.串、、”“1.测量小灯泡”等半角点被revise转成“2。串” → 再修：run开头 数字+。→数字+．。合法“S0。步骤”“I0。则”“V1、V2。已知”保留（字母在数字后）。
- 情理纠错（沿用经验清单：8处改字+公式u到Il+题61删逗号+题36 C/D交换）全部在本次重建中复现应用。

### 验证小循环
- 题号连续1..37/1..12；两卷来源交集=0；media/rels/draw无断链（5图合法复用故draw>media）；无孤儿media（重写zip）；逐题人工验算49题全对；稳分39页PDF、冲刺18页PDF全A4全左对齐x0=36pt（除一处英文句号因Word CJK/拉丁自动间距微超0.4cm，非边距配置错误，接受）。
- 内容守恒终极法：逐段diff raw到成品只应有 （N）到(N)、标点全角化、题号重编、批准纠错；归一化对比“缺失”全是题号残留或上述规范差异，无真实丢失。


---

### PDFCreator 导出实现全流程（第11章验证通过，可直接照抄）

**环境事实**
- PDFCreator 6.3.0 Free 装在 C:\Program Files\PDFCreator\；自带 Ghostscript（gswin64c.exe 10.05.1）在 C:\Program Files\PDFCreator\Ghostscript\bin\。
- PDFCreator 虚拟打印机是系统默认打印机；spool 目录 = %LOCALAPPDATA%\Temp\PDFCreator\spool（截图工件先在 .PS》。
- Word 能直接打开中文路径 docx（传绝对路径即可）。

**注册表配置（先杀进程改完再全新启动才生效，进程只读一次配置）**
- HKCU\Software\pdfforge\PDFCreator\Settings\ConversionProfiles\0\AutoSave：Enabled=1；FileNameTemplate=绝对路径模板（例 C:\Users\xxx\Desktop\PDFout\<Title>.pdf）；ExistingFileBehaviour=EnsureUniqueFilenames。
- 同 profile 的 OpenViewer\Enabled=0（不自动打开查看器）、Preview\Enabled=0（关预览，否则 Ghostscript 预览转换抛错中断）。
- 关键：FileNameTemplate 必须带完整绝对目录，否则 PDFCreator 报 FilePath_InvalidRootedPath；AutoSave.TargetDirectory 在本机不生效。

**三个坑（本轮逐一踩过）**
1. Word PrintOut 卡死直到超时 = PDFCreator 打印确认面板在等交互（agent 无法点）。修好配置+全新进程后不再弹框。
2. PDFCreator 自己 profile 转换产出 56MB 无压缩 PDF、要 3+ 分钟（不可用）。
3. 打印后 PDFCreator app 会立刻把 spool 里的 .PS 转成 56MB 垃圾 PDF → 打印后必须立即 Stop-Process PDFCreator* 保住 .PS。

**最终可用管线（稳、快：40页3.4MB、1分钟内）**
1. Get-Process PDFCreator*,WINWORD | Stop-Process -Force；Start-Sleep 2。
2. $word=New-Object -ComObject Word.Application；$word.Visible=$false；$word.DisplayAlerts=0；$doc=$word.Documents.Open('绝对路径\成品.docx',$false,$true)；$doc.PrintOut()；Start-Sleep 3；$doc.Close($false)；$word.Quit()。（打印到默认 PDFCreator 打印机）
3. 立即 Get-Process PDFCreator* | Stop-Process -Force；Start-Sleep 2（保住 spool 新 GUID.PS）。
4. & 'C:\Program Files\PDFCreator\Ghostscript\bin\gswin64c.exe' -dNOPAUSE -dBATCH -dSAFER -sDEVICE=pdfwrite -dPDFSETTINGS=/prepress -sOutputFile='绝对路径\out.pdf' spool\GUID.PS
   - 注意：-sOutputFile 必须用字面绝对路径（$变量偶发不写文件，可先 Set-Location 再跑）。
5. PyMuPDF 校验：page_count、页尺寸≈595x842(A4)、每页 min content x0≈36pt（1.27cm边距，若奇偶页交错36/72=中过 w:gutter≠0）、无异常居中块（行内公式位于长行中部是正常左对齐，别误报）、标题/页脚文本在。

**其他尝试（别重复踩）**
- PDFCreator CLId /PrintFile=/OutputFile= 会挂起（队列残留），不可靠。
- PDFCreator COM JobQueue + cscript 对版本接口敏感，本次未跑通。
- Remove-Item 对 spool 只读文件在 pwsh7 非交互模式报错，清 spool 用 cmd /c rd /s /q 目录 2>nul。


### 2026-08-18（续）紧凑排版·短步骤横排合并（merge_compact.py）——用户点名“排版要尽量紧凑”后补做的必备步骤
- 只做“公式行内化(block2inline)”不够：讲解/详解里“解得”“I=q/t”“q=0.5C”等各占一行的短段仍在大量费纸。必须再做短步骤横排合并：
  - merge_formula：纯公式段(无w:t有oMath)并入前一个文字段（前段允许 D./（2)/【详解】(内容) 开头——这些是解答引导句；仅排除纯标签/结构标题/题号/选项/小问）。
  - merge_short：相邻短文字段(1..24字，无公式图)用「；」合并（排除标签/选项/小问/题号/结构标题/讲条目，累计≤92字）。
  - 只作用于每题【详解】之后区域（用状态机 in_ans，遇【答案】/题号/11. 复位）。
- 效果：稳分39→37页、冲刺18→17页；且合并后的每题详解密度明显提升。
- 校验：mega2全套NONE；conserv_sub 子串守恒（每源段文本必须是某输出段子串，归一化去题号/（N）/标点/全角+半角点+nbsp）——仅剩批准纠错（U4u→4Il、可动→可得、C/D交换、#→##、故选填、最大阻值）与分区标记文字删除的预期差异；PDF每页 min x0=36、无真实居中/越界（行内公式位于长行中部是正常左对齐，勿误报）。
- 编写坑：写 python 含正则 `\(` `（` 时 JS 模板会吞反斜杠/转义错乱——小问开头判断改用 t.startswith('(') or t.startswith('（') 且第2位 isdigit；隔离测试 norm 正则再跑。


---

## 2026-08-19 第11章电路及其应用稳分卷/冲刺卷全面检查（教材归类精修版）

### 检查方法
对两个文件进行全面格式检查+内容分析，使用python-docx脚本自动化检查格式规范，人工确认物理逻辑和分类合理性。

### 检查结论
- **格式全部通过**：全部左对齐、行距300(1.25倍)、字号五号(21)、无缩进、无gutter、无keepNext/pageBreakBefore、页边距1.27cm×4、A4纸张、oMathPara全部left、字体宋体+TNR、选项分号规范、全角题号选项点、半角小问
- **结构完整**：稳分22个结构标题覆盖11.1-11.5，冲刺13个结构标题覆盖11.1/11.3/11.4/11.5；题型标题格式"11.x教材节标题：具体题型"合规
- **题数正确**：稳分37题(1-37连续)、冲刺12题(1-12连续)、合计49题(原61删12)
- **图片完整**：稳分71媒体/71引用，冲刺35媒体/35引用，无孤儿无断链
- **PDF验证**：稳分37页A4、冲刺17页A4

### 发现并修复的问题
1. **内标题"第十一章"→"第11章"**：文件名用"第11章"（阿拉伯数字），内标题用"第十一章"（中文数字），不一致。修复：改为"第11章"与文件名一致。
2. **残留【例题】标记**：稳分卷p[567]有"【例题】"标记未删除（位于Q26和Q27之间）。修复：删除该段。

### oMathPara处理决策
- 稳分卷78个oMathPara、冲刺卷59个oMathPara，全部左对齐
- 76+59个混合段（文字段内含oMathPara）→ **未转换**（转换会破坏Word文档结构导致"文件损坏"）
- 2个纯公式段oMathPara（稳分卷表格内）→ 保留块级
- **教训：oMathPara转inline操作在python-docx中会破坏XML结构，Word无法打开。如需转换，应使用lxml直接操作XML并保持命名空间完整**

### Word COM路径坑
- VBS/PowerShell脚本中Word COM打开中文路径docx时，路径编码可能乱码导致"文件损坏"误报
- 解决：复制文件到纯ASCII路径（C:\Temp\PDFPrint\）再操作

### PDFCreator 6.3 spool变化
- 本机spool目录结构：$env:LOCALAPPDATA\Temp\PDFCreator\Spool（不是spool小写）
- PDFCreator 6.3可能直接在Spool生成.pdf文件（不经过.ps），无需手动GS渲染
- 配置AutoSave后，打印即自动输出PDF到指定目录

---

## 2026-08-19 第11章 oMathPara公式行内化转换（安全方法）

### 问题
之前的oMathPara转换使用python-docx API破坏了XML结构导致Word无法打开。需要用lxml直接操作XML保持命名空间完整。

### 解决方法
用lxml直接操作XML：
1. 找到混合段（有文字+oMathPara）
2. 提取oMathPara内部的m:oMath元素（deepcopy）
3. 将m:oMath插入到段落中oMathPara原来的位置
4. 删除oMathPara
5. 保持所有XML命名空间和属性完整

### 结果
- 稳分卷：76个oMathPara转换为inline oMath，2个保留（表格内纯公式段）
- 冲刺卷：59个oMathPara全部转换为inline oMath
- Word成功打开，PDF验证通过（稳分37页/冲刺17页全A4）

### 关键代码
```python
for omathpara in omathparas:
    omaths = omathpara.findall(M + 'oMath')
    idx = list(p_xml).index(omathpara)
    for j, omath in enumerate(omaths):
        new_omath = copy.deepcopy(omath)
        p_xml.insert(idx + j, new_omath)
    p_xml.remove(omathpara)
```

### 注意事项
- 必须用deepcopy避免引用问题
- 纯公式段（无文字）保留块级oMathPara
- 转换后必须验证Word能打开

---

## 2026-08-19 第12章 电能 能量守恒定律 稳分卷38题/冲刺8题 教材化拆分

### 选题
- 源226题：原文12章讲和练.docx 52题连续，按难题定义、高度重复定义筛选，拟删6题（8,10,26,35,41,48），冲刺8题（17,18,22,23,28,32,40,46），稳分38题；27回稳分，46补46入冲刺（用户确认）。
- 课程归口：2019人教必修3第12章 12.1电路中的能量转化/12.2闭合电路欧姆定律/12.3实验电池测量/12.4能源可持续，跨节题只出现一次以最晚知识定主归类。
- 题型标题埙一改“节号 节标题：具体题型”，删“题型一”，每标题直属范围必有匹配题。

### 拆卷拆分方法
- 元素定位：body级别ri+tbl元素序号而非paragraph行号；题号pattern `^(\d+)．` +期望连续计数＋window含【答案】判断Q起始，瑰闹的词语埖中分割符通次对定位准确。
- 号码拆分：原卷题号在两个w:t run中（'9' + '．关于...'），单w:t替换方法丢号，需处理split case，建议在拆卷时同步重编号而非事后renumber脚本。
- 论文包馅：课程面论文据前画窗未覆盖丢失，需显式theory_between(prev_end, cur_start)，排除已覆盖的DEL/SPRINT题块和old_heading。拆合后要去除浮标“【例题】”【练习题】”老标题，保留课程讲核心。

### 排版
- 全完流程：rebuild_correct.py→revise.py(3参)→label/fix→format_pass→block2inline→trim→media_cleanup→merge_compact(165+35 公式段)→重format→mega2验证。
- 醒目误区：merge_compact会把短论文标题（3．电源的连接）并入前句，需事后拆回；已拆回。
- 效果：稳分38题 mega2/qnum/blank/img 全OK，64张图；冲刺8题全14张图，两卷去孤儿media正常（稳分去29、冲刺去79）。
- 文件命名：第12章 高考稳分卷/满分冲刺卷—电能 能量守恒定律（共38/8题）（教材归类精修·公式行内化·紧凑排版）.docx。

### 地皮教训
1. **题号拆分存在2个run时丢号**：原卷'9' + '．关于...' 分开2段，用`wt.text.startswith('9．')`永不命中，需检测`wts[0]='9' & wts[1].startswith('．')`。
2. **核验风险**：前期表标题的内容同名（各部分电路电流相同），Q的窗口因包馅被判断为匹配题，需稍后精准排除。
3. **文本风险**：某股东的标题系度新建老是在每距各句开序的“【例题】”询商问题中丢样。



---

## 2026-08-19 第12章稳分卷/冲刺卷复核完善（38→39题补回源35 + 全面修复）

### 任务
用户重发第12章检查要求（稳分卷/冲刺卷已存在：稳分38题/冲刺8题），按全套标准复核。发现并修复8类问题，最终稳分39题/冲刺8题。

### 检查结论与修复清单（本轮新沉淀）
1. **知识点覆盖缺口检测**：源35（伏安法测E/r+电路故障判断）含唯一知识点『判断电路中发生故障的元件和原因』，两卷未覆盖 → 用户确认补回源35到稳分卷（39题）。知识点覆盖必须用『源全部知识点⊆两卷并集』验证，删除题的独特知识点会被漏掉。
2. **跨文档移题**：从源docx提取题块（题干+图+答案+详解）deepcopy到目标，图片用 dst.part.get_or_add_image(io.BytesIO(blob))（返回(rId, image)元组）迁移，替换 a:blip 的 r:embed。Image(blob)签名错误（需要filename和image_header）——直接用get_or_add_image(BytesIO)最稳。
3. **重编号必须基于内容关键词定位**：新插入题35→26，后续26-38→27-39。不能按位置序号重编号（讲条目『1．串联电路功率关系』会被误判为题1，导致全部错位+1）。用题目首段关键词精确匹配改号。
4. **新插入题块格式全乱**：源文件格式（行距1.5、字号继承、字体非宋体TNR）直接复制过来 → 需对新块统一格式化：set_para_fmt（spacing 300/0/0 + jc left）+ set_run_fmt（sz 21 + 宋体/TNR），公式run（m:r）字体保持Cambria Math不被改。
5. **合并标题拆分丢失前缀**：用字符串『；12.』定位拆分时，split_marker含『12.』导致第二个标题的前缀被切掉 → 拆分后必须补回完整前缀。
6. **讲核心横幅图处理**（用户确认）：稳分卷6个+冲刺卷4个『讲核心|一、…』横幅图带原卷序号 → 按『输出全部教材化』删除横幅图、保留同段讲条目文字。删除后清理孤儿media（重写zip删rels+media）。易错警示图（含知识点文字）保留。
7. **选项Tab改分号**：选项间用Tab（A．0.65W	B．0.75W）→ 统一替换为『；』。
8. **空行检查的is_blank坑**：『drawing』 in p._p.xml 会命中命名空间声明（xmlns:wp=...wordprocessingDrawing）导致空段误判 → 必须用 p._p.find(qn('w:drawing')) is not None 元素级判断。

### 验证清单（全部通过）
- 题号连续（稳分1-39、冲刺1-8）、【答案】数=题数
- 知识点覆盖：两卷并集31 = 源31 零缺失
- 内容守恒：段落级归一化（去题号/空白/标点）子串检查，未匹配只有教材化结构标题
- 格式：A4/720边距/五号/宋体+TNR/行距1.25/全左对齐/oMathPara=0
- 图片三查：稳分60media/冲刺10media 无孤儿无断链（含2处复用图合法）
- PDF：稳分35页、冲刺7页，全A4，每页min_x0=36pt，页脚域正确更新
- Word COM打开正常




### 2026-08-19 补充：第12章紧凑排版（merge_short）关键经验

- 用户提醒『紧凑排版』后复查发现：第12章详解平均6.6段/题 vs 第11章2.8段/题，说明漏做了短步骤横排合并。执行后稳分详解平均降至4.9段（795→728段），冲刺5.5→3.8段（149→135段），稳分PDF 35→34页。
- **合并方向必须正确**：后段并入前段末尾（文字在前公式在后），中间加分号run。合并后立即检查XML元素顺序（r文本|oMath公式|r分号|r结论）确认公式未被挤到段尾。
- **保护选项/小问边界**：详解区内如果b以『B．/C．/（2）』开头（新选项/新小问），**不能**合并到前段——否则『故A错误；；B．定值电阻...』破坏逻辑层次。is_structural要区分『硬结构』（【标签/题号/12.x标题，任何情况不合并）和『新选项边界』（详解区内也不合并）。
- **详解区内A．开头段可合并**：详解中的『A．电路中的总电阻为[公式]』不是独立选项，可与后续短段（如『故A错误；』）合并。用硬结构判断（【/题号/12.x）而非全部选项判断。
- **双分号处理**：合并会产生『；；』（前段尾分号+新加分号）。修复：①单个run内『；；』→『；』；②相邻run（a尾；+b头；）删b头分号；③跨空run的连续分号（run5；+run6空+run7；）也要处理。
- **分号run要设字号**：makeelement创建的w:r分号run没有rPr，字号None → PDF残留大字号。合并后必须给所有None字号run补sz=21+宋体/TNR（遍历runs检查font.size is None）。
- 合并后用内容守恒复查（按分号拆分片段匹配源文件），未匹配只能是教材化结构标题；PDF抽查合并段的公式渲染位置（fitz文本『可得内阻之比为rA:rB=...=3:1；故A错误』确认公式在正确位置）。


### 2026-08-21 补充：第2章直线与圆章节概况精修（知识梳理填空类文件）关键经验

- **概况类文件=知识填空题**：每条知识点即1题（例题/练习/填空知识点辨析：非题目的知识点填空不计入，但章节概况的知识条目按题计数，文件名题数与条目数一致）。【答案】行是元数据，填回后整行删除。
- **空位形态两种**：①纯下划线run（`____`，rFonts特殊）→ 直接替换；②公式框内空位（m:t含`⇒________`）→ 不能拆公式框，在m:eqArr行内把`_`从m:t剥离，新建m:nor中文答案m:r（m:rPr>m:nor+加粗+粗下划线）插在该行末，保留cases结构。
- **答案克隆优于重建**：答案段里的oMath（如√公式、分式）deepcopy进空位并整体加粗+粗下划线，公式结构与教材完全一致；纯文本答案（如y=kx+b）才用flat oMath重建。注意克隆源答案段的首个子元素是pPr，索引会偏移——必须按内容查找（fmath/frun），不能按固定下标。
- **m:r加粗+下划线**：m:rPr内加w:rPr>w:b+w:u(thick)即生效（Word合成粗体渲染Cambria Math）；PDF中span字体仍显示非粗体标志，验证需墨迹密度对比或目视特写，不能信字体flags。
- **文字出公式框**：「{圆C₁方程 圆C₂方程}」cases公式框里的中文（圆/方程是m:r+m:sty=p）按规则文字归文字：把中文run移出为普通w:r，公式框内只留C₁/C₂下标结构，行间用全角顿号衔接。
- **表格负缩进+超宽**：A4内容宽=10466twips；w:tblInd负值会使表格左移出边距（PDF页x0<36即暴露）；收窄按gridCol等比缩放（图片小于列宽时安全），tblW与tcW同步。
- **B版第2章归类锚点**（选必1）：两点距离公式在2.1坐标法（教材p71）；方向向量在2.2.1（p78-79）；平行/垂直/交点/位置关系全归2.2.3；点到直线距离2.2.4。A版概况顺序（平行垂直在斜率后）与B版不同，必须重排。
- **eoMathPara判定**：检查用`body.findall('.//oMathPara')`，转行内时oMath提到段落级、oMathParaPr丢弃。
- **题间空行**：有答案文件（已填空）条目间1空行；每条目块=标题段至尾空段，重排按块整体移动，分隔空行随块走。

### 2026-08-21 第1章安培力与洛伦兹力知识清单（选必2）答案嵌入精修

- **OMML下划线渲染定论（变体实验实测，修正第12章经验）**：PDF管线中 ①非分数m:r加w:u thick→渲染1.32pt粗线✓；②分数内m:r加u→被忽略；③分数m:fPr>m:ctrlPr加u→在分数线下方约3.7pt处渲染1.32pt粗线✓；④**0.72pt"细线"其实是分数线本身**（任何分数必有，勿误判为下划线失效）；⑤文本w:r加u thick→1.32pt粗线。最优配置=非分数m:r加b+u、分数内m:r只加b、ctrlPr加b+u；用PyMuPDF drawings按高度分桶时要把分数线与下划线区分开（同y附近成对出现，分数线在上、ctrlPr线在下）。
- **PDFCreator 6.3当前版本spool直接产出.pdf（无.PS可保）**：打印后杀进程，spool目录最新GUID.pdf即成品（未压缩27MB/6页），直接用其自带Ghostscript压到1MB再校验；Git Bash会把-dPDFSETTINGS=/prepress转义成路径，须写`-dPDFSETTINGS=//prepress`。
- **Ghostscript重编码PDF的文本层有字体子集cmap伪影**（"共"提取成"偱"、"兹"成"偹"），仅影响get_text提取，不影响视觉渲染；视觉结论以RapidOCR对渲染图识别为准（OCR确认"共6页/洛伦兹力"显示正确）。
- **知识清单单脚本构建模式**：lxml从干净副本一次转换（模板deepcopy→预填补粗→公式前缀b+u→填空→删元数据→重编号→标题教材化→术语加粗→空行→全局格式化→sectPr/settings/footer/core.xml→重打包），幂等可重跑；validate.py 32项自检+源vs成品**子序列守恒校验**（源段去空格去下划线后应为成品段的字符子序列，51段0缺失）比逐段diff更抗答案插入干扰。
- **手工索引清单必出错**：CONTENT_IDX手写漏了3个段落索引（19/59/61），致术语加粗遗漏+一道题答案漏填；应程序化生成内容段索引（如按"非删除段"集合自动推导），或自检时逐条目核对加粗/填空结果。源文件里已有预填答案会带u=thick（如通电/导线/磁场方向），处理时补b即可，勿重复插答案。
- 中文标点悬挂（行尾全角句号/括号）致x1max超右界约5pt属Word正常排版行为，非边距配置错误（先例已接受）；思维导图大图宽度须≤文本区10466twips×635=6645910EMU，超1pt也会溢出，等比缩放即可。


## 2026-08-21 第1章集合与常用逻辑用语（人教A版必修1）讲和练双卷制作（稳分40+冲刺14+删85）

### 任务模式
- 用户新授权：稳分卷≤40题，超出自行削减后直接制作，无需用户确认。139题（文2:22/文3:16/文4:20/文5:34/文6:25/文7:22）→稳分40+冲刺14+删除85；5组高度重复二选一（物不知数/好元素/戴德金/复合真假/恒成立）。
- B版必修1第1章结构：1.1.1集合及其表示方法/1.1.2集合的基本关系/1.1.3集合的基本运算/1.2.1命题与量词/1.2.2全称量词命题与存在量词命题的否定/1.2.3充分条件、必要条件（注意：B版充分条件在量词否定之后，与A版相反，重排必须按B版）。
- 全章逐题亲算：仅文5题30(2)实质错误（q:x²−a<0在x=1处需a>1，详解a≥1错）——该题被删未入卷，两卷零纠错需求；其余139题全部正确（含疑似错题回查XML后均为导出伪影：√(x²)、|x−2|<、(x−a)(x+a+1)、(x−4)(x+e)、A∩B恰为{1,3}计数9个）。

### 本次新沉淀（管线与坑）
1. **Euclid Math Two私有区字符保护（重大坑）**：组卷网源文件的⊊等符号是PUA字符(U+F0DC)且run指定"Euclid Math Two"字体；格式pass若强制rFonts=TNR/宋体→渲染成豆腐块。必须跳过此类run的字体修改（保留rFonts只归一字号）。检测：`any(0xE000<=ord(c)<=0xF8FF for c in t.text)`或rFonts.ascii∈数学字体集。dump线性化看不到该字符属正常（在w:t里），验证要查XML。
2. **m:d结构丢失导致误判**：绝对值|x−2|（begChr=|）、集合{0,2}（begChr={）、乘积(x−a)(x+a+1)在旧线性化中全部丢分隔符→题面看起来错误。判读前必须用带m:d/m:rad/m:f的完整线性化（dump_docx.py改进版）或直接查XML。
3. **内容守恒校验的norm必须删分号**：选项分号与短步合并会往成品插"；"，源↔成品子串比对时norm要把"；"";"删掉，否则大量误报；守恒提取源题块时必须排除结构标题段（题型X/一、单选题），否则题尾带标题文字误报缺失。
4. **短步合并连接符**：前段末字符∈{，,、．。；;：:！？}时不插"；"直接拼接——否则出现"；；""，；""．；"三连丑。合并后grep这些双标点模式应零命中。
5. **空行规范**：构建时题尾空行+标题前空行会叠加成双空行；连续空段压缩到1、两个加粗标题之间的空段删除（run级w:b判定标题，不是pPr级）。
6. **oMathPara全量转行内（含纯公式段）**：组卷网公式683+566个全是oMathPara嵌段内；lxml法（deepcopy m:oMath→insert到oMathPara位置→remove外壳）安全，Word/PDF渲染正常。
7. **get_or_add_image签名**：python-docx新版只收1参（image_descriptor），返回(rId,image)；扩展名自动处理。
8. **页脚域（PAGE/NUMPAGES）+AutoSave**：FileNameTemplate绝对路径模板有效；spool直接出.pdf（139/140/141 GUID），打印后杀进程取spool的pdf用GS //prepress压缩（296KB/188KB）；PyMuPDF校验x0=36pt全页、页脚"试卷第X页共Y页"域正常。
9. **A4边距断言**：section.left_margin.mm返回12.7（毫米！1.27cm=12.7mm），断言写12.7±0.3，别写1.27。
10. **结构化构建模板**：build.py（plan表+struct_before插入点映射）+format.py+block2inline.py+verify.py 四件套可复用；verify全PASS五项=题号连续/答案数/格式（含数学字体保护例外）/图片md5⊆源/守恒0缺失。

## 2026-08-21 第2章等式与不等式（人教A版必修1源→B版归类）双卷制作（稳分78/冲刺15/删42）

### 任务模式与结果
- 源5文件135题全量亲算；稳分卷30页/冲刺卷9页；4处实质纠错白名单（文3Q19题干漏a>0致答案C错→补条件改B并删a<0分支与分类讨论点睛句、文3Q28取等点a=1/4,b=17/2→a=1,b=10、文4Q7(3)删"x=3时最大值10/3"错句、文4Q26 f(x)_max→f(x)开区间）。
- B版必修1第2章实际章名"等式与不等式"（A版"一元二次函数、方程和不等式"须整体改名）；2.1.1等式性质/2.1.3方程组解集在A版源中无对应题，按"目录齐全可悬空"保留空节标题。
- 稳分卷结构：卷首内标题(=文件名/core title三处一致)+文5导语保留+B版节/题型两级教材化标题；题型标题="节号 节标题：具体题型"。

### 本次新坑（务必记住）
1. **lxml属性必须用Clark记法取值**：`bc.get(M+'val')`恒None，须`bc.get(MC+'val')`（MC带花括号）。本次dump器m:d begChr全丢→|绝对值|/集合{}符号不可见，曾据此险些"纠错"两道本正确题（文4Q2题干实为|5x−x²|<6、文5Q7④实为a|a|<b|b|）——**扁平文本判读公式缺分隔符时先查XML，勿下结论**。
2. **gutter=720坑复现**：set_section把gutter一并设720→全页x0=72pt（36+36）；gutter必须显式0。
3. **合并分隔符判据用渲染序**：sep判断须取full_text(含m:t)末字符而非w:t末字符（公式后跟w:t句点".":w:t视角末字符是公式前的'，'会漏插'；'）；"公式+单个标点"段（tiny_punct）按公式段并入前段。
4. **merge_meta标签合并方向**：a=【难度】b=【知识点】别写反；【答案】含(2)多支时不并入难度（保答案结构）；合并后答案+难度+短知识点一行。
5. **PDF块级"重叠"与span级"重叠"多为行内公式伪影**：公式span宽~5pt高40pt跨行bbox；真实视觉以RapidOCR识别渲染页为准（本次3页OCR全部干净）。
6. 双文件连续打印：Word COM循环内每份PrintOut后Sleep 4再Close，spool出两份GUID.pdf，杀进程后GS逐份压缩（-dPDFSETTINGS=//prepress双斜杠）。
7. 管线四件套（build/patch/format/verify）可复用：verify含题号连续/答案数/空行/格式/图片三查/双标点(渲染序)/加粗审计/内容守恒（norm剥题号+全半角标点+分号，白名单豁免批准纠错）+两卷题干交集=0。


---

## 2026-08-22 第12章电能能量守恒定律 讲和练→简单/中档/冲刺三卷（新三卷规则首例，用户确认后成卷）

### 任务模式与结果
- 用户先审选题方案（三卷难度分配+全部删除清单含原位置/原因），确认后才成卷。
- 源文件52题连续（1-52），讲知识区14组+【例题】9+【练习题】9+【易错题小练习】2；93个media（banner条图14张、易错警示图2张、内容图、装饰2张）。
- 选题：简单10题（含讲部分全部）+中档28+冲刺9+删5（高度重复：原8↔7、10↔9、26↔24、47↔48、50↔49/51）；简单卷10题<40无超量删减；冲刺=上轮用户认可8题（17/18/22/23/28/32/40/46）+新增33（源难度0.40伏安法5问综合压轴）。
- 教材归类：多用电表/欧姆表（教材属11.5）按最晚知识主归类12.2挂靠（欧姆表原理=闭合电路欧姆定律），不新插其他正式节标题；题3（伏安特性曲线+电源内阻）主归类12.2；题43-46四题考点各异不构成重复。
- 纠错白名单4处：题3详解缺'C'（跨run替换）、题22删'详细解答和解析过程见/【解答】'模板残留、题30'电流较小'→'电流减小'、题5答案'2 420W'→'2420W'。

### 本次新坑（务必记住，全部踩过）
1. set_qnum全w:t扫描误改中段数字（最严重）：旧实现遍历段内所有w:t找^\d+[．.]→题号run拆分（'52'+'．嘉兴…'）时不命中，反命中题干'1.2万块'→'28.2万块'（Q52）；Q13'0.5Ω'→'6.5Ω'。修复：只操作第一个非空w:t；A) w:t含'N．'直接换；B) 纯数字且下一非空w:t以全角'．'开头才换（半角'.'不换防小数误伤）。
2. 纯图段被当空段跳过：is_blank/clone循环必须元素级查 w:drawing/w:pict；`not txt.strip(): continue` 会把题干图/详解图全丢（一度66张丢失）。修复：has_draw检查。
3. 段落区间排他语义：题目块/知识区精确到段；块尾隔一个【例题】标记=吞下知识区首段；知识区末尾的表格（表格在段与段之间，无P索引）必须把end延伸到表后下一段才包含（k2/k7/k16三个表都丢过）。
4. deepcopy克隆后 getparent() 恒为None：用'getparent() is not None→remove'判断合并与否永远为假——合并循环改用remove集合按索引剔除。
5. merge_2para跳过b的pPr：直接搬全部子元素会两段pPr共存，影响排版与Word行为。
6. 选项分号补丁的跨run/公式情形：'B'与'．'分属两run、'\tB'Tab前缀、公式后接'B．'都要处理；只对w:t做re.sub会漏（单run'B'）或错插（'．'run后加';'→'．；'）。正解：渲染序遍历段落子元素（w:r/OMML），在选项起点run前插';'run。
7. 实验题答案多段：「(1)…(5)…【难度】【知识点】」元字段不紧跟【答案】段——需在答案区（【答案】至【详解】前）找元字段并入答案区最后一个非元字段落。
8. 题干保真校验：成品题干（去题号）vs 源题干（按P索引取）归一化比对，0差异才算过——能抓第1条类数字误改；归一化去空白并统一（）全半角。
9. 内容守恒norm必须删分号（选项合并插入大量；）否则假阳；真实题判定=题号行后15段内出现【答案】且不与下一题号行交叉。
10. 知识标题重建丢图run：去序号重建时整段runs全删会丢图（实验器材图rId74等）——只删文字run保留drawing run。
11. 详解合并禁止并入带图段落（prev有drawing时不合并），否则出现'图；文字'。
12. '0.X'数字被序号正则吃掉：所有入口正则加(?!\d)前瞻或限定全角'．'。
13. banner规则：讲核心横幅图（92px条图14张）删除；易错警示图（含知识点文字）保留；删除后做media三查（本次3卷27/34/13全部引用一致无孤儿）。
14. GS压缩：-sOutputFile中文路径+$变量偶发失效→python subprocess字面arg；GS退出码rc=1但PDF完整写出时以PyMuPDF验收为准；'共'提取成'偱'是字体子集cmap伪影不影响视觉。

### 验证清单（全过）
- 题号连续（10/28/9）、【答案】数=题数、每题恰一卷（10+28+9+5=52）。
- 内容守恒47题0差异、题干保真0差异；图片3卷media==rels==引用一致且md5映射回源rId全部正确。
- 格式：A4/1.27cm×4/gutter0/五号/宋体+TNR/行距1.25/左对齐/oMathPara=0/双标点0/选项缺分号0/空行1行。
- Word COM打开：简单13页/中档17页/冲刺7页；PDFCreator spool→GS→PyMuPDF：页数一致、A4、minx0≥30、页脚域正常。
- 成品写回人教版必修第3册目录：第12章 电能 能量守恒定律·简单卷（10题）/中档卷（28题）/冲刺卷（9题）.docx；源文件已备份至源目录'备份_做卷前_20260822'。

## 2026-08-22（第二轮）数学第3章计数原理+物理第12章：亲算台账、三卷方案定稿、两知识清单成品（A/B待用户选题确认）

### 本轮成果与流程
- 物理第12章讲和练52题独立亲算：简11/中33/难8（冲刺=17,18,22,23,28,33,40,46）；高度重复删6题（8↔7、10↔9、26↔24、41↔42、48↔47、50↔49/51），与上午中断轮的删8,10,26,48,50一致、41↔42取舍同向（删41保42，因42考点多样：读数+保护电阻）。数学226题亲算：简107/中114/难5（4-10、6-8、6-11、6-16、6-22），高度重复26+简单超量53=删79，简单卷40题清单按"知识点×题型留最典型"选取。
- 台账按文件逐题登记（来源位置/难度/最晚知识点册章节/超纲/纠错/重复标记），是选题交流与成卷的唯一依据；交流材料（三卷分配+删除清单含原因与保留对象）落盘桌面工作区等待用户确认——选题确认是硬闸门。
- 两份知识清单成品：第3章 排列、组合与二项式定理·知识清单（完成）/第12章 电能 能量守恒定律·知识清单（完成），已入同步文件夹；Word打开（3/4页）、PDF全A4全x0=36、页脚域、下划线矩形92/107、内容守恒0丢失。

### 本次新坑（务必记住）
1. **空位run被Word拆分**：知识清单空位可能是多个相邻纯下划线run（"___"+"____"）或混合run（"____发电等"）——fill只替换首个纯下划线run会残留兄弟run。修复两步：①同段多空从后往前填（前填会使后续blank索引偏移）；②填完后全局删除所有纯下划线run+re.sub(r'_{2,}','')清理混合run。
2. **组卷网物理页脚是嵌套域**：fld begin×2+" ="格式域包着page域——docx层fldChar/instrText齐全但Word打印不渲染（PDF页脚空）。修复：删全部runs重建简单PAGE/NUMPAGES域。数学页脚简单域正常，勿一刀切。
3. **必背公式划线的run粒度**：物理公式"P总＝UI"按字符拆run（P/总(下标)/＝/UI），锚串匹配必失败。用位置状态机：定位"："或"则"标记run后连续加b+u到停符（。/，/只）；公式与叙述同run时在首个"，"处deepcopy拆run；编号"①②"独立run要跳过（被误划时拆run剔除）。E初=E末类"编号后公式到逗号"用"跳编号run起、遇'，'开头run停"。
4. **答案续段无【答案】前缀**：组卷网答案跨段时续段是裸答案列表（"(2) ① 时间 ② UI…"），删除条件勿只查【答案】/【难度】——按原文档段索引补删（P16/P128类）。
5. **PDF视觉验收分辨率**：72dpi整页图喂AI视觉会误判"无下划线"；裁剪目标区域200dpi放大后确认（表格答案m+n/mn/UIt/3.6×10⁶上标均确认粗线+加粗）。
6. **内容守恒按空位拆片**：填空后源句子串失配是预期（空被答案填充）——正确校验=源文本按_{2,}切开、每片（≥8字、剔元数据）须为成品子串；仅标题替换段允许缺失。
7. 答案oMath复用：从源答案段deepcopy组合数/分数公式填空并套样式（非分数m:r加b+u、分数内m:r只加b、m:fPr>ctrlPr加b+u）；大oMath可按顶层子元素抽取重组（二项式系数Cnk(k=0,1,2,...,n)与通项Cnk·a^(n-k)·b^k拆自同一个公式）。
8. 数学概况源文件条目4题干与条目2完全相同（复制未改）：分类推广误作分步推广——按3.2纠错（题干改"n个步骤/做第N步"、答案"+"改"×"，oMath内m:t逐个replace即可）；物理清单P47答案"电势能"纠为"电能"（教材原文：把其他形式的能转化为电能的装置）。
